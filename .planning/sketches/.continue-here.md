---
context: sketch
status: paused
last_updated: 2026-05-13T13:47:00Z
---

# Sketch Viewer Setup Complete — Next: Create Better Mockups

<current_state>
Just finished setting up the sketch viewer infrastructure at `psalter.gsdlabs.dev/sketches/001-hymnal-layout-variants/index.html`. User reviewed initial 3 sketches and gave clear feedback on what the next iteration should show.
</current_state>

<completed_work>

- ✓ Created sketch viewer at `/public/sketch-viewer/index.html` with navigation
- ✓ Moved 3 sketches to `/public/sketches/` for direct serving
- ✓ Sketches accessible at `psalter.gsdlabs.dev/sketches/{id}/index.html`
- ✓ Verified working in browser

</completed_work>

<remaining_work>

**Next action (explicit order):**

1. Use `/gsd-sketch` to create new mockups showing:
   - **Mockup 1**: Typical complete tune with 2-line score split + 3 stanzas (6 lyrics lines total)
   - **Mockup 2**: Desktop fullscreen layout
   - **Mockup 3**: Mobile fullscreen layout

2. Implement user feedback in all new sketches:
   - Skip sidebar lyrics display
   - Show lyrics inline with score only
   - Add UI affordance for "Lyrics Only" mode (click to toggle)

3. Place new sketches in `.planning/sketches/` with `/gsd-sketch` (not manual HTML)

</remaining_work>

<decisions_made>

- Public folder serving > Next.js route complications (proven through debugging)
- Sketch viewer as static HTML > complex React component
- Direct file access at `/sketches/` path (simpler, no API routes needed)

</decisions_made>

<context>

User wants realistic tune mockups for Phase 4.9.2 (layout design). Current sketches are too simplistic (single line score, minimal lyrics). Next generation should match the actual use case: a Scottish Psalter tune with multiple stanzas displayed under the score.

Key constraint: abcjs renders notation SVG, so sketches must account for varying widths based on the tune length. The 2-line split represents the minimum viable layout that real tunes will require.

</context>

<next_action>

When resuming with `/gsd-resume-work`:
1. Run `/gsd-sketch` three times for:
   - 2-line tune with 3 stanzas
   - Desktop fullscreen
   - Mobile fullscreen
2. Each should reflect: inline lyrics only, no sidebar, "Lyrics Only" toggle affordance
3. Sketches auto-save to `.planning/sketches/` — commit all three together

</next_action>
