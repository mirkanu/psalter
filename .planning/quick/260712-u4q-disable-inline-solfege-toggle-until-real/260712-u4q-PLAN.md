---
phase: quick-260712-u4q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/singing/GearPopover.tsx
  - src/components/singing/SingingView.tsx
  - src/components/notation/NotationRenderer.tsx
autonomous: true
requirements: [QUICK-260712-u4q]

must_haves:
  truths:
    - "The Solfège notation toggle is disabled/grayed in the gear popover whenever the current layout is Inline (inline Solfège can never be selected as a new state)"
    - "Switching layout to Inline while Solfège is the active notation is blocked (the second path into inline-solfège is closed)"
    - "Split-leaf Solfège remains fully enabled and renders the scanned JPG exactly as before"
    - "A stale localStorage viewMode:'solfege' renders a clear 'coming soon' message instead of a blank/broken view or an accidental JPG fallback"
    - "The disabled-state message uses 'not yet available' / 'coming soon' wording, not old 'approval' wording"
  artifacts:
    - path: "src/components/singing/GearPopover.tsx"
      provides: "Two Solfège availability props (solfegeInlineAvailable hardcoded-false-driven, solfegeSplitAvailable JPG-driven) with layout-dependent gating on both the Solfège notation button and the Inline layout button"
      contains: "solfegeInlineAvailable"
    - path: "src/components/singing/SingingView.tsx"
      provides: "Wiring that passes solfegeInlineAvailable={false} and the JPG-presence split boolean to GearPopover"
      contains: "solfegeInlineAvailable={false}"
    - path: "src/components/notation/NotationRenderer.tsx"
      provides: "Distinct viewMode==='solfege' coming-soon message branch; JPG branch narrowed to viewMode==='solfege-split'"
      contains: "viewMode === 'solfege-split'"
  key_links:
    - from: "src/components/singing/SingingView.tsx"
      to: "src/components/singing/GearPopover.tsx"
      via: "solfegeInlineAvailable={false} + solfegeSplitAvailable={!!(solfegeJpgUrl || activeSolfegePages.length > 0)} props"
      pattern: "solfegeInlineAvailable=\\{false\\}"
    - from: "src/components/singing/GearPopover.tsx"
      to: "Solfège notation + Inline layout buttons"
      via: "solfegeAvailableForCurrentLayout gating disabled/opacity state"
      pattern: "solfegeAvailableForCurrentLayout"
    - from: "src/components/notation/NotationRenderer.tsx"
      to: "viewArea"
      via: "viewMode === 'solfege' branch renders coming-soon message; 'solfege-split' renders JPG"
      pattern: "viewMode === 'solfege'"
---

<objective>
Disable the INLINE Solfège toggle in the singing-view gear popover, treating inline Solfège
as a permanent "not built yet / coming soon" state (real abcjs tonic sol-fa rendering does not
exist). Split-leaf Solfège (the scanned JPG) must remain fully enabled and untouched.

This REVERSES part of quick task 260712-lcg (which had collapsed inline + split-leaf Solfège
into a single ungated `solfegeAvailable` prop showing the JPG for both). The user has clarified:
inline Solfège should NOT show the JPG — showing a static image in "inline" mode does not match
what "inline" means for Staff, so it should signal "not built yet" rather than substitute the scan.

Purpose: Match user intent — inline Solfège is a disabled placeholder, split-leaf Solfège is the
real, working scanned-JPG view.
Output: Layout-dependent Solfège gating in GearPopover, a distinct inline-Solfège "coming soon"
message in NotationRenderer, and updated wiring in SingingView.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

<constraints>
- Do NOT resurrect the abcjs `unifiedSolfegeAbc` render path, the `melismaStatus` fetch, the
  `/api/melisma-status` route, `pickAbcWithMarkers`/`sopranoOnly`, or ANY approval-gate /
  tune-approval concept. Those were deleted in 260712-lcg and are permanently out of scope.
- `solfegeInlineAvailable` is a HARDCODED `false` (not tied to any tune data or approval status).
  It represents "inline Solfège rendering does not exist yet", full stop.
- Split-leaf Solfège (`viewMode === 'solfege-split'`) must remain byte-for-byte functional:
  the scanned JPG + thumbnail strip + 50% desktop cap (260712-tmm) must all still work.
- Read the CURRENT state of all three files (post 260712-lcg revert AND post 260712-tmm
  page-array plumbing) — do NOT assume old CR-04 abcjs/approval code still exists.
</constraints>

<current-state>
GearPopover.tsx (post-260712-lcg): single `solfegeAvailable: boolean` prop.
  - Derived flags: `isStaff = viewMode === 'staff' || 'staff-split'`;
    `isSplit = viewMode === 'staff-split' || 'solfege-split'`.
  - `handleNotationChange('solfege')` → newMode = isSplit ? 'solfege-split' : 'solfege';
    guarded by `if (notation === 'solfege' && !solfegeAvailable) { toast(...); return }`.
  - `handleLayoutChange('inline')` → newMode = isStaff ? 'staff' : 'solfege'; NO guard today.
  - The Solfège notation `<button>` uses `disabled={!solfegeAvailable}` + opacity/checked on it.
  - The Inline layout `<button>` has NO disabled/opacity gating today.

SingingView.tsx (post-260712-tmm): passes
  `solfegeAvailable={!!(solfegeJpgUrl || activeSolfegePages.length > 0)}` to GearPopover.
  `activeSolfegePages = activeTune?.solfegePages ?? []`, `solfegeJpgUrl = activeTune?.solfegeJpgUrl ?? null`.

NotationRenderer.tsx (post-260712-lcg): the Solfège branch is
  `} else if (viewMode === 'solfege' || viewMode === 'solfege-split') {` — both fall through
  uniformly to the scanned-JPG render (mainImageBlock + thumbnailStrip + stanzaBlock, with a
  split vs non-split layout split via `isSplit`). There is NO distinct inline-disabled message.
  `viewMode` persists in localStorage under `STORAGE_MODE_KEY = 'psalter-score-mode'`, so a
  browser can load with a stale `viewMode: 'solfege'` from before this change.
</current-state>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split Solfège availability into layout-dependent inline/split gates in GearPopover + wire SingingView</name>
  <files>src/components/singing/GearPopover.tsx, src/components/singing/SingingView.tsx</files>
  <action>
In GearPopover.tsx:

1. Replace the single `solfegeAvailable: boolean` prop (and its JSDoc) in the `Props` interface
   with TWO props:
   - `solfegeInlineAvailable: boolean` — JSDoc: "Whether INLINE Solfège rendering exists. Hardcoded
     false for now — real abcjs tonic sol-fa rendering is not built yet."
   - `solfegeSplitAvailable: boolean` — JSDoc: "Whether SPLIT-LEAF Solfège (scanned JPG) is available."
   Update the destructured params in the component signature accordingly.

2. After the existing `isStaff` / `isSplit` derivations, add a layout-dependent availability derivation:
   `const solfegeAvailableForCurrentLayout = isSplit ? solfegeSplitAvailable : solfegeInlineAvailable`
   (When the current layout is split-leaf, use the split JPG check; when inline, use the hardcoded
   inline flag — so on an Inline layout, Solfège is unavailable.)

3. In `handleNotationChange`, change the Solfège guard from `!solfegeAvailable` to
   `!solfegeAvailableForCurrentLayout`, and update its toast to "coming soon / not yet available"
   wording (NOT approval wording):
   `toast('Coming soon', { description: 'Inline Solfège notation is not yet available — switch to Split-Leaf to view the scanned Solfège' })`
   (Keep the existing staff guard untouched.)

4. In `handleLayoutChange`, add a guard at the TOP that blocks switching to Inline while Solfège
   is the active notation and inline Solfège is unavailable (this closes the second path into
   inline-solfège — solfege-split → Inline would otherwise land on 'solfege'):
   `if (layout === 'inline' && !isStaff && !solfegeInlineAvailable) { toast('Coming soon', { description: 'Inline Solfège notation is not yet available — Split-Leaf shows the scanned Solfège' }); return }`

5. On the Solfège notation `<button>`: replace all three `solfegeAvailable` references
   (`disabled={!...}`, the `!... && 'opacity-40 cursor-not-allowed'` class, and the
   `!isStaff && ...` checked-highlight condition) with `solfegeAvailableForCurrentLayout`.

6. On the Inline layout `<button>`: add disabled + grayed styling that mirrors the Staff button's
   pattern, gated on `const inlineLayoutDisabled = !isStaff && !solfegeInlineAvailable`:
   - add `disabled={inlineLayoutDisabled}`
   - add `inlineLayoutDisabled && 'opacity-40 cursor-not-allowed'` to the className array
   - add `title="Inline Solfège coming soon"` for a hover hint
   (Do NOT gate the Split-Leaf layout button — split-leaf Solfège works.)

In SingingView.tsx (in the `<GearPopover ... />` props inside the GlassBottomBar `gear` slot):

7. Replace the single `solfegeAvailable={!!(solfegeJpgUrl || activeSolfegePages.length > 0)}` line
   (and the stale 04.9.14-lcg comment above it about "a single ungated boolean covers both") with:
   - `solfegeInlineAvailable={false}` with a short comment: "Inline Solfège rendering is not built
     yet (abcjs has no tonic sol-fa) — permanently disabled until real rendering exists."
   - `solfegeSplitAvailable={!!(solfegeJpgUrl || activeSolfegePages.length > 0)}` — the JPG-presence
     check, unchanged in meaning.
  </action>
  <verify>
    <automated>grep -q "solfegeInlineAvailable" src/components/singing/GearPopover.tsx && grep -q "solfegeSplitAvailable" src/components/singing/GearPopover.tsx && grep -q "solfegeAvailableForCurrentLayout" src/components/singing/GearPopover.tsx && grep -q "solfegeInlineAvailable={false}" src/components/singing/SingingView.tsx && ! grep -q "solfegeAvailable=" src/components/singing/SingingView.tsx && npx tsc --noEmit 2>&1 | grep -E "GearPopover|SingingView" ; test $? -ne 0</automated>
  </verify>
  <done>GearPopover exposes solfegeInlineAvailable + solfegeSplitAvailable; the Solfège notation button and Inline layout button are both gated via layout-dependent derivations; SingingView passes solfegeInlineAvailable={false} + the JPG-presence split boolean; no new tsc errors in either file.</done>
</task>

<task type="auto">
  <name>Task 2: Render a distinct "coming soon" message for inline Solfège and narrow the JPG branch to split-leaf only in NotationRenderer</name>
  <files>src/components/notation/NotationRenderer.tsx</files>
  <action>
In NotationRenderer.tsx, in the `viewArea` if/else-if chain (currently `if (viewMode === 'staff'
|| 'staff-split') { ... } else if (viewMode === 'solfege' || viewMode === 'solfege-split') { ... }
else { lyrics }`):

1. Add a NEW `else if (viewMode === 'solfege')` branch BEFORE the existing Solfège branch. This
   branch renders a graceful "coming soon" message (this only fires for a STALE localStorage
   `viewMode: 'solfege'` from before this change — the gear toggle now prevents NEW inline-solfège
   selections). Respect chromeless padding so it renders cleanly in SingingView:
   ```
   } else if (viewMode === 'solfege') {
     // Inline Solfège is not built yet — abcjs has no tonic sol-fa support, and a static JPG
     // does not match what "inline" means for Staff. The gear toggle disables NEW inline-solfège
     // selections; this branch only handles a stale localStorage viewMode:'solfege'. Split-leaf
     // Solfège (the scanned JPG) is handled by the branch below and is unaffected.
     viewArea = (
       <div className={chromeless ? 'px-4 pt-8 flex flex-col items-center text-center gap-2' : 'py-8 flex flex-col items-center text-center gap-2'}>
         <p className="text-sm font-medium">Inline Solfège is coming soon</p>
         <p className="text-sm text-muted-foreground max-w-xs">
           Tonic sol-fa notation isn&apos;t available inline yet. Use the Split-Leaf layout to view the scanned Solfège, or switch to Staff.
         </p>
       </div>
     )
   }
   ```

2. Change the EXISTING Solfège branch condition from
   `} else if (viewMode === 'solfege' || viewMode === 'solfege-split') {`
   to `} else if (viewMode === 'solfege-split') {`
   Leave the entire body of that branch unchanged (mainImageBlock, thumbnailStrip, stanzaBlock,
   the `isSplit` split-vs-non-split layout logic, 50% cap, etc.). Because only 'solfege-split'
   now enters this branch, `isSplit` is always true inside it; the `!isSplit` sub-branch becomes
   unreachable but harmless — do NOT delete it (minimize churn/risk).

Do NOT touch: the `activePages` / `isSolfegeMode` / `isSplitMode` helpers, the Staff branch, the
lyrics branch, the split-leaf JPG rendering, or any pagination/thumbnail logic.
  </action>
  <verify>
    <automated>grep -q "else if (viewMode === 'solfege') {" src/components/notation/NotationRenderer.tsx && grep -q "else if (viewMode === 'solfege-split') {" src/components/notation/NotationRenderer.tsx && ! grep -q "viewMode === 'solfege' || viewMode === 'solfege-split'" src/components/notation/NotationRenderer.tsx && grep -qi "coming soon" src/components/notation/NotationRenderer.tsx && npx tsc --noEmit 2>&1 | grep -E "NotationRenderer" ; test $? -ne 0</automated>
  </verify>
  <done>Inline Solfège (`viewMode === 'solfege'`) renders a distinct "coming soon" message; the scanned-JPG branch is narrowed to `viewMode === 'solfege-split'` only and otherwise unchanged; no new tsc errors in NotationRenderer.</done>
</task>

</tasks>

<verification>
- `grep` gates in each task confirm the new props, derivations, branch conditions, and message exist.
- `npx tsc --noEmit` shows no NEW errors in GearPopover.tsx, SingingView.tsx, or NotationRenderer.tsx
  (pre-existing unrelated errors in tests/e2e/*.spec.ts are out of scope per 260712-tmm/lcg summaries).
- Manual/live verification is DEFERRED: this session batches one production rebuild after all pending
  260712 quick tasks. After that rebuild, live check on /psalms/78 (mobile 375-wide + desktop):
  1. Open gear → Solfège: on Inline layout the Solfège notation button is grayed/disabled; switching
     to Split-Leaf layout enables it and shows the scanned JPG.
  2. On Solfège + Split-Leaf, the Inline layout button is grayed/disabled (or toasts "coming soon").
  3. Split-leaf Solfège still renders the scanned JPG + thumbnails + 50% desktop cap (260712-tmm intact).
  4. Set localStorage `psalter-score-mode` to `solfege`, reload → the "Inline Solfège is coming soon"
     message renders (no blank/broken view, no accidental JPG).
</verification>

<success_criteria>
- Inline Solfège cannot be newly selected via the gear (both the Solfège-notation path and the
  Inline-layout path are gated).
- Split-leaf Solfège is fully functional and visually unchanged.
- A stale `viewMode: 'solfege'` renders a clear "coming soon" message.
- All disabled-state messaging uses "coming soon / not yet available" wording (no approval wording).
- No new TypeScript errors in the three modified files.
</success_criteria>

<output>
After completion, create `.planning/quick/260712-u4q-disable-inline-solfege-toggle-until-real/260712-u4q-SUMMARY.md`
</output>
