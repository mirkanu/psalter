---
phase: quick-260713-a5b
plan: 01
type: execute
wave: 1
depends_on: []
subsystem: singing-view
tags: [play-mini-bar, soundcloud, iframe, desktop, first-load, layout-transition]
files_modified:
  - src/components/singing/PlayMiniBar.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "On a fresh desktop page load, switching PlayMiniBar to SoundCloud mode for the FIRST time shows the interactive player (play button present), not the static Download/privacy-policy fallback."
    - "The SoundCloud widget's api-widget.soundcloud.com/me bootstrap request completes on first activation (no net::ERR_ABORTED)."
    - "Switching to a DIFFERENT tune while already in SoundCloud mode still shows the real player (no regression to the previously-working case)."
    - "Mobile SoundCloud first-activation still works (no regression)."
    - "The 260712-lp9 fixes remain intact: sandbox stays 'allow-scripts allow-same-origin' and the desktop md:right-[calc(...)] positioning is unchanged."
  artifacts:
    - path: "src/components/singing/PlayMiniBar.tsx"
      provides: "Iframe mount decoupled from the toggle-triggered layout transition so the SoundCloud frame always mounts into a settled, final-size container"
      contains: "iframe"
  key_links:
    - from: "audio-source-toggle click (setAudioSource -> 'soundcloud')"
      to: "SoundCloud <iframe> first mount"
      via: "deferred mount gated until the bar has reached its stable 80px SC-mode layout"
      pattern: "iframe"
    - from: "PlayMiniBar SoundCloud <iframe>"
      to: "w.soundcloud.com widget"
      via: "sandbox allow-scripts allow-same-origin (unchanged from lp9)"
      pattern: "allow-scripts allow-same-origin"
---

<objective>
Fix the desktop-only SoundCloud first-activation bug: on a fresh page load, the FIRST switch to SoundCloud mode renders SoundCloud's static fallback page (track title + "Download" + "SoundCloud privacy policy" / "Privacy policy" links) inside the iframe instead of the interactive player with a play button. Switching to a different tune afterward works, because at that point only the iframe `src` changes on an already-mounted element in an already-stable bar.

Purpose: SoundCloud recordings are unusable on desktop first-try today. The fix must not regress the two 260712-lp9 fixes (sandbox `allow-same-origin`; desktop `md:right-[calc(...)]` positioning) and must not regress mobile (which currently works first-try).

Output: A modified `PlayMiniBar.tsx` that mounts the SoundCloud iframe into a settled, final-size container so the widget's bootstrap fetch (`api-widget.soundcloud.com/me`) is not aborted mid-init.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@src/components/singing/PlayMiniBar.tsx
@.planning/quick/260712-lp9-fix-playminibar-soundcloud-playback-and-/260712-lp9-SUMMARY.md

<diagnostic_evidence>
Live diagnostic evidence already gathered — do NOT re-derive, build on it:

FROM THE PLANNING SESSION (real deployed build, /psalms/78, desktop 1200x900, via Playwright daemon):
- Reproduced consistently on FRESH load: Play -> dismiss tour -> click [data-audio-source-toggle].
- After ~2.5-3s the [data-play-mini-bar] iframe `src` is CORRECT (https://w.soundcloud.com/player/?url=... correctly encoded), but iframe.contentFrame() body innerText is only:
  "Psalm 37 (Tune: Azmon / Denfield)\nDownload\nSoundCloud privacy policy\nPrivacy policy" — the static fallback, NOT the interactive player.
- page.on('requestfailed') captured `https://api-widget.soundcloud.com/me?client_id=...` failing with net::ERR_ABORTED during this same load. Console showed "SoundCloud Embed Player (api-web)" started but /me never completed.

FROM THIS PLANNING TASK (build-independent isolation harness on a blank page, via daemon):
- Mounting a real SoundCloud iframe into (a) a stable pre-sized 80px/448px container vs (b) a 44px->80px `transition:all 200ms` + width:auto container with the iframe injected in the SAME tick did NOT reproduce the /me abort in EITHER case (repeated 2x each; meFailed empty all 4 runs; a play button was detectable in all). NOTE: SoundCloud now serves a small static shell and hydrates the player client-side, so body innerText length is a POOR discriminator — use presence of the interactive player control (real play button / player DOM), the /me request outcome, and the requestfailed listener as the reliable signals.
- CONCLUSION: the pure "CSS height/width transition at mount" theory does NOT reproduce in isolation. The trigger is context-dependent (React unmount/remount of the audio-controls subtree + real-app first-load timing), so Task 1 must instrument the REAL running app to identify the discriminating factor between the broken first-mount and the working tune-switch, and the fix must be mechanism-agnostic.

CODE FACTS (confirmed, PlayMiniBar.tsx):
- Line 87: `barHeight = isScMode ? 'h-[80px]' : 'h-[44px]'` on the bar root.
- Line 145: bar root has `transition-all duration-200 ease-out` — height AND width are animated when the toggle flips isScMode.
- Line 197: `audioSource === 'abc' ? <AbcAudioControls/> : <>...<iframe .../>...</>` — on first toggle the AbcAudioControls subtree UNMOUNTS and a FRESH <iframe> element MOUNTS, concurrent with the 44->80px + width transition. On a later tune-switch the SAME iframe element stays mounted and only its `src` attribute changes (the proven-working path).
- Line 224: `sandbox="allow-scripts allow-same-origin"` (lp9 fix — MUST stay).
- Line 144: `md:right-[calc(max(0px,(100vw_-_56rem)/2)_+_0.5rem)]` (lp9 fix — MUST stay).
- Runtime env: THIS fix cannot be measured live against localhost:3005 this session — the orchestrator batches ONE rebuild + full live verification of ALL pending fixes after this plan executes. The daemon CAN be used against the CURRENT (pre-fix) build for diagnostics, since the bug reproduces on already-deployed code.
</diagnostic_evidence>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument the real app to identify the broken-first-mount vs working-tune-switch discriminator</name>
  <files>.planning/quick/260713-a5b-fix-soundcloud-desktop-privacy-policy-fa/diagnostic.js (throwaway; not shipped)</files>
  <action>
Write a diagnostic script under this quick-task directory that drives the Playwright daemon (require('/home/services/playwright-daemon/client.js'); runPlaywright returns the script's return value as a string) against the CURRENT deployed build at http://localhost:3005. Do NOT modify any source in this task — this is investigation only. Confirm the daemon is ready first (GET http://localhost:3099/status -> browserReady:true).

Reproduce and instrument BOTH cases on a fresh page load at http://localhost:3005/psalms/78 (or /psalms/37), desktop viewport 1200x900:

CASE A (broken): fresh load -> click [data-singing-play] -> if the onboarding tour appears click its "Got it"/dismiss -> click [data-audio-source-toggle].
CASE B (working control): after A settles into the fallback, switch to a DIFFERENT tune (via the tune switcher / ?tune= change) while staying in SoundCloud mode.

For each case capture, at the moment the SoundCloud iframe first appears / its src is (re)set:
  1. page.on('requestfailed') entries whose URL contains 'soundcloud' — specifically whether api-widget.soundcloud.com/me fails with ERR_ABORTED.
  2. The [data-play-mini-bar] root element's getBoundingClientRect() height and width AND its computed height, sampled repeatedly for ~300ms after the toggle, to determine whether the bar is MID-TRANSITION (height climbing 44->80, width shifting) at the instant the iframe element is inserted, versus already stable in Case B.
  3. Whether the <iframe> element is a NEWLY inserted node in Case A vs a persistent node whose src attribute merely changed in Case B (e.g. tag a mutation observer or compare element identity across the transition).
  4. The reliable "player works" signal: inside iframe.contentFrame(), presence of the interactive player control (a real play button in the widget DOM) — NOT body innerText length (SoundCloud's static shell text is present even when the player works; see diagnostic_evidence).

Determine the discriminating factor and record a short findings note (in your task output / a comment in the script — no SUMMARY file needed):
  - If Case A's bar is measurably mid-transition / not-yet-80px at the instant the fresh iframe mounts, while Case B mounts into a stable 80px bar -> CONFIRMS the "fresh iframe mounts into unsettled layout" mechanism -> Task 2 proceeds with the deferred-mount fix.
  - If Case A's bar is already stable-sized at iframe mount -> the mechanism is NOT layout timing (likely the fresh unmount/remount of the subtree itself, or a client_id/bootstrap race) -> record this so Task 2 adapts (still deferred-mount, but the emphasis shifts to "keep the iframe element persistent / avoid the concurrent subtree remount" rather than "wait for height").
  </action>
  <verify>
    <automated>test -f .planning/quick/260713-a5b-fix-soundcloud-desktop-privacy-policy-fa/diagnostic.js && curl -s http://localhost:3099/status | grep -q '"browserReady":true' && echo INVESTIGATION-READY</automated>
  </verify>
  <done>Diagnostic script exists and has been run against the current build; the discriminating factor between the broken first-mount and the working tune-switch is recorded, and the Task 2 fix approach (deferred/persistent iframe mount) is confirmed or its emphasis adjusted based on the evidence. No source files changed in this task.</done>
</task>

<task type="auto">
  <name>Task 2: Decouple the SoundCloud iframe mount from the toggle-triggered layout change</name>
  <files>src/components/singing/PlayMiniBar.tsx</files>
  <action>
Implement a mechanism-agnostic fix so the SoundCloud iframe always mounts into a settled, final-size bar — the same condition under which the tune-switch case already works. Base the exact emphasis on Task 1's findings, but the DEFAULT approach (use unless Task 1 refutes it) is DEFERRED MOUNT:

1. Add state `const [scIframeReady, setScIframeReady] = useState(false)`.
2. Add an effect keyed on `isScMode` (and `hasSc`): when the bar enters SoundCloud mode, DEFER mounting the iframe until AFTER the bar's layout has settled at its stable 80px SC-mode size. Use a double requestAnimationFrame (or a rAF followed by a ~220ms timeout that outlasts the `duration-200` transition) to flip `setScIframeReady(true)`. When leaving SC mode (isScMode false), reset `setScIframeReady(false)` so a future re-entry defers again. Clean up any pending rAF/timeout on unmount / dep change.
   - IMPORTANT (no regression to the working tune-switch case): once `scIframeReady` is true it STAYS true while in SC mode, so switching tunes (which changes `soundcloudUrl`/`src` without toggling isScMode) keeps the SAME iframe element mounted and merely updates src — exactly today's working path. Do NOT key/remount the iframe on tune change.
3. In the render, gate ONLY the iframe on readiness: render the <iframe> when `audioSource === 'soundcloud' && scIframeReady`; while `isScMode && !scIframeReady` render a lightweight non-iframe placeholder occupying the same space (e.g. a muted "Loading recording..." span in the existing `flex-1 min-w-0` slot) so the bar reaches 80px BEFORE the iframe is inserted. The disclaimer span logic (showDisclaimer) is unaffected.

Constraints (hard):
  - Do NOT change line 224's `sandbox="allow-scripts allow-same-origin"` — it must remain (lp9 fix).
  - Do NOT change line 144's `md:right-[calc(max(0px,(100vw_-_56rem)/2)_+_0.5rem)]` or any lp9 positioning — must remain.
  - Do NOT change the iframe `src` construction (encodeURIComponent(soundcloudUrl!) with the same query params) — only WHEN it mounts.
  - Mobile currently works first-try: the deferred mount only adds a short settle delay before mounting (it never mounts the iframe EARLIER than today), so it cannot regress mobile playback. Verify the effect is not md:-gated — it must run identically on all viewports.
  - Keep the `onHeightChange` ResizeObserver effect (lines 94-108) working: the placeholder must give the bar its 80px height immediately on entering SC mode so reported height stays correct; the effect's `isScMode` dep already re-measures on mode change.
  - If Task 1 showed the bar is ALREADY stable-sized at first mount (layout-timing refuted), still apply the deferred mount (a rAF defer makes the fresh mount happen after the current React commit/paint settles, decoupling it from the concurrent AbcAudioControls unmount) — the fix is robust either way.
  </action>
  <verify>
    <automated>cd /home/services/psalter && grep -c 'sandbox="allow-scripts allow-same-origin"' src/components/singing/PlayMiniBar.tsx | grep -qx 1 && grep -q 'md:right-\[calc(max(0px,(100vw_-_56rem)/2)_+_0.5rem)\]' src/components/singing/PlayMiniBar.tsx && grep -q 'scIframeReady' src/components/singing/PlayMiniBar.tsx && npx tsc --noEmit -p . 2>&1 | grep -c 'PlayMiniBar' | grep -qx 0 && echo FIX-OK</automated>
  </verify>
  <done>PlayMiniBar defers the SoundCloud iframe's first mount until the bar has settled at its 80px SC-mode size (scIframeReady gate), keeping the iframe persistent across tune switches; the lp9 sandbox and desktop positioning tokens are byte-for-byte intact; tsc reports no PlayMiniBar errors. Live confirmation (fallback replaced by real player on desktop first-toggle; mobile + tune-switch unregressed) is deferred to the orchestrator's batched rebuild + verification.</done>
</task>

</tasks>

<verification>
- `grep -c 'sandbox="allow-scripts allow-same-origin"' src/components/singing/PlayMiniBar.tsx` == 1 (lp9 sandbox intact).
- `grep 'md:right-\[calc(max(0px,(100vw_-_56rem)/2)_+_0.5rem)\]'` present (lp9 desktop positioning intact).
- `grep 'scIframeReady'` present (deferred-mount gate added).
- `npx tsc --noEmit -p .` produces no lines mentioning PlayMiniBar.
- Deferred to batched rebuild (orchestrator): on desktop fresh load, first toggle to SoundCloud shows the interactive player (play button) and api-widget.soundcloud.com/me does NOT ERR_ABORT; tune-switch while in SC mode still works; mobile SC first-activation still works.
</verification>

<success_criteria>
Desktop first-activation of SoundCloud mode shows the real interactive player instead of the static Download/privacy-policy fallback, achieved by mounting the iframe only after the bar settles at its stable 80px size — with no regression to the lp9 sandbox/positioning fixes, the working tune-switch path, or mobile.
</success_criteria>

<output>
After completion, create `.planning/quick/260713-a5b-fix-soundcloud-desktop-privacy-policy-fa/260713-a5b-SUMMARY.md`.
</output>
