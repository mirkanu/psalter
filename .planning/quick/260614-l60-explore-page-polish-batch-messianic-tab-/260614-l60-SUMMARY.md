---
phase: quick
plan: 260614-l60
subsystem: explore
tags: [explore, tabs, url-routing, messianic, naves, search, ux-polish]
tech-stack:
  added: [NavesSubTopicList.tsx]
  patterns: [useSearchParams + useRouter for URL-routed tabs, Suspense wrapper for client search params]
key-files:
  created:
    - src/components/NavesSubTopicList.tsx
  modified:
    - src/components/ExploreTabShell.tsx
    - src/components/QuotedInNT.tsx
    - src/components/NavesExpand.tsx
    - src/app/explore/naves/[slug]/page.tsx
    - src/app/explore/topics/[slug]/page.tsx
decisions:
  - ExploreTabShell split into ExploreTabInner (useSearchParams) + ExploreTabShell (Suspense wrapper) to enable URL routing without moving Suspense to page.tsx
  - NavesSubTopicList extracted as separate client component to keep naves/[slug]/page.tsx as server component
metrics:
  duration: ~20min
  completed: 2026-06-14T15:41:39Z
  tasks: 5
  files: 6
---

# Phase quick Plan 260614-l60: Explore Page Polish Batch (Messianic Tab) Summary

10 polish fixes applied across 5 files — URL-routed tabs, dedicated Messianic tab, NT canonical sort, card text wrap, source credits, search filter, collapsible Nave's sub-topics, and mobile alignment.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | ExploreTabShell — URL routing, Messianic tab, card wrap, source credits, tab-bar scroll | 176c46d | ExploreTabShell.tsx |
| 2 | QuotedInNT — canonical NT sort + dedup verse citation | 176c46d | QuotedInNT.tsx |
| 3 | NavesExpand — search input + card wrap | 176c46d | NavesExpand.tsx |
| 4 | naves/[slug] — collapsible sub-topics + dedup citations | 176c46d | NavesSubTopicList.tsx, naves/[slug]/page.tsx |
| 5 | topics/[slug] — items-start on psalm rows | 176c46d | topics/[slug]/page.tsx |

## Fixes Implemented (a-j)

**(a) Messianic tab** — New `<TabsTrigger value="messianic">` with Gem icon between In the NT and Other Topics. Shows MessianicByTopic component + All Messianic Psalms link card.

**(b) URL routing** — ExploreTabInner uses `useSearchParams` + `useRouter`. Tab clicks call `router.replace(?tab=value)`. Direct URL load (`?tab=messianic`) correctly activates the right tab. Wrapped in `<Suspense fallback={null}>` in ExploreTabShell.

**(c) Card wrap fix** — TopicGrid and NavesExpand cards: `items-start`, `h-auto`, `leading-snug` span, no `truncate`. Long topic names now wrap naturally.

**(d) Source credits** — "From Nave's Topical Bible" `<p>` added under "Quoted in the New Testament" heading (In the NT tab) and as first child of Other Topics panel.

**(e) Tab-bar scroll fix** — `overflow-y-hidden` added to both outer and inner `<TabsList>` elements.

**(f) topics/[slug] items-start** — Psalm list rows changed from `items-center` to `items-start`; psalm number span gets `pt-0.5` for top-aligned visual alignment on wrap.

**(g) NT canonical sort** — `NT_BOOK_ORDER` constant array drives `.sort()` in QuotedInNT. Verified: Matthew entries appear before Hebrews/Revelation in rendered output.

**(h) Dedup verse citation** — Both QuotedInNT and NavesSubTopicList: link shows `Psalm X` (no `:verseNumber`); quotation line shows `verseNumber quotation` with leading `X:Y` prefix stripped via regex.

**(i) NavesExpand search** — `useState(query)` + filtered array. Search bypasses the 24-item cap. Show/Hide toggle only appears when query is empty.

**(j) Collapsible Nave's sub-topics** — New `NavesSubTopicList` client component with `SubTopicCollapsible` using Radix Collapsible. `useState(true)` — open by default. Chevron flips on toggle.

## Playwright Verification

Tested via Playwright daemon at localhost:3099 against localhost:3005:

- 10 tabs rendered (6 main + 4 sub-tabs for Themes) — Messianic tab confirmed present
- Clicking "In the NT" sets URL to `?tab=in-the-nt`; source credit visible; Messianic content absent
- Clicking "Other Topics" sets URL to `?tab=other-topics`; source credit visible; search input present
- Nave's sub-topic page `/explore/naves/god`: 6 collapsible triggers, 104 psalm links visible (all open by default)
- NT entries in canonical order: Matthew → Mark → Luke → Acts → Romans → Hebrews → Revelation

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED

- Commit 176c46d exists: `git log --oneline | grep 176c46d`
- All 6 files modified/created and committed
- TypeScript build: 0 errors
- GitHub push: 176c46d on origin/master

## Known Stubs

None.

## Threat Flags

None.
