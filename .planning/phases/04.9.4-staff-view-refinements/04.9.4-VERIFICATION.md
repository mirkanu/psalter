---
phase: 04.9.4-staff-view-refinements
verified: 2026-05-16T00:00:00Z
status: passed
score: 6/6 must-haves verified
overrides_applied: 0
---

# Phase 04.9.4: Staff View Refinements & Onboarding — Verification Report

**Phase Goal:** Header cleanup, first-run tour, dynamic zoom, glass bottom bar, A+/A- relocation, Play/Gear split — staff-view chrome refinements + onboarding.

**Verified:** 2026-05-16
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| #   | Truth (SC) | Status     | Evidence       |
| --- | ---------- | ---------- | -------------- |
| SC1 | Staff view header shows `Psalm N` and tune name on a single row, tune name in smaller font with music-icon prefix; no pencil icon, no `Tune ([meter])` label | VERIFIED | `PsalmTopBar.tsx` lines 103-134: single-row centre group with `Psalm N` button + ♩ glyph + tune name (text-sm text-muted-foreground); no pencil icon present; tapping tune slot calls `onOpenTuneSwitcher`. Playwright `test-04.9.4-full.js` PASS. |
| SC2 | First-visit onboarding tour highlights prev/next, tune name, view controls (+ play); persists 'seen' flag; no re-trigger | VERIFIED | `OnboardingTour.tsx` 157 lines: createPortal to document.body, 4 steps querying `[data-tour-target]`, localStorage key `psalter_tour_v1='done'`, SSR-safe (assumes seen=true pre-mount). Mounted in `SingingView.tsx:352`. Quick + full UAT verify first-visit shows and reload hides. |
| SC3 | Viewport changes recompute zoom proportionally, clamped, respecting no-overflow rule | VERIFIED | `SingingView.tsx:139-178`: `referenceWidthRef` + `baseSizeRef` + debounced 150ms resize/orientationchange/visualViewport listener; computes `clamp(current * newWidth/refWidth, 8, 40)`; 8px delta threshold suppresses URL-bar churn. Manual A+/A- (`handleBaseSizeChange` line 182) resets reference width. Full UAT asserts no scrollWidth overflow at 375/768/1024. |
| SC4 | Glass bottom bar — translucent + backdrop blur — A+/A- anchored left, stanzas centred | VERIFIED | `GlassBottomBar.tsx:51-87`: `bg-background/70 backdrop-blur-md border-t`; A-/A+ buttons left (lines 58-75); `data-stanzas-indicator` centred with text matching `^Stanza N / M$` pattern (line 84). Quick UAT regex-asserts indicator text. |
| SC5 | Bottom-right split — Play toggles glass mini-bar with media controls; Gear opens drawer minus player controls | VERIFIED | `GlassBottomBar.tsx:121-142`: separate Gear + Play buttons (Gear before Play, Play rightmost). `PlayMiniBar.tsx`: sliding glass bar `fixed inset-x-0 bottom-[56px]` with `bg-background/90 backdrop-blur-sm` wrapping `AbcAudioControls`. `GearDrawer.tsx`: Sheet side=bottom with View + About sections only — **no AbcAudioControls import**, audio removed. SingingView wires all three (lines 320-351). |
| SC6 | All controls reachable at 375px with no overflow; FAB/burger drawer behaviour fully replaced | VERIFIED | `TuneSubBar.tsx` and `PsalmActionsFAB.tsx` files do NOT exist under `src/components/singing/`. Only one residual reference exists — a comment in `SiteHeader.tsx:19` noting removal. `loading.tsx` updated to glass-strip skeleton ("replaces FAB + sub-bar" comment line 27). Full UAT asserts `scrollWidth ≤ clientWidth` at 375/768/1024. |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `src/components/singing/PsalmTopBar.tsx` | Single-row header with prev/next, psalm label, tune label slot, `onOpenTuneSwitcher` prop | VERIFIED | 158 lines; `onOpenTuneSwitcher` prop wired to TuneSwitcherSheet via SingingView |
| `src/components/singing/GlassBottomBar.tsx` | Fixed bottom chrome with A+/A-, stanzas indicator, gear, play (≥80 lines) | VERIFIED | 145 lines; all elements present + data attributes for UAT |
| `src/components/singing/PlayMiniBar.tsx` | Sliding mini bar wrapping AbcAudioControls (≥50 lines) | VERIFIED | 82 lines; controlled `isPlaying` + `onPlayingChange`; two-phase mount; transition-end cleanup |
| `src/components/singing/GearDrawer.tsx` | Sheet side=bottom with View + About sections, no audio (≥40 lines) | VERIFIED | 108 lines; no AbcAudioControls import; Separator between View and About |
| `src/components/singing/OnboardingTour.tsx` | Custom React portal tour with spotlight cutout, 4 steps, localStorage persistence (≥60 lines) | VERIFIED | 157 lines; createPortal + measure + Escape + 4 STEPS array + `psalter_tour_v1` localStorage |
| `src/components/singing/SingingView.tsx` | referenceWidthRef + baseSizeRef + debounced resize/orientation listener; lifted isPlaying/currentStanza state | VERIFIED | 355 lines; all required state hoisted; resize handler lines 143-178 |
| `src/components/singing/TuneSubBar.tsx` | DELETED | VERIFIED | File absent from filesystem |
| `src/components/singing/PsalmActionsFAB.tsx` | DELETED | VERIFIED | File absent from filesystem |
| `src/app/psalms/[id]/loading.tsx` | Updated to match new chrome (no sub-bar/FAB; glass strip) | VERIFIED | 35 lines; explicit comment "replaces FAB + sub-bar" |
| `scripts/uat/test-04.9.4-quick.js` | Quick per-commit UAT | VERIFIED | PASS at 375px |
| `scripts/uat/test-04.9.4-full.js` | Full per-wave UAT covering all 6 SCs at 3 breakpoints | VERIFIED | PASS at 375/768/1024 |
| `scripts/uat/test-04.9.4-study-regression.js` | Regression: /psalms/[id]/study still renders | VERIFIED | PASS |

### Key Link Verification

| From | To  | Via | Status |
| ---- | --- | --- | ------ |
| PsalmTopBar | TuneSwitcherSheet | `onOpenTuneSwitcher` callback (set in SingingView:265 → opens `tuneSwitcherOpen`) | WIRED |
| GlassBottomBar | SingingView | `onPlayToggle`, `onGearOpen`, `onBaseSizeChange`, `currentStanza`, `totalStanzas` props (SingingView:320-331) | WIRED |
| PlayMiniBar | AbcAudioControls | `isPlaying` + `onPlayingChange` controlled props (PlayMiniBar:73-78) | WIRED |
| SingingView | GlassBottomBar + PlayMiniBar + GearDrawer + OnboardingTour | All four rendered (lines 320, 333, 342, 352) | WIRED |
| Resize handler | setBaseSize | `window.addEventListener('resize'|'orientationchange', handler)` + `window.visualViewport.addEventListener('resize', ...)` (SingingView:169-171) | WIRED |
| OnboardingTour | document.body | `createPortal(overlay, document.body)` (line 154) | WIRED |
| OnboardingTour | DOM targets | `document.querySelector('[data-tour-target=...]')` (line 29) — targets present in PsalmTopBar (`prev-next`, `tune-name`) and GlassBottomBar (`view-controls`, `play-button`) | WIRED |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| GlassBottomBar | `currentStanza` / `totalStanzas` | NotationRenderer → `onStanzaChange(current,total)` → `handleStanzaChange` (SingingView:201-204) → state → GlassBottomBar prop | Yes — UAT regex `^Stanza \d+ / \d+$` matches | FLOWING |
| PlayMiniBar | `isPlaying` | Lifted in SingingView line 194; updated by `handlePlayToggle` (line 206) and `handlePlayingChange` (line 221) wired from AbcAudioControls | Yes | FLOWING |
| PsalmTopBar | `tuneName` | `activeTune?.name` (SingingView:254) — derived from `primaryTune` / `overrideTune` (URL param) | Yes — rendered in UAT screenshots | FLOWING |
| OnboardingTour | step rect | `getBoundingClientRect()` of `[data-tour-target="..."]` element | Yes — UAT verifies tour mounts | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| `/psalms/23` returns 200 | `curl -s -o /dev/null -w "%{http_code}" http://localhost:3005/psalms/23` | `200` | PASS |
| Quick UAT (375px SC1/SC4/SC6) | `node scripts/uat/test-04.9.4-quick.js` | `PASS: test-04.9.4-quick` | PASS |
| Full UAT (375/768/1024 all 6 SCs) | `node scripts/uat/test-04.9.4-full.js` | `PASS: test-04.9.4-full` | PASS |
| Study regression | `node scripts/uat/test-04.9.4-study-regression.js` | `PASS: test-04.9.4-study-regression` | PASS |

### Requirements Coverage

All plan `requirements` fields reference SC1-SC6 — these are ROADMAP success criteria local to phase 04.9.4, not REQUIREMENTS.md IDs. REQUIREMENTS.md contains no entries scoped to this phase (search for `04.9.4` returned zero matches), which is consistent with the ROADMAP entry stating "Requirements: TBD (to be derived during /gsd-discuss-phase)" — the success criteria became the canonical contract.

| Source Plan | Requirement Ref | Description | Status | Evidence |
| ----------- | --------------- | ----------- | ------ | -------- |
| 04.9.4-01 | SC1 | Header single-row with tune name | SATISFIED | PsalmTopBar.tsx + UAT |
| 04.9.4-02 | SC4, SC5, SC6 | Glass bottom bar; Play/Gear split; no overflow | SATISFIED | GlassBottomBar / PlayMiniBar / GearDrawer + UAT |
| 04.9.4-03 | SC3 | Dynamic zoom on viewport change | SATISFIED | SingingView resize handler + UAT |
| 04.9.4-04 | SC2 | First-run onboarding tour | SATISFIED | OnboardingTour.tsx + UAT |
| 04.9.4-05 | SC1-SC6 | Playwright UAT scripts proving all SCs | SATISFIED | All three UAT scripts PASS |

No orphaned requirements detected.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none) | — | — | — | No TODO/FIXME/placeholder/stub patterns found in modified files; no hardcoded empty data flowing to render |

### Human Verification Required

(None — visual sign-off per Plan 05's "human visual checkpoint" was completed as part of the wave-5 execution, and all automated checks pass at this verification.)

### Gaps Summary

No gaps. All six ROADMAP success criteria are realised in code and verified by three independent Playwright suites. Legacy components (TuneSubBar, PsalmActionsFAB) are fully removed from the filesystem; only a removal-trail comment remains in SiteHeader.tsx. Dynamic zoom uses refs to avoid stale closures, debounced at 150ms with an 8px delta threshold and clamp(8,40). The OnboardingTour is SSR-safe (defaults seen=true pre-mount to suppress flash) and persists `psalter_tour_v1` to localStorage.

---

_Verified: 2026-05-16_
_Verifier: Claude (gsd-verifier)_
