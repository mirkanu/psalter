---
phase: 04.9.4-staff-view-refinements
plan: 01
subsystem: ui
tags: [react, nextjs, tailwind, header, singing-view]

requires:
  - phase: 04.9.3-staff-view-mvp
    provides: PsalmTopBar base implementation, TuneSwitcherSheet pattern
provides:
  - Single-row PsalmTopBar with promoted tune label
  - Optional tuneName + onOpenTuneSwitcher props (back-compat preserved)
  - data-tour-target hooks (prev-next, psalm-label, tune-name) for upcoming onboarding tour
affects: [04.9.4-02 (SingingView wiring), 04.9.4 tour, 04.9.4 glass bottom-bar]

tech-stack:
  added: []
  patterns:
    - Unicode glyph (U+2669 quarter note) replaces lucide icon for tune affordance
    - Optional callback props with non-interactive span fallback for staged rollout

key-files:
  created: []
  modified:
    - src/components/singing/PsalmTopBar.tsx

key-decisions:
  - "Make tuneName + onOpenTuneSwitcher optional so SingingView (pre-Plan-02) still compiles"
  - "Render tune slot as non-interactive span when onOpenTuneSwitcher is undefined"
  - "Use Unicode ♩ glyph instead of a lucide icon (matches UI-SPEC §1 semantic intent)"
  - "Apply data-tour-target='prev-next' only to the prev arrow (left spotlight anchor)"

patterns-established:
  - "Header centre group: flex-1 justify-center items-center gap-2 with two content-sized buttons"
  - "Promoted-but-secondary control: text-sm font-normal text-muted-foreground + hover:text-foreground"

requirements-completed: [SC1]

duration: 5min
completed: 2026-05-16
---

# Phase 04.9.4 Plan 01: Header Restructure Summary

**PsalmTopBar collapsed to a single row with the tune name promoted alongside the psalm number via a tappable ♩-prefixed label, removing the pencil affordance and the "Tune (meter)" prefix.**

## Performance

- **Duration:** ~5 min
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Centre group now holds two tap targets (psalm label, tune label) instead of one stretched psalm button
- Tune name truncates at `max-w-[40vw]` while psalm label never truncates (UI-SPEC overflow rule)
- Three `data-tour-target` hooks added for the upcoming onboarding tour
- Back-compat preserved: legacy SingingView callsite (no tune props) still renders a non-interactive `♩ —` placeholder

## Task Commits

1. **Task 1: Add two-slot centre group to PsalmTopBar** — `4d1277d` (feat)

## Files Created/Modified
- `src/components/singing/PsalmTopBar.tsx` — Single-row header with prev/next arrows, psalm label button, tune label button (or span fallback), tour-target hooks

## Decisions Made
- Optional props with safe defaults so Plan 02 can wire SingingView independently
- Unicode `♩` over a lucide icon — matches UI-SPEC and avoids importing a new icon
- Only `prev` arrow carries `data-tour-target="prev-next"` (next arrow untouched, per plan instruction)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- `pnpm` in this worktree errors with `ERR_UNKNOWN_BUILTIN_MODULE` (corepack/Node 20 issue, unrelated to this change). Worked around by invoking `node_modules/.bin/tsc` and `node_modules/.bin/eslint` from the parent repo. Both passed for `PsalmTopBar.tsx`. Pre-existing TS errors in `tests/e2e/abc-player.spec.ts` and `tests/tune-notation.spec.ts` are out of scope (Rule scope boundary).

## User Setup Required

None.

## Next Phase Readiness
- PsalmTopBar exposes `tuneName` + `onOpenTuneSwitcher` props — ready for Plan 02 to wire from SingingView and mount the TuneSwitcherSheet.
- Tour-target attributes in place for the onboarding tour plan.

## Self-Check: PASSED
- File exists: `src/components/singing/PsalmTopBar.tsx`
- Commit found: `4d1277d`
- grep `data-tour-target="prev-next"`: 2
- grep `data-tour-target="psalm-label"`: 1
- grep `data-tour-target="tune-name"`: 1
- grep `♩`: 2
- grep pencil|"Tune (" outside comments: 0
- tsc: no PsalmTopBar errors
- eslint PsalmTopBar.tsx: clean

---
*Phase: 04.9.4-staff-view-refinements*
*Plan: 01*
*Completed: 2026-05-16*
