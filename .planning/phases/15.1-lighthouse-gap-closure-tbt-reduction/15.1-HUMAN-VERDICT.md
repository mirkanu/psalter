---
phase: 15.1-lighthouse-gap-closure-tbt-reduction
verdict: branch_c_iterate
recorded_at: 2026-08-12T22:00:00Z
---

# Human Verdict — Phase 15.1 Task 5

## The user's instruction (verbatim)

> Re-run on localhost, compare apples-to-apples (Recommended)

## What this meant in practice

Run the same `lighthouse` CLI used in Phase 15's baseline (v13.4.1, mobile, `--throttling-method=simulate`) against the wave-1+wave-2 production build at `http://localhost:3005`, with no Cloudflare Tunnel or `cdn-cgi` bot-challenge in front. Then compare Phase-15-baseline-on-localhost ↔ Phase-15.1-on-localhost, treating the earlier production-URL re-audit (which had Cloudflare's cdn-cgi tax added) as supplementary noise rather than the primary truth.

## What the apples-to-apples measurement showed

| Route | Phase 15 baseline (localhost, q=75) | Phase 15.1 (localhost, q=90) | Delta |
|---|---|---|---|
| / | 0.69 | 0.47 | **-22 (regression)** |
| /psalms | 0.61 | 0.60 | -1 (flat) |
| /psalms/23 | 0.65 | 0.67 | +2 (flat) |

Branch C of Plan 15.1-03 Task 5 is therefore triggered: any score < 0.80 → iterate. All three are below 0.80.

JSON evidence:
- `.planning/phases/15.1-lighthouse-gap-closure-tbt-reduction/15.1-lighthouse-{homepage,psalms,psalm-detail}-localhost.json` (apples-to-apples, controlled)
- `.planning/phases/15.1-lighthouse-gap-closure-tbt-reduction/15.1-lighthouse-{homepage,psalms,psalm-detail}.json` (production URL, includes cdn-cgi penalty)
- `.planning/phases/15.1-lighthouse-gap-closure-tbt-reduction/15.1-lighthouse-analysis.txt` (full diagnostic + recommended followups)

## Outcome — recorded by orchestrator

- [x] Branch C applied — Phase 15.1 closes as "code shipped, goal unmet"
- [x] REQUIREMENTS.md POLISH-03 row flipped back to `[ ] deferred` with the apples-to-apples evidence inline
- [x] STATE.md `stopped_at` updated to reflect 15.1 wrap-up, plan count back to 49
- [x] ROADMAP.md Phase 15.1 marked `[x]` with explanation that the goal was not met
- [ ] Next phase not auto-advanced. User to choose (Cloudflare WAF rule, debug `/` HTML-parse regression, or skip Phase 15 entirely and move to Phase 6 / 10)
