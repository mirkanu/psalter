---
wave: 1
depends_on: []
files_modified:
  - src/components/notation/NotationRenderer.tsx
  - src/components/singing/SingingView.tsx
  - src/components/singing/GearPopover.tsx
autonomous: true
requirements: [UI-06, UI-07]
---

# Phase 04.9.14-02-PLAN: Inline View — Solfege ABC Rendering + Approval Gate

## Objective
Implement inline view for solfege using abcjs (matching staff view behavior) with approval gate from melisma editor.

## Tasks

### Task 1: Enable abcjs rendering for solfege in inline view
<read_first>
  - src/components/notation/NotationRenderer.tsx (lines 1078-1160 solfege branch)
  - src/components/singing/SingingView.tsx (viewMode handling)
  - src/components/singing/GearPopover.tsx (inline toggle)
</read_first>
<acceptance_criteria>
  - NotationRenderer.tsx: solfege branch renders abcjs SVG when `viewMode === 'solfege'` or `'solfege-split'` (inline modes)
  - Uses `activeTune.abcNotation` with `sopranoOnly(pickAbcWithMarkers())` pipeline
  - Falls back to JPG when no ABC available
  - Inline mode (not split-leaf) shows notation + lyrics interleaved like staff view
</acceptance_criteria>
<action>
In NotationRenderer.tsx solfege branch (around line 1078), add condition for inline solfege modes:
- When `viewMode === 'solfege'` (inline, not split), render AbcPlayer with solfege ABC
- Derive solfege ABC from `activeTune.abcNotation` via `sopranoOnly(pickAbcWithMarkers(abcSatb, abcNotation))`
- Use same AbcPlayer props as staff view (scale, staffWidthFactor, etc.)
- Keep existing JPG fallback via `solfegeJpgUrl` when ABC not available
- In split-leaf solfege mode (`viewMode === 'solfege-split'`), keep existing JPG + thumbnail behavior
</action>

### Task 2: Wire solfege ABC through SingingView
<read_first>
  - src/components/singing/SingingView.tsx (activeTune derivation, abc variable)
  - src/lib/utils.ts (pickAbcWithMarkers, sopranoOnly)
</read_first>
<acceptance_criteria>
  - SingingView.tsx computes `solfegeAbc` variable using `sopranoOnly(pickAbcWithMarkers(activeTune.abcSatb, activeTune.abcNotation))`
  - Passes `solfegeAbc` to NotationRendererClient as new prop
  - NotationRenderer accepts `solfegeAbc?: string` prop
  - TypeScript compiles without errors
</acceptance_criteria>
<action>
1. In SingingView.tsx, add import: `import { pickAbcWithMarkers, sopranoOnly } from '@/lib/utils'`
2. After `const abc = ...` (line 320), add:
   ```typescript
   const solfegeAbc = activeTune
     ? sopranoOnly(pickAbcWithMarkers(
         (activeTune as { abcSatb?: string | null }).abcSatb ?? null,
         activeTune.abcNotation ?? null
       ) ?? '')
     : ''
   ```
3. Pass `solfegeAbc` to NotationRendererClient
4. In NotationRenderer.tsx, add `solfegeAbc?: string` to props interface
5. In solfege branch, use `solfegeAbc` prop when `viewMode === 'solfege'` (inline)
</action>

### Task 3: Approval gate for inline solfege view
<read_first>
  - src/components/singing/SingingView.tsx (melismaStatus state, line 245)
  - src/components/singing/GearPopover.tsx (solfegeAvailable prop, line 44)
  - src/app/api/dev/melisma-decision/route.ts (status values)
</read_first>
<acceptance_criteria>
  - When `melismaStatus === 'not_approved'`, inline solfege view (`viewMode === 'solfege'`) is disabled
  - GearPopover shows solfege toggle as disabled (opacity-40, cursor-not-allowed) with tooltip "Notation not yet approved"
  - If user somehow reaches solfege inline view with unapproved tune, show inline message: "This tune's notation hasn't been approved yet. Please check the melisma editor."
  - Staff view (`viewMode === 'staff'`) remains available regardless of approval status
</acceptance_criteria>
<action>
1. In SingingView.tsx, add derived state:
   ```typescript
   const isTuneApproved = melismaStatus === 'approved'
   const canUseSolfegeInline = isTuneApproved || viewMode === 'staff' // staff always allowed
   ```
2. In GearPopover.tsx, `solfegeAvailable` prop becomes `solfegeAvailable={isTuneApproved}` for inline, but keep `staffAvailable={true}` always
3. In viewMode change handler, if target is 'solfege' or 'solfege-split' and !isTuneApproved, show toast and prevent change
4. In NotationRenderer.tsx solfege inline branch, add guard:
   ```tsx
   if (viewMode === 'solfege' && !isTuneApproved) {
     return <div className="p-4 text-center text-muted-foreground">
       This tune's notation hasn't been approved yet. Please check the melisma editor.
     </div>
   }
   ```
5. Pass `isTuneApproved` from SingingView to NotationRendererClient
</action>

### Task 4: Update GearPopover approval logic
<read_first>
  - src/components/singing/GearPopover.tsx (handleNotationChange, solfegeAvailable)
</read_first>
<acceptance_criteria>
  - GearPopover shows solfege radio as disabled when `solfegeAvailable={false}`
  - Tooltip on disabled solfege: "Notation not yet approved — use melisma editor to approve"
  - Staff radio always enabled
  - Layout sub-toggle (split-leaf/inline) only shows when Music Notes active
</acceptance_criteria>
<action>
In GearPopover.tsx:
1. Update `handleNotationChange` to check `solfegeAvailable` before allowing solfege selection
2. Add `disabled={!solfegeAvailable}` to solfege radio button
3. Add tooltip/description when disabled: "This tune's melisma positions haven't been approved. Approve in the melisma editor to enable solfege view."
4. Ensure `solfegeAvailable` prop is correctly passed from SingingView (based on `melismaStatus === 'approved'`)
</action>

## Verification
- [ ] Inline solfege view renders abcjs SVG (not JPG) for approved tunes
- [ ] Inline solfege shows lyrics interleaved like staff view
- [ ] Unapproved tunes show disabled solfege toggle in gear popover
- [ ] Unapproved tunes show informative message in inline solfege view
- [ ] Staff view works regardless of approval status
- [ ] Split-leaf solfege still uses JPG + thumbnails (unchanged)
- [ ] TypeScript compiles, no console errors