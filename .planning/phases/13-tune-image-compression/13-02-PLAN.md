---
phase: 13-tune-image-compression
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - scripts/verify-tunes-compression.ts
  - .planning/phases/13-tune-image-compression/BACKUP-PREFLIGHT-EVIDENCE.md
  - public/tunes-compressed/
autonomous: true
requirements: [ASSET-01]

must_haves:
  truths:
    - "The Phase 6 backup is proven restorable one last time while the live tree still matches it"
    - "A verifier exists that checks every one of the 326 files, not a sample"
    - "All 320 compressed JPEGs are strictly smaller than their originals"
    - "All 320 compressed JPEGs decode as valid JPEG with non-zero, aspect-preserved dimensions"
    - "The 6 PNGs and the two dotfiles are carried into the staging directory byte-identical"
    - "public/tunes/ is still byte-for-byte unchanged after the full batch run"
  artifacts:
    - path: "scripts/verify-tunes-compression.ts"
      provides: "Full-set size/decode/dimension/byte-identity verification with assert() + exit code"
      contains: "COMPRESSION_VERIFIED"
    - path: ".planning/phases/13-tune-image-compression/BACKUP-PREFLIGHT-EVIDENCE.md"
      provides: "Recorded pre-swap proof that the Phase 6 archive is restorable and byte-identical to the live tree"
      contains: "BACKUP_VERIFIED"
    - path: "public/tunes-compressed/"
      provides: "328 staged files (320 compressed jpg + 6 copied png + .gitignore + .gitkeep) awaiting the Plan 03 swap"
  key_links:
    - from: "scripts/verify-tunes-compression.ts"
      to: "public/tunes/ and public/tunes-compressed/"
      via: "per-file statSync size comparison + sharp metadata decode"
      pattern: "statSync"
    - from: "scripts/compress-tunes.ts"
      to: "public/tunes-compressed/"
      via: "default --out staging directory"
      pattern: "public/tunes-compressed"
---

<objective>
Take the last full proof that the Phase 6 backup is restorable, then compress all 326 files into a staging directory and verify every single one of them — while the live site is still serving the untouched originals.

Purpose: This is the point of no return preparation. After this plan, a fully verified compressed tree exists on disk but nothing user-facing has changed. Plan 03 does the swap. Splitting it this way means the swap step has zero unknowns left in it.
Output: `scripts/verify-tunes-compression.ts`, `BACKUP-PREFLIGHT-EVIDENCE.md`, and a fully verified `public/tunes-compressed/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-tune-image-compression/13-RESEARCH.md
@.planning/phases/13-tune-image-compression/13-PATTERNS.md
@.planning/phases/13-tune-image-compression/13-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 and from Phase 6. Use these directly. -->

From scripts/compress-tunes.ts (Plan 01 — already built and human-tuned):
```
npx tsx scripts/compress-tunes.ts [--quality=N] [--width=N] [--out=<dir>] [--limit=N] [--only=<substr>] [--force] [--skip-backup-check]
# defaults: quality/width = the human-approved values from Plan 01 Task 3
#           out = public/tunes-compressed
# prints "Backup pre-flight OK: <sha256>" then "COMPRESSION_COMPLETE"
# writes scripts/output/compress-tunes-report.json
```

From scripts/verify-migration.ts (lines 29-38, 249-273) — the assert/report/exit-code idiom to reuse verbatim:
```typescript
let failures = 0

function assert(condition: boolean, message: string): void {
  if (condition) {
    console.log(`  + ${message}`)
  } else {
    console.error(`  FAIL: ${message}`)
    failures++
  }
}

async function main() {
  // ... run checks, populating `failures` via assert() ...
  console.log('\n==============================')
  if (failures === 0) { console.log('All checks passed') }
  else { console.error(`${failures} check(s) failed`) }
  console.log('==============================')
  process.exit(failures > 0 ? 1 : 0)
}

main().catch((err) => { console.error('Verification failed:', err); process.exit(1) })
```

From scripts/verify-tunes-backup.sh (Phase 6, DO NOT MODIFY) — what it does and what it prints:
```
bash scripts/verify-tunes-backup.sh [archive-path]
# 1. extracts the archive to a mktemp dir
# 2. file-count check: live public/tunes vs restored == 326 each
# 3. md5 byte-identity diff: live public/tunes vs restored  <-- ONLY valid BEFORE the swap
# 4. sharp decode of 10 restored samples (>=3 solfege, >=3 staff)
# prints "BACKUP_VERIFIED" and exits 0 on success
```
</interfaces>

<ground_truth>
- Live `public/tunes/` contains 328 entries: 320 `.jpg`, 6 `.png`, `.gitignore`, `.gitkeep`.
- Backup archive sha256: `28a83a7d0497db7353d7919293004b07533144a26f62a7a3fb899482e5b714a9` at `/home/services/psalter-backups/tunes-pre-compression-20260730.tar.gz`.
- `verify-tunes-backup.sh` extracts ~1.4GB into a `/tmp` mktemp dir; `/` has 9.5G available, so this is safe. It cleans up via a `trap ... EXIT`.
- Step 3's md5 diff passes **only while `public/tunes/` is untouched**. This plan is the last moment it can pass. Plan 03 must never re-run it (RESEARCH.md Pitfall 3).
- `src/lib/tune-jpg-urls.test.ts` mocks `fs.existsSync` — it does not read real files, so compression cannot break it. It is still re-run here as a regression guard.
</ground_truth>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Last-chance full backup restorability proof, recorded as evidence</name>

  <files>.planning/phases/13-tune-image-compression/BACKUP-PREFLIGHT-EVIDENCE.md</files>

  <read_first>
    - scripts/verify-tunes-backup.sh (all 97 lines — understand exactly which check becomes invalid after the swap)
    - .planning/phases/06-security-data-safety-prerequisites/BACKUP-MANIFEST.md (the recorded checksum and restore procedure)
    - .planning/phases/13-tune-image-compression/13-RESEARCH.md lines 192-196 (Pitfall 3 — why this must happen now, not after)
  </read_first>

  <action>
Run these four commands and capture their real output — do not paraphrase, paste the actual values into the evidence file:

1. `bash scripts/verify-tunes-backup.sh` — must print `BACKUP_VERIFIED` and exit 0. This runs the file-count check (326 vs 326), the md5 byte-identity diff of every one of the 326 images against the live tree, and a 10-file sharp decode. **This is the last run where step 2's md5 diff can legitimately pass** — after Plan 03's swap, `public/tunes/` is intentionally different and this script must never be used as the post-compression backup check again.
2. `sha256sum /home/services/psalter-backups/tunes-pre-compression-20260730.tar.gz`
3. `stat -c '%Y %s %n' /home/services/psalter-backups/tunes-pre-compression-20260730.tar.gz` — expect mtime `1785394660` and size `1431381138`
4. `find public/tunes -type f | wc -l` (expect 328) and `find public/tunes -type f \( -name '*.jpg' -o -name '*.png' \) | wc -l` (expect 326) and `du -sh public/tunes`

Create `.planning/phases/13-tune-image-compression/BACKUP-PREFLIGHT-EVIDENCE.md` with:
- An `## Archive identity (pre-compression)` table: path, sha256, size in bytes, mtime epoch, human-readable mtime
- An `## Restorability proof` section: the full stdout of `verify-tunes-backup.sh`, in a fenced block, ending with `BACKUP_VERIFIED`
- An `## Live tree state (pre-compression)` section: the three counts/sizes from step 4
- A `## Post-swap verification note` section stating explicitly, in these words: "After the Plan 03 swap, `scripts/verify-tunes-backup.sh` will FAIL its md5 byte-identity step by design — `public/tunes/` is intentionally different from the archive. Post-swap, backup integrity is proven by `scripts/verify-backup-archive.sh` (archive-internal checks + sha256), never by diffing against the live tree."
- A `## Restore command` section repeating the one-liner from BACKUP-MANIFEST.md: `tar xzf /home/services/psalter-backups/tunes-pre-compression-20260730.tar.gz -C /home/services/psalter/public`

If `verify-tunes-backup.sh` does NOT print `BACKUP_VERIFIED`, stop the whole phase and report — do not proceed to Task 2. A compression phase with an unproven backup is the exact failure mode Phase 6 existed to prevent.
  </action>

  <verify>
    <automated>bash scripts/verify-tunes-backup.sh 2>&1 | tail -1 | grep -qx BACKUP_VERIFIED && grep -q BACKUP_VERIFIED .planning/phases/13-tune-image-compression/BACKUP-PREFLIGHT-EVIDENCE.md && grep -q 28a83a7d0497db7353d7919293004b07533144a26f62a7a3fb899482e5b714a9 .planning/phases/13-tune-image-compression/BACKUP-PREFLIGHT-EVIDENCE.md && echo TASK1_OK</automated>
  </verify>

  <acceptance_criteria>
    - `bash scripts/verify-tunes-backup.sh` exits 0 and its final stdout line is exactly `BACKUP_VERIFIED`
    - `.planning/phases/13-tune-image-compression/BACKUP-PREFLIGHT-EVIDENCE.md` exists and contains the literal string `28a83a7d0497db7353d7919293004b07533144a26f62a7a3fb899482e5b714a9`
    - The evidence file contains `1431381138` (archive size) and `1785394660` (archive mtime epoch)
    - The evidence file contains `BACKUP_VERIFIED` inside a fenced code block reproducing the script's real output
    - The evidence file contains a section headed `## Post-swap verification note` naming `scripts/verify-backup-archive.sh`
    - `git diff --stat scripts/verify-tunes-backup.sh` is empty — the Phase 6 script was not modified
  </acceptance_criteria>

  <done>There is a dated, recorded proof on disk that the Phase 6 archive restores to a tree byte-identical to the live originals, taken at the last moment that proof is possible.</done>
</task>

<task type="auto">
  <name>Task 2: verify-tunes-compression.ts — every file, not a sample</name>

  <files>scripts/verify-tunes-compression.ts</files>

  <read_first>
    - scripts/verify-migration.ts lines 1-45 and 245-274 (assert helper, main/report/exit-code shape)
    - scripts/verify-tunes-backup.sh lines 52-97 (the sharp decode-check block being generalised from 10 files to all 326)
    - scripts/compress-tunes.ts (Plan 01 — the routing rules this verifier must mirror: jpg re-encoded, png/dotfiles copied verbatim)
    - .planning/phases/13-tune-image-compression/13-PATTERNS.md lines 122-192 (the exact patterns to port)
  </read_first>

  <action>
Create `scripts/verify-tunes-compression.ts` using the `scripts/verify-migration.ts` skeleton: `let failures = 0`, the `assert(condition, message)` helper copied verbatim, `async function main()`, and `main().catch(...)` at the bottom. No DB connection needed — drop the `client.end()` calls from the analog. Manual argv parsing.

Constants and flags:
```typescript
const ORIGINAL_DIR = join(process.cwd(), 'public/tunes')
const DEFAULT_COMPRESSED_DIR = join(process.cwd(), 'public/tunes-compressed')
const MAX_WIDTH = 2000              // upper bound; --width=N overrides to match the Plan 01 setting
```
Flags: `--compressed=<dir>` (default `public/tunes-compressed`), `--original=<dir>` (default `public/tunes`), `--width=N` (the width used at compression time, default 2000), `--partial` (skip the whole-set count equality check and only verify files that exist in the compressed dir — used for fast smoke runs).

Set `sharp.cache(false)` and `sharp.concurrency(1)` at module top. All checks run in a plain sequential `for...of` — no `Promise.all`.

Checks, in order:

**A. Set-level (skipped under `--partial`):** `assert` that the compressed dir contains exactly as many entries as the original dir (expect 328), and that the set of filenames is identical in both directions (report any file present in one and missing in the other by name, not just a count).

**B. Per-file, iterating every entry of the original dir sorted ascending:**
- `assert(existsSync(dest), \`${name}: output exists\`)`; if missing, `continue` (matches PATTERNS.md's loop shape).
- For `.jpg` / `.jpeg`:
  - `const origSize = statSync(src).size; const compSize = statSync(dest).size`
  - `assert(compSize < origSize, \`${name}: ${origSize} -> ${compSize} bytes (smaller)\`)` — strict `<`, not `<=`. This is success criterion #1 and it must hold for all 320 JPEGs.
  - `const meta = await sharp(dest).metadata()`
  - `assert(meta.format === 'jpeg', ...)`
  - `assert(!!meta.width && !!meta.height && meta.width > 0 && meta.height > 0, \`${name}: decodes (${meta.width}x${meta.height})\`)`
  - `assert(meta.width! <= width, \`${name}: width ${meta.width} <= ${width}\`)` — proves the resize was applied and nothing was upscaled.
  - Aspect ratio preserved: read `const origMeta = await sharp(src).metadata()` and `assert(Math.abs((meta.width!/meta.height!) - (origMeta.width!/origMeta.height!)) < 0.01, \`${name}: aspect ratio preserved\`)`.
- For `.png` and for any non-image file (`.gitignore`, `.gitkeep`): `assert` byte-identity — same `statSync().size` AND same sha256 (`createHash('sha256').update(readFileSync(path)).digest('hex')`; these files are all under 200KB so reading them into memory is fine). Message shape: `\`${name}: copied byte-identical\``.

**C. Aggregate summary** printed before the final banner: total original bytes, total compressed bytes, absolute saving, percentage reduction to one decimal, count of jpegs checked, count of files copied verbatim, and the single largest and smallest per-file reduction percentage with their filenames.

**D. Exit contract:** print `COMPRESSION_VERIFIED` on its own line ONLY when `failures === 0`, then `process.exit(failures > 0 ? 1 : 0)`. Mirror `verify-tunes-backup.sh`'s `BACKUP_VERIFIED` sentinel convention.

Progress: log `[i/total]` every 50 files so a 326-file run does not look hung.

Smoke-test it as part of this task (this is the task's automated verify): compress 12 files to a throwaway dir and verify them in `--partial` mode, then delete the throwaway dir.
  </action>

  <verify>
    <automated>npx tsx scripts/compress-tunes.ts --limit=12 --out=/tmp/t13-verify-smoke --force >/dev/null 2>&1 && npx tsx scripts/verify-tunes-compression.ts --compressed=/tmp/t13-verify-smoke --partial 2>&1 | tail -3 | grep -q COMPRESSION_VERIFIED && rm -rf /tmp/t13-verify-smoke && echo TASK2_OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -q "COMPRESSION_VERIFIED" scripts/verify-tunes-compression.ts` succeeds
    - `grep -q "function assert" scripts/verify-tunes-compression.ts` succeeds
    - `grep -c "Promise.all" scripts/verify-tunes-compression.ts` returns 0
    - `grep -q "sharp.concurrency(1)" scripts/verify-tunes-compression.ts` succeeds
    - `grep -q "compSize < origSize" scripts/verify-tunes-compression.ts` succeeds (strict inequality, not `<=`)
    - `npx tsx scripts/verify-tunes-compression.ts --compressed=/tmp/t13-verify-smoke --partial` on a 12-file staging dir exits 0 and prints `COMPRESSION_VERIFIED`
    - Deliberate-failure check: `cp public/tunes/.gitkeep /tmp/t13-neg/` style tampering is not required, but the verifier must exit 1 when pointed at an empty dir — confirm with `mkdir -p /tmp/t13-empty && npx tsx scripts/verify-tunes-compression.ts --compressed=/tmp/t13-empty; echo $?` printing a non-zero code and NOT printing `COMPRESSION_VERIFIED`
    - `git status --porcelain public/tunes/` is empty
  </acceptance_criteria>

  <done>A verifier exists that checks all 326 images plus both dotfiles for existence, strict size reduction, valid JPEG decode, width bound, aspect preservation, and byte-identity of the copied-through files — and refuses to print its success sentinel unless every check passes.</done>
</task>

<task type="auto">
  <name>Task 3: Full 326-file batch compression run and full verification</name>

  <files>public/tunes-compressed/</files>

  <read_first>
    - scripts/compress-tunes.ts (confirm DEFAULT_QUALITY / DEFAULT_WIDTH match the Plan 01 Task 3 decision before running)
    - scripts/verify-tunes-compression.ts (the verifier just written)
    - .planning/phases/13-tune-image-compression/13-01-SUMMARY.md (the chosen variantId)
  </read_first>

  <action>
Pre-flight, in order:
1. `free -h` and `df -h /home/services` — record both in the summary. If available memory is under 200Mi or free disk is under 2GB, stop and report rather than starting a 326-file run.
2. Confirm `scripts/compress-tunes.ts`'s `DEFAULT_QUALITY` and `DEFAULT_WIDTH` equal the human-approved variant recorded in `13-01-SUMMARY.md`. If they disagree, the Plan 01 checkpoint was not applied — stop and report.

Run the full batch with defaults (no `--quality`/`--width` overrides — the defaults ARE the approved decision, and passing them again invites drift):
```bash
npx tsx scripts/compress-tunes.ts --force 2>&1 | tee scripts/output/compress-tunes-run.log
```
This writes to `public/tunes-compressed/` and will take several minutes. Expect: `Backup pre-flight OK: 28a83a7d...` near the start, periodic `[i/326] rss=...MB` lines, and `COMPRESSION_COMPLETE` at the end. Watch the rss values — if they climb past ~800MB the sequential guarantee has been broken and the run should be killed.

Then verify the full set:
```bash
npx tsx scripts/verify-tunes-compression.ts 2>&1 | tee scripts/output/verify-tunes-compression.log
```
This must print `COMPRESSION_VERIFIED` and exit 0.

If any file reports `did not shrink` or any assert fails: do NOT proceed to Plan 03 and do NOT hand-patch individual files. Report the failing filenames and their before/after sizes, and stop. The likeliest cause is an already-low-resolution source (RESEARCH.md measured even the smallest 172KB/1170x930 file shrinking 69%, so a non-shrinking file is genuinely anomalous and worth a human look).

Finally, run the regression guard: `npx vitest run` (non-watch — never plain `npm test`, which watches). Nothing in `src/` changed, so this must be green apart from any pre-existing failures already logged in `.planning/` (14 pre-existing failures are documented in STATE.md under Phase 09-07; compare against that baseline rather than assuming green).

Record in the summary: files processed, total bytes before/after, percentage reduction, `notSmaller` count (must be 0), peak rss observed, and wall-clock runtime.
  </action>

  <verify>
    <automated>test $(ls -A public/tunes-compressed | wc -l) -eq 328 && npx tsx scripts/verify-tunes-compression.ts 2>&1 | tail -3 | grep -q COMPRESSION_VERIFIED && test $(find public/tunes -type f | wc -l) -eq 328 && sha256sum /home/services/psalter-backups/tunes-pre-compression-20260730.tar.gz | grep -q 28a83a7d0497db7353d7919293004b07533144a26f62a7a3fb899482e5b714a9 && echo TASK3_OK</automated>
  </verify>

  <acceptance_criteria>
    - `ls -A public/tunes-compressed | wc -l` returns exactly `328`
    - `ls public/tunes-compressed/*.jpg | wc -l` returns `320` and `ls public/tunes-compressed/*.png | wc -l` returns `6`
    - `test -f public/tunes-compressed/.gitignore && test -f public/tunes-compressed/.gitkeep` succeeds
    - `grep -q COMPRESSION_COMPLETE scripts/output/compress-tunes-run.log` succeeds
    - `grep -c "did not shrink" scripts/output/compress-tunes-run.log` returns `0`
    - `npx tsx scripts/verify-tunes-compression.ts` exits 0 and prints `COMPRESSION_VERIFIED`
    - `du -sh public/tunes-compressed` reports a total under 500M (originals are 1.4G; RESEARCH.md measured 78–91% reduction)
    - `find public/tunes -type f | wc -l` still returns `328` and `du -sh public/tunes` still reports `1.4G` — the live tree is untouched
    - `sha256sum /home/services/psalter-backups/tunes-pre-compression-20260730.tar.gz` still returns `28a83a7d0497db7353d7919293004b07533144a26f62a7a3fb899482e5b714a9`
    - `git status --porcelain` shows no `public/tunes-compressed` entries (the Plan 01 .gitignore rule is holding)
    - `npx vitest run` failure count equals the documented pre-existing baseline (no new failures)
  </acceptance_criteria>

  <done>A fully verified compressed copy of all 328 files sits in `public/tunes-compressed/`, every JPEG strictly smaller and validly decodable, with the live site still serving the untouched originals and the backup archive checksum unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| batch script → `public/tunes/` | 326 irreplaceable images; the only other copy is one tarball |
| batch script → `/home/services/psalter-backups/` | Read-only for this entire phase |
| 326-file sharp loop → shared 3.7GB VPS RAM | earlyoom kills the greediest process at ~5% free; other PM2 services share the box |
| staging write → filesystem at 87% used | ~250MB staged output plus a 1.4GB `/tmp` extraction during backup verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | full batch run writing into `public/tunes/` | mitigate | `compress-tunes.ts` Guard 1 (resolved-path equality abort, built and tested in Plan 01); Task 3 acceptance criteria re-assert `find public/tunes -type f | wc -l == 328` and `du -sh == 1.4G` after the run |
| T-13-02 | Denial of Service | 326 sequential sharp decodes with 265Mi free RAM | mitigate | `sharp.concurrency(1)` + `sharp.cache(false)` + sequential `for...of` in both scripts; pre-flight `free -h` gate refusing to start under 200Mi; rss logged every 25 files with an explicit ~800MB kill threshold |
| T-13-03 | Denial of Service | staging write + 1.4GB `/tmp` archive extraction on an 87%-full disk | mitigate | `compress-tunes.ts`'s `df -Pk` ≥2GB guard; Task 3 pre-flight `df -h /home/services` check; `verify-tunes-backup.sh` cleans its mktemp dir via `trap ... EXIT` |
| T-13-04 | Tampering | backup archive modified or deleted during the run | mitigate | Task 1 records sha256 + size + mtime as evidence before the run; Task 3's automated verify re-asserts the same sha256 after the run; no script in this phase writes to `/home/services/psalter-backups/` |
| T-13-07 | Repudiation | claiming "backup verified" using a check that will be invalid post-swap | mitigate | Task 1 runs the full `verify-tunes-backup.sh` **now**, while its md5 step is still meaningful, and records an explicit written note that post-swap verification must use `scripts/verify-backup-archive.sh` instead |
| T-13-10 | Spoofing (of test results) | a verifier that passes on an incomplete set | mitigate | Set-level filename-set equality check (both directions) plus strict `compSize < origSize` on all 320 JPEGs; sentinel `COMPRESSION_VERIFIED` printed only when `failures === 0`; negative test asserts exit 1 on an empty compressed dir |
</threat_model>

<verification>
- `bash scripts/verify-tunes-backup.sh` printed `BACKUP_VERIFIED` and the output is recorded in `BACKUP-PREFLIGHT-EVIDENCE.md`
- `npx tsx scripts/verify-tunes-compression.ts` prints `COMPRESSION_VERIFIED` and exits 0
- `npx tsx scripts/verify-tunes-compression.ts --compressed=/tmp/t13-empty` exits non-zero and does not print the sentinel
- `ls -A public/tunes-compressed | wc -l` → 328
- `find public/tunes -type f | wc -l` → 328 and `du -sh public/tunes` → 1.4G (untouched)
- `sha256sum` of the backup archive still matches `28a83a7d…b714a9`
- `npx vitest run` shows no new failures against the documented baseline
</verification>

<success_criteria>
1. The Phase 6 backup's restorability is proven and recorded before anything is compressed
2. `scripts/verify-tunes-compression.ts` checks all 326 images plus both dotfiles, not a sample
3. All 320 staged JPEGs are strictly smaller than their originals and decode as valid JPEGs within the approved width bound
4. The 6 PNGs and both dotfiles are staged byte-identical
5. `public/tunes/` and the backup archive are both provably unchanged at the end of this plan
</success_criteria>

<output>
After completion, create `.planning/phases/13-tune-image-compression/13-02-SUMMARY.md`. Record total bytes before/after, percentage reduction, peak rss, runtime, and confirm the `notSmaller` count was 0.
</output>
