---
phase: 04.5-psalm-overhaul
plan: "02"
subsystem: psalm-detail-page, psalm-tabs
tags: [layout, tabs, responsive, soundcloud, psalm-detail]
dependency_graph:
  requires:
    - fetchPsalmDetail extended with dailyReadings and psalmTopics (04.5-01)
  provides:
    - Restructured psalm detail page with below-tabs two-column layout
    - PsalmTabs component with 7-tab desktop / 8-tab mobile structure
    - Notation player slot in page.tsx left column for Plan 03 wiring
  affects:
    - src/app/psalms/[id]/page.tsx
    - src/components/PsalmTabs.tsx
tech_stack:
  added: []
  patterns:
    - md:hidden CSS-only tab visibility (Tune tab mobile-only, D-05)
    - md:grid-cols-[3fr_2fr] below-tabs two-column layout (D-01/D-02/D-03)
    - encodeURIComponent on soundcloudUrl iframe src (T-4.5-04 mitigation)
    - rel="noopener noreferrer" on all target="_blank" anchors (T-4.5-05 mitigation)
key_files:
  created: []
  modified:
    - src/app/psalms/[id]/page.tsx
    - src/components/PsalmTabs.tsx
decisions:
  - "Suspense wrapper removed from page.tsx — PsalmTabs is a regular 'use client' component with no dynamic(ssr:false) imports; notation player dynamic import comes in Plan 03"
  - "Tasks 1 and 2 co-dependent for tsc: page.tsx passes primaryTune to PsalmTabs; both must be rewritten before tsc exits 0"
  - "next build fails on /explore/authors/[author] (password auth error) — pre-existing infrastructure constraint documented in 04.5-01-SUMMARY.md; unrelated to this plan's changes"
metrics:
  duration: 10
  completed_date: "2026-05-09"
  tasks_completed: 2
  files_created: 0
  files_modified: 2
---

# Phase 4.5 Plan 02: Psalm Detail Page Restructure Summary

Rewrote page.tsx with a below-tabs two-column grid (lyrics+notation left, SoundCloud right hidden on mobile) and replaced PsalmTabs with the correct 8-tab structure (Tune tab mobile-only via md:hidden, 7 tabs visible on desktop) where Overview is metadata-only and Tune tab carries SoundCloud for mobile.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Restructure page.tsx with below-tabs two-column section | 6293238 | src/app/psalms/[id]/page.tsx |
| 2 | Rewrite PsalmTabs.tsx with correct 7-tab/8-tab structure | d332b76 | src/components/PsalmTabs.tsx |

## Tab Structure Confirmation

All 8 tab values implemented:

| Value | Trigger | Visible | Content |
|-------|---------|---------|---------|
| `tune` | Tune | Mobile only (md:hidden) | SoundCloud + tune name + meter + precentingComment |
| `overview` | Overview | Always | book, author, meter, precentingComment, sectionHeadings, psalmTopics badges — NO lyrics, NO SoundCloud |
| `365days` | 365 Days | Always | dayNumber + readingDate + notes; null-safe empty state |
| `study` | Study | Always | Haddington intro + KJV verse-by-verse + external links |
| `messianic` | Messianic | Always | classification + ntVerification + messianicVerses; null-safe |
| `parallel` | Parallel | Always | Scottish Psalter lyrics vs KJV two-column grid |
| `backup-tunes` | Backup Tunes | Always | Non-primary tunes de-duped against primaryTune |
| `historical-tunes` | Historical Tunes | Always | "No historical tunes recorded." placeholder |

## Overview Tab Confirmation

Overview tab contains NO lyrics block and NO soundcloudUrl reference. Content: book, author, primaryVersion.meter, primaryTune.precentingComment, sectionHeadings list, psalmTopics Badge components.

## page.tsx Below-Tabs Section Confirmation

- `md:grid-cols-[3fr_2fr]` two-column grid renders below PsalmTabs
- Left column: Metrical Lyrics heading + stanza paragraphs + NOTATION PLAYER SLOT placeholder
- Right column: SoundCloud iframe wrapped in `hidden md:block` div — hidden on mobile (SoundCloud moves to Tune tab on mobile per D-03)
- `generateStaticParams` and `generateMetadata` preserved exactly

## TypeScript / Build Results

- `npx tsc --noEmit`: exit 0 (clean)
- `npx next build`: fails on `/explore/authors/[author]` — pre-existing DB password auth error (postgres password out of sync in dev environment). Documented in 04.5-01-SUMMARY.md as pre-existing infrastructure constraint. Unrelated to this plan's changes.

## Security (Threat Register)

| Threat ID | Mitigation Applied |
|-----------|-------------------|
| T-4.5-03 | All text rendered as React children (auto-escaped); no dangerouslySetInnerHTML |
| T-4.5-04 | encodeURIComponent() applied to soundcloudUrl in both page.tsx right column and Tune tab iframe src |
| T-4.5-05 | rel="noopener noreferrer" applied to all three external links in Study tab |

## Deviations from Plan

None — plan executed exactly as written. Both files match the full implementation provided in the plan spec.

## Known Stubs

- Notation player slot in page.tsx left column renders a dashed-border placeholder ("Notation player (coming in Plan 03)"). This is intentional — Plan 03 wires PsalmNotationPlayer into this slot.
- Historical Tunes tab renders "No historical tunes recorded." — schema has no historical tune distinction yet; placeholder is correct per plan spec.

## Threat Flags

None — no new network endpoints, auth paths, or trust boundary crossings beyond those in the plan's threat model.

## Self-Check: PASSED

- src/app/psalms/[id]/page.tsx: FOUND
- src/components/PsalmTabs.tsx: FOUND
- Commit 6293238: FOUND
- Commit d332b76: FOUND
- md:grid-cols-[3fr_2fr] in page.tsx: FOUND
- hidden md:block in page.tsx: FOUND
- NOTATION PLAYER SLOT in page.tsx: FOUND
- md:hidden in PsalmTabs.tsx: FOUND
- All 8 tab values in PsalmTabs.tsx: FOUND
- dailyReadings in PsalmTabs.tsx: FOUND
