---
phase: 04.9.13-single-psalm-ui-streamlining
verified: 2026-06-28T22:15:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
---

# Phase 04.9.13: Single Psalm UI Streamlining Verification Report

**Phase Goal:** Redesign the Single Psalm page for clarity and compactness: (1) Gear menu becomes a compact popover with main toggle (Music Notes / Lyrics Only) + conditional Staff/Solfege and Split-leaf/Inline sub-toggles; "Restart tour" retained; "About this Psalm" collapsed into "Study" button reusing the existing full-study panel; (2) Player bar narrows and right-aligns on desktop; abc ↔ SoundCloud toggle with localStorage persistence + "lyrics may not match" disclaimer; applied to both /psalms/[id] and /tunes/[id]; (3) Split-leaf view overhaul — full-width mobile image, multi-page thumbnail + arrow navigation, separate no-scroll stanza browser, remove redundant "Play recording" button; (4) Desktop max-width constraint matching /tunes/[id]
**Verified:** 2026-06-28T22:15:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Gear menu replaced with compact top/end-anchored Popover: Music Notes / Lyrics Only main toggle + conditional Staff/Solfege and Split-leaf/Inline sub-toggles + Study + Restart tour | VERIFIED | `GearPopover.tsx` (236 lines) at `src/components/singing/GearPopover.tsx`. Radiogroup main toggle, Notation/Layout sub-toggles with opacity fade, Separator, Study (`data-settings-study`, `aria-label="Open full study view"`, BookOpen icon, `router.push(studyHref)`), Restart tour (`data-restart-tour`, `aria-label="Restart the onboarding tour"`, 160ms setTimeout). Popover uses `side="top" align="end"`. `GearDrawer.tsx` deleted. |
| 2 | Player bar narrows and right-aligns on desktop (floating card); mobile unchanged full-width strip; abc/SoundCloud toggle with localStorage + disclaimer | VERIFIED | `PlayMiniBar.tsx`: desktop classes `md:ml-auto md:max-w-md md:right-4 md:rounded-lg md:shadow-lg` (line 103); mobile `inset-x-0 bottom-[44px]` preserved. SC toggle (`data-audio-source-toggle`) when `hasSc`. `psalter-audio-source` key with two-effect pattern (lines 61-75). Disclaimer `Lyrics shown are metrical; recording may use a different text.` with `aria-live="polite"` (line 153). Synth stops on SC switch (lines 78-80). |
| 3 | abc/SoundCloud toggle reached on BOTH /psalms/[id] (SingingView PlayMiniBar) AND /tunes/[id] (TuneMiniBarSection) | VERIFIED | `SingingView.tsx` passes `soundcloudUrl` + `tuneName` (lines 436-437). `TuneMiniBarSection.tsx` created as client wrapper owning playback state, renders PlayMiniBar. `/tunes/[id]/page.tsx` renders `<TuneMiniBarSection>` in ABC-tune score section (lines 167-171). |
| 4 | Split-leaf 5-col desktop grid (60/40), independent stanza scroll, full-width flush mobile image, multi-page thumbnail strip with ChevronLeft/ChevronRight + ring-2 highlight | VERIFIED | `NotationRenderer.tsx` solfège branch (lines 1047-1134): `isSplit = layout === 'split-leaf'` (line 1052), `md:grid-cols-5` with `md:col-span-3`/`md:col-span-2`, stanza column `max-h-[60vh] overflow-y-auto` (line 1104), `-mx-4` flush edges (line 1057), thumbnail strip with `ring-2 ring-foreground` (line 1087) + `loading="lazy"` (line 1090), `Solfège score` caption (line 1065), `Score image not available` fallback (line 1068). `pageIndex` state with reset effect (lines 334-335). |
| 5 | Desktop max-w-4xl constraint on /psalms/[id] matching /tunes/[id]; PsalmTopBar + GlassBottomBar inner constrained via inner wrapper; gear slot preserved | VERIFIED | `psalms/[id]/page.tsx` wraps SingingView in `<div className="max-w-4xl mx-auto">` (line 182). `PsalmTopBar.tsx` inner div has `max-w-4xl mx-auto` (line 100). `GlassBottomBar.tsx` inner `<div className="max-w-4xl mx-auto ...">` (line 59); gear slot `{gear ?? (...)}` preserved (line 127). `deriveTuneJpgPages` called server-side only (psalms page.tsx line 178; SingingView has 0 actual imports — only JSDoc). |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/components/singing/GearPopover.tsx` | Compact settings popover | VERIFIED | 236 lines, `'use client'`, exports `GearPopover`, all UI-SPEC toggles wired |
| `src/components/singing/PlayMiniBar.tsx` | Desktop right-aligned mini-bar + abc/SC toggle + localStorage | VERIFIED | Props: `soundcloudUrl?`, `tuneName?`; desktop classes; two-effect localStorage; SC iframe + disclaimer |
| `src/components/notation/NotationRenderer.tsx` | Split-leaf 2-col + thumbnail nav + flush image + stanza scroll | VERIFIED | Props: `layout`, `staffPages`, `solfegePages`; solfège branch overhauled |
| `src/components/singing/SingingView.tsx` | GearPopover wiring + layout/pages props forwarded | VERIFIED | GearPopover as `gear` slot; `layout` state; 3 new props to NotationRendererClient |
| `src/components/singing/GlassBottomBar.tsx` | `gear` slot + inner max-w-4xl wrapper | VERIFIED | `gear?: ReactNode` prop; inner `max-w-4xl` div; gear slot preserved |
| `src/components/singing/PsalmTopBar.tsx` | Inner max-w-4xl wrapper | VERIFIED | `max-w-4xl mx-auto` on inner div |
| `src/components/TuneMiniBarSection.tsx` | Client wrapper for static PlayMiniBar on tune pages | VERIFIED | 43 lines, `'use client'`, owns playback state, renders PlayMiniBar |
| `src/app/psalms/[id]/page.tsx` | max-w-4xl wrapper + server-side pages | VERIFIED | Wrapper div; `deriveTuneJpgPages(primaryTune.name)` at line 178 |
| `src/app/tunes/[id]/page.tsx` | TuneMiniBarSection + pages threaded | VERIFIED | Imports + renders TuneMiniBarSection; passes staffPages/solfegePages to NotationRendererClient |
| `src/components/singing/GearDrawer.tsx` | DELETED | VERIFIED | File absent; only JSX comment at SingingView.tsx:440 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `SingingView.tsx` | `GearPopover.tsx` | `gear={...}` prop to GlassBottomBar | WIRED | Lines 413-426 |
| `GlassBottomBar.tsx` | `GearPopover.tsx` | `gear` slot renders GearPopover trigger | WIRED | Line 127: `{gear ?? ...}` |
| `SingingView.tsx` | `PlayMiniBar.tsx` | `<PlayMiniBar>` with soundcloudUrl + tuneName | WIRED | Lines 429-438 |
| `PlayMiniBar.tsx` | `AbcAudioControls.tsx` | Renders in abc mode | WIRED | Line 135 |
| `NotationRenderer.tsx` | (TuneAudioPlayer) | Removed from solfege/lyrics | WIRED | Count = 0 in NotationRenderer |
| `psalms/[id]/page.tsx` | `SingingView.tsx` | Wrapper + staffPages/solfegePages | WIRED | Lines 182, 199-200 |
| `SingingView.tsx` | `NotationRenderer.tsx` | `layout`, `staffPages`, `solfegePages` | WIRED | Lines 377-379 |
| `tunes/[id]/page.tsx` | `PlayMiniBar.tsx` | Via TuneMiniBarSection | WIRED | Lines 167-171 |
| `tunes/[id]/page.tsx` | `NotationRendererClient` | staffPages + solfegePages | WIRED | Lines 164-165 |

### Requirements Coverage

| Requirement | Source | Description | Status | Evidence |
|-------------|--------|-------------|--------|----------|
| UI-04 | REQUIREMENTS.md | Compact GearPopover, split-leaf 5-col grid, independent stanza scroll, full-width flush image, thumbnail strip + ring-2 highlight, redundant Play recording removed | SATISFIED | GearPopover.tsx, NotationRenderer solfège branch, TuneAudioPlayer count=0 |
| UI-05 | REQUIREMENTS.md | PlayMiniBar desk-top floating card + abc/SC toggle + localStorage + disclaimer; both routes; max-w-4xl on psalm page + inner bar wrappers | SATISFIED | PlayMiniBar desktop classes, TuneMiniBarSection on /tunes/[id], psalm page + PsalmTopBar + GlassBottomBar inner `max-w-4xl` wrappers |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/components/notation/NotationRenderer.tsx` | 765 | `TODO(RENDER-07b)` | Info | Pre-existing from Phase 4.9.7; not introduced by this phase |

### Human Verification Required

### 1. Visual Popover Layout

**Test:** Open `/psalms/1` (or any psalm), tap the gear icon in the bottom bar.
**Expected:** Compact popover opens upward at the right edge of the screen. Music Notes / Lyrics Only toggle visible. With Music Notes active: Notation (Staff/Solfège) and Layout (Split-leaf/Inline) sub-toggles visible with icon buttons. Separator below. Study + Restart tour buttons below that. Lyrics Only hides sub-toggles (faded, pointer-events:none).
**Why human:** Visual popover anchoring, fade animation, and radiogroup styling are not verifiable via grep.

### 2. Desktop Mini-Bar Floating Card

**Test:** Open a psalm with `soundcloudUrl` (e.g., a known recorded psalm) at ≥1024px viewport width.
**Expected:** PlayMiniBar is a narrower floating card bottom-right (not full-width strip). abc/SoundCloud toggle visible. Tapping switches to SoundCloud iframe + disclaimer. Persists across page reload. At 375px width: full-width strip, no toggle if no soundcloudUrl.
**Why human:** Responsive layout and localStorage persistence require a running browser.

### 3. Split-Leaf Desktop Layout

**Test:** On a psalm page ≥768px, open GearPopover, select Music Notes + Split-leaf.
**Expected:** Solfège/score image takes ~60% width on left, stanza list takes ~40% on right. Scrolling the stanza list does not scroll the score. At 375px: stacked layout with image flush to screen edges.
**Why human:** CSS grid behavior and independent scroll containers require visual confirmation.

### 4. Multi-Page Thumbnails

**Test:** Open a multi-page tune (e.g., a tune with staff-solfege-1.jpg, staff-solfege-2.jpg, etc.) in solfège view.
**Expected:** Thumbnail strip renders below the main image with ChevronLeft/ChevronRight arrows. Tap thumbnail or arrow → main image swaps. Current page highlighted with ring-2. Single-page tunes: no thumbnail strip.
**Why human:** Image swap and thumbnail strip visibility depend on actual file system probe.

### Gaps Summary

No gaps found. All 5 observable truths verified against source code. All artifacts exist, are substantive, and are wired. TypeScript compiles cleanly (only pre-existing unrelated script error). Two requirements from REQUIREMENTS.md (UI-04, UI-05) fully satisfied.

---

_Verified: 2026-06-28T22:15:00Z_
_Verifier: Claude (gsd-verifier)_
