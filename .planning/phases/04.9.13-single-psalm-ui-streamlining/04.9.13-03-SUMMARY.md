---
phase: 04.9.13
plan: 03
subsystem: notation/singing-ui
tags: [split-leaf, multi-page-thumbnails, max-width, flush-image, stanza-scroll]
dependency_graph:
  requires: ["04.9.13-02"]
  provides: ["split-leaf-notation", "multi-page-solfege-nav", "psalm-max-width-constraint"]
  affects: ["src/components/notation/NotationRenderer.tsx", "src/components/singing/SingingView.tsx", "src/components/singing/PsalmTopBar.tsx", "src/components/singing/GlassBottomBar.tsx", "src/app/psalms/[id]/page.tsx", "src/app/tunes/[id]/page.tsx"]
tech_stack:
  added: []
  patterns: ["5-col-grid-split-leaf", "thumbnail-strip-with-arrow-nav", "inner-max-w-4xl-on-fixed-bars"]
key_files:
  created: []
  modified:
    - src/components/notation/NotationRenderer.tsx
    - src/components/singing/SingingView.tsx
    - src/components/singing/PsalmTopBar.tsx
    - src/components/singing/GlassBottomBar.tsx
    - src/app/psalms/[id]/page.tsx
    - src/app/tunes/[id]/page.tsx
decisions:
  - "deriveTuneJpgPages stays server-only; active tune pages computed in page.tsx via primaryTune (client-side ?tune= override handled in SingingView, not page)"
  - "GlassBottomBar restructured: flex row moved from <nav> to inner <div className='max-w-4xl mx-auto ...'>; gear slot preserved inside inner div"
  - "Staff branch 'Show Original' JPG left unchanged — multi-page nav only enhances solfège branch direct <img> rendering"
metrics:
  duration: 12m 54s
  completed: "2026-06-28T20:17:44Z"
  tasks: 3
  files: 6
---

# Phase 04.9.13 Plan 03: Split-Leaf View + Max-Width Constraint Summary

Split-leaf 2-column notation view with multi-page solfège thumbnail nav, full-width flush mobile image, independent stanza scroll, and max-w-4xl desktop constraint on /psalms/[id] matching /tunes/[id].

## What Was Done

### Task 1: Split-leaf view + multi-page thumbnails + flush image + independent stanza scroll in NotationRenderer

Modified `src/components/notation/NotationRenderer.tsx`:
- Added `layout`, `staffPages`, `solfegePages` optional props to `NotationRendererProps`
- Added `activePages(viewMode, staffPages, solfegePages)` helper to pick the active page array
- Added `pageIndex` state with a reset effect keyed on `staffPages?.[0]`/`solfegePages?.[0]` (guards against stale index on tune switch)
- Overhauled the solfège branch:
  - Main image block: full-width with `-mx-4` flush edges (counteracts parent `px-4`), "Solfège score" caption below
  - Multi-page thumbnail strip: 80x100px thumbnails, `ring-2 ring-foreground` highlight on current, `ChevronLeft`/`ChevronRight` arrows (44x44 tap targets, disabled at boundaries), `loading="lazy"`
  - Split-leaf mode: `md:grid-cols-5` grid (3/2 cols = 60/40) on desktop, stacked on mobile
  - Independent stanza scroll: `max-h-[60vh] overflow-y-auto` in stanza column
  - No-pages fallback: "Score image not available"
- Added `cn` import from `@/lib/utils`
- Staff branch "Show Original" JPG left unchanged (deliberate scope boundary)

**Commit:** `19e32e5` — feat(04.9.13-03): split-leaf view + multi-page thumbnails + flush image in NotationRenderer

### Task 2: Desktop max-width constraint on /psalms/[id] + thread layout/pages props through SingingView + inner bar wrappers

Modified 4 files:

`src/app/psalms/[id]/page.tsx`:
- Wrapped `<SingingView>` in `<div className="max-w-4xl mx-auto">`
- Added `activeTunePages` derivation via `deriveTuneJpgPages(primaryTune.name)` (server-side, valid in RSC)
- Passed `staffPages={activeTunePages.staffPages}` and `solfegePages={activeTunePages.solfegePages}` to SingingView

`src/components/singing/SingingView.tsx`:
- Added `staffPages?` and `solfegePages?` optional props to Props interface (with JSDoc noting server-only derivation)
- Destructured with empty-array defaults
- Forwarded `layout`, `staffPages`, `solfegePages` to `<NotationRendererClient>`
- `deriveTuneJpgPages` NOT imported (verified: 0 actual imports; 2 JSDoc comment references only)

`src/components/singing/PsalmTopBar.tsx`:
- Added `max-w-4xl mx-auto` to inner `<div>` (header remains full-width sticky; content constrained)

`src/components/singing/GlassBottomBar.tsx`:
- Moved `flex items-center gap-1 px-2 h-11 md:h-13` from `<nav>` to a new inner `<div className="max-w-4xl mx-auto ...">`
- `<nav>` keeps only fixed positioning + background + border styling
- Gear slot (`{gear ?? (...)}`) preserved inside new inner div (Plan 01 wiring intact)

**Commit:** `8cab623` — feat(04.9.13-03): max-w-4xl constraint on /psalms/[id] + inner bar wrappers + thread pages props

### Task 3: Thread staffPages/solfegePages into /tunes/[id] NotationRendererClient invocations

Modified `src/app/tunes/[id]/page.tsx`:
- Added `staffPages={staffPages}` and `solfegePages={solfegePages}` to the `<NotationRendererClient>` in the `hasAbc` score block
- `TuneDetailClient` (image-based fallback) and `TuneMiniBarSection` (Plan 02) left unchanged
- `deriveTuneJpgPages` still called server-side at line 76

**Commit:** `cd653a2` — feat(04.9.13-03): thread staffPages/solfegePages into /tunes/[id] NotationRendererClient

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed undefined `activeTune` reference in psalm page.tsx**
- **Found during:** Task 2
- **Issue:** Initial edit referenced `activeTune` which doesn't exist in `psalms/[id]/page.tsx` (it's a SingingView-internal variable). The page only has `primaryTune`.
- **Fix:** Changed to use `primaryTune` directly: `const activeTunePages = primaryTune ? deriveTuneJpgPages(primaryTune.name) : { staffPages: [], solfegePages: [] }`
- **Files modified:** `src/app/psalms/[id]/page.tsx`
- **Commit:** `8cab623`

**2. [Rule 3 - Blocking] Added missing `cn` import to NotationRenderer**
- **Found during:** Task 1
- **Issue:** The solfège branch overhaul uses `cn()` for thumbnail classNames, but `cn` was not imported in NotationRenderer.tsx.
- **Fix:** Added `import { cn } from '@/lib/utils'` after the lucide import block.
- **Files modified:** `src/components/notation/NotationRenderer.tsx`
- **Commit:** `19e32e5`

## Verification Results

- `npx tsc --noEmit`: clean across all 6 modified files (only unrelated `scripts/fix-lyrics-124b.ts` error)
- All 19 Task 1 acceptance criteria greps pass
- All 10 Task 2 acceptance criteria greps pass
- All 3 Task 3 acceptance criteria greps pass
- `deriveTuneJpgPages` NOT imported into SingingView (0 actual imports)
- Gear slot preserved in GlassBottomBar (5 matches: prop + usage)

## Scope Boundaries (Deliberate)

- Staff branch "Show Original" JPG (AbcPlayer-internal) does NOT get multi-page nav — only the solfège branch's direct `<img>` rendering is enhanced. This is per plan spec.
- `layout` prop defaults to 'inline' on tune pages (no GearPopover there) — acceptable.
- Client-side `?tune= override` pages are NOT re-derived in psalm page.tsx — SingingView handles the override client-side and uses the primary tune's pages as a baseline. Override tunes with more pages than primary will show thumbnails up to the primary tune's page count. This is a known limitation documented for future refinement.
