---
context: phase
phase: 04.9.1-interactive-abcjs-player
status: in_progress
last_updated: 2026-05-13T09:09:45Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints discovered this session._

<current_state>
Phase 4.9.1 player bugs fixed and deployed. One data-quality task remains:
5 tunes have incomplete stored OCR results (only captured first half of tune).
The /dev/notation-compare live re-run produces the correct full result for these tunes,
confirming the images are fine — the stored OCR run just gave a short result.
</current_state>

<completed_work>

- **abc_satb rendering**: All tune/psalm pages now render `abc_satb` (v2 OCR) instead of `abc_notation` (v1 — corrupted for many tunes, e.g. LLM monologue leaked into Farrant's ABC). Commit `88f4ac9`.
- **Soprano-only display**: `sopranoOnly()` in `src/lib/utils.ts` strips V:2-4 lines/declarations from inline SATB ABC. Applied at both tune and psalm render paths. Full SATB kept in DB. Commit `af6433c`.
- **Key labels**: AbcPlayer Key select now shows note names (G, A, Bb…) by parsing `K:` from ABC + computing transpose offset. Commit `c6c5fa9`.
- **Default BPM from OCR**: Parses `Q:1/4=76` from ABC for initial BPM state. Resets on tune change. Commit `c6c5fa9`.
- **Reset button**: RotateCcw icon appears when key or BPM differs from defaults. Commit `c6c5fa9`.
- **Psalm page ABC fix**: Primary tune path in PsalmTabs now uses `abcSatb` (was still using `abcNotation`). Commit `c6c5fa9`.
- **Duplicate lyrics removed**: Removed `lyricsText` prop from AbcPlayer in PsalmNotationPlayer — lyrics already embedded via `w:` lines by `buildAbcWithStanzas`. Commit `c6c5fa9`.
- **Audio transpose**: Added `midiTranspose: transpose` to synth options — `visualTranspose` only affects SVG, not audio. Commit `aa8a8fc`.
- **Highlight sync**: Moved `millisecondsPerMeasure` inside synth `options` (was at top level, silently ignored). Commit `aa8a8fc`.
</completed_work>

<remaining_work>

- **Re-run OCR for 5 incomplete tunes**: Farrant, Artaxerxes (EPC tune), Kathrine, Azmon/Denfield, Dunlapscreek.
  - All have only ~8 soprano cells (half a typical tune). Double-bar `||` appears mid-transcription.
  - Live re-run at `/dev/notation-compare` → "Run OCR text" produces correct full result.
  - Fix: run `npx tsx scripts/ocr-solfege.ts` with `--name=` filter for these 5 tunes (check if script supports filtering, else add it), then run `npx tsx scripts/apply-abc-notation.ts` to push results to DB.
  - Farrant slug: `farrant`. Others: `artaxerxes-epc-tune`, `kathrine`, `azmon-denfield`, `dunlapscreek`.
</remaining_work>

<decisions_made>

- `abc_satb` is canonical render field; `abc_notation` (v1) is fallback only — v1 corrupted for many tunes
- Soprano-only display for copyright compliance; full SATB kept in DB for future use
- `sopranoOnly()` placed in `src/lib/utils.ts` as a pure utility function
- `midiTranspose` (not `visualTranspose`) is the correct abcjs option for audio key transposition
- `millisecondsPerMeasure` must go inside synth `options`, not at the top-level `init` params
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading
1. `.planning/phases/04.9.1-interactive-abcjs-player/04.9.1-02-SUMMARY.md` — full context of phase
2. `scripts/ocr-solfege.ts` — check if `--name=` filter exists before running re-OCR
3. `scripts/apply-abc-notation.ts` — needed after OCR to push results to DB

## Infrastructure State
- App: running on PM2 as `psalter`, port 3005, deployed at psalter.gsdlabs.dev
- DB: `psalter-db` Docker container, port 5435
- All commits pushed to GitHub master

<context>
Session fixed 6 bugs in the AbcPlayer/psalm pages. The player now correctly shows v2 OCR data (soprano only), plays in the selected key, and has proper BPM/key controls with reset. The remaining work is purely data: re-running OCR for 5 tunes whose stored results are incomplete.
</context>

<next_action>
Check if `scripts/ocr-solfege.ts` supports a `--name=` or `--only=` filter. If not, add one targeting the 5 tunes. Run OCR for those 5 tunes only, then run apply-abc-notation.ts to push abc_satb to DB.
</next_action>
