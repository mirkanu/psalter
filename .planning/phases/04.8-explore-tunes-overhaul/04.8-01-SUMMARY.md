---
phase: 04.8-explore-tunes-overhaul
plan: "01"
subsystem: ui
tags: [nextjs, drizzle-orm, postgresql, tailwind, explore]

# Dependency graph
requires:
  - phase: 04.7-psalm-listing-overhaul
    provides: psalm listing UI patterns and grid layouts
provides:
  - "When you... section on /explore page showing topic_type='When you...' topics"
  - "fetchWhenYouTopics DB query"
  - "fetchTopicsWithCounts excludes When you... rows"
  - "h1 font-semibold fix on /explore"
affects: [04.8-02, 04.8-03, explore-page]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Two-query split for topic_type filtering: fetchTopicsWithCounts (excludes When you...) + fetchWhenYouTopics (only When you...)"
    - "ne() + and() drizzle-orm combinators for multi-condition WHERE"

key-files:
  created: []
  modified:
    - src/db/queries/explore.ts
    - src/app/explore/page.tsx

key-decisions:
  - "fetchTopicsWithCounts uses and(isNotNull(topics.name), ne(topics.topicType, 'When you...')) to exclude When-you rows without touching Song Type or Mood rows"
  - "When you... section placed before Topics section, matching UI-SPEC section order"
  - "whenYouSlugMap uses same buildDisambiguatedSlugMap logic as topicsSlugMap for consistent slug generation"

patterns-established:
  - "Topic type filtering: use ne() drizzle combinator rather than raw SQL for type exclusion"

requirements-completed: [EXP-01, EXP-02]

# Metrics
duration: 15min
completed: 2026-05-10
---

# Phase 4.8 Plan 01: /explore When-you Section + Font Fix Summary

**New "When you..." topic section added to /explore using fetchWhenYouTopics (10 rows, topic_type='When you...'), Topics section now excludes those rows, and h1 corrected from font-bold to font-semibold**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-05-10T00:44:00Z
- **Completed:** 2026-05-10T00:59:37Z
- **Tasks:** 3 (Tasks 1 & 2 committed, Task 3 verification-only)
- **Files modified:** 2

## Accomplishments

- Added `fetchWhenYouTopics` query returning only `topic_type = 'When you...'` topics ordered by psalm count
- Updated `fetchTopicsWithCounts` to exclude `When you...` rows via `ne(topics.topicType, 'When you...')`
- Rendered "When you..." as the first section on `/explore`, before Topics, with same grid layout and badge style
- Fixed `h1` from `font-bold` to `font-semibold` per UI-SPEC
- All 4 Playwright smoke checks pass (When you..., Topics, Nave's Topics, Messianic Psalms)

## Task Commits

1. **Task 1: Add fetchWhenYouTopics and update fetchTopicsWithCounts** - `ff44692` (feat)
2. **Task 2: Update /explore page to add When you... section** - `56746c8` (feat)
3. **Task 3: Playwright smoke test** - verification only, no files committed

## Files Created/Modified

- `src/db/queries/explore.ts` - Added `ne`, `and` imports; updated `fetchTopicsWithCounts` WHERE clause; added `fetchWhenYouTopics` export
- `src/app/explore/page.tsx` - Imported `fetchWhenYouTopics`, added to Promise.all, built `whenYouSlugMap`, rendered "When you..." section first, fixed h1 font-semibold

## Decisions Made

- `fetchTopicsWithCounts` now uses `and(isNotNull(topics.name), ne(topics.topicType, 'When you...'))` — keeps Mood and Song Type rows in the Topics section while only removing When you... rows
- Placed "When you..." before Topics per UI-SPEC section ordering

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Next.js static build required rebuild + server restart to reflect changes**
- **Found during:** Task 3 (Playwright smoke test)
- **Issue:** `/explore` is pre-rendered as static HTML at build time. Source code changes had no effect on the running `next start` server without a rebuild. Playwright returned FAIL for "When you..." because the old static HTML was still being served.
- **Fix:** Ran `npm run build` to regenerate `.next/server/app/explore.html`, then killed the old `next-server` process and started a fresh one on port 3005
- **Files modified:** None (build artifact regeneration)
- **Verification:** `curl http://localhost:3005/explore | grep "When you"` returns 2 matches; all 4 Playwright checks PASS
- **Committed in:** Not committed (build artifacts are gitignored)

---

**Total deviations:** 1 auto-fixed (blocking: static build cache)
**Impact on plan:** Expected operational step. No scope creep.

## Issues Encountered

- Old `next-server` process (PID 2418054) was still listening on port 3005 after initial `pkill -f "next start --port 3005"` because that process was started by PM2/a shell wrapper. Required targeting the PID directly.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `/explore` page now has When you... section as first section, Topics section cleaned of When-you rows
- Ready for 04.8-02 (Tunes overhaul) and 04.8-03 (any remaining Explore polish)
- Server is running on port 3005 with new build

---
*Phase: 04.8-explore-tunes-overhaul*
*Completed: 2026-05-10*
