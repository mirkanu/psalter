---
phase: 04.9.8
plan: 03
type: execute
wave: 3
depends_on: ["04.9.8-02"]
files_modified:
  - src/lib/lyrics.ts
  - src/lib/lyrics.test.ts
autonomous: true
requirements: [RENDER-08]

must_haves:
  truths:
    - "All 25 archaic -eth verb forms in PSALM_SYLLABLE_OVERRIDES return 2 syllables instead of 1."
    - "syllabifyForAbc('leadeth') produces 'lead- eth' (or equivalent 2-syllable joined output)."
    - "Existing overrides (prayer, enemy, righteous, majesty, tongues) remain unchanged and pass their tests."
    - "After build + restart, a psalm containing 'leadeth' (e.g., Psalm 23 verse 'He leadeth me') aligns the word across 2 note positions, not 1."
  artifacts:
    - path: "src/lib/lyrics.ts"
      provides: "Extended PSALM_SYLLABLE_OVERRIDES with 25 archaic -eth verb forms."
      contains: "leadeth: ['lead', 'eth']"
    - path: "src/lib/lyrics.test.ts"
      provides: "Unit test coverage asserting all 25 new overrides return the correct syllable arrays."
      contains: "archaic"
  key_links:
    - from: "src/lib/lyrics.ts PSALM_SYLLABLE_OVERRIDES"
      to: "src/lib/lyrics.ts syllabifyForAbc (line ~88-91)"
      via: "lookup map consulted before falling back to nlp-syllables"
      pattern: "PSALM_SYLLABLE_OVERRIDES\\[lowerStripped\\]"
    - from: "src/lib/lyrics.ts syllabifyForAbc"
      to: "src/components/notation/NotationRenderer.tsx buildAbcWithSyllables"
      via: "imported and called per word when building w: lines"
      pattern: "syllabifyForAbc"
---

<objective>
Add 25 archaic `-eth` verb-form overrides to `PSALM_SYLLABLE_OVERRIDES` so words like "leadeth", "maketh", "cometh" align to 2 note positions instead of being miscounted as 1 syllable by `nlp-syllables`. Each wrong count was shifting all following syllables left by 1, dropping the final syllable off the right edge of the staff.

Purpose: This closes the third root cause of the staff alignment bug (after the PHRASE_BREAK migration and z2 fix). Without it, any psalm containing an archaic -eth verb still shows misalignment even with the new 4-row layout.

Output: Updated lyrics.ts override map + unit tests covering all 25 words + redeployed app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.9.8-staff-display-word-alignment-fix/04.9.8-RESEARCH.md
@.planning/phases/04.9.8-staff-display-word-alignment-fix/04.9.8-PATTERNS.md
@.planning/phases/04.9.8-staff-display-word-alignment-fix/04.9.8-02-SUMMARY.md

@src/lib/lyrics.ts
@src/lib/lyrics.test.ts

<interfaces>
<!-- Existing override map signature and consumer (from src/lib/lyrics.ts). -->

```typescript
const PSALM_SYLLABLE_OVERRIDES: Record<string, string[]> = {
  prayer: ['pray', 'er'],
  // ... existing entries ...
  majesty: ['maj', 'es', 'ty'],
}

// Consumer (lines ~88-91):
const lowerStripped = stripped.toLowerCase()
const syllables: string[] =
  PSALM_SYLLABLE_OVERRIDES[lowerStripped] ?? syllabize(lowerStripped)
```

The 25 archaic words to add (from RESEARCH.md):
beareth, causeth, cometh, doeth, fadeth, faileth, giveth, goeth, hateth,
healeth, heareth, hideth, keepeth, layeth, leadeth, liveth, looketh,
maketh, raiseth, ruleth, seeketh, shineth, taketh, useth, waxeth
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-01: Add 25 archaic -eth overrides + unit tests + rebuild/restart</name>
  <files>src/lib/lyrics.ts, src/lib/lyrics.test.ts</files>
  <read_first>
    - src/lib/lyrics.ts (full file — PSALM_SYLLABLE_OVERRIDES object + syllabifyForAbc consumer)
    - src/lib/lyrics.test.ts (full file — existing test patterns and describe blocks)
    - .planning/phases/04.9.8-staff-display-word-alignment-fix/04.9.8-RESEARCH.md (Code Examples — exact 25 entries with split arrays)
    - .planning/phases/04.9.8-staff-display-word-alignment-fix/04.9.8-PATTERNS.md (src/lib/lyrics.ts section — placement guidance)
  </read_first>
  <behavior>
    - For each of the 25 words: `syllabifyForAbc(word)` returns a joined output corresponding to the 2-element array `[stem, 'eth']`.
    - Spot-check assertions (exact expected outputs depend on how syllabifyForAbc joins syllables — likely with `- ` or `-` separator; mirror existing tests):
      - `syllabifyForAbc('leadeth')` joined output contains `lead` and `eth` as distinct syllable tokens.
      - `syllabifyForAbc('maketh')` similar.
      - `syllabifyForAbc('LEADETH')` (uppercase) also works (lowerStripped normalization).
      - `syllabifyForAbc('leadeth.')` (with punctuation) also works (stripped normalization).
    - Existing overrides remain functional:
      - `syllabifyForAbc('prayer')` unchanged.
      - `syllabifyForAbc('enemy')` returns 3 syllables.
      - `syllabifyForAbc('righteousness')` returns 3 syllables.
  </behavior>
  <action>
    Step 1 — Open `src/lib/lyrics.ts` and find the closing `}` of `PSALM_SYLLABLE_OVERRIDES`. INSIDE the object, AFTER the existing `majesty: ['maj', 'es', 'ty'],` entry, add this block verbatim (preserving alphabetical ordering and the leading comment):

    ```typescript
      // Archaic -eth verb forms: NLP gives 1 syllable; correct singing = 2 (verb-eth)
      beareth: ['bear', 'eth'],
      causeth: ['cause', 'eth'],
      cometh: ['come', 'eth'],
      doeth: ['do', 'eth'],
      fadeth: ['fade', 'eth'],
      faileth: ['fail', 'eth'],
      giveth: ['give', 'eth'],
      goeth: ['go', 'eth'],
      hateth: ['hate', 'eth'],
      healeth: ['heal', 'eth'],
      heareth: ['hear', 'eth'],
      hideth: ['hide', 'eth'],
      keepeth: ['keep', 'eth'],
      layeth: ['lay', 'eth'],
      leadeth: ['lead', 'eth'],
      liveth: ['live', 'eth'],
      looketh: ['look', 'eth'],
      maketh: ['make', 'eth'],
      raiseth: ['raise', 'eth'],
      ruleth: ['rule', 'eth'],
      seeketh: ['seek', 'eth'],
      shineth: ['shine', 'eth'],
      taketh: ['take', 'eth'],
      useth: ['use', 'eth'],
      waxeth: ['wax', 'eth'],
    ```

    Step 2 — Open `src/lib/lyrics.test.ts`. Inside the existing top-level `describe` (or alongside the existing `describe('buildAbcWithSyllables')` if that's the structure), add a new `describe` block:

    ```typescript
    describe('PSALM_SYLLABLE_OVERRIDES — archaic -eth verbs', () => {
      const ARCHAIC_ETH: Array<[string, string[]]> = [
        ['beareth', ['bear', 'eth']],
        ['causeth', ['cause', 'eth']],
        ['cometh', ['come', 'eth']],
        ['doeth', ['do', 'eth']],
        ['fadeth', ['fade', 'eth']],
        ['faileth', ['fail', 'eth']],
        ['giveth', ['give', 'eth']],
        ['goeth', ['go', 'eth']],
        ['hateth', ['hate', 'eth']],
        ['healeth', ['heal', 'eth']],
        ['heareth', ['hear', 'eth']],
        ['hideth', ['hide', 'eth']],
        ['keepeth', ['keep', 'eth']],
        ['layeth', ['lay', 'eth']],
        ['leadeth', ['lead', 'eth']],
        ['liveth', ['live', 'eth']],
        ['looketh', ['look', 'eth']],
        ['maketh', ['make', 'eth']],
        ['raiseth', ['raise', 'eth']],
        ['ruleth', ['rule', 'eth']],
        ['seeketh', ['seek', 'eth']],
        ['shineth', ['shine', 'eth']],
        ['taketh', ['take', 'eth']],
        ['useth', ['use', 'eth']],
        ['waxeth', ['wax', 'eth']],
      ]

      for (const [word, expected] of ARCHAIC_ETH) {
        it(`${word} splits into ${expected.join('-')}`, () => {
          const out = syllabifyForAbc(word)
          // syllabifyForAbc joins syllables with a known separator; verify each expected
          // syllable token appears in the output as a discrete piece. Adapt the check to
          // mirror the assertion style in existing tests (e.g., toContain('lead') + toContain('eth')
          // OR direct equality to the joined form). Read existing test format first.
          for (const syl of expected) {
            expect(out).toContain(syl)
          }
        })
      }

      it('uppercase input is normalised to lowercase before lookup', () => {
        const upper = syllabifyForAbc('LEADETH')
        const lower = syllabifyForAbc('leadeth')
        expect(upper).toBe(lower)
      })

      it('punctuation around the word does not break the override', () => {
        const withDot = syllabifyForAbc('leadeth.')
        expect(withDot).toContain('lead')
        expect(withDot).toContain('eth')
      })
    })
    ```

    NOTE: Before writing the test, read the existing test file to confirm the exact assertion style used (e.g., does `syllabifyForAbc` return a string joined by `'- '` or an array?). Match the existing pattern — the example above uses `toContain` as a robust check that works for either string or array return types.

    Step 3 — Run the tests locally:
    ```bash
    npm test -- --run src/lib/lyrics.test.ts
    ```
    All 25 + 2 new tests should pass. Existing tests must remain green (no regressions).

    Step 4 — Type-check:
    ```bash
    npx tsc --noEmit
    ```

    Step 5 — Rebuild and restart the app:
    ```bash
    cd /data/home/psalter
    npm run build
    pm2 restart psalter
    pm2 logs psalter --lines 20 --nostream
    ```
    Confirm no build errors. Smoke-test:
    ```bash
    curl -sS http://localhost:3005/psalms/23 -o /dev/null -w "%{http_code}\n"
    ```
    Expect 200.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/lyrics.test.ts &amp;&amp; npx tsc --noEmit &amp;&amp; curl -sS http://localhost:3005/psalms/23 -o /dev/null -w "%{http_code}" | grep -q 200</automated>
  </verify>
  <acceptance_criteria>
    - All 25 archaic words are present in `src/lib/lyrics.ts`. Verifiable: `for w in beareth causeth cometh doeth fadeth faileth giveth goeth hateth healeth heareth hideth keepeth layeth leadeth liveth looketh maketh raiseth ruleth seeketh shineth taketh useth waxeth; do grep -q "$w:" src/lib/lyrics.ts || echo "MISSING: $w"; done` produces no MISSING output.
    - `grep -c "'eth'" src/lib/lyrics.ts` returns at least `25`.
    - `grep -c "Archaic -eth" src/lib/lyrics.ts` returns at least `1` (the comment is present).
    - `grep -c "archaic" src/lib/lyrics.test.ts` (case-insensitive: `grep -ic`) returns at least `1`.
    - All existing override entries still present: `grep -E "prayer:|enemy:|righteous:|majesty:|tongues:" src/lib/lyrics.ts | wc -l` returns at least `5`.
    - `npm test -- --run src/lib/lyrics.test.ts` exits 0 with all tests passing (existing + 27 new).
    - `npx tsc --noEmit` exits 0.
    - `pm2 list | grep psalter` shows status `online`.
    - `curl http://localhost:3005/psalms/23` returns 200.
    - `pm2 logs psalter --lines 20 --nostream` shows no error stacks.
  </acceptance_criteria>
  <done>25 overrides added; tests green; app rebuilt; smoke test passes. Ready for E2E verification in Plan 04.</done>
</task>

</tasks>

<verification>
- All 25 archaic verb overrides present and tested.
- Existing overrides unchanged (no regressions).
- App rebuilt and serves 200s.
</verification>

<success_criteria>
- syllabifyForAbc returns 2-syllable output for all 25 archaic words.
- Psalm 23 ("he leadeth me", "preparest", etc.) loads in browser without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/04.9.8-staff-display-word-alignment-fix/04.9.8-03-SUMMARY.md` summarising:
- 25 overrides added
- Test count delta (27 new test cases)
- Build + restart confirmed
</output>
