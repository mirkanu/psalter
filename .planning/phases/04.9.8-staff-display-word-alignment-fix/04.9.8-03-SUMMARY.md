---
phase: 04.9.8
plan: "03"
subsystem: lyrics-syllabification
tags: [syllabification, archaic-words, alignment, tdd]
dependency_graph:
  requires: ["04.9.8-02"]
  provides: ["25-archaic-eth-overrides"]
  affects: ["src/lib/lyrics.ts", "src/lib/lyrics.test.ts"]
tech_stack:
  added: []
  patterns: ["PSALM_SYLLABLE_OVERRIDES override map", "TDD RED/GREEN cycle"]
key_files:
  created: []
  modified:
    - src/lib/lyrics.ts
    - src/lib/lyrics.test.ts
decisions:
  - "Split 25 archaic -eth verbs as [stem, 'eth'] — consistent with single-syllable base rule"
  - "Used toContain assertions in tests to stay robust regardless of exact join format"
  - "Exact equality assertions (toBe) used since syllabifyForAbc output format is known"
metrics:
  duration: "~8 minutes"
  completed: "2026-05-28"
  tasks: 1
  files: 2
---

# Phase 04.9.8 Plan 03: Archaic -eth Verb Overrides Summary

**One-liner:** Added 25 archaic `-eth` verb overrides to `PSALM_SYLLABLE_OVERRIDES` so words like "leadeth" align to 2 note positions instead of 1, fixing Root Cause 3 of the staff alignment bug.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| RED | Add failing tests for 25 archaic -eth verb overrides | 6dcc3ce | src/lib/lyrics.test.ts |
| GREEN | Add 25 archaic -eth verb overrides to PSALM_SYLLABLE_OVERRIDES | ec689f1 | src/lib/lyrics.ts |

## What Was Built

### 25 Archaic -eth Overrides (src/lib/lyrics.ts)

Added to `PSALM_SYLLABLE_OVERRIDES` after the existing `majesty` entry, with comment:

```typescript
// Archaic -eth verb forms: NLP gives 1 syllable; correct singing = 2 (verb-eth)
beareth: ['bear', 'eth'],
causeth: ['cause', 'eth'],
cometh: ['come', 'eth'],
// ... 22 more ...
waxeth: ['wax', 'eth'],
```

All 25 words: beareth, causeth, cometh, doeth, fadeth, faileth, giveth, goeth,
hateth, healeth, heareth, hideth, keepeth, layeth, leadeth, liveth, looketh,
maketh, raiseth, ruleth, seeketh, shineth, taketh, useth, waxeth.

### Test Coverage (src/lib/lyrics.test.ts)

New `describe` block: `PSALM_SYLLABLE_OVERRIDES — archaic -eth verbs`
- 25 parametric tests: each word asserts exact `'stem- eth'` output
- Uppercase normalisation test: `LEADETH` → same as `leadeth`
- Punctuation test: `leadeth.` → `'lead- eth.'`
- 3 regression guards: prayer, enemy, righteousness unchanged

**Test count delta:** +28 new test cases (25 parametric + uppercase + punctuation + regression header)
**Total tests in file:** 60 (was 33, now 60 — 27 new tests added; regression guards overlap with existing suite)

### Build + Restart

- `npm run build` — no errors
- `pm2 restart psalter` — status: online
- `curl http://localhost:3005/psalms/23` — 200 OK
- `pm2 logs psalter --lines 20` — no error stacks

## Acceptance Criteria Verified

- [x] All 25 words present in `src/lib/lyrics.ts` (no MISSING output)
- [x] `grep -c "'eth'" src/lib/lyrics.ts` = 25
- [x] `grep -c "Archaic -eth" src/lib/lyrics.ts` = 1
- [x] `grep -ic "archaic" src/lib/lyrics.test.ts` = 6
- [x] Existing overrides (prayer, enemy, righteous, majesty, tongues) still present: 10 matches
- [x] `npm test -- --run src/lib/lyrics.test.ts` — 60/60 pass
- [x] `pm2 list | grep psalter` — status online
- [x] `curl http://localhost:3005/psalms/23` — 200

## Deviations from Plan

### Pre-existing type errors in test spec files

**Found during:** Task 03-01, Step 4 (type-check)
**Issue:** `tests/e2e/abc-player.spec.ts` and `tests/tune-notation.spec.ts` have pre-existing TS2451/TS2345 errors (redeclared `chromium` variable, incompatible `TestResult` type). These exist before this plan's changes.
**Action:** Logged as out-of-scope per deviation rule scope boundary. Not fixed.
**Files modified:** None (pre-existing, unrelated to this plan)

No other deviations — plan executed as written.

## Known Stubs

None. The 25 override entries are complete and wired directly into `syllabifyForAbc` via the `PSALM_SYLLABLE_OVERRIDES` lookup.

## Threat Flags

None. This plan modifies only a static lookup dictionary in a pure function — no network endpoints, auth paths, file access, or schema changes.

## Self-Check: PASSED

- [x] src/lib/lyrics.ts — exists and contains all 25 overrides
- [x] src/lib/lyrics.test.ts — exists and contains archaic describe block
- [x] Commit 6dcc3ce exists (RED: failing tests)
- [x] Commit ec689f1 exists (GREEN: implementation)
- [x] App online, serving 200 on /psalms/23

## TDD Gate Compliance

- RED gate commit: `6dcc3ce` — `test(04.9.8-03): add failing tests for 25 archaic -eth verb overrides`
- GREEN gate commit: `ec689f1` — `feat(04.9.8-03): add 25 archaic -eth verb overrides to PSALM_SYLLABLE_OVERRIDES`
- REFACTOR gate: Not needed — code is clean as written
