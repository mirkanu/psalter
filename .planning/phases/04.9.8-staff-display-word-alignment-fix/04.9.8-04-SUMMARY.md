---
phase: "04.9.8"
plan: "04"
subsystem: "uat/staff-alignment"
tags: ["playwright", "e2e", "uat", "staff-alignment", "RENDER-08", "failed"]
dependency_graph:
  requires: ["04.9.8-02", "04.9.8-03"]
  provides: ["RENDER-08-gate"]
  affects: ["scripts/uat/verify-staff-alignment.js", "scripts/output/staff-alignment-report.json"]
tech_stack:
  added: []
  patterns: ["Playwright DOM inspection", "per-note-group lyric check", "abcjs-staff-wrapper selector"]
key_files:
  created:
    - scripts/uat/verify-staff-alignment.js
    - scripts/output/staff-alignment-report.json
    - scripts/uat/screenshots/staff-alignment-failures/ (136 PNG files)
  modified: []
decisions:
  - "Count g.abcjs-note children per g.abcjs-staff-wrapper, not total SVG note elements — avoids double-counting nested elements"
  - "trailingEmpty metric (consecutive final notes without lyrics) is the correct alignment signal"
  - "Image-fallback psalms counted as PASS — no abcjs SVG = valid state, not a bug"
  - "Failure threshold: trailingEmpty > TOLERANCE (default 1) — allows 1 legitimate melisma"
metrics:
  duration: "~35 minutes"
  completed: "2026-05-28"
  tasks: 2
  files: 139
  psalms_checked: 150
  passed: 14
  failed: 136
  key_psalm_failures: [23, 100, 119]
---

# Phase 04.9.8 Plan 04: Staff Alignment UAT — Playwright E2E Sweep Summary

**One-liner:** Playwright UAT script verifies per-note-group lyric presence across all 150 psalms; full sweep reveals 136/150 failing with trailing empty notes, confirming Plan 02 DB migration placed PHRASE_BREAKs at ABC text-line boundaries rather than note-head count boundaries.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 04-01 | Write Playwright staff alignment UAT script | 152a06f | scripts/uat/verify-staff-alignment.js |
| 04-02 | Run full 150-psalm sweep and capture report | 2a9cdd4 | scripts/output/staff-alignment-report.json, 136 screenshots |

## What Was Built

### `scripts/uat/verify-staff-alignment.js`

Playwright E2E that loads each psalm at `/psalms/<n>?view=staff`, inspects the abcjs SVG DOM, and asserts alignment.

**DOM selector strategy (verified against live abcjs 6.6.3):**
- `g.abcjs-staff-wrapper` — one element per rendered staff row (confirmed via DOM inspection)
- `g.abcjs-note` within each wrapper — one element per note position (one syllable slot)
- `.abcjs-lyric` within each `g.abcjs-note` — present if note has lyrics; absent = empty position

**Failure signal:** `trailingEmpty > TOLERANCE` — counts consecutive final notes in a staff row that have no lyric child. This correctly identifies the "words cut off at right edge" bug.

**Features:**
- Configurable `PSALM_LIST`, `UAT_BASE`, `TOLERANCE` env vars
- Failure screenshots saved to `scripts/uat/screenshots/staff-alignment-failures/`
- JSON report at `scripts/output/staff-alignment-report.json`
- Hard 20-minute timeout
- Exits non-zero when failures detected (CI gate-compatible)

### Full Sweep Results

| Metric | Value |
|--------|-------|
| Psalms checked | 150 |
| Passed | 14 |
| Failed | 136 |
| Failure reason | `empty-notes` (trailing notes without lyrics) |
| Key psalm 23 | FAIL — l0: 15 notes, 8 lyrics, 7 trailing empty |
| Key psalm 100 | FAIL — l0: 12 notes, 10 lyrics, 2 trailing empty |
| Key psalm 119 | FAIL — l0: 16 notes, 9 lyrics, 7 trailing empty |

**Passing psalms (14):** 4, 25, 45, 67, 69, 70, 71, 83, 99, 103, 104, 124, 136, 143 — these are psalms whose default tunes either render via image fallback (no abcjs SVG) or have correct note-to-lyric alignment by coincidence.

## Root Cause Confirmed by UAT

The sweep confirms Plan 02's DB migration did NOT correctly fix the alignment bug. Inspection of Crimond's `abc_notation` after migration:

```
c2a4bg | c'4bgf4 | e2f4a2 | a2g2g2=b4 |
% PHRASE_BREAK
% PHRASE_BREAK          ← consecutive empty phrase
=b2c'4a2 | ... | e2f4
% PHRASE_BREAK
```

Phrases after `splitOnPhraseBreaks`:
- Phrase 0: 15 note heads (CM lines 1+2 combined — should be 8)
- Phrase 1: EMPTY (between consecutive PHRASE_BREAKs)
- Phrase 2: 19 note heads (CM lines 3+4 combined — should be 8 and 6 separately)
- Phrase 3: EMPTY

The migration inserted PHRASE_BREAKs at ABC text-line boundaries (where the original ABC had a newline), not after counting exactly 8 note heads. For Crimond, the entire first half of the tune is on one ABC text line — so the algorithm put 2 PHRASE_BREAKs together at the end of that line (creating an empty phrase) rather than inserting them mid-line after 8 and 14 note heads.

## Task 04-03 Outcome — Human Review Result: FAILED

**User response received:** `failed: PHRASE_BREAKs placed at text-line boundaries not mid-line at note-head count boundaries — Plan 02 insertPhraseBreaks must insert markers within ABC text lines at the exact note-head count split point`

The phase has **NOT met RENDER-08**. The E2E sweep and human review confirm the root cause: `insertPhraseBreaks` inserts `% PHRASE_BREAK` at the positions of ABC text-line boundaries (newlines in the stored ABC notation), not at the exact character offset within a single ABC text line where N note heads have been counted.

### What Needs to Be Fixed in the Gap-Closure Phase

`insertPhraseBreaks` must be rewritten to:

1. **Tokenize each ABC music line character-by-character** — do not rely on newline positions.
2. **Count note heads inline.** Recognise all ABC note tokens (pitches `[A-Ga-gz]` + modifiers + durations). Skip rests (`z`), barlines (`|`), chord symbols, decorations, and grace-note groups.
3. **Insert `% PHRASE_BREAK` at the exact character offset** after the N-th note head within the line — the comment must appear mid-line, not at the end or on a separate line.
4. **Handle `double_length` flag** — when set, each written note counts as 2 beat positions.
5. **Re-run this UAT script** after the fix to confirm 150/150 pass.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] page.evaluate argument serialization — tolerance not defined in browser**
- **Found during:** Task 04-01, initial smoke test
- **Issue:** `page.evaluate(fn, { tolerance, psalmNum })` serialized as object but the function signature used destructuring syntax incompatible with Playwright's arg passing
- **Fix:** Changed to pass `{ tolerance: TOLERANCE, psalmNum }` as args object and access via `args.tolerance` inside evaluate
- **Files modified:** scripts/uat/verify-staff-alignment.js
- **Commit:** 152a06f (inline fix before commit)

**2. [Rule 3 - Blocker] Stale Next.js build causing chunk load errors**
- **Found during:** Task 04-01, initial DOM inspection
- **Issue:** App was serving 500 on JS chunks — build artifacts stale from prior wave
- **Fix:** Ran `npm run build && pm2 restart psalter` from main repo directory
- **Files modified:** None (operational fix)
- **Commit:** N/A

**3. [Rule 1 - Bug] Note count selector over-counting — total notes vs per-note-group**
- **Found during:** Task 04-01, selector verification
- **Issue:** Counting `.abcjs-note` elements (which includes the `g` wrapper AND nested paths) inflated note counts (34 total vs 15 expected for Psalm 23 l0). Each `g.abcjs-note` is one note position; using `querySelectorAll('.abcjs-note')` on the parent counted nested elements too.
- **Fix:** Changed to `g.abcjs-staff-wrapper` parent selector + `g.abcjs-note` note-position groups + `.abcjs-lyric` presence check per note group
- **Files modified:** scripts/uat/verify-staff-alignment.js
- **Commit:** 152a06f (inline fix before commit)

## Known Stubs

None. The UAT script is complete and functional. The failures detected are real alignment bugs, not stubs.

## Threat Flags

None. This plan adds a read-only Playwright test script — no new network endpoints, auth paths, or schema changes.

## Self-Check: FAILED

The phase acceptance criterion (RENDER-08: all 150 psalms render with no trailing empty notes) was **not met**.

File/artifact checks:
- scripts/uat/verify-staff-alignment.js — FOUND
- scripts/output/staff-alignment-report.json — FOUND (psalmsChecked: 150, passed: 14, failed: 136)
- Commit 152a06f — FOUND (Task 04-01: Playwright script)
- Commit 2a9cdd4 — FOUND (Task 04-02: full sweep report)
- `passed + failed = 150` — verified

**Phase exit status: INCOMPLETE — gap closure required.**

The UAT script and sweep report are valuable artifacts and are correctly committed. The fix required is a rewrite of `insertPhraseBreaks` in Plan 02 to place `% PHRASE_BREAK` markers at mid-line note-head count boundaries rather than at ABC text-line boundaries.
