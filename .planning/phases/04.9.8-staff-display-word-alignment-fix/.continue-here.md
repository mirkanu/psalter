# Continue Here — Phase 04.9.8 Plan 05 Checkpoint

## Status
Plan 05 task 05-04 (human-verify checkpoint) received **partial** response.

## What was completed
- Task 05-01: insertPhraseBreaks rewritten with token-level tokenizer ✓
- Task 05-02: DB re-migrated with --overwrite (134 tunes) ✓  
- Task 05-03: 150-psalm Playwright sweep (66/150 passing) ✓
- Mobile rendering fix committed: `67e741d` — proportional w: split on <480px screens ✓
- All three ROADMAP psalms (23, 100, 119) now show zero empty staves on mobile ✓

## User response: "partial"
The last 2 staff rows (lines 3 and 4 of Crimond/CM) still have trailing notes without lyrics at the end.

## Root cause of trailing notes
Crimond ABC phrase 4 has **12 note heads** but CM final line has only **6 syllables**. The extra 6 notes are a musical coda/cadence encoded in the source ABC. After the 3rd PHRASE_BREAK (at note 22), the remaining music is notes 23-34 (12 notes) vs 6 syllables.

This is a **DATA issue** in the ABC source, not a rendering bug. The insertPhraseBreaks algorithm correctly places markers at note-head 8/14/22 offsets; the 4th phrase just has more notes than the final CM line needs.

## What needs to happen next
1. Decide: is this acceptable variance (Scottish Psalter tunes traditionally have cadential extensions), or does the ABC data need trimming?
2. If acceptable: document as accepted variance and reply "approved" to close the checkpoint
3. If not acceptable: the ABC notation for CM tunes needs the coda trimmed before re-migration

## Agent to continue
Use `SendMessage` to agent `aed78c20330da0bf6` with the user's decision, OR start fresh with /gsd-execute-phase 4.9.8 which will resume from the checkpoint.

## Commits since last session
- `eada40b` — feat: rewrite insertPhraseBreaks + DB migration
- `108181e` — feat: NotationRenderer phrase-line join fix  
- `65a13a6` — docs: plan 05 SUMMARY
- `67e741d` — fix(notation): mobile staff lyric alignment — proportional w: split
