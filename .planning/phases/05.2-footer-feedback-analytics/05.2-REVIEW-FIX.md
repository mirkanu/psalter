---
phase: "05.2"
fixed_at: "2026-06-20T17:26:24Z"
review_path: ".planning/phases/05.2-footer-feedback-analytics/05.2-REVIEW.md"
iteration: 1
findings_in_scope: 7
fixed: 7
skipped: 0
status: all_fixed
---

# Phase 05.2: Code Review Fix Report

**Fixed at:** 2026-06-20T17:26:24Z
**Source review:** `.planning/phases/05.2-footer-feedback-analytics/05.2-REVIEW.md`
**Iteration:** 1

**Summary:**
- Findings in scope: 7 (CR-01, CR-02, CR-03, WR-01, WR-02, WR-04, WR-05)
- Fixed: 7
- Skipped: 0

## Fixed Issues

### CR-01 + CR-02 + CR-03: API route hardening (grouped, same file)

**Files modified:** `src/app/api/feedback/route.ts`
**Commit:** 64d954f
**Applied fix:**
- Added `MAX_MESSAGE = 5000` and `MAX_SHORT = 200` constants
- Message length validated; returns 400 if exceeded
- `name` and `pageUrl` silently truncated to `MAX_SHORT` before insert
- Email validated with `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`; stored as `null` if invalid or empty
- `db.insert` wrapped in `try/catch`; logs `[feedback] DB insert failed:` and returns 500 JSON on error

---

### WR-01: Guard createdAt.toLocaleString in admin feedback page

**Files modified:** `src/app/admin-only/feedback/page.tsx`
**Commit:** 4197314
**Applied fix:** Changed `row.createdAt.toLocaleString('en-GB')` to an `instanceof Date` guard that casts to `new Date(row.createdAt)` when the value is a string, preventing a runtime TypeError when Drizzle returns a string timestamp.

---

### WR-02: Log errors in FeedbackModal catch block

**Files modified:** `src/components/FeedbackModal.tsx`
**Commit:** 67cad51
**Applied fix:** Changed `catch {` to `catch (err) {` and added `console.error('[FeedbackModal] submission failed:', err)` before `setStatus('error')`.

---

### WR-04: Conditionally mount Dialog content in SiteFooter

**Files modified:** `src/components/SiteFooter.tsx`
**Commit:** 39a6301
**Applied fix:** Wrapped both the About and Copyright `<Dialog>` blocks in `{open === '...' && (...)}` guards so portal content is only mounted when the corresponding modal is active. The `open` prop on each Dialog is now a static `true` (the guard ensures it only renders when open). FeedbackModal already receives `open={open === 'feedback'}` and manages its own lifecycle, so it was left unchanged.

---

### WR-05: Fix garbled check name in UAT error handler

**Files modified:** `scripts/uat/phase-05.2-uat.js`
**Commit:** 68d8ff4
**Applied fix:** Replaced `check.name.replace(/^check/, '').replace(/([A-Z])/g, ' $1').trim()` with `check.name.replace(/^check/, '')`. The per-capital-letter split was splitting `FOOT01` into `F O O T 01` and `UMAMI01` into `U M A M I 01`. The raw function name (minus the `check` prefix) is already readable (`FOOT01Footer`, `UMAMI01Script`).

---

_Fixed: 2026-06-20T17:26:24Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
