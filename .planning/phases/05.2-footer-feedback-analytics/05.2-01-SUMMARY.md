---
phase: 05.2-footer-feedback-analytics
plan: "01"
subsystem: foundation
tags: [umami, analytics, feedback, database, shadcn, playwright, uat]
dependency_graph:
  requires: []
  provides: [NEXT_PUBLIC_PSALTER_UMAMI_WEBSITE_ID, feedbackSubmissions-table, Textarea-component, phase-05.2-uat-stub]
  affects: [src/db/schema.ts, src/components/ui/textarea.tsx, scripts/uat/phase-05.2-uat.js]
tech_stack:
  added: [shadcn/Textarea]
  patterns: [drizzle-kit-push, umami-rest-provisioning, playwright-daemon-uat]
key_files:
  created:
    - src/components/ui/textarea.tsx
    - scripts/uat/phase-05.2-uat.js
  modified:
    - src/db/schema.ts
    - .env (gitignored)
    - /home/services/.env.production (outside repo)
decisions:
  - "Umami UUID 772cede6-dcd9-46d6-8e31-395c853ba538 stored as NEXT_PUBLIC_PSALTER_UMAMI_WEBSITE_ID in .env (bakes in at build time)"
  - "feedback_submissions table is unauthenticated — no FK references, plain text columns, JSX auto-escaping guards XSS"
  - "RED UAT runs against live psalter.gsdlabs.dev — all 6 checks fail as expected until Wave 2 builds features"
metrics:
  duration: "~8 min"
  completed: 2026-06-20
  tasks_completed: 3
  files_modified: 3
requirements: [FOOT-04, FOOT-05, UMAMI-01]
---

# Phase 05.2 Plan 01: Foundation Prerequisites Summary

**One-liner:** Umami website provisioned via REST API (UUID in .env), Textarea shadcn component installed, feedback_submissions DB table live, RED Playwright UAT stub committed.

## What Was Built

Three prerequisite capabilities that downstream waves depend on:

1. **Umami website entry** — Provisioned `psalter.gsdlabs.dev` in the shared Umami instance at `http://localhost:3007` via REST API. UUID `772cede6-dcd9-46d6-8e31-395c853ba538` stored as `NEXT_PUBLIC_PSALTER_UMAMI_WEBSITE_ID` in `/data/home/psalter/.env` and as `PSALTER_UMAMI_WEBSITE_ID` in `/home/services/.env.production`. This var bakes into the build, so it must exist before any `npm run build`.

2. **Textarea shadcn component** — `npx shadcn@latest add textarea` created `src/components/ui/textarea.tsx` exporting `Textarea`. Required for `FeedbackModal.tsx` to compile in Wave 2.

3. **feedback_submissions DB table** — `feedbackSubmissions` pgTable appended to `src/db/schema.ts` with columns: `id` (serial PK), `message` (text notNull), `name` (text nullable), `email` (text nullable), `pageUrl` (text nullable), `createdAt` (timestamp defaultNow notNull). `drizzle-kit push` applied; table confirmed live in psalter-db.

4. **RED Playwright UAT stub** — `scripts/uat/phase-05.2-uat.js` (337 lines) exercises all 6 requirements (FOOT-01..05 + UMAMI-01) against `https://psalter.gsdlabs.dev`. Currently exits 1 (0/6 checks pass) — correct RED state. Wave 2 turns it GREEN.

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| Task 1 (Umami) | — | No repo commit — .env is gitignored; shared env file outside repo |
| Task 2 (Textarea + schema) | 4c4bd4c | feat(05.2-01): Textarea component + feedback_submissions schema |
| Task 3 (UAT stub) | 4227521 | test(05.2-01): RED Playwright UAT stub |

## Deviations from Plan

None — plan executed exactly as written.

## Threat Surface Scan

No new network endpoints, auth paths, or schema changes at trust boundaries introduced in this plan beyond what is documented in the plan's threat model. The `feedback_submissions` table is provisioned here; the unauthenticated insert path (T-05.2-03) is addressed in Plan 02 (API route). Plan 01 does not expose any new public surface.

## Known Stubs

None. No UI components created in this plan — foundation only.

## Self-Check: PASSED

| Item | Status |
|------|--------|
| src/components/ui/textarea.tsx | FOUND |
| scripts/uat/phase-05.2-uat.js | FOUND |
| feedbackSubmissions in schema.ts | FOUND |
| commit 4c4bd4c (Textarea + schema) | FOUND |
| commit 4227521 (UAT stub) | FOUND |
