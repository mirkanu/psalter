---
phase: 04.11
plan: 01
subsystem: rollback-infra
tags: [phase-04.11, rollback, snapshot, cleanup, d-04, d-06]
dependency_graph:
  requires: []
  provides:
    - rollback-snapshot.json (single source of truth for restoring pre-phase abc_notation)
    - rollback-recipe.md (executable recovery procedure)
  affects:
    - tunes.abc_notation (read-only in this plan; future Waves A/B may need rollback against this snapshot)
tech_stack:
  added: []
  patterns:
    - "Drizzle SELECT → JSON snapshot, mirroring scripts/apply-abc-notation.ts DB-init pattern"
    - "git rm for auditable deletion of wrong-direction artifacts"
key_files:
  created:
    - scripts/snapshot-abc-rollback.ts
    - .planning/phases/04.11-solfege-underline-ocr-melisma/rollback-snapshot.json
    - .planning/phases/04.11-solfege-underline-ocr-melisma/rollback-recipe.md
  modified: []
  deleted:
    - scripts/convert-crimond-musicxml.ts
    - scripts/convert-crimond-musicxml.test.ts
    - .planning/research/abc-samples/crimond-verified.abc
decisions:
  - "Snapshot captured 172 rows (28 with NULL abcNotation, 144 populated). Plan threshold was rowCount >= 150; PASS."
  - "Snapshot committed BEFORE any Wave A DB write, per D-04 — single rollback source of truth lives in git history."
  - "Crimond_deBoer.musicxml retained as reference-only per D-06; tune-digitisation-research.md rewrite deferred to Plan 06."
metrics:
  duration: ~5 minutes
  completed: 2026-05-30
  tasks: 3
  files_created: 3
  files_deleted: 3
---

# Phase 04.11 Plan 01: Rollback Snapshot + D-06 Cleanup Summary

Captured pre-phase `tunes.abc_notation` for all 172 rows into a committed JSON snapshot, authored the copy-pasteable recovery recipe, and removed the three 04.10 wrong-direction artifacts so the new underline-OCR recipe (Plan 06) lands on a clean slate.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Write snapshot script + produce rollback-snapshot.json | b5f4421 | scripts/snapshot-abc-rollback.ts, rollback-snapshot.json |
| 2 | Write rollback-recipe.md | d745234 | rollback-recipe.md |
| 3 | Delete D-06 wrong-direction artifacts | 8b77bda | (3 deletions) |

## Snapshot Results

- **rowCount:** 172 (plan threshold ≥150 — PASS)
- **rows with abcNotation populated:** 144
- **rows with NULL abcNotation:** 28 (un-OCR'd tunes — heuristic fallback path per D-05; will be restored to NULL on rollback)
- **File:** `.planning/phases/04.11-solfege-underline-ocr-melisma/rollback-snapshot.json`
- **Structure validated:** every row has `id`, `name`, `abcNotation` keys; `s.phase === '04.11'`; `s.rows.length === s.rowCount`.

## Recovery Recipe

`.planning/phases/04.11-solfege-underline-ocr-melisma/rollback-recipe.md` contains 5 sections:

1. When to roll back (Wave A sing-test fail OR Wave B `verify-staff-alignment.js` fails with TOLERANCE=0)
2. Full restoration — embedded one-shot `scripts/restore-from-rollback.ts` snippet (Drizzle UPDATE loop over `snap.rows`) + `npm run build && pm2 restart psalter`
3. Single-tune restoration — `npx tsx -e` one-liner template, filters snapshot by `name`
4. Post-rollback verification — `TOLERANCE=0 node scripts/uat/verify-staff-alignment.js`
5. Pre-Wave-B gate — operator checklist + `--wave-a-passed` flag requirement

## Deleted Files (D-06)

| File | Reason |
|------|--------|
| `scripts/convert-crimond-musicxml.ts` | 04.10 wrong-direction (per-tune MusicXML conversion is not the path forward) |
| `scripts/convert-crimond-musicxml.test.ts` | Test for the deleted script |
| `.planning/research/abc-samples/crimond-verified.abc` | "Actively misleading" per 04.10-FINDINGS.md |

`Crimond_deBoer.musicxml` was preserved (reference-only, not canonical) as required by D-06.

## Deviations from Plan

None — plan executed exactly as written. Snapshot count (172) is consistent with STATE.md's "~172" expectation; the planned `rowCount >= 150` floor was met with margin.

## Threat Flags

None — no new trust boundaries introduced; rollback-snapshot.json contains only `(id, name, abcNotation)` data already in the public-facing tunes column (no secrets).

## Known Stubs

None.

## Self-Check: PASSED

- FOUND: scripts/snapshot-abc-rollback.ts
- FOUND: .planning/phases/04.11-solfege-underline-ocr-melisma/rollback-snapshot.json
- FOUND: .planning/phases/04.11-solfege-underline-ocr-melisma/rollback-recipe.md
- FOUND commit b5f4421 (Task 1)
- FOUND commit d745234 (Task 2)
- FOUND commit 8b77bda (Task 3)
- VERIFIED: scripts/convert-crimond-musicxml.ts removed
- VERIFIED: scripts/convert-crimond-musicxml.test.ts removed
- VERIFIED: .planning/research/abc-samples/crimond-verified.abc removed
- VERIFIED: .planning/research/abc-samples/Crimond_deBoer.musicxml retained
