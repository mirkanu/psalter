---
phase: 04.11
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/lib/ocr-melisma-v3.ts
  - src/lib/ocr-melisma-v3.test.ts
  - src/lib/ocr-solfege-v2.ts
autonomous: true
requirements: []
tags: [phase-04.11, vision, ocr, prompt, d-01, d-05]

must_haves:
  truths:
    - "A new Vision-OCR module exists that calls Claude Haiku 4.5 with a prompt that PRESERVES underlines (reversing the explicit anti-instruction at ocr-solfege-v2.ts line 70)"
    - "The new module returns structured JSON with per-token { tok, dur, underlined, conf } arrays for each voice — not a marked-up string"
    - "The retry/backoff (4-retry exponential on 529) and image preprocessing (sharp resize at 3.5MB threshold) are REUSED verbatim from ocr-solfege-v2.ts via exported helpers, not re-implemented"
    - "A small unit-test fixture proves the JSON-parsing layer handles the new shape correctly (no live Vision call required in tests)"
  artifacts:
    - path: "src/lib/ocr-melisma-v3.ts"
      provides: "ocrMelismaV3(imageBuffers, opts) → MelismaTranscriptionResult JSON"
      contains: "ocrMelismaV3"
    - path: "src/lib/ocr-melisma-v3.test.ts"
      provides: "Vitest suite: prompt-content assertions + JSON-extraction round-trip on fixture"
      contains: "describe.*ocrMelismaV3"
    - path: "src/lib/ocr-solfege-v2.ts"
      provides: "Re-exports callClaude and prepareImageBuffer for v3 reuse"
      contains: "export.*callClaude"
  key_links:
    - from: "src/lib/ocr-melisma-v3.ts"
      to: "src/lib/ocr-solfege-v2.ts"
      via: "import { callClaude, prepareImageBuffer }"
      pattern: "import.*from ['\"]\\./ocr-solfege-v2['\"]"
---

<objective>
Build the new Vision OCR module that reverses CLAUDE.md hard rule #3: where the current pipeline discards underlines, the new prompt preserves them and returns structured per-token JSON suitable for downstream consumption by `buildEmbeddedWline` (Plan 02).

Purpose: this is the only Vision-touching code in 04.11. By splitting it from `build-embedded-wline.ts` (deterministic) we keep the deterministic layer 100% unit-testable and isolate non-determinism to a single module. The new prompt is the technical core: rewriting the OCR Vision prompt to emit per-token `underlined` + `conf` flags (RESEARCH §Pattern 1).

Output: `src/lib/ocr-melisma-v3.ts` calling Claude Haiku 4.5 with the new prompt; unit tests proving JSON-parsing on fixtures; minimal additive export from v2 to share `callClaude` + `prepareImageBuffer` so 04.11 does NOT fork retry/backoff logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.11-solfege-underline-ocr-melisma/04.11-RESEARCH.md
@.planning/phases/04.11-solfege-underline-ocr-melisma/04.11-PATTERNS.md
@.planning/research/lyric-to-note-alignment.md
@.planning/research/tonic-solfa-notation.md
@CLAUDE.md
@src/lib/ocr-solfege-v2.ts

<interfaces>
From src/lib/ocr-solfege-v2.ts:
- `callClaude(imageBuffers: Buffer | Buffer[], prompt: string, maxTokens?: number): Promise<string>` (lines 99-132) — 4-retry exponential backoff on 529.
- `prepareImageBuffer(imagePath: string): Promise<Buffer>` (lines 91-97) — sharp resize at 3.5MB threshold.
- Both are currently module-private (`async function`, not `export`). v3 must convert these to `export async function` OR add re-export statements (preferred: convert in place).
- Anti-instruction at line 70: `"Underlined notes: passing notes — transcribe the letters, ignore the underline."` — DO NOT modify v2's transcription path; just leave v2 unchanged for the heuristic fallback (D-05). Only export the helpers.
- Model id used: `'claude-haiku-4-5-20251001'` (line ~111).

From CLAUDE.md hard rules:
- #1: melisma = slur = underline = `_` in ABC w-line.
- #2: "passing note" pedagogical sense (melisma) ≠ harmonic sense (chromatic auxiliary). New prompt uses "underlined" terminology, not "passing".
- #3: current pipeline drops underlines — this phase fixes that.

From .planning/research/lyric-to-note-alignment.md §3:
"The underline beneath a note in solfège print is the canonical melisma-continuation marker — it indicates the note is sung as a continuation of the previous syllable's vowel."

From .planning/research/tonic-solfa-notation.md "Other Symbols":
- Underline (under a note letter): melisma continuation.
- Dot subdivisions (`.`) BETWEEN notes: rhythm subdivision — NOT a melisma marker.
- Em-dash (`—`) BETWEEN notes: sustained hold.
- Subscript digit (`₁`) BELOW a letter: lower octave indicator.
These three lookalikes MUST be distinguished by the prompt.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert ocr-solfege-v2 helpers to exports (additive, non-breaking)</name>
  <read_first>
    - src/lib/ocr-solfege-v2.ts (lines 91-132 — the two helpers to export)
    - src/lib/ocr-solfege-v2.ts existing exports (top of file) — verify no name conflicts
  </read_first>
  <files>src/lib/ocr-solfege-v2.ts</files>
  <action>
Make TWO surgical edits to `src/lib/ocr-solfege-v2.ts`:

1. Change `async function prepareImageBuffer(imagePath: string): Promise&lt;Buffer&gt;` (line ~91) to `export async function prepareImageBuffer(imagePath: string): Promise&lt;Buffer&gt;`.
2. Change `async function callClaude(imageBuffers: Buffer | Buffer[], prompt: string, maxTokens = 3000): Promise&lt;string&gt;` (line ~99) to `export async function callClaude(imageBuffers: Buffer | Buffer[], prompt: string, maxTokens = 3000): Promise&lt;string&gt;`.

DO NOT change anything else in this file. In particular: DO NOT modify the transcribeOnly prompt (line 70 anti-instruction stays — heuristic fallback per D-05 continues to use it). DO NOT rename functions. DO NOT touch retry semantics.

Verify by grep that two exports exist and the heuristic prompt line is unchanged.
  </action>
  <verify>
    <automated>grep -c "^export async function callClaude" src/lib/ocr-solfege-v2.ts | grep -q "^1$" &amp;&amp; grep -c "^export async function prepareImageBuffer" src/lib/ocr-solfege-v2.ts | grep -q "^1$" &amp;&amp; grep -q "ignore the underline" src/lib/ocr-solfege-v2.ts &amp;&amp; echo OK</automated>
  </verify>
  <done>Two helpers now exported; heuristic-fallback prompt line preserved (the "ignore the underline" string still present — that v2 module remains the D-05 fallback path).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write ocr-melisma-v3.ts with new prompt + structured JSON return</name>
  <read_first>
    - src/lib/ocr-solfege-v2.ts (lines 1-160 — full pattern to mirror for module shape, JSON extraction)
    - .planning/research/lyric-to-note-alignment.md §3 (underline = melisma)
    - .planning/research/tonic-solfa-notation.md "Other Symbols" (underline vs dot vs em-dash vs subscript distinction)
    - .planning/phases/04.11-solfege-underline-ocr-melisma/04.11-RESEARCH.md §"Pattern 1" (JSON shape + prompt design recommendations)
    - .planning/phases/04.11-solfege-underline-ocr-melisma/04.11-PATTERNS.md §"src/lib/ocr-melisma-v3.ts"
  </read_first>
  <files>src/lib/ocr-melisma-v3.ts, src/lib/ocr-melisma-v3.test.ts</files>
  <behavior>
- The exported function `ocrMelismaV3(imagePaths: string[]): Promise&lt;MelismaTranscriptionResult&gt;` reads each JPG, prepares buffers via the v2 helper, calls Claude with the new prompt via the v2 helper, and parses the response JSON.
- Output type:
  ```typescript
  export interface MelismaTokenEntry { tok: string; dur: number; underlined: boolean; conf: number }
  export interface MelismaTranscriptionResult {
    doh: string
    time: string
    soprano: MelismaTokenEntry[]
    alto?: MelismaTokenEntry[]
    tenor?: MelismaTokenEntry[]
    bass?: MelismaTokenEntry[]
    raw: string  // raw JSON text for review JSONs and audit
  }
  ```
- Test 1: prompt-content assertion — read the constant TRANSCRIPTION_PROMPT_V3 from the module and assert it contains: (a) the literal phrase "PRESERVE every underline", (b) the literal phrase "underlined" (token field name), (c) the literal phrase from tonic-solfa-notation.md "Other Symbols" distinguishing underline from dot subdivision, (d) does NOT contain the string "ignore the underline" anywhere.
- Test 2: JSON extraction — given a fixed raw model output string (hand-crafted to match the expected JSON shape), the module's JSON-extraction helper parses it correctly into MelismaTranscriptionResult.
- Test 3: per-token confidence default — if a token in the JSON lacks `conf`, the extractor sets it to 0 (so downstream gating in Plan 04 treats it as low-confidence).
- Test 4: malformed JSON — input with no `{...}` block throws a descriptive error (matches v2 line 156-157 pattern).
- No tests in this plan call Claude Vision live (cost-free).
  </behavior>
  <action>
Create `src/lib/ocr-melisma-v3.ts` matching the structure of `src/lib/ocr-solfege-v2.ts` but with these specific differences:

**Imports:**
```typescript
import { callClaude, prepareImageBuffer } from './ocr-solfege-v2'
```
DO NOT import `solFaToAbc` or `solFaToAbcMultiVoice` (Pattern Map §ocr-melisma-v3 says "drop the solFaToAbc* imports").

**Prompt constant** — name it `TRANSCRIPTION_PROMPT_V3` and include EXACTLY the following directives (you may add context, but these literal substrings MUST appear so the tests pass):

```
You will be shown one or more JPG scans of a Scottish Psalter tune in tonic sol-fa notation.

For each voice (soprano, alto, tenor, bass — if present), return a JSON array of per-note token entries.

CRITICAL — UNDERLINE PRESERVATION:
The underline beneath a note letter in tonic sol-fa print is the canonical melisma-continuation
marker. It indicates that note is sung as a continuation of the previous syllable's vowel.
PRESERVE every underline. Mark each underlined note token with `"underlined": true`.

DISTINGUISH the underline from three visual lookalikes:
- Dot subdivisions BETWEEN notes (e.g. `f.,r`): a rhythm subdivision — NOT a melisma marker.
- Em-dash BETWEEN notes (`—`): a sustained hold — NOT a melisma marker.
- Subscript digit BELOW a letter (e.g. `d₁`): a lower-octave indicator — NOT a melisma marker.

Only the underline DIRECTLY BENEATH a note letter marks melisma continuation.

An underline may span 2, 3, or more consecutive notes. Mark EVERY underlined note (not just
the first) as `"underlined": true`.

For each token, supply:
- `tok`: the solfège letter with any octave markers (e.g. "m", "f", "d'", "s,", "_1", "te")
- `dur`: relative duration (1 = quarter, 2 = half, 4 = whole, 0.5 = eighth — best estimate)
- `underlined`: boolean
- `conf`: your confidence 0.0–1.0 for the (tok, underlined) pair on this token

Return ONLY a JSON object with this exact shape:
{
  "doh": "<key, e.g. F>",
  "time": "<time signature, e.g. 3>",
  "soprano": [ { "tok": "...", "dur": <num>, "underlined": <bool>, "conf": <num> }, ... ],
  "alto":    [ ... ],   // optional
  "tenor":   [ ... ],   // optional
  "bass":    [ ... ]    // optional
}
Do not include any prose outside the JSON object.
```

**Function** `ocrMelismaV3(imagePaths: string[]): Promise&lt;MelismaTranscriptionResult&gt;`:
1. For each path: `const buf = await prepareImageBuffer(path)`.
2. `const raw = await callClaude(buffers, TRANSCRIPTION_PROMPT_V3, 4000)` (maxTokens 4000 per RESEARCH).
3. `const m = raw.match(/\{[\s\S]*\}/)` — if no match, `throw new Error('No JSON in Claude response:\n' + raw.slice(0, 500))` (mirror v2 lines 155-157).
4. `const parsed = JSON.parse(m[0])` — coerce to MelismaTranscriptionResult.
5. For each voice array, ensure each entry has `conf` (default 0 if missing).
6. Return `{ ...parsed, raw }`.

**Tests** (`src/lib/ocr-melisma-v3.test.ts`):
- Export TRANSCRIPTION_PROMPT_V3 from the module so the test can read it directly.
- Test 1 uses `expect(TRANSCRIPTION_PROMPT_V3).toContain('PRESERVE every underline')`, `toContain('"underlined":')`, `toContain('Dot subdivisions BETWEEN notes')`, `not.toContain('ignore the underline')`.
- Test 2 uses a hand-crafted raw string with one soprano entry and asserts the parsed shape.
- Test 3 uses a raw string where one entry lacks `conf` and asserts `parsed.soprano[0].conf === 0`.
- Test 4 uses a raw string with no `{...}` block and asserts the function throws with a message containing "No JSON".

To enable Test 2-4, expose a thin internal `extractMelismaJson(raw: string): MelismaTranscriptionResult` helper (also exported) so tests don't need to mock the Claude SDK.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ocr-melisma-v3.test.ts 2&gt;&amp;1 | tee /tmp/v3-test.out &amp;&amp; grep -E "Tests.*[0-9]+ passed" /tmp/v3-test.out &amp;&amp; ! grep -E "[0-9]+ failed" /tmp/v3-test.out &amp;&amp; grep -q "PRESERVE every underline" src/lib/ocr-melisma-v3.ts &amp;&amp; ! grep -q "ignore the underline" src/lib/ocr-melisma-v3.ts &amp;&amp; grep -q "import { callClaude, prepareImageBuffer } from './ocr-solfege-v2'" src/lib/ocr-melisma-v3.ts &amp;&amp; echo OK</automated>
  </verify>
  <done>All 4 tests pass; prompt contains required strings and does NOT contain the anti-instruction; module imports retry/preprocess helpers from v2 (no forked implementation).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JPG scan → Vision API → response | JPG content is pre-controlled (sourced from R2/local public/tunes — no adversarial upload path). Vision response is untrusted text; JSON.parse path must not be DoS'd by oversize responses. |
| ocr-melisma-v3 → ocr-solfege-v2 helpers | Same-process module import; trust boundary is just code review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.11-03-01 | Spoofing | Vision response forged | accept | Anthropic SDK is the only callsite; transport is HTTPS; API key in env. |
| T-04.11-03-02 | Tampering | Prompt injection from JPG (e.g. text embedded in scan saying "ignore underlines") | mitigate | Prompt is explicit and stable; Claude is instructed to return ONLY a JSON object; downstream extractMelismaJson rejects non-JSON. Plus per-token `conf` enables gating in Plan 04. |
| T-04.11-03-03 | DoS | Oversized response (Claude returns 100MB of text) | mitigate | `maxTokens: 4000` cap on response; per-call timeout inherited from SDK default; sharp resize keeps inputs ≤ 5MB. |
| T-04.11-03-04 | Information Disclosure | API key logging | mitigate | callClaude does not log key; established v2 pattern. |
| T-04.11-03-05 | Cost runaway (severity: medium per planning context) | Vision spend | mitigate | This plan ONLY builds the module; Plan 04 batch driver enforces tune-count limits. Tests in this plan do NOT make live Vision calls. |
</threat_model>

<verification>
- All 4 Vitest tests pass
- Required prompt strings present; anti-instruction absent
- v2 helpers exported and reused (no forked retry logic)
- Module passes no live Vision call in test scope (cost-free)
</verification>

<success_criteria>
- ocr-melisma-v3.ts module exports ocrMelismaV3, extractMelismaJson, TRANSCRIPTION_PROMPT_V3
- Prompt content asserted via tests (CLAUDE.md hard rule #3 reversal verified)
- v2 helpers exported additively; heuristic fallback path (D-05) preserved
</success_criteria>

<output>
After completion, create `.planning/phases/04.11-solfege-underline-ocr-melisma/04.11-03-SUMMARY.md` summarising: prompt design decisions (esp. the three lookalike distinctions), the exact MelismaTranscriptionResult schema, and any deviations from the RESEARCH §Pattern 1 sketch.
</output>
