# Phase 04.11: Solfège Underline OCR — Universal Melisma Extraction - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-30
**Phase:** 04.11-Solfège Underline OCR — Universal Melisma Extraction
**Areas discussed:** Output format & storage; Verification gate / sampling strategy; Migration path, rollback & heuristic retirement

---

## Output format & storage

| Option | Description | Selected |
|--------|-------------|----------|
| Edit the tune's ABC string directly | `abc_notation` row contains full w: line with `_` continuations baked in (04.10's Crimond model). One canonical string per tune. | ✓ (Claude's discretion) |
| Edit melisma data separately, ABC stays clean | Separate column/table for underline positions; NotationRenderer merges at render. ABC and melisma evolve independently. | |
| Not sure — you decide | Defer to Claude | (user picked this; Claude then chose option 1) |

**User's choice:** "Not sure — you decide" → Claude selected **Edit the tune's ABC string directly**.
**Notes:** Rationale: matches 04.10 runtime path; avoids two-system drift (per `melisma-analysis-handoff` memory); rollback is single-column UPDATE; diffs are human-readable.

---

## Verification gate / sampling strategy

### Sub-question 1: How are sample tunes picked?

| Option | Description | Selected |
|--------|-------------|----------|
| Hardest cases per family | Tunes most likely to expose melisma bugs | |
| Most-used tunes per family | Tunes congregations actually sing most (Crimond, Martyrdom) | |
| Mix: 1 hardest + 1 most-used per family | ~10–12 tunes total; balances coverage and effort | ✓ |
| You decide based on catalog | Pick strategy after inventorying 150 tunes | |

**User's choice:** Mix: 1 hardest + 1 most-used per meter family.

### Sub-question 2: Pass bar before un-sampled tunes auto-promote

| Option | Description | Selected |
|--------|-------------|----------|
| All sampled tunes pass exactly | Strict gate; zero defects on samples | |
| All pass + automated alignment check passes for the rest | Two-layer: human ear on samples + machine sweep on population | ✓ |
| Tolerate small defects with documented exceptions | Up to 1 minor defect/sample if logged | |

**User's choice:** Two-layer gate — sing-test on samples + `verify-staff-alignment.js` 150/150 on the rest.

---

## Migration path, rollback & heuristic retirement

### Sub-question 1: Rollout shape

| Option | Description | Selected |
|--------|-------------|----------|
| Single batch — all 150 at once | One UPDATE replaces everything | |
| Per-meter rollout | CM first, then LM, SM, CMD, alt — sing-test per family | |
| Per-meter, no production write until all meters pass | Compromise: per-meter sing-test cadence, single-batch production cutover | |
| (User free-text response) | "currently I'm sing-testing from production build - the key is that if Claude Vision (costly) or anything else that's computing/time expensive is involved, it should only be executed on a dozen psalms/tunes at most. These should be pushed to production for me to sing-test there" | ✓ |

**User's choice:** Free-text constraint → restructured into **two-wave rollout**:
- **Wave A:** OCR only the ~12 sampled tunes, push to production, user sing-tests there
- **Wave B:** If Wave A passes, OCR remaining ~138 in one batch, machine-gate via `verify-staff-alignment.js`, single production UPDATE with rollback recipe

**Notes:** Vision cost is bounded by Wave A's go/no-go. DB snapshot must be captured BEFORE Wave A.

### Sub-question 2: Heuristic code retirement

| Option | Description | Selected |
|--------|-------------|----------|
| Delete in this phase | Heuristic path is dead code once canonical w-lines exist for all 150 tunes | |
| Keep as fallback for un-OCR'd tunes | NotationRenderer's w-branch auto-selects between embedded w: lines and heuristic | ✓ |
| Schedule for a follow-up phase | Defer deletion to a separate stabilisation phase | |

**User's choice:** Keep as fallback. Tunes that fail OCR (missing JPG, ambiguous underlines) simply don't get a DB write — heuristic continues to render them transparently.

---

## Claude's Discretion

User skipped the "Underline detection & OCR prompt" gray area. Captured in CONTEXT.md `<decisions>` → "Claude's Discretion":

- New Vision prompt rewrite (preserve underlines as per-syllable continuation flags)
- Ambiguity / faint-print handling (per-syllable confidence flag; below threshold → skip tune)
- Missing / low-quality JPGs (skip tune, no DB write, heuristic fallback applies)
- Tokenisation (reuse abc-embedded-lyrics helpers from 04.10 — no new representation)

The "storage" sub-question above also defaulted to Claude — captured there.

## Deferred Ideas

- **Full heuristic retirement** — follow-up phase after 04.11 stabilises
- **Per-tune underline curation UI** — admin tool for hand-fixing tunes that fail OCR
- **Re-running annotate-phrase-breaks with syllabic-aware counting** — may be moot once canonical w-lines exist; defer
