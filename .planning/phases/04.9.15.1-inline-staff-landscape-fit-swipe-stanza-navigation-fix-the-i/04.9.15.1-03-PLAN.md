---
phase: 04.9.15.1
plan: 03
type: execute
wave: 2
depends_on: ["04.9.15.1-01", "04.9.15.1-02"]
files_modified:
  - src/components/singing/SingingView.tsx
  - src/components/singing/OnboardingTour.tsx
autonomous: false
requirements: [MOBILE-10]

must_haves:
  truths:
    - "Swiping left over the notation region advances one stanza-set and swiping right goes back one, reusing handleStanzaNext/handleStanzaPrev, whenever totalStanzas > 1"
    - "When phoneLandscapeChromeHide && totalStanzas > 1, the StanzaDotIndicator renders (bottom-right pill) and the GlassBottomBar stanza indicator is not visible (bar is hidden in that state)"
    - "The dot indicator does NOT render when the bottom bar is visible (portrait phone, tablet, desktop, landscape-with-bar-visible)"
    - "On first encounter (and when totalStanzas > 1) a swipe tour step appears using the existing OnboardingTour spotlight on the notation area, showing an animated hand + a live dot-indicator preview, restartable via the Gear 'Restart tour' entry"
    - "Desktop (mouse-only) attaches no swipe listener and keeps the existing bottom-bar prev/next buttons unchanged"
  artifacts:
    - path: "src/components/singing/SingingView.tsx"
      provides: "Swipe wiring + dot-indicator mount + totalStanzas threaded into OnboardingTour"
      contains: "useSwipeGesture"
    - path: "src/components/singing/OnboardingTour.tsx"
      provides: "Conditional swipe tour step + totalStanzas prop + in-spotlight hand/dot visuals"
      contains: "SWIPE_STEPS"
  key_links:
    - from: "SingingView useSwipeGesture onSwipeLeft/onSwipeRight"
      to: "handleStanzaNext / handleStanzaPrev"
      via: "same callbacks already passed to GlassBottomBar"
      pattern: "onSwipeLeft: handleStanzaNext"
    - from: "OnboardingTour totalStanzas prop"
      to: "SingingView OnboardingTour usage"
      via: "prop threading (mirrors existing viewMode prop)"
      pattern: "totalStanzas="
    - from: "OnboardingTour swipe-step dot preview"
      to: "StanzaDotIndicator dot visuals"
      via: "in-spotlight clone using the same pulse classes"
      pattern: "psalter-dot-pulse"
---

<objective>
Integrate MOBILE-10 into the singing view using the building blocks from Plan 02:
1. Attach `useSwipeGesture` to the notation region so left/right swipes page stanza-sets
   (reusing the existing `handleStanzaNext`/`handleStanzaPrev`, NOT a parallel pagination path).
2. Mount `StanzaDotIndicator` only when the bottom chrome bar is auto-hidden in phone landscape
   (`phoneLandscapeChromeHide && totalStanzas > 1`) — replacing the now-invisible bar stanza display.
3. Extend `OnboardingTour` with one conditional swipe-tutorial step (existing spotlight system,
   Sketch 007 Variant C), including an animated hand + a live dot-indicator preview.

Purpose: Deliver the full swipe navigation + carousel-dot + first-run tutorial experience,
additive alongside the untouched bottom-bar stanza display.
Output: SingingView + OnboardingTour edits. Ends with a human-verify checkpoint (touch physics
and real-device landscape cannot be reliably confirmed by desktop Playwright — see STATE.md
precedent on iOS touch simulation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04.9.15.1-inline-staff-landscape-fit-swipe-stanza-navigation-fix-the-i/04.9.15.1-CONTEXT.md
@.planning/phases/04.9.15.1-inline-staff-landscape-fit-swipe-stanza-navigation-fix-the-i/04.9.15.1-PATTERNS.md
@.planning/phases/04.9.15.1-inline-staff-landscape-fit-swipe-stanza-navigation-fix-the-i/04.9.15.1-UI-SPEC.md
@.planning/sketches/007-swipe-tutorial-tour/README.md
@.planning/sketches/007-swipe-tutorial-tour/index.html

<interfaces>
<!-- Contracts from Plan 02 (already built) — import and use directly. -->

useSwipeGesture (src/hooks/useSwipeGesture.ts):
  export function useSwipeGesture(
    ref: RefObject<HTMLElement | null>,
    options: { onSwipeLeft?: () => void; onSwipeRight?: () => void; enabled?: boolean },
  ): void

StanzaDotIndicator (src/components/singing/StanzaDotIndicator.tsx):
  export function StanzaDotIndicator(props: { current: number; total: number }): JSX.Element | null
  Its active-dot pulse uses:
    animate-[psalter-dot-pulse_1.8s_ease-in-out_infinite] motion-reduce:animate-none
  (the `psalter-dot-pulse` keyframe now exists in globals.css).

Existing SingingView state/handlers to REUSE (do NOT duplicate — SingingView.tsx):
  const mainRef = useRef<HTMLElement | null>(null)   // <main data-notation-region data-tour-target="scroll-area">
  const [currentStanza] = useState<number | null>(null)
  const [totalStanzas] = useState<number | null>(null)
  const [stanzaPage] = useState<number>(1)
  const handleStanzaPrev = useCallback(() => setStanzaPage((p) => Math.max(1, p - 1)), [])
  const handleStanzaNext = useCallback(() => setStanzaPage((p) => (totalStanzas ? Math.min(totalStanzas, p + 1) : p + 1)), [totalStanzas])
  const phoneLandscapeChromeHide = isPhone && isLandscapeOrientation && !showRotatePrompt   // line 397
  <OnboardingTour key={tourKey} viewMode={viewMode} />                                       // line 1002

Existing OnboardingTour conditional-step precedent to mirror (OnboardingTour.tsx):
  const SCROLL_HIDE_STEPS: Step[] = [ ... ]
  interface Props { viewMode?: ViewMode }
  const isSplitLeafMobile = isMobile && (viewMode === 'staff-split' || viewMode === 'solfege-split')
  const steps = useMemo<Step[]>(() => (isSplitLeafMobile ? [...STEPS, ...SCROLL_HIDE_STEPS] : STEPS), [isSplitLeafMobile])
  const TOUR_KEY = 'psalter_tour_v2'   // line 9 — bump to v3 (precedent: 04.9.14-01 comment bumped v1→v2 to re-trigger)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire useSwipeGesture into SingingView</name>
  <files>src/components/singing/SingingView.tsx</files>
  <read_first>
    - src/components/singing/SingingView.tsx (lines 380-470 — device/orientation state, currentStanza/totalStanzas/stanzaPage, handleStanzaPrev/Next; lines 626-780 — the existing scroll-hide touch/scroll effect the swipe listener must NOT fight; lines 884-894 — the main ref element)
    - src/hooks/useSwipeGesture.ts (the hook contract built in Plan 02)
    - .planning/phases/04.9.15.1-inline-staff-landscape-fit-swipe-stanza-navigation-fix-the-i/04.9.15.1-UI-SPEC.md ("Component Spec: Swipe Gesture" + the note about not breaking vertical scroll-hide)
  </read_first>
  <action>
    1. Import the hook: add `import { useSwipeGesture } from '@/hooks/useSwipeGesture'` near the other imports.

    2. Derive a touch-capability + enable flag near the existing device state (around line 397).
    Swipe is universal on touch devices whenever there is more than one stanza-set; desktop
    mouse-only attaches nothing.

    CRITICAL — SSR-safe pattern (do NOT read `window`/`navigator` in the render body): reading
    touch capability synchronously in render produces a different value during SSR (`window`
    undefined) vs. the client's first hydration render, causing a React hydration mismatch. Seed
    a touch-capability boolean via `useState(false)` + a mount-only `useEffect` (deps `[]`) — this
    is the EXACT pattern SingingView already uses for `isPhone` at lines 392-395 — THEN combine it
    with `totalStanzas > 1` in the render body:

        // near the other device state (after the isPhone useState/useEffect, ~line 395)
        const [isTouchDevice, setIsTouchDevice] = useState(false)
        useEffect(() => {
          setIsTouchDevice('ontouchstart' in window || navigator.maxTouchPoints > 0)
        }, [])

        // in the render body — pure derivation, no window/navigator access
        const enableSwipe = isTouchDevice && (totalStanzas ?? 0) > 1

    On first client render `isTouchDevice` is `false` (matching SSR output → no hydration
    mismatch); the mount effect then flips it, and React re-renders with swipe enabled on real
    touch devices. Desktop mouse-only stays `false` forever, so nothing is attached.

    3. Call the hook, reusing the SAME handlers already passed to GlassBottomBar — swipeLeft →
    next, swipeRight → prev (per UI-SPEC):

        useSwipeGesture(mainRef, {
          onSwipeLeft: handleStanzaNext,
          onSwipeRight: handleStanzaPrev,
          enabled: enableSwipe,
        })

    Attach point is `mainRef` (the <main data-notation-region> wrapping the notation region). Do
    NOT create a new pagination path — `handleStanzaNext`/`handleStanzaPrev` already drive
    `stanzaPage`, which `NotationRendererClient` consumes via `stanzaPage`/`onStanzaPageChange`.

    4. Add the swipe-region aria-label to the existing `<main ref={mainRef} ...>` element (around
    line 884-888), WITHOUT removing any existing attribute (`data-notation-region`,
    `data-tour-target="scroll-area"`, className, style):

        aria-label={enableSwipe ? 'Swipe left or right for previous or next stanza group' : undefined}

    The Plan 02 hook uses passive listeners and never calls `preventDefault()`, so the existing
    capture-phase scroll-hide `scroll` listener and vertical scrolling remain unaffected.
  </action>
  <verify>
    <automated>cd /home/services/psalter && npx tsc --noEmit 2>&1 | grep -i "SingingView" ; grep -c "useSwipeGesture(mainRef" src/components/singing/SingingView.tsx ; grep -c "onSwipeLeft: handleStanzaNext" src/components/singing/SingingView.tsx ; grep -c "onSwipeRight: handleStanzaPrev" src/components/singing/SingingView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` prints nothing for SingingView
    - `grep -c "useSwipeGesture(mainRef" src/components/singing/SingingView.tsx` returns 1
    - `grep -c "onSwipeLeft: handleStanzaNext" src/components/singing/SingingView.tsx` returns 1
    - `grep -c "onSwipeRight: handleStanzaPrev" src/components/singing/SingingView.tsx` returns 1
    - `grep -n "Swipe left or right for previous or next stanza group" src/components/singing/SingingView.tsx` matches (aria-label on main)
    - `grep -n "import { useSwipeGesture }" src/components/singing/SingingView.tsx` matches
    - `grep -c "data-notation-region" src/components/singing/SingingView.tsx` is unchanged (main attributes preserved)
    - No new stanza-pagination state introduced: `grep -c "setStanzaPage" src/components/singing/SingingView.tsx` unchanged vs before this task
  </acceptance_criteria>
  <done>Left/right swipes over the notation region drive the existing stanza-page handlers on touch devices with >1 stanza-set; desktop attaches nothing; vertical scroll-hide still works.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mount StanzaDotIndicator + thread totalStanzas into OnboardingTour</name>
  <files>src/components/singing/SingingView.tsx</files>
  <read_first>
    - src/components/singing/SingingView.tsx (lines 392-397 — phoneLandscapeChromeHide; lines 432-434 — currentStanza/totalStanzas/stanzaPage; lines 956-1002 — GlassBottomBar mount + OnboardingTour usage)
    - src/components/singing/StanzaDotIndicator.tsx (props contract from Plan 02)
    - .planning/phases/04.9.15.1-inline-staff-landscape-fit-swipe-stanza-navigation-fix-the-i/04.9.15.1-UI-SPEC.md ("Component Spec: Page-Dot Indicator" render condition — do not broaden or narrow)
  </read_first>
  <action>
    1. Import the component: `import { StanzaDotIndicator } from './StanzaDotIndicator'`.

    2. Mount it conditionally, as a sibling near the `GlassBottomBar` / `PlayMiniBar` block
    (around line 986-1000). Render condition is EXACTLY (per UI-SPEC — do not broaden/narrow):

        {phoneLandscapeChromeHide && (totalStanzas ?? 0) > 1 && (
          <StanzaDotIndicator current={currentStanza ?? 1} total={totalStanzas as number} />
        )}

    `phoneLandscapeChromeHide` and `totalStanzas`/`currentStanza` already exist as state in this
    file — no new derivation beyond this AND. Because `phoneLandscapeChromeHide` is precisely the
    state where the bottom bar auto-hides, the dot indicator only appears when the bar's stanza
    display is gone (they are mutually exclusive) — satisfying "additive, not duplicated".

    3. Thread `totalStanzas` into the existing OnboardingTour usage (line 1002). Change:

        <OnboardingTour key={tourKey} viewMode={viewMode} />

    to:

        <OnboardingTour key={tourKey} viewMode={viewMode} totalStanzas={totalStanzas} />

    (Mirrors how `viewMode` is already threaded as a gating prop.)
  </action>
  <verify>
    <automated>cd /home/services/psalter && npx tsc --noEmit 2>&1 | grep -i "SingingView" ; grep -c "phoneLandscapeChromeHide && (totalStanzas ?? 0) > 1" src/components/singing/SingingView.tsx ; grep -c "totalStanzas={totalStanzas}" src/components/singing/SingingView.tsx ; grep -c "import { StanzaDotIndicator }" src/components/singing/SingingView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` prints nothing for SingingView
    - `grep -c "phoneLandscapeChromeHide && (totalStanzas ?? 0) > 1" src/components/singing/SingingView.tsx` returns 1 (exact render condition)
    - `grep -c "<StanzaDotIndicator" src/components/singing/SingingView.tsx` returns 1
    - `grep -c "totalStanzas={totalStanzas}" src/components/singing/SingingView.tsx` returns 1 (prop threaded to OnboardingTour)
    - `grep -c "import { StanzaDotIndicator }" src/components/singing/SingingView.tsx` returns 1
    - `grep -c "<OnboardingTour" src/components/singing/SingingView.tsx` returns 1 (single mount, still keyed by tourKey)
  </acceptance_criteria>
  <done>The dot indicator renders only in the phone-landscape chrome-hidden state with >1 stanza-set; OnboardingTour receives totalStanzas for its gate.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add the conditional swipe tour step to OnboardingTour</name>
  <files>src/components/singing/OnboardingTour.tsx</files>
  <read_first>
    - src/components/singing/OnboardingTour.tsx (full file — TOUR_KEY line 9, STEPS/SCROLL_HIDE_STEPS lines 17-34, Props/isSplitLeafMobile/steps useMemo lines 72-101, clamp effect 103-109, measure effect 134-157, spotlight+bubble render 188-245)
    - .planning/sketches/007-swipe-tutorial-tour/index.html (Variant C — the exact hand-swipe animation keyframes/timing and dotted trail; the live dot-indicator preview placement)
    - .planning/sketches/007-swipe-tutorial-tour/README.md (winner rationale, tone: restrained, matches existing tour)
    - src/components/singing/StanzaDotIndicator.tsx (dot visual classes to clone for the preview)
    - .planning/phases/04.9.15.1-inline-staff-landscape-fit-swipe-stanza-navigation-fix-the-i/04.9.15.1-UI-SPEC.md ("Onboarding Tour Extension")
  </read_first>
  <action>
    1. Bump the localStorage key: change `const TOUR_KEY = 'psalter_tour_v2'` to
    `const TOUR_KEY = 'psalter_tour_v3'`. Update the adjacent comment to note this re-triggers
    the tour for existing users so they encounter the new swipe step (matching the existing
    v1→v2 precedent already documented in that comment). This reuses the EXISTING localStorage
    key name pattern — no new key is introduced.

    2. Add the new conditional step array beside `SCROLL_HIDE_STEPS` (verbatim locked copy):

        const SWIPE_STEPS: Step[] = [
          { target: 'scroll-area', copy: 'Swipe left or right to move between stanza groups — the dots show where you are.' },
        ]

    Reuse the existing `scroll-area` target (the `<main data-tour-target="scroll-area">` already
    exists and is already measured today) — do NOT invent a new data-tour-target.

    3. Extend `Props` with the new optional prop (mirrors the optional `viewMode?: ViewMode`):

        interface Props {
          viewMode?: ViewMode
          totalStanzas?: number | null
        }

    Update the destructure to `export function OnboardingTour({ viewMode, totalStanzas }: Props = {})`.

    4. Gate + append the step in the existing `steps` useMemo (preserve SCROLL_HIDE_STEPS behavior
    unchanged):

        const hasMultipleStanzas = totalStanzas != null && totalStanzas > 1
        const steps = useMemo<Step[]>(
          () => [
            ...STEPS,
            ...(isSplitLeafMobile ? SCROLL_HIDE_STEPS : []),
            ...(hasMultipleStanzas ? SWIPE_STEPS : []),
          ],
          [isSplitLeafMobile, hasMultipleStanzas],
        )

    The existing clamp-on-shrink effect (lines 103-109) already watches `steps.length`, so no
    change is needed there.

    5. Add the in-spotlight visuals for the swipe step ONLY. Compute a flag in the render body
    (after `currentStep` is derived, near line 173):

        const isSwipeStep = steps[currentStep]?.target === 'scroll-area' &&
          steps[currentStep]?.copy === SWIPE_STEPS[0].copy

    When `isSwipeStep`, render an absolutely-positioned overlay INSIDE the spotlight cutout,
    using the already-computed `bbox` (from `unionBbox(rects)`, line 178). Add it inside the
    `overlay` JSX, positioned within `bbox` (e.g. an absolutely-positioned div at
    `top: bbox.top, left: bbox.left, width: bbox.width, height: bbox.height`, `pointer-events-none`,
    `zIndex: 201` so it sits above the mask but below the bubble at 202). Inside it:

      (a) An animated hand glyph (use the lucide `Hand` or `Pointer` icon, or an inline SVG
      matching Sketch 007 Variant C's `.hand-swipe`) that slides left↔right with a dotted trail.
      Implement the slide via an inline `<style>` scoped keyframe OR a Tailwind arbitrary
      `animate-[...]` utility, 1.8s ease-in-out infinite with opacity fade at the ends (match the
      sketch's timing). Gate it behind reduced motion: `motion-reduce:animate-none` — reduced
      motion falls back to a static, centered hand glyph.

      (b) A live preview of the page-dot indicator, positioned bottom-right of the spotlight
      (`absolute bottom-2 right-2`). Clone StanzaDotIndicator's dot-row visuals inline (3
      illustrative dots is fine for the preview, matching the sketch) using the SAME classes:
      inactive `h-1.5 w-1.5 rounded-full bg-muted-foreground/40`, active
      `h-1.5 w-1.5 rounded-full bg-foreground animate-[psalter-dot-pulse_1.8s_ease-in-out_infinite] motion-reduce:animate-none`,
      wrapped in the same pill container classes
      (`flex items-center gap-1.5 rounded-full border border-border bg-background/85 backdrop-blur-sm px-2 py-1`).
      This is a visual clone (not the fixed-position real component) because the real one is
      `fixed bottom-right` and cannot be relocated into the spotlight.

    The bubble copy, "Step N of M" counter, Skip/Next/Done buttons, backdrop alpha (0.30), and
    SVG-mask spotlight are all UNCHANGED — this step reuses them verbatim. Only the in-cutout
    hand + dot-preview block is new, and only for `isSwipeStep`.
  </action>
  <verify>
    <automated>cd /home/services/psalter && npx tsc --noEmit 2>&1 | grep -i "OnboardingTour" ; grep -c "psalter_tour_v3" src/components/singing/OnboardingTour.tsx ; grep -c "SWIPE_STEPS" src/components/singing/OnboardingTour.tsx ; grep -c "hasMultipleStanzas" src/components/singing/OnboardingTour.tsx ; grep -c "psalter-dot-pulse" src/components/singing/OnboardingTour.tsx ; grep -c "SCROLL_HIDE_STEPS" src/components/singing/OnboardingTour.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` prints nothing for OnboardingTour
    - `grep -c "psalter_tour_v3" src/components/singing/OnboardingTour.tsx` returns 1 AND `grep -c "psalter_tour_v2" ...` returns 0 (key bumped)
    - `grep -c "SWIPE_STEPS" src/components/singing/OnboardingTour.tsx` returns >= 3 (declaration + gate + isSwipeStep reference)
    - `grep -n "Swipe left or right to move between stanza groups" src/components/singing/OnboardingTour.tsx` matches (locked copy verbatim)
    - `grep -n "totalStanzas?: number | null" src/components/singing/OnboardingTour.tsx` matches (prop added)
    - `grep -n "hasMultipleStanzas" src/components/singing/OnboardingTour.tsx` matches and appears in the useMemo dependency array
    - `grep -c "SCROLL_HIDE_STEPS" src/components/singing/OnboardingTour.tsx` unchanged behavior (still conditionally appended; existing steps preserved)
    - `grep -c "psalter-dot-pulse" src/components/singing/OnboardingTour.tsx` >= 1 (live dot preview) AND `motion-reduce:animate-none` present
    - `grep -c "target: 'scroll-area'" src/components/singing/OnboardingTour.tsx` >= 2 (existing scroll-hide entry + new swipe entry; no new data-tour-target invented)
  </acceptance_criteria>
  <done>A single swipe tutorial step appends to the existing tour when totalStanzas > 1, spotlighting the notation area with an animated hand + live dot preview (reduced-motion safe), restartable via Restart tour; existing tour steps and scroll-hide gating are unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — landscape fit + swipe + dots + tutorial on device</name>
  <what-built>
    MOBILE-09 landscape-fit fix (Plan 01) + MOBILE-10 swipe navigation, page-dot indicator, and
    first-run swipe tutorial step (Plans 02-03). Desktop Playwright cannot reliably simulate real
    touch physics or on-device landscape safe-area behavior (see STATE.md iOS-touch precedent),
    so a human confirms the touch + landscape experience.
  </what-built>
  <how-to-verify>
    Build + deploy (or run the app) and open a psalm with more than one stanza-set (e.g. Psalm 1,
    inline Staff view, a non-double CM tune). Then:

    1. LANDSCAPE FIT (MOBILE-09): On a phone in LANDSCAPE (Android in a browser tab, and/or iOS
       home-screen-installed), confirm one stanza-set fits on screen with NO vertical scrolling —
       previously ~1.5 lines showed and it required scrolling. Compare against portrait (already fine).
    2. SWIPE (MOBILE-10): On any touch device, swipe LEFT over the notation → advances to the next
       stanza-set; swipe RIGHT → goes back. Confirm short/vertical drags do NOT change the set and
       vertical scrolling (portrait/split-leaf) still works. At the first/last set, an over-swipe is a no-op.
    3. DOT INDICATOR (MOBILE-10): In phone landscape with the chrome bar auto-hidden (Android
       landscape, or iOS home-screen landscape), confirm a small carousel dot pill appears
       bottom-right showing current-of-total, the active dot pulses, and it updates as you swipe.
       Confirm it is ABSENT everywhere the bottom bar is visible (portrait phone, tablet, desktop).
    4. DESKTOP: Confirm the bottom-bar prev/next stanza buttons still work and NO dot pill appears.
    5. TUTORIAL (MOBILE-10): Trigger the tour (fresh visitor / cleared localStorage, or Gear →
       Restart tour). Confirm the new swipe step appears (only when >1 stanza-set), spotlights the
       notation area, shows the animated hand + a pulsing dot preview, uses the same bubble/counter/
       buttons as the rest of the tour, and can be completed/skipped. Verify reduced-motion (OS
       setting) yields static hand + non-pulsing dots.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. "landscape still scrolls on iOS", "dots appear when bar is visible", "tutorial hand not animating").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| touch input → stanza navigation | Client-only touch gestures invoke in-app page handlers. No network/PII crosses. |
| localStorage tour flag → tutorial gating | `psalter_tour_v3` value controls whether the client-side tutorial shows. Client-only. |
| static tour copy → DOM | Fixed string literals rendered as text; no user-generated content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.9.15.1-05 | D (DoS) | SingingView swipe listener churn | mitigate | Listeners are owned by `useSwipeGesture`'s single `useEffect` (Plan 02), attached/detached with cleanup and re-run only on `[ref, enabled, callbacks]`; SingingView adds no raw listener. |
| T-04.9.15.1-06 | T (Tampering) | `psalter_tour_v3` localStorage flag | accept | Tampering only re-shows/hides the client tutorial; no privilege, data, or server state depends on it (identical risk profile to the existing psalter_tour flag). |
| T-04.9.15.1-07 | S/XSS | OnboardingTour swipe-step copy & dot preview | accept | Copy is a static string literal; dot preview is numeric/decorative markup via JSX (auto-escaped); no `dangerouslySetInnerHTML`, no injection surface. |
</threat_model>

<verification>
- tsc + grep gates prove wiring, render condition, prop threading, key bump, and the conditional swipe step.
- Autonomous behavioral spot-check (optional, via Playwright daemon against a local build): assert
  the dot pill (`[data-stanza-dot-indicator]`) is present with an Android-landscape context and
  absent in portrait/desktop; assert the swipe tour step copy renders after clearing the tour key.
- Real touch + on-device landscape confirmation is the blocking human-verify checkpoint (Task 4),
  covering both MOBILE-09 and MOBILE-10 (hence depends_on includes Plan 01).
</verification>

<success_criteria>
- Swipe left/right pages stanza-sets on touch devices with >1 set, reusing the existing handlers; desktop unchanged.
- Dot indicator appears only when the chrome bar is auto-hidden in phone landscape; absent otherwise.
- First-run swipe tutorial step appears (when >1 set) via the existing tour, restartable, reduced-motion safe.
- Human verification confirms landscape fit + swipe + dots + tutorial on a real device.
</success_criteria>

<output>
After completion, create `.planning/phases/04.9.15.1-inline-staff-landscape-fit-swipe-stanza-navigation-fix-the-i/04.9.15.1-03-SUMMARY.md`
</output>
