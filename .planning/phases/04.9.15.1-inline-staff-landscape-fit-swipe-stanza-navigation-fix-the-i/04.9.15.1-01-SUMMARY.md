---
phase: 04.9.15.1-inline-staff-landscape-fit-swipe-stanza-navigation-fix-the-i
plan: 01
subsystem: ui
tags: [react, hooks, media-query, mobile, notation, abcjs]

# Dependency graph
requires:
  - phase: 04.9.15
    provides: chromeless-mode staffWidthFactor/compactSplitMobile fit gate, --staff-base-size, isPhoneDevice/useMediaQuery landscape-detection pattern (SingingView.tsx)
provides:
  - Landscape-aware compact-fit gate (isPhoneLandscapeForFit) in NotationRenderer.tsx feeding staffWidthFactor and compactSplitMobile
affects: [04.9.15.1-02, 04.9.15.1-03]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "useMediaQuery hook always called unconditionally at top level; combined with SSR-safe isPhone state via && AFTER the hook call — never short-circuited into the hook call itself (rules-of-hooks safety)"

key-files:
  created: []
  modified:
    - src/components/notation/NotationRenderer.tsx

key-decisions:
  - "Reused the exact isPhone state + '(orientation: landscape) and (max-width: 926px)' media query pattern already established in SingingView.tsx, rather than inventing a new phone-landscape detection mechanism"

patterns-established:
  - "Phone-landscape detection for fit/layout gates: SSR-safe isPhone state (set via isPhoneDevice() in a useEffect) combined with an unconditionally-called useMediaQuery('(orientation: landscape) and (max-width: 926px)') hook"

requirements-completed: [MOBILE-09]

# Metrics
duration: 15min
completed: 2026-07-27
---

# Phase 04.9.15.1 Plan 01: Landscape-Aware Compact-Fit Gate Summary

**Extended NotationRenderer's inline-Staff compact-fit gate (`staffWidthFactor` + `compactSplitMobile`) to also trigger on phone landscape (768-926px wide, height-constrained), not just narrow portrait (<768px width) — fixing MOBILE-09's scroll-forcing bug on landscape phones.**

## Performance

- **Duration:** ~15 min
- **Completed:** 2026-07-27T08:22:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Added `isPhoneLandscapeForFit` boolean (SSR-safe `isPhone` state + unconditional `useMediaQuery('(orientation: landscape) and (max-width: 926px)')` call) reusing SingingView.tsx's established phone/orientation detection pattern
- OR'd `isPhoneLandscapeForFit` into both `staffWidthFactor` and `compactSplitMobile` gates alongside the existing `viewportW < 768` check
- Preserved the `chromeless` guard on both gates, so desktop and non-chromeless callers (`/tunes/[id]`, `/study`) remain byte-identical
- Left `cyclePage`/`totalStanzaPages`/`CYCLES_PER_PAGE` pagination system, `isNarrowPortrait`, and the split-leaf `beside` layout completely untouched (out of scope per plan)

## Task Commits

1. **Task 1: Extend the compact-fit gate to cover phone landscape** - `c8ed4d9` (feat)

**Plan metadata:** (this commit, to follow)

## Files Created/Modified
- `src/components/notation/NotationRenderer.tsx` - Added `isPhoneDevice` import, SSR-safe `isPhone` state, `isPhoneLandscapeMQ`/`isPhoneLandscapeForFit` booleans, and widened the `staffWidthFactor`/`compactSplitMobile` gates to OR in `isPhoneLandscapeForFit`

## Decisions Made
None beyond what the plan specified — followed the plan's exact interface contract (reuse of SingingView.tsx's pattern, unconditional hook call, exact media query string).

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. One process note: the first verification run was executed against the wrong working directory (`/home/services/psalter` instead of the worktree checkout), which produced misleading grep results suggesting the edits hadn't applied. Re-running the same verification commands against the correct worktree cwd confirmed all edits were present and all acceptance criteria passed. No code was affected by this — it was purely a verification-tooling mistake, caught and corrected before committing.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The landscape-aware fit gate is in place and structurally verified (`tsc --noEmit` clean, all grep-based acceptance criteria pass, `CYCLES_PER_PAGE` count unchanged, no rules-of-hooks violations).
- Behavioral confirmation (measuring actual rendered SVG height vs. viewport height on a live/built app via the Playwright daemon, per this plan's `<verification>` section) was not run in this session — it requires `npm run build && npm run start`, which is out of scope for a pure code-edit task-commit loop. This is expected to be covered by Plan 03's consolidated human-verify checkpoint alongside the other two plans in this phase.
- No blockers for Plan 02 (swipe stanza navigation) or Plan 03 (consolidated verification) — this plan's change is additive and isolated to the fit-gate booleans.

---
*Phase: 04.9.15.1-inline-staff-landscape-fit-swipe-stanza-navigation-fix-the-i*
*Completed: 2026-07-27*
