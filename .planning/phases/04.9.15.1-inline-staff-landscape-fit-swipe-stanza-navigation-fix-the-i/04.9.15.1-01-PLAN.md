---
phase: 04.9.15.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/notation/NotationRenderer.tsx
autonomous: true
requirements: [MOBILE-09]

must_haves:
  truths:
    - "Inline Staff on a landscape phone fits one stanza-set without vertical scrolling for a non-double CM tune (e.g. Psalm 1)"
    - "The compact-fit gate (staffWidthFactor + compactSplitMobile) triggers on phone landscape regardless of viewportW reading 768-926px"
    - "Desktop and non-chromeless callers (/tunes/[id], /study) render byte-identically — the gate only widens for chromeless phone-landscape"
  artifacts:
    - path: "src/components/notation/NotationRenderer.tsx"
      provides: "Landscape-aware compact-fit gate"
      contains: "isPhoneLandscapeForFit"
  key_links:
    - from: "src/components/notation/NotationRenderer.tsx isPhoneLandscapeForFit"
      to: "staffWidthFactor + compactSplitMobile"
      via: "(viewportW < 768 || isPhoneLandscapeForFit) condition"
      pattern: "viewportW < 768 \\|\\| isPhoneLandscapeForFit"
---

<objective>
Fix MOBILE-09: the inline Staff "fit one stanza-set on screen without scrolling" system
(`compactSplitMobile` + `staffWidthFactor` in `NotationRenderer.tsx`) is gated purely on
`viewportW < 768`, a width-only check. It correctly catches narrow portrait phones but
misses landscape phones, which are wide (768-926px, so they escape the mobile-compact
branch) yet just as height-constrained (390-430px). Live-measured repro: Psalm 1 inline
Staff rendered a 566px-tall SVG into a 412px-tall landscape viewport (915x412, Android UA),
forcing scroll.

Purpose: Extend the existing fit gate to also trigger on phone landscape (Android AND iOS),
reusing this session's established phone/orientation detection — NOT inventing a new gate.
Output: A landscape-aware boolean feeding the existing `staffWidthFactor`/`compactSplitMobile`
logic. Zero new visual elements (silent calculation fix per UI-SPEC).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04.9.15.1-inline-staff-landscape-fit-swipe-stanza-navigation-fix-the-i/04.9.15.1-CONTEXT.md
@.planning/phases/04.9.15.1-inline-staff-landscape-fit-swipe-stanza-navigation-fix-the-i/04.9.15.1-PATTERNS.md
@.planning/phases/04.9.15.1-inline-staff-landscape-fit-swipe-stanza-navigation-fix-the-i/04.9.15.1-UI-SPEC.md
@.planning/notes/inline-staff-landscape-scaling.md

<interfaces>
<!-- Executor uses these directly — no codebase exploration needed. -->

Existing gate to extend (src/components/notation/NotationRenderer.tsx, lines ~551-578):
```typescript
const isSplitForWidth = isSplitMode(viewMode)
const staffWidthFactor = chromeless
  ? isSplitForWidth
    ? 1
    : viewportW < 768
    ? 0.55
    : 0.85
  : 1

const compactSplitMobile =
  chromeless && viewportW < 768 && (viewMode === 'staff' || viewMode === 'staff-split')
```

Already imported: `useMediaQuery` from '@/hooks/useMediaQuery' (line 23), `useState`/`useEffect` (React).
NOT yet imported: `isPhoneDevice` from '@/lib/device'.

Device helper (src/lib/device.ts) — SSR-safe, touches navigator so must run in effect:
```typescript
export function isPhoneDevice(): boolean  // true for iPhone/iPod + Android "Mobile"
```

Established phone-landscape media-query constant (from SingingView.tsx line 382 — reuse this
EXACT query, do NOT use unbounded '(orientation: landscape)' which would sweep in tablets):
```typescript
useMediaQuery('(orientation: landscape) and (max-width: 926px)')
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend the compact-fit gate to cover phone landscape</name>
  <files>src/components/notation/NotationRenderer.tsx</files>
  <read_first>
    - src/components/notation/NotationRenderer.tsx (lines 510-620 — the viewportW state, isNarrowPortrait useMediaQuery, staffWidthFactor and compactSplitMobile definitions, and surrounding comments)
    - src/lib/device.ts (isPhoneDevice signature + why it must run in an effect)
    - src/components/singing/SingingView.tsx (lines 382, 392-397 — the isPhone useState/useEffect + '(orientation: landscape) and (max-width: 926px)' pattern being reused)
    - .planning/notes/inline-staff-landscape-scaling.md (root cause + scope boundary)
  </read_first>
  <action>
    1. Add import: `import { isPhoneDevice } from '@/lib/device'` (alongside the existing imports near line 22-23; `useMediaQuery` is already imported).

    2. Add SSR-safe phone detection state near the existing `viewportW` state (around line 518). Do NOT call `isPhoneDevice()` in the render body — it touches `navigator`. Use:
    ```typescript
    const [isPhone, setIsPhone] = useState(false)
    useEffect(() => {
      setIsPhone(isPhoneDevice())
    }, [])
    ```

    3. Add the landscape-fit boolean. CRITICAL — do NOT write `const isPhoneLandscapeForFit = isPhone && useMediaQuery(...)`: that short-circuits the hook call and violates the rules of hooks. Call the hook UNCONDITIONALLY at top level, then combine:
    ```typescript
    const isPhoneLandscapeMQ = useMediaQuery('(orientation: landscape) and (max-width: 926px)')
    const isPhoneLandscapeForFit = isPhone && isPhoneLandscapeMQ
    ```
    Place these on the same lines-region as `isNarrowPortrait` (line ~530), before the `staffWidthFactor` definition.

    4. Widen the two gates by OR-ing in `isPhoneLandscapeForFit` at exactly the two `viewportW < 768` sites:
    ```typescript
    const staffWidthFactor = chromeless
      ? isSplitForWidth
        ? 1
        : (viewportW < 768 || isPhoneLandscapeForFit)
        ? 0.55
        : 0.85
      : 1

    const compactSplitMobile =
      chromeless &&
      (viewportW < 768 || isPhoneLandscapeForFit) &&
      (viewMode === 'staff' || viewMode === 'staff-split')
    ```

    5. Do NOT touch `cyclePage`/`totalStanzaPages`/`CYCLES_PER_PAGE` (a separate, already-correct pagination system per CONTEXT.md scope note). Do NOT touch `isNarrowPortrait` or the split-leaf `beside` layout. Only the boolean feeding `staffWidthFactor`/`compactSplitMobile` changes. The `chromeless` guard is preserved on BOTH gates so desktop / /tunes/[id] / /study callers stay byte-identical.
  </action>
  <verify>
    <automated>cd /home/services/psalter && npx tsc --noEmit 2>&1 | grep -i "NotationRenderer" ; echo "---gate check---" ; grep -c "viewportW < 768 || isPhoneLandscapeForFit" src/components/notation/NotationRenderer.tsx ; echo "---hook-safety check (must be 0)---" ; grep -c "&& useMediaQuery" src/components/notation/NotationRenderer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` produces no NotationRenderer errors (grep line above prints nothing for NotationRenderer)
    - `grep -c "viewportW < 768 || isPhoneLandscapeForFit"` returns 2 (both staffWidthFactor and compactSplitMobile gates widened)
    - `grep -c "&& useMediaQuery"` returns 0 (no rules-of-hooks short-circuit)
    - `grep -n "isPhoneLandscapeMQ = useMediaQuery('(orientation: landscape) and (max-width: 926px)')" src/components/notation/NotationRenderer.tsx` matches exactly one line
    - `grep -n "import { isPhoneDevice } from '@/lib/device'" src/components/notation/NotationRenderer.tsx` matches
    - `grep -n "setIsPhone(isPhoneDevice())" src/components/notation/NotationRenderer.tsx` matches (SSR-safe effect present)
    - `grep -c "CYCLES_PER_PAGE" src/components/notation/NotationRenderer.tsx` is unchanged vs. before (cycle pagination untouched)
  </acceptance_criteria>
  <done>The compact-fit gate triggers on phone landscape (Android + iOS) as well as narrow portrait, with `chromeless` still gating both branches and no hooks violation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewport/device → render logic | Client-only measurement (navigator UA, matchMedia). No untrusted network input crosses into this code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.9.15.1-01 | D (DoS) | useMediaQuery change listener added to NotationRenderer | mitigate | Reuse existing `useMediaQuery` hook, which already attaches + cleans up its `change` listener in a `useEffect` return; no raw listener added by this plan. |
| T-04.9.15.1-02 | T (Tampering) | navigator UA / viewport reporting | accept | A spoofed UA only alters cosmetic notation scaling on the client; no data, privilege, or server state depends on it. |
</threat_model>

<verification>
Task-level automated checks (tsc + grep gates) prove the gate structure.

Behavioral confirmation for MOBILE-09 (recommended, autonomous via the shared Playwright
daemon — pure measurement, no touch physics needed):
1. Build + serve locally: `npm run build && PORT=3005 npm run start` (or measure against the
   deployed `psalter.gsdlabs.dev` after the phase ships).
2. Via the daemon (`/home/services/playwright-daemon/client.js`), load `/psalms/1` singing
   view with a landscape phone context (Android UA, 915x412 viewport, staff view mode).
3. Assert the inline-Staff SVG height ≤ the `[data-notation-viewarea]` client height (no
   vertical overflow). Before this fix it was 566px vs 412px available.
Real on-device confirmation (iOS home-screen landscape; Android landscape) is covered by the
consolidated human-verify checkpoint in Plan 03.
</verification>

<success_criteria>
- A non-double CM tune's inline Staff fits one stanza-set without vertical scrolling in phone landscape (Android + iOS), same as it already does in portrait/desktop.
- No regression on desktop, /tunes/[id], or /study (chromeless guard preserved).
</success_criteria>

<output>
After completion, create `.planning/phases/04.9.15.1-inline-staff-landscape-fit-swipe-stanza-navigation-fix-the-i/04.9.15.1-01-SUMMARY.md`
</output>
