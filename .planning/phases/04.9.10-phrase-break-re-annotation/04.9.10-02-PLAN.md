---
phase: 04.9.10
plan: 02
type: execute
wave: 2
depends_on:
  - 04.9.10-01
files_modified:
  - scripts/output/staff-alignment-report.json
  - .planning/phases/04.9.10-phrase-break-re-annotation/04.9.10-VERIFICATION.md
autonomous: true
requirements:
  - RENDER-08

must_haves:
  truths:
    - "150-psalm Playwright sweep runs to completion against the re-annotated DB"
    - "148+/150 psalms pass the sweep (trailingEmpty=0, lyric alignment correct)"
    - "Psalm 23 (Crimond) passes with trailingEmpty=0 on abcjs-l3 (D-03 canary)"
    - "Residual failures are each assigned a root-cause label: data-gap, redirect, or meter-gap"
    - "VERIFICATION.md documents all residual failures with psalm number, tune name, and root-cause label"
    - "RENDER-08 is marked complete if 148+/150 gate is met"
  artifacts:
    - path: "scripts/output/staff-alignment-report.json"
      provides: "Sweep results: passed/failed counts, per-psalm detail"
    - path: ".planning/phases/04.9.10-phrase-break-re-annotation/04.9.10-VERIFICATION.md"
      provides: "Phase verification record with triage table"
  key_links:
    - from: "scripts/uat/verify-staff-alignment.js"
      to: "http://localhost:3005/psalms/<n>"
      via: "Playwright page.goto"
      pattern: "goto.*localhost:3005.*psalms"
    - from: "scripts/output/staff-alignment-report.json"
      to: "04.9.10-VERIFICATION.md"
      via: "manual triage after sweep"
      pattern: "\"passed\":\\s*\\d+"
---

<objective>
Run the 150-psalm Playwright sweep against the re-annotated DB (Plan 01 output),
triage any residual failures, and produce VERIFICATION.md that either closes
RENDER-08 (if ≥ 148/150 pass) or documents what remains.

Purpose: This is the final gate for RENDER-08. The root cause (stale PHRASE_BREAKs
from text-line-boundary algorithm) was fixed in 04.9.8-05; the data was re-annotated
in Plan 01. This plan confirms the fix holds across all 150 psalms and documents
any remaining exceptions with root-cause labels so future phases know exactly what
is left and why.

Output: Updated staff-alignment-report.json; VERIFICATION.md with pass/fail counts
and triage table; RENDER-08 closed (requirement checkbox updated in REQUIREMENTS.md).
</objective>

<execution_context>
@/data/home/psalter/.claude/get-shit-done/workflows/execute-plan.md
@/data/home/psalter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/data/home/psalter/.planning/PROJECT.md
@/data/home/psalter/.planning/ROADMAP.md
@/data/home/psalter/.planning/STATE.md
@/data/home/psalter/.planning/phases/04.9.10-phrase-break-re-annotation/04.9.10-CONTEXT.md
@/data/home/psalter/.planning/phases/04.9.10-phrase-break-re-annotation/04.9.10-01-SUMMARY.md

<interfaces>
<!-- Sweep script interface. Executor reads the full file before running. -->

From scripts/uat/verify-staff-alignment.js:
  BASE       = process.env.UAT_BASE || 'http://localhost:3005'
  PSALM_LIST = process.env.PSALM_LIST (comma-separated psalm numbers, default 1–150)
  TOLERANCE  = process.env.TOLERANCE  (default 1 — allow 1 melisma per sub-staff)

Output: scripts/output/staff-alignment-report.json
Format:
  {
    "passed": <n>,
    "failed": <n>,
    "psalms": [
      { "psalm": <n>, "status": "pass"|"fail", "tune": "<name>", "failures": [...] }
    ]
  }

Each failure entry in "failures":
  { "substaff": <n>, "notes": <n>, "lyrics": <n>, "trailingEmpty": <n> }

Known exceptions (D-02, D-03 from CONTEXT.md):
  - Psalm 45: Playwright redirects from localhost to psalter.gsdlabs.dev — label "redirect"
  - Bays of Harris, Effingham, Farrant, Kingsfold, Wallace: truncated ABC (~14-19
    note heads vs ~44 expected) — label "data-gap"
  - Psalm 23 (Crimond) MUST show trailingEmpty=0 on all abcjs-l<N> sub-staves — D-03 canary

From .planning/phases/04.9.9-staff-alignment-melisma-support/04.9.9-VERIFICATION.md (lines 118–238):
  Prior failure table: 102 psalms failing pre-Plan 01 re-annotation.
  Root causes documented at lines 100–107.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Psalm 23 canary check before full sweep</name>

  <read_first>
    - scripts/uat/verify-staff-alignment.js  (understand PSALM_LIST env var and output format)
    - scripts/output/staff-alignment-report.json  (current state: passed=48, failed=102 — confirms Plan 01 re-annotation was needed)
  </read_first>

  <files>scripts/output/staff-alignment-report.json</files>

  <action>
Before spending ~42 minutes on the full 150-psalm sweep, run the canary check for
Psalm 23 (Crimond) to confirm D-03. This is a fast spot-check (single psalm, < 30s).

Step 1: Confirm the dev server is running at localhost:3005:
```
curl -s -o /dev/null -w "%{http_code}" http://localhost:3005/psalms/23
```
Must return 200. If not, start the dev server:
```
cd /data/home/psalter && npm run dev &
sleep 10
```

Step 2: Run the canary sweep for Psalm 23 only:
```
cd /data/home/psalter && PSALM_LIST=23 node scripts/uat/verify-staff-alignment.js 2>&1
```

Step 3: Read the updated `scripts/output/staff-alignment-report.json`.
Look for: `{ "psalm": 23, "status": "pass", ... }`.
Specifically verify that no failure entry has `"trailingEmpty": <n>` where n > 0
(meaning there are no sub-staves with notes that have no lyrics trailing them).

If Psalm 23 fails:
  - Check which sub-stave fails: `failures[i].substaff` and `failures[i].trailingEmpty`
  - This indicates Plan 01 re-annotation did not apply correctly to Crimond
  - STOP and diagnose: run `docker exec -i psalter-db psql -U postgres -d psalter -c "SELECT name, (LENGTH(abc_notation) - LENGTH(REPLACE(abc_notation, E'% PHRASE_BREAK', ''))) / 14 AS marker_count FROM tunes WHERE name = 'Crimond';"`
  - Expected: marker_count = 3. If 0, the overwrite did not persist — re-run Plan 01.

Only proceed to Task 2 if Psalm 23 shows status: "pass".
  </action>

  <verify>
    <automated>PSALM_LIST=23 node /data/home/psalter/scripts/uat/verify-staff-alignment.js 2>/dev/null; cat /data/home/psalter/scripts/output/staff-alignment-report.json | grep -o '"passed":[0-9]*'</automated>
  </verify>

  <acceptance_criteria>
    - `scripts/output/staff-alignment-report.json` (after canary run) contains `"psalm":23` with `"status":"pass"`
    - No entry for psalm 23 contains `"trailingEmpty"` > 0 in any failure entry
    - Canary run exits with code 0
  </acceptance_criteria>

  <done>Psalm 23 (Crimond) passes with trailingEmpty=0 — D-03 canary confirmed. Full sweep may proceed.</done>
</task>

<task type="auto">
  <name>Task 2: Full 150-psalm sweep + triage + VERIFICATION.md</name>

  <read_first>
    - scripts/uat/verify-staff-alignment.js  (confirm hard timeout 50 min, output path, no --single-process flag needed)
    - .planning/phases/04.9.9-staff-alignment-melisma-support/04.9.9-VERIFICATION.md  (lines 118–238 — prior failure table for comparison)
    - .planning/phases/04.9.10-phrase-break-re-annotation/04.9.10-CONTEXT.md  (D-02 known exceptions, D-04 triage rules)
  </read_first>

  <files>
    scripts/output/staff-alignment-report.json
    .planning/phases/04.9.10-phrase-break-re-annotation/04.9.10-VERIFICATION.md
  </files>

  <action>
Step 1: Run the full 150-psalm sweep. This takes ~42 minutes. Run unattended:
```
cd /data/home/psalter && node scripts/uat/verify-staff-alignment.js 2>&1 | tee scripts/output/staff-alignment-sweep-04.9.10.log
```

The sweep writes results to `scripts/output/staff-alignment-report.json` as it runs.
When complete, the final line will show total passed/failed counts.

Step 2: Read `scripts/output/staff-alignment-report.json` and extract:
  a) Total passed count
  b) Total failed count
  c) List of failed psalms: psalm number + tune name + failure detail

Step 3: Triage each failure against the known exception list (D-02):
  - Psalm 45: status = "redirect" — localhost redirects to production; unrelated to PHRASE_BREAKs
  - Bays of Harris tune: status = "data-gap" — truncated ABC, only first half OCR-captured
  - Effingham tune: status = "data-gap"
  - Farrant tune: status = "data-gap"
  - Kingsfold tune: status = "data-gap"
  - Wallace tune: status = "data-gap"
  - Any other failures: investigate via:
    a) Check marker_count SQL: `SELECT name, (LENGTH(abc_notation) - LENGTH(REPLACE(abc_notation, E'% PHRASE_BREAK', ''))) / 14 AS marker_count FROM tunes WHERE name = '<tune>';`
    b) Check meter in getSplitPointsForMeter — if meter returns undefined, it uses Path A (line-split); label as "meter-gap" if alignment still fails
    c) Check trailingEmpty count — if > 2, likely a structural variance (label "structural")

Step 4: Determine phase gate result:
  - If passed ≥ 148: PHASE GATE MET — proceed to close RENDER-08
  - If passed < 148: PHASE GATE NOT MET — document all unexpected failures, do NOT close RENDER-08

Step 5: Write `.planning/phases/04.9.10-phrase-break-re-annotation/04.9.10-VERIFICATION.md`
with the following structure:

```markdown
# Phase 04.9.10 — PHRASE_BREAK Re-annotation Verification

**Sweep date:** 2026-05-29
**Sweep script:** `scripts/uat/verify-staff-alignment.js`
**Report:** `scripts/output/staff-alignment-report.json`
**Dev server:** http://localhost:3005
**DB state:** All 150 tunes re-annotated with Path NH (Plan 01)

## Result

**Passed:** <n> / 150
**Failed:** <n> / 150
**Phase gate (148+/150):** PASS / FAIL

## Canary Check

| Psalm | Tune | Result | trailingEmpty | Notes |
|-------|------|--------|---------------|-------|
| 23 | Crimond | PASS | 0 | D-03 canary ✓ |

## Residual Failures

| Psalm | Tune | Root Cause | Detail |
|-------|------|------------|--------|
| 45 | (any) | redirect | localhost:3005/psalms/45 redirects to psalter.gsdlabs.dev |
| <n> | Bays of Harris | data-gap | Truncated ABC: ~14-19 note heads vs ~44 expected |
| <n> | Effingham | data-gap | Truncated ABC |
| <n> | Farrant | data-gap | Truncated ABC |
| <n> | Kingsfold | data-gap | Truncated ABC |
| <n> | Wallace | data-gap | Truncated ABC |
<!-- add any unexpected failures below with their root cause -->

## Root Cause Labels

- **redirect**: Psalm 45 navigation redirect (Next.js canonical URL config, unrelated to PHRASE_BREAKs)
- **data-gap**: Tune ABC is truncated (OCR only captured first half); padWLineToNoteCount handles alignment at render time
- **meter-gap**: Tune meter not in getSplitPointsForMeter; fell through to Path A; still misaligned
- **structural**: Unusual phrase length variance for this specific tune

## RENDER-08 Status

<CLOSED — 148+/150 psalms pass; all residuals are documented exceptions>
  OR
<OPEN — only <n>/150 pass; <n> unexpected failures listed above>

## Prior Phase Comparison

Phase 04.9.9 sweep result: passed=48, failed=102
Phase 04.9.10 sweep result: passed=<n>, failed=<n>
Delta: +<delta> psalms fixed by Path NH re-annotation
```

Step 6: If PHASE GATE MET — update REQUIREMENTS.md to mark RENDER-08 as fully closed
by adding a note to the existing checkbox line:
  Find line: `- [x] **RENDER-08**: Staff view renders...`
  This is already marked [x]; add at the end of that line:
  ` — Verified 04.9.10: <n>/150 pass post-Path-NH re-annotation.`

Step 7: Commit everything:
```
cd /data/home/psalter && git add \
  scripts/output/staff-alignment-report.json \
  scripts/output/staff-alignment-sweep-04.9.10.log \
  .planning/phases/04.9.10-phrase-break-re-annotation/04.9.10-VERIFICATION.md \
  .planning/REQUIREMENTS.md

git commit -m "$(cat <<'EOF'
feat(04.9.10-02): 150-psalm sweep confirms Path NH re-annotation; close RENDER-08

Playwright sweep: <n>/150 pass (up from 48 before re-annotation).
Psalm 23 (Crimond) trailingEmpty=0 — D-03 canary confirmed.
Residuals: 6 documented exceptions (5 data-gap, 1 redirect).
RENDER-08 closed per D-02 gate (148+/150).

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
git push
```
  </action>

  <verify>
    <automated>cat /data/home/psalter/scripts/output/staff-alignment-report.json 2>/dev/null | grep -o '"passed":[0-9]*'</automated>
  </verify>

  <acceptance_criteria>
    - `scripts/output/staff-alignment-report.json` exists and contains `"passed":<n>` where n ≥ 148
    - `scripts/output/staff-alignment-report.json` contains `"failed":<n>` where n ≤ 2 (not counting Psalm 45 redirect if it shows as fail)
    - `.planning/phases/04.9.10-phrase-break-re-annotation/04.9.10-VERIFICATION.md` exists
    - VERIFICATION.md contains `## Result` section with Passed count ≥ 148
    - VERIFICATION.md contains `## Residual Failures` table
    - VERIFICATION.md contains `## RENDER-08 Status` section
    - VERIFICATION.md contains `| 23 | Crimond | PASS |` in the Canary Check table
    - `.planning/REQUIREMENTS.md` line for RENDER-08 contains "Verified 04.9.10"
    - Git commit with message containing "close RENDER-08" exists in log
    - `git push` completes without error
  </acceptance_criteria>

  <done>
148+/150 psalms pass the staff alignment sweep; Psalm 23 (Crimond) shows trailingEmpty=0;
all residuals are labelled; VERIFICATION.md written; RENDER-08 closed in REQUIREMENTS.md;
commits pushed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright→localhost | Playwright fetches pages from the local Next.js dev server; no external network calls |
| script→filesystem | Sweep script writes JSON report and screenshots to scripts/output/ and scripts/uat/screenshots/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.9.10-03 | Information Disclosure | staff-alignment-report.json | accept | File contains only psalm numbers and alignment metrics; no PII; not served publicly |
| T-04.9.10-04 | Denial of Service | Playwright 50-min hard timeout | accept | Timeout is built into verify-staff-alignment.js; exits with code 2; does not hang indefinitely |
</threat_model>

<verification>
1. `scripts/output/staff-alignment-report.json` shows "passed" ≥ 148
2. Psalm 23 entry in report: `"status":"pass"`, no trailingEmpty > 0
3. `04.9.10-VERIFICATION.md` exists with Result, Canary Check, Residual Failures, and RENDER-08 Status sections
4. REQUIREMENTS.md RENDER-08 line ends with "Verified 04.9.10"
5. Git log shows commit "feat(04.9.10-02): 150-psalm sweep confirms Path NH re-annotation; close RENDER-08"
</verification>

<success_criteria>
- 148+/150 psalms pass the staff alignment sweep
- Psalm 23 (Crimond) trailingEmpty=0 on all sub-staves (D-03)
- All residual failures have a root-cause label (data-gap, redirect, or meter-gap)
- VERIFICATION.md is written and committed
- RENDER-08 is marked closed in REQUIREMENTS.md
- Phase 04.9.10 is complete
</success_criteria>

<output>
After completion, create `.planning/phases/04.9.10-phrase-break-re-annotation/04.9.10-02-SUMMARY.md`
</output>
