---
phase: 04.9.12-melisma-positions-as-tune-level-data
plan: 01
subsystem: database
tags: [drizzle-orm, postgres, jsonb, schema, melisma]

# Dependency graph
requires:
  - phase: 04.9.11
    provides: melisma decision audit log (tune_melisma_decisions table) and editor save flow

provides:
  - melismaPositions jsonb column on tunes table (number[][] | null)
  - melisma_positions column in live psalter-db

affects:
  - 04.9.12-02 (melisma migration script — reads approved tunes and writes melismaPositions)
  - 04.9.12-03 (NotationRenderer — reads melismaPositions instead of embedded w: lines)
  - 04.9.12-04 (melisma-editor save route — writes melismaPositions on approval)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "jsonb column with TypeScript generic type via .$type<number[][] | null>() for structured tune data"

key-files:
  created: []
  modified:
    - src/db/schema.ts

key-decisions:
  - "melismaPositions stored as number[][] (per-phrase array of 0-based note indices) — matches the exact structure consumed by w: line generation at render time"
  - "NULL sentinel means heuristic path; non-null means approved canonical data — no separate boolean flag needed"

patterns-established:
  - "melismaPositions[phraseIdx] = 0-based note indices within that phrase where _ hold token applies"

requirements-completed:
  - MELISMA-01

# Metrics
duration: 5min
completed: 2026-06-07
---

# Phase 04.9.12 Plan 01: Add melisma_positions Column Summary

**Drizzle schema extended with melismaPositions jsonb (number[][] | null) column and live ALTER TABLE applied to psalter-db via drizzle-kit push**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-06-07T19:00:00Z
- **Completed:** 2026-06-07T19:05:00Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- Added `melismaPositions: jsonb('melisma_positions').$type<number[][] | null>()` to the tunes table in `src/db/schema.ts`, placed logically after `phraseShapeOverride`
- Ran `drizzle-kit push` (with DATABASE_URL sourced from `/data/home/psalter/.env`) — confirmed `[✓] Changes applied`
- Verified `melisma_positions` column exists in live psalter-db via `information_schema.columns` query
- Confirmed no new TypeScript errors introduced (pre-existing test-file errors unaffected)

## Task Commits

1. **Task 1: Add melismaPositions column to tunes schema** - `135d3e2` (feat)
2. **Task 2: Push schema to psalter-db** - no file changes (DB-only via drizzle-kit push)

**Plan metadata:** committed with SUMMARY (docs commit)

## Files Created/Modified

- `src/db/schema.ts` — Added `melismaPositions` jsonb column with JSDoc comment to the `tunes` pgTable definition

## Decisions Made

- Used `.$type<number[][] | null>()` to match the per-phrase, per-note-index structure that w: line generation needs at render time — no separate flag or wrapper column required
- `NULL` is the correct default (no `.default()` call) — absent in DB rows means heuristic path, exactly as designed

## Deviations from Plan

None — plan executed exactly as written. The only minor issue was that `drizzle-kit push` needed `DATABASE_URL` sourced from the main project `.env` file (not present in the worktree), which is expected worktree behaviour and required no deviation.

## Issues Encountered

- Worktree does not have a `.env` symlink — sourced `/data/home/psalter/.env` explicitly before running `drizzle-kit push`. Not a deviation; expected worktree isolation.
- psql authentication: `DATABASE_URL` uses user `postgres` not `psalter` — adjusted verification command accordingly. Column confirmed present.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- `melisma_positions` column is live in psalter-db, defaulting to NULL for all 172 tunes
- Plan 02 (migration script) can now read `tune_melisma_decisions` approved tunes and write `melismaPositions` JSON arrays into the new column
- Plans 03 and 04 can read/write `melismaPositions` via Drizzle ORM with full TypeScript type safety (`number[][] | null`)

## Self-Check: PASSED

- `src/db/schema.ts` — FOUND, contains 2 occurrences of `melismaPositions`
- `04.9.12-01-SUMMARY.md` — FOUND
- Commit `135d3e2` — FOUND in git log
- `melisma_positions` column — FOUND in live psalter-db `information_schema.columns`

---
*Phase: 04.9.12-melisma-positions-as-tune-level-data*
*Completed: 2026-06-07*
