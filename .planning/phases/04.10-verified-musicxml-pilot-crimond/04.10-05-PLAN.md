---
phase: 04.10
plan: 05
type: execute
wave: 5
depends_on: ["04.10-04"]
files_modified: []
autonomous: false
requirements:
  - MXML-05
must_haves:
  truths:
    - "User has sing-tested /psalms/23 against the iOS Scottish Psalter app and confirmed match (the phase's explicit completion predicate per ROADMAP)"
  artifacts: []
  key_links:
    - from: "User's ear"
      to: "/psalms/23 rendered staff view"
      via: "live sing-along comparison to iOS Scottish Psalter app"
      pattern: "(verified by human, not grep)"
---

<objective>
Final phase gate: user sing-tests `/psalms/23` against the iOS Scottish Psalter
app and confirms the alignment matches. This is the ROADMAP-mandated completion
predicate — "complete" is not on a passing Playwright check alone.

Purpose: Convert "all the automation says green" into "the user has heard it and
believes it." This is the only place in the phase where the user's ear is the
arbiter.

Output: User decision — approved, approved-with-notes (logged for future
tunes), or rejected (re-opens phase via /gsd-plan-phase --gaps).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04.10-verified-musicxml-pilot-crimond/04.10-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Sing-test /psalms/23 against the iOS Scottish Psalter app</name>
  <decision>Does the Crimond rendering at /psalms/23 match the iOS Scottish Psalter reference when sung?</decision>
  <context>
    Per ROADMAP Phase 04.10:
    > **Predicate for "complete"**: user has sing-tested `/psalms/23` against the
    > iOS Scottish Psalter app and confirmed match.

    All automated checks are GREEN:
    - Plan 04 Task 3: `psalm-23-crimond-verified.js` PASSED (DOM-level lyric content correct)
    - Plan 04 Task 3: `psalm-24-unchanged.js` PASSED (heuristic path intact)
    - Plan 04 Task 2: exactly 1 tune row updated (Crimond, id=30)

    What we are NOT able to automate:
    - Whether the syllable-to-note alignment "sounds right" when sung
    - Whether the staff-row line breaks match the iOS reference visually
    - Whether the Eleanor Gow / de Boer arrangement audibly matches what the
      precentor would expect

    Reference materials available (review BEFORE singing):
    - Plan 04 SUMMARY: `scripts/uat/screenshots/psalm-23-crimond-verified.png`
      (rendered Crimond staff view at 1024×768)
    - Plan 04 SUMMARY: `scripts/uat/screenshots/psalm-24-after.png` (Psalm 24
      heuristic-path render — should look identical to before this phase)
    - Plan 02 SUMMARY: chosen PHRASE_BREAK option (a/b/c) and exact positions
    - Plan 03 SUMMARY: chosen multi-stanza handling option

    Suggested verification steps:
    1. Open `https://psalter.gsdlabs.dev/psalms/23` on a desktop browser
    2. Open the iOS Scottish Psalter app on a phone, navigate to Psalm 23 →
       Crimond tune
    3. Sing stanza 1 from the psalter.gsdlabs.dev rendering while comparing
       syllable-by-syllable to the iOS reference (especially the melismas on
       "shep-herd" and "want;")
    4. Page to stanzas 2-6 on psalter.gsdlabs.dev — note any obvious mis-alignment
       (these use the heuristic fallback per Plan 03's O-2 decision — option A)
    5. Compare to `https://psalter.gsdlabs.dev/psalms/24` (Psalm 24, heuristic
       path) — should look/feel identical to how it did before this phase
  </context>
  <options>
    <option id="approved">
      <name>Approved — sing-test passed, Crimond renders correctly</name>
      <pros>Phase 04.10 is complete. The per-tune migration recipe (Plan 04 Task 4) is now battle-tested. Future de Boer tunes can follow the same recipe (deferred to a later phase).</pros>
      <cons>None — close out the phase.</cons>
    </option>
    <option id="approved-with-notes">
      <name>Approved with notes — stanzas 2-6 mis-alignment is acceptable for the pilot but should be tracked</name>
      <pros>Pilot completes. The mis-alignment on stanzas 2-6 (heuristic path) is documented as a known limitation per Plan 03's O-2 option A. Notes feed into the next phase's planning.</pros>
      <cons>Future user expectations need calibration: "verified" currently means stanza 1 only.</cons>
    </option>
    <option id="rejected">
      <name>Rejected — alignment does not match the iOS reference</name>
      <pros>Catches a real mis-alignment before any other tune follows the same recipe.</pros>
      <cons>Re-opens the phase. Likely causes (in order of likelihood):
        - PHRASE_BREAK positions need re-derivation (Plan 02 O-1 chose the wrong option)
        - Slur-walk algorithm produced wrong w-tokens (Plan 02 buildWTokens bug — verify against doc §8 in the script's .test.ts)
        - NotationRenderer branch misroutes the embedded w-line (Plan 03 — verify the per-cycle interaction)
        - de Boer's source file itself diverges from the iOS reference (re-examine `.planning/research/abc-samples/Crimond_deBoer.musicxml`)
      Action: invoke `/gsd-plan-phase 04.10 --gaps` with the specific mis-alignment reported.
      </cons>
    </option>
  </options>
  <resume-signal>Select: approved | approved-with-notes | rejected (and include specific mis-alignment notes if rejected or approved-with-notes)</resume-signal>
</task>

</tasks>

<verification>
- User has reviewed the live `https://psalter.gsdlabs.dev/psalms/23` rendering AND the iOS Scottish Psalter app reference
- User has chosen one of the three options (approved / approved-with-notes / rejected)
- If rejected: a gap report exists for `/gsd-plan-phase 04.10 --gaps` to consume
</verification>

<success_criteria>
- ROADMAP "Predicate for complete" satisfied via user sign-off
- Phase 04.10 either closes out (approved) or re-opens with concrete gap notes (rejected)
</success_criteria>

<output>
After completion, create `.planning/phases/04.10-verified-musicxml-pilot-crimond/04.10-05-SUMMARY.md` per template. Record:
- Decision (approved / approved-with-notes / rejected)
- Free-text notes from the user's sing-test
- Recommended next phase (close-out, follow-up for stanzas 2-6, gap closure)
</output>
