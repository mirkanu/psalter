---
phase: 04.10-verified-musicxml-pilot-crimond
plan: 01
subsystem: notation
tags: [musicxml, abcjs, lyrics, playwright, vitest, tdd, fixtures]

requires:
  - phase: 04.9.x
    provides: NotationRenderer + abc-phrases pure helper pattern
provides:
  - de Boer Crimond MusicXML canonical fixture
  - abc-embedded-lyrics pure helper (hasEmbeddedWLines, extractEmbeddedWLines)
  - Three Playwright UAT scaffolds (RED) covering preview, verified path, regression
affects: [04.10-02-musicxml-conversion, 04.10-03-notation-renderer-integration, 04.10-04-db-update-and-verification]

tech-stack:
  added: []
  patterns:
    - "Pure helper modules under src/lib (no DOM/abcjs/React imports)"
    - "RED-first UAT scaffolds committed before consumer code (test-contract-first)"
    - "Dual-viewport Playwright assertions (desktop + mobile-375) for mobile sub-staff splitting"
    - "Region-scoped SHA-256 hashing (.abcjs-container innerHTML only) to avoid timestamp drift"

key-files:
  created:
    - .planning/research/abc-samples/Crimond_deBoer.musicxml
    - src/lib/abc-embedded-lyrics.ts
    - src/lib/abc-embedded-lyrics.test.ts
    - scripts/uat/musicxml-preview-smoke.js
    - scripts/uat/psalm-23-crimond-verified.js
    - scripts/uat/psalm-24-unchanged.js
  modified: []

key-decisions:
  - "Committed canonical de Boer Crimond.musicxml at 52KB (exceeds plan's <10KB hint) — upstream file is the canonical source; size hint was advisory"
  - "Hash .abcjs-container innerHTML only (W-1), not body.textContent — avoids timestamp / hydration / footer drift"
  - "Dual viewport (1024 + 375) in psalm-23-crimond-verified.js to catch mobile sub-staff splitting interaction (Pitfall 4)"

patterns-established:
  - "Pure helper module: src/lib/abc-embedded-lyrics.ts mirrors abc-phrases.ts shape (no imports, vitest tests colocated)"
  - "RED stub UAT scripts: syntactically valid, fail until consumer code lands; header line declares which plan turns it GREEN"

requirements-completed: [MXML-02, MXML-03, MXML-05]

duration: 12min
completed: 2026-05-30
---

# Phase 04.10 Plan 01: Wave 1 Foundation Summary

**de Boer Crimond MusicXML fixture committed, pure `abc-embedded-lyrics` helper shipped GREEN with 7 vitest tests, and three Playwright UAT scaffolds (RED) wired for the verified-source pilot.**

## Performance

- **Duration:** ~12 min
- **Tasks:** 3
- **Files created:** 6
- **Files modified:** 0

## Accomplishments
- Canonical de Boer Crimond MusicXML source committed under per-source-distinguished filename per Open Question O-4
- Pure helper module `abc-embedded-lyrics` exports `hasEmbeddedWLines` + `extractEmbeddedWLines`, ready for NotationRenderer (Plan 03) import
- Three Playwright UATs scaffolded with all hardening baked in from Wave 1: dual viewports (Pitfall 4) and region-scoped hashing (W-1)

## Task Commits

1. **Task 1: Commit de Boer Crimond.musicxml fixture** — `c7b1bc2` (feat)
2. **Task 2 RED: Failing tests for abc-embedded-lyrics** — `123759a` (test)
3. **Task 2 GREEN: Implement abc-embedded-lyrics helpers** — `aac77a7` (feat)
4. **Task 3: Scaffold three Playwright UAT scripts (RED stubs)** — `654d611` (feat)

## Files Created/Modified
- `.planning/research/abc-samples/Crimond_deBoer.musicxml` — canonical de Boer Crimond source (50 `<slur` elements, 52KB)
- `src/lib/abc-embedded-lyrics.ts` — pure helpers `hasEmbeddedWLines` + `extractEmbeddedWLines` (no DOM/React/abcjs imports)
- `src/lib/abc-embedded-lyrics.test.ts` — 7 vitest unit tests (4 hasEmbeddedWLines, 3 extractEmbeddedWLines)
- `scripts/uat/musicxml-preview-smoke.js` — RED stub; navigates `/dev/musicxml-preview`, asserts ≥1 `text.abcjs-lyric`
- `scripts/uat/psalm-23-crimond-verified.js` — RED stub; dual viewport (1024 + 375), asserts line 0 matches `/^the\s+lord'?s\s+my\s+shep/i` and ≥4 sub-staff lines
- `scripts/uat/psalm-24-unchanged.js` — RED stub; SHA-256 of `.abcjs-container` innerHTML region only (per W-1), writes baseline on first run

## Decisions Made
- **52KB fixture size:** The upstream de Boer Crimond.musicxml from `dieuwedeboer/scottishmetricalpsalter` is 52,306 bytes. Plan's acceptance criterion `< 10000 bytes` was inconsistent with the canonical source (which is fixed upstream). The automated `<verify>` block did NOT enforce the upper bound — only `> 1000` — so the verify gate passed. Documented as deviation R1-A below.
- **Dual viewport assertion in psalm-23-crimond-verified.js:** Implemented per Pitfall 4 to catch mobile sub-staff splitting interaction at 375px in addition to 1024px desktop.
- **Region-scoped hashing in psalm-24-unchanged.js:** Per W-1, hashes only `.abcjs-container` innerHTML — avoids timestamp / hydration / footer drift that would plague `body.textContent` hashing.

## Deviations from Plan

### Auto-fixed / Documented Issues

**1. [Rule 1 - Plan/reality mismatch] Fixture size exceeds plan's `<10000` byte hint**
- **Found during:** Task 1 (Commit de Boer Crimond.musicxml fixture)
- **Issue:** Plan acceptance criterion stated file size should be `< 10000 bytes`. The canonical upstream file at `dieuwedeboer/scottishmetricalpsalter/docs/tunes/Crimond.musicxml` is 52,306 bytes.
- **Fix:** Committed the canonical file as-is. The `<verify><automated>` block only required `> 1000`, which passes. The upper bound in acceptance criteria appears to have been an outdated estimate.
- **Files modified:** `.planning/research/abc-samples/Crimond_deBoer.musicxml`
- **Verification:** `head -1` shows `<?xml`, `grep -c "<slur"` returns 50 (≥ 19), `wc -c` returns 52306 (> 1000). Existing `crimond.xml` and `crimond_pd.xml` preserved.
- **Committed in:** `c7b1bc2`

---

**Total deviations:** 1 documented (plan/reality mismatch on size hint, no code/behaviour change)
**Impact on plan:** None — automated verify gate passed, canonical fixture is suitable for Plan 02's conversion script.

## Issues Encountered
None.

## User Setup Required
None — no external service configuration required.

## Next Phase Readiness
- **Plan 02 (MusicXML conversion):** Can read `Crimond_deBoer.musicxml` fixture directly.
- **Plan 03 (NotationRenderer integration):** Can import `hasEmbeddedWLines` / `extractEmbeddedWLines` from `@/lib/abc-embedded-lyrics`.
- **Plan 04 (DB update + verification):** Can run the three Playwright UATs; psalm-24-unchanged.js will write baseline on first run (before DB update) and assert against it on rerun (after DB update).
- **Mobile hardening:** psalm-23-crimond-verified.js already exercises 375px viewport — no follow-up needed for Pitfall 4.
- **Drift protection:** psalm-24-unchanged.js hashes only the SVG region — no timestamp drift risk.

## Self-Check: PASSED

- `.planning/research/abc-samples/Crimond_deBoer.musicxml`: FOUND (52306 bytes, 50 `<slur` elements)
- `src/lib/abc-embedded-lyrics.ts`: FOUND
- `src/lib/abc-embedded-lyrics.test.ts`: FOUND (7/7 vitest pass)
- `scripts/uat/musicxml-preview-smoke.js`: FOUND, `node --check` OK
- `scripts/uat/psalm-23-crimond-verified.js`: FOUND, `node --check` OK, contains 1024 + 375
- `scripts/uat/psalm-24-unchanged.js`: FOUND, `node --check` OK, hashes `.abcjs-container`, no `body.textContent`
- Commits: c7b1bc2, 123759a, aac77a7, 654d611 all present in `git log`

---
*Phase: 04.10-verified-musicxml-pilot-crimond*
*Completed: 2026-05-30*
