---
phase: 04.10-verified-musicxml-pilot-crimond
artifact: human-sing-test-findings
plan: 05
status: defects-found
verdict: rejected-with-precise-diagnostics
captured: 2026-05-30
captured-by: user (Manuel Kuhs)
reference: iOS Scottish Psalter app, Psalm 23, Crimond
target: https://psalter.gsdlabs.dev/psalms/23 (Crimond, verified de Boer ABC, DB row id=30)
---

# Plan 05 Sing-Test Findings

## Verdict

**Rejected with precise diagnostics.** Plan 05 NOT marked complete. Phase 04.10 NOT marked complete. The user took the trouble to identify the failure location exactly, so this routes cleanly to gap closure via `/gsd-plan-phase 04.10 --gaps`.

## Defects

### Defect 1 — Syllable alignment breaks at "down to" (end of line 2)

> "the syllable alignment is 100% perfect until 'down to' near end of second line. Then syllables are all messed up."

- **Stanza:** 1
- **Phrase / line:** end of line 2
- **First mis-aligned token:** `"down to"`
- **Severity:** alignment is correct for ~14 notes then breaks; everything after the break is wrong
- **Hypothesis (untriaged):** the `[10, 16, 24]` phrase-break decision (option-b from Plan 02) may interact with `buildWTokens` token-shape at phrase boundary 2 (note 16, "down to"). The verified w-line lengths in `crimond-verified.abc` should be re-counted against the sung syllable stream from "down" onwards.
- **Files to investigate first:**
  - `.planning/research/abc-samples/crimond-verified.abc` (the rendered w-lines)
  - `scripts/convert-crimond-musicxml.ts` (the generator)
  - `src/lib/abc-embedded-w-branch.ts` (the runtime branch)

### Defect 2 — Note-3 duration is longer than note-4 (should be equal)

> "The tune is 99% perfect until the same point — except that the third note is slightly longer than the fourth (they should be identical lengths)"

- **Stanza:** 1 (tune is per-stanza so same defect across all stanzas)
- **Notes affected:** note 3 vs note 4
- **Symptom:** note 3 renders slightly longer than note 4; they should be identical
- **Hypothesis (untriaged):** the MusicXML → ABC duration mapping in `scripts/convert-crimond-musicxml.ts` may be losing a tie / wrong divisor at that position. de Boer's source likely has them equal; the conversion is the suspect.
- **Files to investigate first:**
  - `.planning/research/abc-samples/Crimond_deBoer.musicxml` (source duration at note 3 / 4)
  - `scripts/convert-crimond-musicxml.ts` (duration converter)
  - `.planning/research/abc-samples/crimond-verified.abc` (rendered note lengths)

## Co-located cause is plausible

Both defects manifest "at the same point" (the user's words). The note-3/note-4 duration error and the line-2 alignment break may share a root cause in the conversion script (e.g., a single mishandled MusicXML element that throws both the duration count and downstream syllable mapping out of sync). Worth investigating Defect 2 first — if note durations are wrong, every downstream token count is also wrong.

## What survived the sing-test

- **First ~14 notes:** syllables and tune both correct (the verified path works for the majority of stanza 1)
- **The infrastructure:** w-branch decision module, conversion script's overall architecture, NotationRenderer wiring, per-tune migration recipe — all functioning. The bug is in the data produced by the converter, not the wiring.
- **Negative control:** `/psalms/24` regression status not noted in the user's verdict (asked to compare but they focused on Crimond defects); assume unchanged unless a follow-up report contradicts.

## Recommended next step

```
/gsd-plan-phase 04.10 --gaps
```

This will read the phase artifacts (including this FINDINGS.md) and create gap-closure plans. The two defects above are precise enough to be a single gap-closure plan with two task slices.

Rollback path (if user wants to revert before fixing): `04.10-04-SUMMARY.md → "Rollback Recipe"` (~1 min, restores the heuristic ABC for Crimond).
