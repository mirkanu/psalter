---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - .gitignore
autonomous: false
requirements:
  - MIGR-01
  - MIGR-02
  - MIGR-03
  - MIGR-04

must_haves:
  truths:
    - "psalter-db PostgreSQL 16 container is running and accepting connections on host port 5435"
    - ".env is not tracked by git (present in .gitignore)"
    - ".env.example documents every required environment variable with descriptions"
    - "R2 bucket exists and its credentials have been added to .env"
  artifacts:
    - path: "docker-compose.yml"
      provides: "psalter-db PostgreSQL 16 container definition"
      contains: "psalter-db"
    - path: ".env.example"
      provides: "Template for all required environment variables"
      contains: "DATABASE_URL"
    - path: ".gitignore"
      provides: "Git exclusion rules"
      contains: ".env"
  key_links:
    - from: "docker-compose.yml"
      to: "host port 5435"
      via: "ports mapping"
      pattern: "5435:5432"
    - from: ".env"
      to: "DATABASE_URL"
      via: "postgres.js connection string"
      pattern: "postgresql://postgres:postgres@localhost:5435/psalter"

user_setup:
  - service: cloudflare-r2
    why: "Tune score sheet JPGs must be stored permanently in R2 (Airtable attachment URLs expire in ~2 hours)"
    env_vars:
      - name: R2_ACCOUNT_ID
        source: "Cloudflare Dashboard → R2 → Overview → Account ID (right sidebar)"
      - name: R2_ACCESS_KEY_ID
        source: "Cloudflare Dashboard → R2 → Manage R2 API Tokens → Create API Token → R2 Token with Object Read & Write"
      - name: R2_SECRET_ACCESS_KEY
        source: "Same R2 API Token creation — copy the Secret Access Key immediately (shown only once)"
      - name: R2_BUCKET_NAME
        source: "Cloudflare Dashboard → R2 → Create Bucket → name it 'psalter' (or your choice)"
      - name: R2_PUBLIC_URL
        source: "Cloudflare Dashboard → R2 → psalter bucket → Settings → Public Access → Enable → copy the public URL (e.g. https://pub-xxx.r2.dev or custom domain)"
    dashboard_config:
      - task: "Create R2 bucket named 'psalter'"
        location: "Cloudflare Dashboard → R2 → Create Bucket"
      - task: "Enable public access on the bucket (required for tune images to be publicly viewable)"
        location: "Cloudflare Dashboard → R2 → psalter bucket → Settings → Public Access → Allow Access"
---

<objective>
Create the infrastructure prerequisites for Phase 1: a running PostgreSQL 16 container (psalter-db), environment variable template, and git safety rules. This wave establishes the foundation that every subsequent plan depends on.

Purpose: Without a running database and correct environment configuration, no schema can be pushed and no migration can run.
Output: docker-compose.yml, .env.example, .gitignore — all committed. psalter-db container running. R2 bucket created (human step).
</objective>

<execution_context>
@/data/home/psalter/.claude/get-shit-done/workflows/execute-plan.md
@/data/home/psalter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/data/home/psalter/.planning/ROADMAP.md
@/data/home/psalter/.planning/phases/01-foundation/01-RESEARCH.md
@/data/home/psalter/.planning/phases/01-foundation/01-CONTEXT.md

<interfaces>
<!-- Verified from RESEARCH.md Pattern 7 — Docker Compose for psalter-db -->
<!-- Port conflict confirmed: other DB containers use 5432 internally; use 5435 for psalter-db -->

docker-compose.yml must use:
  container_name: psalter-db
  image: postgres:16-alpine
  ports: ["5435:5432"]
  POSTGRES_DB: psalter
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: postgres

DATABASE_URL = postgresql://postgres:postgres@localhost:5435/psalter

R2 env vars required:
  R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET_NAME, R2_PUBLIC_URL

Airtable env vars required:
  AIRTABLE_PAT (already in .env per CONTEXT.md)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docker-compose.yml, .env.example, and .gitignore</name>
  <files>docker-compose.yml, .env.example, .gitignore</files>
  <read_first>
    - /data/home/psalter/CLAUDE.md (project conventions)
    - /data/home/psalter/.planning/phases/01-foundation/01-RESEARCH.md (Pattern 7: Docker Compose, env var list, port 5435 rationale)
  </read_first>
  <action>
Create three files in /data/home/psalter/:

**docker-compose.yml:**
```yaml
services:
  db:
    image: postgres:16-alpine
    container_name: psalter-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: psalter
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - psalter_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    ports:
      - "5435:5432"

volumes:
  psalter_db_data:
```

**Port rationale:** 5432 is occupied by debates-db and other containers on this server. Use 5435.

**.env.example:**
```
# PostgreSQL (psalter-db container — host port 5435)
DATABASE_URL=postgresql://postgres:postgres@localhost:5435/psalter

# Airtable
AIRTABLE_PAT=your_personal_access_token_here
AIRTABLE_BASE_ID=appY3dB1EHtex0fUJ

# Cloudflare R2 (tune score sheet JPG storage)
# R2_ACCOUNT_ID: Cloudflare Dashboard → R2 → Account ID (right sidebar)
R2_ACCOUNT_ID=your_account_id_here
# R2_ACCESS_KEY_ID / R2_SECRET_ACCESS_KEY: Dashboard → R2 → Manage R2 API Tokens → Create Token
R2_ACCESS_KEY_ID=your_access_key_id_here
R2_SECRET_ACCESS_KEY=your_secret_access_key_here
# R2_BUCKET_NAME: name of the R2 bucket you created (e.g. psalter)
R2_BUCKET_NAME=psalter
# R2_PUBLIC_URL: public base URL for the bucket (e.g. https://pub-xxx.r2.dev)
R2_PUBLIC_URL=https://your-bucket.r2.dev
```

**.gitignore** (comprehensive — Next.js + secrets):
```
# Environment variables — NEVER commit
.env
.env.local
.env.*.local

# Next.js
.next/
out/
build/

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Drizzle
drizzle/migrations/

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
```
  </action>
  <verify>
    <automated>grep -c "5435:5432" /data/home/psalter/docker-compose.yml && grep -c "psalter-db" /data/home/psalter/docker-compose.yml && grep -c "DATABASE_URL" /data/home/psalter/.env.example && grep -c "R2_ACCOUNT_ID" /data/home/psalter/.env.example && grep "^\.env$" /data/home/psalter/.gitignore</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml exists and contains `psalter-db` as container_name
    - docker-compose.yml contains `5435:5432` port mapping
    - docker-compose.yml contains `postgres:16-alpine` image
    - .env.example contains DATABASE_URL, AIRTABLE_PAT, R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET_NAME, R2_PUBLIC_URL
    - .gitignore contains `.env` as a standalone line (not part of another pattern)
    - .gitignore contains `node_modules/`
    - .gitignore contains `.next/`
  </acceptance_criteria>
  <done>Three infrastructure files committed to git with correct content</done>
</task>

<task type="auto">
  <name>Task 2: Start psalter-db container and verify connectivity</name>
  <files></files>
  <read_first>
    - /data/home/psalter/docker-compose.yml (just created in Task 1)
  </read_first>
  <action>
Start the psalter-db PostgreSQL container and verify it accepts connections:

```bash
cd /data/home/psalter
docker compose up -d
```

Wait for healthcheck to pass (up to 30 seconds):
```bash
docker compose ps
# Expected: db container Status shows "healthy"
```

Verify connectivity using docker exec (psql not installed on host):
```bash
docker exec psalter-db pg_isready -U postgres -d psalter
# Expected: localhost:5432 - accepting connections
```

If startup fails: check `docker compose logs db` for errors. Common cause: port 5435 conflict — verify with `ss -tlnp | grep 5435`.
  </action>
  <verify>
    <automated>docker exec psalter-db pg_isready -U postgres -d psalter</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose ps` shows psalter-db with status "healthy" or "running"
    - `docker exec psalter-db pg_isready -U postgres -d psalter` exits 0 and prints "accepting connections"
    - No errors in `docker compose logs db`
  </acceptance_criteria>
  <done>psalter-db PostgreSQL 16 container is running, healthy, and accepting connections on host port 5435</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: Create R2 bucket and add credentials to .env</name>
  <what-built>docker-compose.yml, .env.example, and .gitignore are in place. psalter-db is running.</what-built>
  <how-to-verify>
    **This step requires manual action in the Cloudflare Dashboard:**

    1. Go to https://dash.cloudflare.com → R2 Object Storage
    2. Click "Create bucket" → name it `psalter` → choose a region near your users → Create
    3. On the bucket page → Settings → Public Access → click "Allow Access" → confirm
    4. Copy the public URL shown (e.g. `https://pub-xxxxxxxx.r2.dev` or your custom domain)
    5. Go to R2 Overview → "Manage R2 API Tokens" → "Create API Token"
    6. Set permissions: Object Read & Write → Specific bucket: psalter → Create
    7. Copy Account ID (shown on R2 Overview right sidebar), Access Key ID, and Secret Access Key

    Then create `/data/home/psalter/.env` with:
    ```
    DATABASE_URL=postgresql://postgres:postgres@localhost:5435/psalter
    AIRTABLE_PAT=<your existing PAT from /data/home/.env>
    AIRTABLE_BASE_ID=appY3dB1EHtex0fUJ
    R2_ACCOUNT_ID=<from step 7>
    R2_ACCESS_KEY_ID=<from step 7>
    R2_SECRET_ACCESS_KEY=<from step 7>
    R2_BUCKET_NAME=psalter
    R2_PUBLIC_URL=<public URL from step 4>
    ```

    Verify:
    - `cat /data/home/psalter/.env` shows all 8 variables with real values (not placeholder text)
    - `git -C /data/home/psalter status` does NOT list `.env` as a tracked file
  </how-to-verify>
  <resume-signal>Type "r2-ready" when .env is populated with all R2 credentials and the bucket is publicly accessible</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env → git | Secrets must never cross into version control |
| Airtable API → migration script | AIRTABLE_PAT is a bearer token; must not be logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env file | mitigate | `.env` added to .gitignore as standalone line; verified before first commit |
| T-01-02 | Information Disclosure | R2 credentials in .env | mitigate | `.env` in .gitignore; R2_SECRET_ACCESS_KEY never logged or returned in API responses |
| T-01-03 | Elevation of Privilege | psalter-db open on host port 5435 | accept | Development environment only; no public internet exposure; Cloudflare Tunnel does not forward raw PostgreSQL traffic |
</threat_model>

<verification>
```bash
# All three checks must pass
grep -c "5435:5432" /data/home/psalter/docker-compose.yml
grep "^\.env$" /data/home/psalter/.gitignore
docker exec psalter-db pg_isready -U postgres -d psalter
```
</verification>

<success_criteria>
- psalter-db container running, healthy, accepting connections on 5435
- .env is gitignored and populated with all required variables including real R2 credentials
- .env.example committed with all variable names documented
- docker-compose.yml committed with correct container name and port
</success_criteria>

<output>
After completion, create /data/home/psalter/.planning/phases/01-foundation/01-01-SUMMARY.md
</output>
