---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - .env
  - .gitignore
autonomous: true
requirements:
  - MIGR-01
  - MIGR-02
  - MIGR-03
  - MIGR-04

must_haves:
  truths:
    - "psalter-db PostgreSQL 16 container is running and accepting connections on host port 5435"
    - ".env is not tracked by git (present in .gitignore)"
    - ".env.example documents every required environment variable with descriptions"
    - "psalter_tunes Docker volume is defined and mounted at /app/public/tunes/ for tune score sheet JPG storage"
    - ".env is populated with DATABASE_URL, AIRTABLE_PAT, and AIRTABLE_BASE_ID"
  artifacts:
    - path: "docker-compose.yml"
      provides: "psalter-db PostgreSQL 16 container definition"
      contains: "psalter-db"
    - path: ".env.example"
      provides: "Template for all required environment variables"
      contains: "DATABASE_URL"
    - path: ".gitignore"
      provides: "Git exclusion rules"
      contains: ".env"
  key_links:
    - from: "docker-compose.yml"
      to: "host port 5435"
      via: "ports mapping"
      pattern: "5435:5432"
    - from: "docker-compose.yml"
      to: "psalter_tunes volume"
      via: "volumes declaration"
      pattern: "psalter_tunes"
    - from: ".env"
      to: "DATABASE_URL"
      via: "postgres.js connection string"
      pattern: "postgresql://postgres:postgres@localhost:5435/psalter"

---

<objective>
Create the infrastructure prerequisites for Phase 1: a running PostgreSQL 16 container (psalter-db), a tunes image volume, environment variable template, and git safety rules. This wave establishes the foundation that every subsequent plan depends on.

Purpose: Without a running database, a persistent volume for tune JPGs, and correct environment configuration, no schema can be pushed and no migration can run.
Output: docker-compose.yml (with psalter_tunes volume), .env.example, .env, .gitignore — all committed. psalter-db container running.
</objective>

<execution_context>
@/data/home/psalter/.claude/get-shit-done/workflows/execute-plan.md
@/data/home/psalter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/data/home/psalter/.planning/ROADMAP.md
@/data/home/psalter/.planning/phases/01-foundation/01-RESEARCH.md
@/data/home/psalter/.planning/phases/01-foundation/01-CONTEXT.md

<interfaces>
<!-- Verified from RESEARCH.md Pattern 7 — Docker Compose for psalter-db -->
<!-- Port conflict confirmed: other DB containers use 5432 internally; use 5435 for psalter-db -->

docker-compose.yml must use:
  container_name: psalter-db
  image: postgres:16-alpine
  ports: ["5435:5432"]
  POSTGRES_DB: psalter
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: postgres

DATABASE_URL = postgresql://postgres:postgres@localhost:5435/psalter

Airtable env vars required:
  AIRTABLE_PAT (already in /home/services/.env.production)
  AIRTABLE_BASE_ID = appY3dB1EHtex0fUJ

Tune image storage: Docker named volume psalter_tunes mounted at /app/public/tunes/
  - Migration writes JPEGs to this path on the host filesystem
  - Next.js serves them as static assets at /tunes/<filename>.jpg
  - No external object storage needed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docker-compose.yml, .env.example, and .gitignore</name>
  <files>docker-compose.yml, .env.example, .gitignore</files>
  <read_first>
    - /data/home/psalter/CLAUDE.md (project conventions)
    - /data/home/psalter/.planning/phases/01-foundation/01-RESEARCH.md (Pattern 7: Docker Compose, env var list, port 5435 rationale)
  </read_first>
  <action>
Create three files in /data/home/psalter/:

**docker-compose.yml:**
```yaml
services:
  db:
    image: postgres:16-alpine
    container_name: psalter-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: psalter
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - psalter_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    ports:
      - "5435:5432"

volumes:
  psalter_db_data:
```

**Port rationale:** 5432 is occupied by debates-db and other containers on this server. Use 5435.

**.env.example:**
```
# PostgreSQL (psalter-db container — host port 5435)
DATABASE_URL=postgresql://postgres:postgres@localhost:5435/psalter

# Airtable
AIRTABLE_PAT=your_personal_access_token_here
AIRTABLE_BASE_ID=appY3dB1EHtex0fUJ

# Cloudflare R2 (tune score sheet JPG storage)
# R2_ACCOUNT_ID: Cloudflare Dashboard → R2 → Account ID (right sidebar)
R2_ACCOUNT_ID=your_account_id_here
# R2_ACCESS_KEY_ID / R2_SECRET_ACCESS_KEY: Dashboard → R2 → Manage R2 API Tokens → Create Token
R2_ACCESS_KEY_ID=your_access_key_id_here
R2_SECRET_ACCESS_KEY=your_secret_access_key_here
# R2_BUCKET_NAME: name of the R2 bucket you created (e.g. psalter)
R2_BUCKET_NAME=psalter
# R2_PUBLIC_URL: public base URL for the bucket (e.g. https://pub-xxx.r2.dev)
R2_PUBLIC_URL=https://your-bucket.r2.dev
```

**.gitignore** (comprehensive — Next.js + secrets):
```
# Environment variables — NEVER commit
.env
.env.local
.env.*.local

# Next.js
.next/
out/
build/

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Drizzle
drizzle/migrations/

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
```
  </action>
  <verify>
    <automated>grep -c "5435:5432" /data/home/psalter/docker-compose.yml && grep -c "psalter-db" /data/home/psalter/docker-compose.yml && grep -c "DATABASE_URL" /data/home/psalter/.env.example && grep -c "R2_ACCOUNT_ID" /data/home/psalter/.env.example && grep "^\.env$" /data/home/psalter/.gitignore</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml exists and contains `psalter-db` as container_name
    - docker-compose.yml contains `5435:5432` port mapping
    - docker-compose.yml contains `postgres:16-alpine` image
    - .env.example contains DATABASE_URL, AIRTABLE_PAT, R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET_NAME, R2_PUBLIC_URL
    - .gitignore contains `.env` as a standalone line (not part of another pattern)
    - .gitignore contains `node_modules/`
    - .gitignore contains `.next/`
  </acceptance_criteria>
  <done>Three infrastructure files committed to git with correct content</done>
</task>

<task type="auto">
  <name>Task 2: Start psalter-db container and verify connectivity</name>
  <files></files>
  <read_first>
    - /data/home/psalter/docker-compose.yml (just created in Task 1)
  </read_first>
  <action>
Start the psalter-db PostgreSQL container and verify it accepts connections:

```bash
cd /data/home/psalter
docker compose up -d
```

Wait for healthcheck to pass (up to 30 seconds):
```bash
docker compose ps
# Expected: db container Status shows "healthy"
```

Verify connectivity using docker exec (psql not installed on host):
```bash
docker exec psalter-db pg_isready -U postgres -d psalter
# Expected: localhost:5432 - accepting connections
```

If startup fails: check `docker compose logs db` for errors. Common cause: port 5435 conflict — verify with `ss -tlnp | grep 5435`.
  </action>
  <verify>
    <automated>docker exec psalter-db pg_isready -U postgres -d psalter</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose ps` shows psalter-db with status "healthy" or "running"
    - `docker exec psalter-db pg_isready -U postgres -d psalter` exits 0 and prints "accepting connections"
    - No errors in `docker compose logs db`
  </acceptance_criteria>
  <done>psalter-db PostgreSQL 16 container is running, healthy, and accepting connections on host port 5435</done>
</task>

<task type="auto">
  <name>Task 3: Add tunes volume to docker-compose.yml, update .env.example, and create .env</name>
  <files>docker-compose.yml, .env.example, .env</files>
  <read_first>
    - /home/services/.env.production (shared secrets — read AIRTABLE_PAT from here)
    - /data/home/psalter/docker-compose.yml (already created in Task 1)
  </read_first>
  <action>
1. Update docker-compose.yml to add the psalter_tunes volume and mount it in a tunes-server service:

```yaml
services:
  db:
    image: postgres:16-alpine
    container_name: psalter-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: psalter
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - psalter_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    ports:
      - "5435:5432"

volumes:
  psalter_db_data:
  psalter_tunes:
```

Note: The psalter_tunes volume will be mounted into the Next.js app container at /app/public/tunes/ when the app container is defined in Plan 01-02. For now, just declare the volume.

2. Update .env.example to remove R2 vars and add TUNES_DIR:

```
# PostgreSQL (psalter-db container — host port 5435)
DATABASE_URL=postgresql://postgres:postgres@localhost:5435/psalter

# Airtable
AIRTABLE_PAT=your_personal_access_token_here
AIRTABLE_BASE_ID=appY3dB1EHtex0fUJ

# Tune score sheet images
# Directory where migration downloads JPEGs (served as /tunes/<file> by Next.js)
TUNES_DIR=/app/public/tunes
```

3. Read AIRTABLE_PAT from /home/services/.env.production and create /data/home/psalter/.env:

```
DATABASE_URL=postgresql://postgres:postgres@localhost:5435/psalter
AIRTABLE_PAT=<value from /home/services/.env.production>
AIRTABLE_BASE_ID=appY3dB1EHtex0fUJ
TUNES_DIR=/app/public/tunes
```
  </action>
  <verify>
    <automated>grep "psalter_tunes" /data/home/psalter/docker-compose.yml && grep "TUNES_DIR" /data/home/psalter/.env.example && grep "DATABASE_URL" /data/home/psalter/.env && ! git -C /data/home/psalter ls-files .env | grep -q ".env"</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml contains psalter_tunes volume declaration
    - .env.example contains TUNES_DIR (no R2 vars)
    - .env exists with DATABASE_URL, AIRTABLE_PAT (real value), AIRTABLE_BASE_ID, TUNES_DIR
    - .env is NOT tracked by git
  </acceptance_criteria>
  <done>Infrastructure files updated; .env populated from shared secrets; no external storage dependencies</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env → git | Secrets must never cross into version control |
| Airtable API → migration script | AIRTABLE_PAT is a bearer token; must not be logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env file | mitigate | `.env` added to .gitignore as standalone line; verified before first commit |
| T-01-02 | Elevation of Privilege | psalter-db open on host port 5435 | accept | Development environment only; no public internet exposure; Cloudflare Tunnel does not forward raw PostgreSQL traffic |
</threat_model>

<verification>
```bash
# All three checks must pass
grep -c "5435:5432" /data/home/psalter/docker-compose.yml
grep "^\.env$" /data/home/psalter/.gitignore
docker exec psalter-db pg_isready -U postgres -d psalter
```
</verification>

<success_criteria>
- psalter-db container running, healthy, accepting connections on 5435
- psalter_tunes volume declared in docker-compose.yml
- .env is gitignored and populated with DATABASE_URL, AIRTABLE_PAT, AIRTABLE_BASE_ID, TUNES_DIR
- .env.example committed with all variable names documented (no R2 vars)
- docker-compose.yml committed with correct container name, port, and tunes volume
</success_criteria>

<output>
After completion, create /data/home/psalter/.planning/phases/01-foundation/01-01-SUMMARY.md
</output>
