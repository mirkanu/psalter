# Phase 4.9.2: Dynamic ABC Polish - Discussion Log

**Session:** 2026-05-14
**Participants:** User + Claude (Haiku 4.5)

---

## Summary

Discussion focused on four implementation areas: phrase-splitting, font sizing, component composition, and syllable alignment. User provided critical clarifications on lyrical meter as the driver for phrase structure, and confirmed strategic decisions on render-time syllabification, CSS-based font sizing, and component refactoring toward a new NotationRenderer.

---

## Areas Discussed

### 1. Phrase-Splitting (4 decisions locked)

**Area intro:** UI-SPEC specifies "phrase-split hymnal format" with meter-dependent phrase lengths. Current code groups stanzas by 4 regardless of meter.

**Q1: When should phrase boundaries be calculated?**
- Options: At import time | At render time
- **Selected:** At import time (user chose "Recommended")
- **Rationale:** Simpler rendering, consistent across pages

**Q2: How should phrase breaks be marked in ABC?**
- Options: ABC comment markers | Separate DB columns | Measure numbers
- **Selected:** ABC comment markers (user chose "Recommended")
- **Rationale:** Non-invasive, simple parsing

**Q3: How to determine phrase lengths for different meters?**
- Options: Predefined meter mapping | Parse meter from ABC
- **Selected:** Predefined meter mapping (user chose "Recommended")
- **Rationale:** Simple, consistent, manually controllable

**Q4: Should stanzas align with phrase structure?**
- Options: Phrase-aware grouping | Fixed 4-stanza groups
- **Selected (with correction):** User clarified that phrase structure is actually driven by **lyrical meter**, not just tune meter. For CM (8.6.8.6 syllable structure), split after first 8+6 syllables (matching lyric line breaks). Each stanza's 4 lines map to phrase structure.
- **Key insight:** Lyric structure is the source of truth; notation should align with it.

**Decisions captured:**
- D-01: Phrase boundaries calculated at ABC import time
- D-02: Phrase breaks marked with ABC comment markers (`% PHRASE_BREAK`)
- D-03: Meter-dependent phrase lengths via predefined mapping
- D-04: Phrase structure follows lyrical meter (line breaks), not tune meter alone

---

### 2. Font Sizing (3 decisions locked)

**Area intro:** UI-SPEC specifies CSS custom property `--staff-base-size` controlling staff scale and lyric sizing. Current code uses React state.

**Q1: CSS custom properties or React state?**
- Options: CSS custom properties | React state with Tailwind
- **Selected:** CSS custom properties (user chose "Recommended")
- **Rationale:** Cleaner, no re-renders, cascade-friendly

**Q2: Font size range and step?**
- Options: 12–18px in 2px steps | 12–20px in 2px steps | Configurable
- **Selected:** 12–18px in 2px steps (user confirmed "Recommended")
- **Rationale:** Matches UI-SPEC baseline, 4 sizes (12, 14, 16, 18)

**Q3: Should font size persist?**
- Options: Yes, localStorage | No, session-only
- **Selected:** Yes, localStorage (user chose "Recommended")
- **Rationale:** Better UX for repeat users (precentors)

**Decisions captured:**
- D-05: Font sizing via CSS custom property `--staff-base-size`
- D-06: Font size range 12–18px in 2px increments
- D-07: Font size persists to localStorage

---

### 3. Component Composition (3 decisions locked)

**Area intro:** Currently AbcPlayer and PsalmNotationPlayer are separate. Need to decide on component structure for phrase-splitting and fullscreen mode.

**Q1: Enhance AbcPlayer or create NotationRenderer?**
- Options: New NotationRenderer | Enhance AbcPlayer
- **Selected:** New NotationRenderer (user chose "Recommended")
- **Rationale:** Cleaner separation of concerns

**Q2: Replace PsalmNotationPlayer or wrap with NotationRenderer?**
- Options: Replace it | Wrap it
- **Selected:** Replace it (user chose "Recommended")
- **Rationale:** Cleaner architecture

**Q3: Should NotationRenderer manage phrase navigation internally or expose to parent?**
- Options: Internal state | Lift to parent
- **Selected:** Internal state (user chose "Recommended")
- **Rationale:** Simpler API for consumers

**Q4: Render all phrases at once (scrollable) or one phrase at a time?**
- Options: One phrase at a time | All phrases visible
- **Selected:** One phrase at a time (user chose "Recommended")
- **Rationale:** Lighter DOM, matches UI-SPEC fullscreen layout

**Decisions captured:**
- D-08: Create new NotationRenderer component
- D-09: NotationRenderer manages phrase state internally
- D-10: Render one phrase at a time

---

### 4. Syllable Alignment (3 decisions locked)

**Area intro:** ABC is ~99% complete. Need to decide when/where to syllabify lyrics for abcjs `w:` fields.

**Q1: When should lyrics be syllabified for w: fields?**
- User asked: Is ABC conversion already done? (Yes, ~99% complete, visible on site)
- **Follow-up:** Should syllabification happen in Phase 4.9 (ABC generation) or Phase 4.9.2 (Polish)?
- **Selected:** Phase 4.9.2 (recommended to do on render)
- **Rationale:** Phase 4.9 focus is ABC generation; phase 4.9.2 handles presentation polish

**Q2: Syllabify on render or pre-compute in DB?**
- Options: On render (Recommended) | Pre-compute/backfill
- **Selected:** On render (user chose "Recommended")
- **Rationale:** Simpler, no DB changes, flexibility for logic tweaks

**Q3: Where should syllabification logic live?**
- Options: Enhance lib/lyrics.ts (Recommended) | New lib/syllabifier.ts
- **Selected:** Enhance lib/lyrics.ts (user chose "Recommended")
- **Rationale:** Keeps all lyrics logic in one place

**Q4: Generate w: lines for all stanzas or just current phrase?**
- Options: Just current phrase (Recommended) | All stanzas
- **Selected:** Just current phrase (user chose "Recommended")
- **Rationale:** Reduces computation, matches visible content

**Decisions captured:**
- D-11: Syllabify on render, not pre-computed
- D-12: Enhance lib/lyrics.ts with syllable embedding
- D-13: Generate w: lines for current phrase only

---

## Claude Discretion Items

Two areas where user deferred to Claude:

**D-14: Solfège rendering strategy** — User did not specify how to render solfège dynamically. Recommendation: show original solfège JPEG as fallback (from `solfegeJpgUrl`). Dynamic solfège generation deferred per UI-SPEC note.

**D-15: Fullscreen implementation** — User did not specify technical approach. Recommendation: CSS `position: fixed` layout with sticky bottom navigation, not separate Portal. Simpler state management.

---

## Key Clarifications from User

1. **Lyrical meter drives phrase structure** — Not just meter abbreviations. For CM (8.6.8.6), split where lyric lines break (after 8+6 syllables). This ensures visual alignment.

2. **Phrase boundaries in original JPEGs** — The printed scores already show phrase boundaries (via staff line breaks matching lyric structure). No special markers needed; meter structure is predictable.

3. **ABC already available** — Phase 4.9 is ~99% complete. No blocking dependency for 4.9.2 to start.

4. **localStorage for font size** — User confirmed repeat-user benefit (precentors setting preferred size once).

---

## Deferred or Out-of-Scope Items

- **Solfège dynamic generation** — Reserved for future enhancement per UI-SPEC
- **Multi-voice SATB rendering** — Out of scope (project decision)
- **In-app ABC editor** — Out of scope

---

## Next Steps

1. **Planning phase** (`/gsd-plan-phase 4.9.2`) — Researchers will read CONTEXT.md + UI-SPEC.md and identify existing patterns in AbcPlayer/PsalmNotationPlayer to guide implementation.
2. **Execution** — Implement NotationRenderer, extend lib/lyrics.ts, wire into psalm/tune detail pages.
3. **Verification** — Test phrase rendering at 375px/768px/1024px+; verify font sizing via CSS custom property; check localStorage persistence; verify syllable alignment under notes.

---

*Discussion log recorded: 2026-05-14*
*Phase: 4.9.2 - Dynamic ABC Polish*
