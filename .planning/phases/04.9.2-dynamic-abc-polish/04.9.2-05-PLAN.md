---
phase: 04.9.2-dynamic-abc-polish
plan: 05
type: execute
wave: 4
depends_on: [04.9.2-02, 04.9.2-04]
files_modified:
  - src/app/psalms/[id]/page.tsx
  - src/components/PsalmTabs.tsx
  - src/app/tunes/[id]/page.tsx
  - src/components/AbcPlayerSection.tsx
  - src/components/PsalmNotationPlayer.tsx
  - src/components/PsalmNotationPlayerClient.tsx
  - scripts/uat/04.9.2-notation.js
autonomous: false
requirements: [NOTATION-01, NOTATION-02, NOTATION-03, NOTATION-04, NOTATION-05, NOTATION-06]
tags: [integration, deletion, playwright, uat]

must_haves:
  truths:
    - "/psalms/[id] renders NotationRendererClient in place of PsalmNotationPlayerClient with no other layout changes"
    - "/tunes/[id] renders NotationRendererClient in place of AbcPlayerSection (per D-18)"
    - "Both pages pass stanzaMeter (psalm-version meter) to NotationRendererClient so D-20 stanza-cycle pairing works correctly"
    - "PsalmNotationPlayer.tsx and PsalmNotationPlayerClient.tsx are deleted from the repo"
    - "AbcPlayerSection.tsx is deleted (no remaining consumer after /tunes/[id] migration)"
    - "Playwright UAT script verifies: phrase nav, A+/A- size persistence, view-mode switching, fullscreen enter/exit on Chromium at 375 / 768 / 1024 viewports"
    - "Static rendering is preserved: generateStaticParams still works on both pages; abcjs only runs client-side via the dynamic wrapper"
  artifacts:
    - path: src/app/psalms/[id]/page.tsx
      provides: "Psalm detail page wired to NotationRendererClient"
    - path: src/app/tunes/[id]/page.tsx
      provides: "Tune detail page wired to NotationRendererClient (D-18)"
    - path: scripts/uat/04.9.2-notation.js
      provides: "Playwright UAT covering all 6 NOTATION-* requirements"
  key_links:
    - from: src/app/psalms/[id]/page.tsx
      to: src/components/notation/NotationRendererClient.tsx
      via: "NotationRendererClient JSX inside PsalmTabs / below-tabs section"
      pattern: "NotationRendererClient"
    - from: src/app/tunes/[id]/page.tsx
      to: src/components/notation/NotationRendererClient.tsx
      via: "Replaces <AbcPlayerSection> in the Score section"
      pattern: "NotationRendererClient"
---

<objective>
Wire the NotationRenderer component (Plan 04) into BOTH `/psalms/[id]` and `/tunes/[id]` (per D-18), delete the now-orphaned `PsalmNotationPlayer`, `PsalmNotationPlayerClient`, and `AbcPlayerSection` files, and run the Playwright UAT script that validates NOTATION-01..06 end-to-end. Final task is a human checkpoint to approve the visual outcome at the three documented viewports.

Purpose: This is the cutover plan. Up to Wave 3, NotationRenderer existed but was unused. After this plan, every notation surface on the site is the new component, the old code is gone, and verification has been performed against the live deployment.

Output: Two modified pages, three deleted component files, one new UAT script, one approval checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.9.2-dynamic-abc-polish/04.9.2-CONTEXT.md
@.planning/phases/04.9.2-dynamic-abc-polish/04.9.2-UI-SPEC.md
@.planning/phases/04.9.2-dynamic-abc-polish/04.9.2-04-SUMMARY.md
@src/app/psalms/[id]/page.tsx
@src/components/PsalmTabs.tsx
@src/app/tunes/[id]/page.tsx
@src/components/AbcPlayerSection.tsx
@src/components/PsalmNotationPlayer.tsx
@src/components/PsalmNotationPlayerClient.tsx

<interfaces>
From Plan 04:
```typescript
// src/components/notation/NotationRendererClient.tsx
export interface NotationRendererProps {
  abc: string
  lyrics: string
  scoreJpgUrl: string | null
  solfegeJpgUrl: string | null
  tuneName: string
  tuneMeter: string | null
  stanzaMeter: string | null   // NEW per D-20
}
export function NotationRendererClient(props: NotationRendererProps): JSX.Element
```

Existing /tunes/[id] integration point (src/app/tunes/[id]/page.tsx lines 128-143 per PATTERNS.md):
```tsx
{hasAbc && (
  <section>
    <h2 className="text-sm font-semibold uppercase tracking-wide text-muted-foreground mb-3">Score</h2>
    <AbcPlayerSection abc={bestAbc!} title={tune.name ?? undefined} tuneName={tune.name ?? `Tune ${tune.id}`}
      staffJpgUrl={staffPages[0] ?? null} solfegeJpgUrl={solfegePages[0] ?? null} initialMode="staff" />
  </section>
)}
```

Existing /psalms/[id] integration point: PsalmTabs.tsx renders `<PsalmNotationPlayerClient>` in the below-tabs section.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire NotationRendererClient into /psalms/[id] and /tunes/[id]</name>
  <files>src/app/psalms/[id]/page.tsx, src/components/PsalmTabs.tsx, src/app/tunes/[id]/page.tsx</files>
  <read_first>
    - src/app/psalms/[id]/page.tsx (entire — confirm generateStaticParams + fetchPsalmDetail unchanged; identify where the psalm version's meter is loaded — Plan 05 needs it to pass as stanzaMeter)
    - src/components/PsalmTabs.tsx (find where PsalmNotationPlayerClient is rendered)
    - src/app/tunes/[id]/page.tsx (lines 6, 68, 128-143 — confirm AbcPlayerSection import + usage; locate the linked psalm version's meter for stanzaMeter)
    - src/components/PsalmNotationPlayer.tsx (review existing prop shape)
    - src/db/schema.ts (confirm psalmVersion has a `meter` column; if not, derive stanzaMeter from psalm.meter or pass null)
    - .planning/phases/04.9.2-dynamic-abc-polish/04.9.2-CONTEXT.md D-18, D-20
    - .planning/phases/04.9.2-dynamic-abc-polish/04.9.2-04-SUMMARY.md (final prop shape)
  </read_first>
  <behavior>
    /psalms/[id]:
    - PsalmTabs (or whichever component currently mounts PsalmNotationPlayerClient) replaces that mount with `<NotationRendererClient abc={...} lyrics={...} scoreJpgUrl={...} solfegeJpgUrl={...} tuneName={...} tuneMeter={...} stanzaMeter={...} />`.
    - Source of each prop:
      - abc: `primaryTune.abcNotation ?? ''`.
      - lyrics: the psalm version's `lyrics` string.
      - scoreJpgUrl: `primaryTune.scoreJpgUrl ?? null`.
      - solfegeJpgUrl: `primaryTune.solfegeJpgUrl ?? null`.
      - tuneName: `primaryTune.name ?? 'Tune'`.
      - tuneMeter: `primaryTune.meter ?? null`.
      - **stanzaMeter: `psalmVersion.meter ?? null`** (D-20 — psalm-version meter drives stanza-cycle pairing).
    - Remove import of PsalmNotationPlayerClient; add NotationRendererClient import.
    - generateStaticParams unchanged.

    /tunes/[id]:
    - Replace `<AbcPlayerSection ... />` with `<NotationRendererClient abc={bestAbc!} lyrics={tunesLyrics} scoreJpgUrl={staffPages[0] ?? null} solfegeJpgUrl={solfegePages[0] ?? null} tuneName={tune.name ?? \`Tune ${tune.id}\`} tuneMeter={tune.meter ?? null} stanzaMeter={firstLinkedPsalmVersion?.meter ?? null} />`.
    - `tunesLyrics`: from first associated psalm version's `lyrics` field, else empty string.
    - `firstLinkedPsalmVersion`: `tune.psalmVersionTunes?.[0]?.psalmVersion ?? null`.
    - Remove `import { AbcPlayerSection }`; add NotationRendererClient import.

    NO other layout changes.
  </behavior>
  <action>
1. Read `src/components/PsalmTabs.tsx` to locate the PsalmNotationPlayerClient usage.

2. Edit `src/components/PsalmTabs.tsx`:
   - Update import: `PsalmNotationPlayerClient` → `NotationRendererClient` from '@/components/notation/NotationRendererClient'.
   - Replace JSX element; add `stanzaMeter={psalmVersion.meter ?? null}` prop.
   - Keep surrounding skeleton/fallback identical.

3. Edit `src/app/psalms/[id]/page.tsx`:
   - If the page directly imports PsalmNotationPlayerClient, update that too.
   - Ensure the psalm version's meter flows down to PsalmTabs (add a prop if necessary).
   - Verify generateStaticParams block untouched.

4. Edit `src/app/tunes/[id]/page.tsx`:
   - Line 6: replace `AbcPlayerSection` import with `NotationRendererClient`.
   - Score section JSX: pass `stanzaMeter={firstLinkedPsalmVersion?.meter ?? null}` and `tunesLyrics` from the first linked psalm version.

5. Type-check: `npx tsc --noEmit` exits 0.

6. `npm run build` to confirm generateStaticParams still succeeds for both routes.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -20; grep -c "NotationRendererClient" src/app/tunes/\[id\]/page.tsx; grep -c "NotationRendererClient" src/components/PsalmTabs.tsx; grep -c "AbcPlayerSection" src/app/tunes/\[id\]/page.tsx; grep -c "PsalmNotationPlayerClient" src/components/PsalmTabs.tsx; grep -c "stanzaMeter" src/components/PsalmTabs.tsx; grep -c "stanzaMeter" src/app/tunes/\[id\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `grep -c "NotationRendererClient" src/app/tunes/\[id\]/page.tsx` >= 2.
    - `grep -c "NotationRendererClient" src/components/PsalmTabs.tsx` >= 2.
    - `grep -c "AbcPlayerSection" src/app/tunes/\[id\]/page.tsx` == 0.
    - `grep -c "PsalmNotationPlayerClient" src/components/PsalmTabs.tsx` == 0.
    - `grep -c "PsalmNotationPlayerClient" src/app/psalms/\[id\]/page.tsx` == 0.
    - **`grep -c "stanzaMeter" src/components/PsalmTabs.tsx` >= 1** (D-20 prop wired on psalm page).
    - **`grep -c "stanzaMeter" src/app/tunes/\[id\]/page.tsx` >= 1** (D-20 prop wired on tune page).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Both pages render NotationRendererClient with stanzaMeter passed through; old components no longer referenced; production build succeeds with static rendering preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete orphaned components</name>
  <files>src/components/PsalmNotationPlayer.tsx, src/components/PsalmNotationPlayerClient.tsx, src/components/AbcPlayerSection.tsx</files>
  <read_first>
    - Search for ALL references: `grep -rn "PsalmNotationPlayer\|AbcPlayerSection" src/ scripts/ --include="*.ts" --include="*.tsx"` — every hit must point to a file slated for deletion or to NotationRendererClient. If any other consumer exists, STOP and report.
  </read_first>
  <behavior>
    - After deletion: `grep -rn "PsalmNotationPlayer" src/` returns 0; `grep -rn "AbcPlayerSection" src/` returns 0.
    - `npx tsc --noEmit` exits 0.
    - `npm run build` exits 0.
  </behavior>
  <action>
1. Reference search first.
2. `rm src/components/PsalmNotationPlayer.tsx src/components/PsalmNotationPlayerClient.tsx src/components/AbcPlayerSection.tsx`.
3. Type-check.
4. `npm run build`.
  </action>
  <verify>
    <automated>! test -f src/components/PsalmNotationPlayer.tsx && ! test -f src/components/PsalmNotationPlayerClient.tsx && ! test -f src/components/AbcPlayerSection.tsx && echo "deleted-OK"; grep -rn "PsalmNotationPlayer\|AbcPlayerSection" src/ --include="*.ts" --include="*.tsx" | grep -v "^#" | wc -l; npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Verify prints "deleted-OK".
    - grep for old component names returns 0 lines.
    - `npx tsc --noEmit` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Three legacy files deleted; no dangling references; production build still succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Playwright UAT script covering NOTATION-01..06</name>
  <files>scripts/uat/04.9.2-notation.js</files>
  <read_first>
    - /data/home/psalter/CLAUDE.md (Playwright setup)
    - .planning/phases/04.9.2-dynamic-abc-polish/04.9.2-RESEARCH.md lines 732-744
    - .planning/phases/04.9.2-dynamic-abc-polish/04.9.2-VALIDATION.md
  </read_first>
  <behavior>
    Run command: `NODE_PATH=/usr/lib/node_modules node scripts/uat/04.9.2-notation.js`. Target `http://localhost:3000` or `PSALTER_BASE_URL`.

    Test cases (each writes PASS/FAIL; final summary):
    1. **N01-syllable**: /psalms/23 at 768px. Wait for SVG. Assert at least one element with class containing `abcjs-l` (lyric line).
    2. **N02-solfege**: Click Solfège toggle; expect `<img>` with src matching `/solfege/`. Toggle back; SVG re-appears.
    3. **N03-responsive**: Reload at 375x800. Measure SVG width: assert <= 375 && > 200.
    4. **N04-asize**: Read computed `--staff-base-size` on `[data-notation-renderer]`. Click A+; assert 14px → 16px. Reload; assert persisted.
    5. **N04-persistent-modes**: Switch to "Lyrics Only"; A+/A- still visible and clickable.
    6. **N05-fullscreen**: Click Expand. Assert body overflow hidden and `.fixed.inset-0.z-50` mounted. Press Escape; overlay gone, body overflow restored.
    7. **N06-control-groups**: Count vertical dividers in control bar: >= 2.
    8. **Tune page parity** (D-18): /tunes/1. Assert NotationRenderer mounted.

    On failure, print diagnostic and continue. Final summary: `PASS: X / 8, FAIL: Y / 8`. Exit 0 if all pass.
  </behavior>
  <action>
1. Create `scripts/uat/04.9.2-notation.js` using the Playwright shape from CLAUDE.md.
2. Implement an `async function runTest(name, fn)` helper.
3. Each test acquires a fresh page, navigates, asserts, reports.
4. If `data-notation-renderer` is missing from NotationRenderer.tsx, add it. (Plan 04 already requires it, so this should pre-exist.)
5. Start dev server in background; poll until ready; run UAT; stop dev server.

   Production target: `PSALTER_BASE_URL=https://psalter.gsdlabs.dev`.

6. Expected output: `PASS: 8 / 8`.
  </action>
  <verify>
    <automated>test -f scripts/uat/04.9.2-notation.js && echo "exists"; grep -c "chromium.launch" scripts/uat/04.9.2-notation.js; grep -c "psalms/" scripts/uat/04.9.2-notation.js; grep -c "tunes/" scripts/uat/04.9.2-notation.js; grep -cE "Escape|staff-base-size|fixed inset-0" scripts/uat/04.9.2-notation.js</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/uat/04.9.2-notation.js` exists.
    - `grep -c "chromium.launch" scripts/uat/04.9.2-notation.js` == 1.
    - `grep -cE "psalms/" scripts/uat/04.9.2-notation.js` >= 1.
    - `grep -cE "tunes/" scripts/uat/04.9.2-notation.js` >= 1.
    - `grep -c "staff-base-size" scripts/uat/04.9.2-notation.js` >= 1.
    - `grep -c "Escape" scripts/uat/04.9.2-notation.js` >= 1.
    - Run against dev server: prints `PASS: 8 / 8`.
  </acceptance_criteria>
  <done>UAT script exists; 8/8 PASS against dev server.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human visual verification at 375/768/1024 + manual-only checks</name>
  <what-built>Phase 4.9.2 complete: phrase-aware ABC notation with syllable-aligned lyrics under the staff (one row per phrase per D-19), stanza-cycle pairing for double-meter tunes (D-20), two independent pagination axes — stanza pagination (always available) and tune-half pagination (mobile landscape only) with tune-half-flips-first cycle order (D-21), A+/A- sizing (12-18px, persisted), Staff/Solfège/Lyrics-only view modes, CSS pseudo-fullscreen with sticky bottom bar, control bar grouped into size/playback/view sections with dividers. Wired into BOTH /psalms/[id] and /tunes/[id] with stanzaMeter wired; legacy PsalmNotationPlayer and AbcPlayerSection deleted; Playwright UAT 8/8 PASS.</what-built>
  <how-to-verify>
    Run against the LIVE deployment (https://psalter.gsdlabs.dev). Local dev acceptable if not yet deployed.

    ```bash
    PSALTER_BASE_URL=https://psalter.gsdlabs.dev NODE_PATH=/usr/lib/node_modules node scripts/uat/04.9.2-notation.js
    ```
    Expect: `PASS: 8 / 8`.

    Manual checks (per VALIDATION.md):

    1. **Syllable alignment quality** — Open `/psalms/23` at 375 / 768 / 1024px. Each syllable under its note; hyphens between split syllables; no overlap; verse rows stack under each phrase row.

    2. **One row per phrase (D-19)** — At any viewport on a CM tune (e.g. /psalms/23), confirm the staff renders as TWO separate stacked staff rows (phrase a + phrase b), NOT a single long staff line.

    3. **Stanza pagination (D-21 axis A)** — On a long psalm (e.g. /psalms/119 if available, or any psalm with >3 stanza-cycles), confirm stanza Prev/Next pages through 3 cycles per page. The staff rows do NOT move; only the w: lines beneath them change.

    4. **Tune-half pagination (D-21 axis B)** — Find a double-meter tune (DCM). View on a narrow landscape viewport (or resize browser to landscape narrow). Confirm Prev/Next pages through half-A → half-B → next stanza-group's half-A → half-B (cycle order per D-21).

    5. **A+/A- behaviour across modes** — Click A+/A- to all four sizes; confirm disabled at min/max. Switch view modes — A+/A- remain visible.

    6. **Fullscreen** — Click Expand. Body scroll locked; bottom bar with Prev/Exit/Next; Escape exits.

    7. **Tune page parity (D-18)** — Open `/tunes/[id-with-abc]`. Same NotationRenderer behaviour.

    Reply: "approved" or list defects.
  </how-to-verify>
  <resume-signal>Type "approved" if 8/8 automated PASS and all 7 manual checks pass.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exits 0.
- `npx tsc --noEmit` exits 0.
- `npx vitest run` exits 0.
- `grep -rn "PsalmNotationPlayer\|AbcPlayerSection" src/` returns 0 lines.
- `scripts/uat/04.9.2-notation.js` exits 0.
</verification>

<success_criteria>
1. Both pages render NotationRendererClient with `stanzaMeter` wired (D-20).
2. Three legacy component files deleted; no dangling references.
3. Production build succeeds with static rendering of both routes.
4. 8/8 automated UAT pass.
5. Human approval recorded in Task 4 reply.
</success_criteria>

<output>
After completion, create `.planning/phases/04.9.2-dynamic-abc-polish/04.9.2-05-SUMMARY.md` documenting:
- Final prop wiring on each page (lyrics source and stanzaMeter source for /tunes/[id])
- Any UI tweaks made during UAT debugging
- Final 8/8 UAT output paste
- Human approval signal
- Confirmation that PsalmNotationPlayer.tsx, PsalmNotationPlayerClient.tsx, AbcPlayerSection.tsx are deleted

Also update `.planning/STATE.md`:
- Add accumulated-context entries for Plans 01-05
- Mark Phase 4.9.2 row as "Complete"
- Update Recent Trend
- Move v1 Requirements NOTATION-01..06 to "Complete" in REQUIREMENTS.md
</output>
