---
phase: 04.9.2-dynamic-abc-polish
plan: 05
type: execute
wave: 4
depends_on: [04.9.2-02, 04.9.2-04]
files_modified:
  - src/app/psalms/[id]/page.tsx
  - src/components/PsalmTabs.tsx
  - src/app/tunes/[id]/page.tsx
  - src/components/AbcPlayerSection.tsx
  - src/components/PsalmNotationPlayer.tsx
  - src/components/PsalmNotationPlayerClient.tsx
  - scripts/uat/04.9.2-notation.js
autonomous: false
requirements: [NOTATION-01, NOTATION-02, NOTATION-03, NOTATION-04, NOTATION-05, NOTATION-06]
tags: [integration, deletion, playwright, uat]

must_haves:
  truths:
    - "/psalms/[id] renders NotationRendererClient in place of PsalmNotationPlayerClient with no other layout changes"
    - "/tunes/[id] renders NotationRendererClient in place of AbcPlayerSection (per D-18)"
    - "PsalmNotationPlayer.tsx and PsalmNotationPlayerClient.tsx are deleted from the repo"
    - "AbcPlayerSection.tsx is deleted (no remaining consumer after /tunes/[id] migration)"
    - "Playwright UAT script verifies: phrase nav, A+/A- size persistence, view-mode switching, fullscreen enter/exit on Chromium at 375 / 768 / 1024 viewports"
    - "Static rendering is preserved: generateStaticParams still works on both pages; abcjs only runs client-side via the dynamic wrapper"
  artifacts:
    - path: src/app/psalms/[id]/page.tsx
      provides: "Psalm detail page wired to NotationRendererClient"
    - path: src/app/tunes/[id]/page.tsx
      provides: "Tune detail page wired to NotationRendererClient (D-18)"
    - path: scripts/uat/04.9.2-notation.js
      provides: "Playwright UAT covering all 6 NOTATION-* requirements"
  key_links:
    - from: src/app/psalms/[id]/page.tsx
      to: src/components/notation/NotationRendererClient.tsx
      via: "NotationRendererClient JSX inside PsalmTabs / below-tabs section"
      pattern: "NotationRendererClient"
    - from: src/app/tunes/[id]/page.tsx
      to: src/components/notation/NotationRendererClient.tsx
      via: "Replaces <AbcPlayerSection> in the Score section"
      pattern: "NotationRendererClient"
---

<objective>
Wire the NotationRenderer component (Plan 04) into BOTH `/psalms/[id]` and `/tunes/[id]` (per D-18), delete the now-orphaned `PsalmNotationPlayer`, `PsalmNotationPlayerClient`, and `AbcPlayerSection` files, and run the Playwright UAT script that validates NOTATION-01..06 end-to-end. Final task is a human checkpoint to approve the visual outcome at the three documented viewports.

Purpose: This is the cutover plan. Up to Wave 3, NotationRenderer existed but was unused. After this plan, every notation surface on the site is the new component, the old code is gone, and verification has been performed against the live deployment.

Output: Two modified pages, three deleted component files, one new UAT script, one approval checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.9.2-dynamic-abc-polish/04.9.2-CONTEXT.md
@.planning/phases/04.9.2-dynamic-abc-polish/04.9.2-UI-SPEC.md
@.planning/phases/04.9.2-dynamic-abc-polish/04.9.2-04-SUMMARY.md
@src/app/psalms/[id]/page.tsx
@src/components/PsalmTabs.tsx
@src/app/tunes/[id]/page.tsx
@src/components/AbcPlayerSection.tsx
@src/components/PsalmNotationPlayer.tsx
@src/components/PsalmNotationPlayerClient.tsx

<interfaces>
From Plan 04:
```typescript
// src/components/notation/NotationRendererClient.tsx
export interface NotationRendererProps {
  abc: string
  lyrics: string
  scoreJpgUrl: string | null
  solfegeJpgUrl: string | null
  tuneName: string
  tuneMeter: string | null
}
export function NotationRendererClient(props: NotationRendererProps): JSX.Element
```

Existing /tunes/[id] integration point (src/app/tunes/[id]/page.tsx lines 128-143 per PATTERNS.md):
```tsx
{hasAbc && (
  <section>
    <h2 className="text-sm font-semibold uppercase tracking-wide text-muted-foreground mb-3">Score</h2>
    <AbcPlayerSection abc={bestAbc!} title={tune.name ?? undefined} tuneName={tune.name ?? `Tune ${tune.id}`}
      staffJpgUrl={staffPages[0] ?? null} solfegeJpgUrl={solfegePages[0] ?? null} initialMode="staff" />
  </section>
)}
```

Existing /psalms/[id] integration point: PsalmTabs.tsx renders `<PsalmNotationPlayerClient>` in the below-tabs section. The dynamic wrapper takes the SAME props shape as PsalmNotationPlayer.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire NotationRendererClient into /psalms/[id] and /tunes/[id]</name>
  <files>src/app/psalms/[id]/page.tsx, src/components/PsalmTabs.tsx, src/app/tunes/[id]/page.tsx</files>
  <read_first>
    - src/app/psalms/[id]/page.tsx (entire — confirm generateStaticParams + fetchPsalmDetail unchanged; only the component render swap)
    - src/components/PsalmTabs.tsx (find where PsalmNotationPlayerClient is rendered — typically in the below-tabs JSX)
    - src/app/tunes/[id]/page.tsx (lines 6, 68, 128-143 — confirm AbcPlayerSection import and usage)
    - src/components/PsalmNotationPlayer.tsx (review the existing prop shape to ensure all required data is available — lyrics, tune, score/solfege JPGs, tune meter)
    - .planning/phases/04.9.2-dynamic-abc-polish/04.9.2-CONTEXT.md D-18 (BOTH pages adopt NotationRenderer)
    - .planning/phases/04.9.2-dynamic-abc-polish/04.9.2-04-SUMMARY.md (final prop shape)
  </read_first>
  <behavior>
    /psalms/[id]:
    - PsalmTabs (or whichever component currently mounts PsalmNotationPlayerClient) replaces that mount with `<NotationRendererClient abc={...} lyrics={...} scoreJpgUrl={...} solfegeJpgUrl={...} tuneName={...} tuneMeter={...} />`.
    - Source of each prop:
      - abc: `primaryTune.abcNotation ?? ''` (skip rendering if empty — keep the existing "no ABC" fallback message).
      - lyrics: the psalm version's `lyrics` string (already passed to PsalmNotationPlayerClient).
      - scoreJpgUrl: `primaryTune.scoreJpgUrl ?? null`.
      - solfegeJpgUrl: `primaryTune.solfegeJpgUrl ?? null`.
      - tuneName: `primaryTune.name ?? 'Tune'`.
      - tuneMeter: `primaryTune.meter ?? null`.
    - Remove import of PsalmNotationPlayerClient; add import of NotationRendererClient from '@/components/notation/NotationRendererClient'.
    - generateStaticParams MUST remain unchanged.

    /tunes/[id]:
    - Replace the `<AbcPlayerSection ... />` block with `<NotationRendererClient abc={bestAbc!} lyrics={tunesLyrics} scoreJpgUrl={staffPages[0] ?? null} solfegeJpgUrl={solfegePages[0] ?? null} tuneName={tune.name ?? \`Tune ${tune.id}\`} tuneMeter={tune.meter ?? null} />`.
    - `tunesLyrics`: since tune pages don't have a single canonical psalm, derive lyrics from the FIRST associated psalm version (use `tune.psalmVersionTunes?.[0]?.psalmVersion?.lyrics ?? ''`) OR pass empty string if no associated version. If empty, NotationRenderer's stanzas array is empty and no w: lines are emitted — staff renders alone. (D-18 says tune pages get the FULL feature set; "using verse 1 by default for tune-only context" — i.e. show the first associated psalm's verses as the lyric reference.)
    - Remove `import { AbcPlayerSection } from "@/components/AbcPlayerSection"` (line 6); add `import { NotationRendererClient } from "@/components/notation/NotationRendererClient"`.

    NO other layout changes — sidebar, metadata, tabs, sticky positioning all preserved.
  </behavior>
  <action>
1. Read `src/components/PsalmTabs.tsx` first to locate the PsalmNotationPlayerClient usage and confirm what props it passes.

2. Edit `src/components/PsalmTabs.tsx`:
   - Update import: `PsalmNotationPlayerClient` -> `NotationRendererClient` from '@/components/notation/NotationRendererClient'.
   - Replace the JSX element, mapping props per the behavior block above.
   - Keep the surrounding skeleton/fallback/conditional rendering identical.

3. Edit `src/app/psalms/[id]/page.tsx`:
   - If the page itself directly imports PsalmNotationPlayerClient (not only via PsalmTabs), update that import too.
   - Verify generateStaticParams block (around lines 16-39) is untouched.

4. Edit `src/app/tunes/[id]/page.tsx`:
   - Update line 6 import: replace `AbcPlayerSection` with `NotationRendererClient` from '@/components/notation/NotationRendererClient'.
   - Update the Score section JSX (lines 128-143). Use `bestAbc!` as the abc value. Derive `tunesLyrics` from the first linked psalm version if available; otherwise pass empty string.

5. Type-check: `npx tsc --noEmit` must exit 0.

6. Run `npm run build` (Next.js production build) to confirm `generateStaticParams` still succeeds for both routes. Failure here usually means abcjs leaked to the server — verify the dynamic({ssr:false}) wrapper is being used.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -20; grep -c "NotationRendererClient" src/app/tunes/\[id\]/page.tsx; grep -c "NotationRendererClient" src/components/PsalmTabs.tsx; grep -c "AbcPlayerSection" src/app/tunes/\[id\]/page.tsx; grep -c "PsalmNotationPlayerClient" src/components/PsalmTabs.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `grep -c "NotationRendererClient" src/app/tunes/\[id\]/page.tsx` >= 2 (one import + one usage).
    - `grep -c "NotationRendererClient" src/components/PsalmTabs.tsx` >= 2.
    - `grep -c "AbcPlayerSection" src/app/tunes/\[id\]/page.tsx` == 0 (replaced).
    - `grep -c "PsalmNotationPlayerClient" src/components/PsalmTabs.tsx` == 0 (replaced).
    - `grep -c "PsalmNotationPlayerClient" src/app/psalms/\[id\]/page.tsx` == 0.
    - `npm run build` exits 0 (Next.js production build succeeds, including static export of /psalms/[id] and /tunes/[id]).
  </acceptance_criteria>
  <done>Both pages render NotationRendererClient; old components no longer referenced; production build succeeds with static rendering preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete orphaned components</name>
  <files>src/components/PsalmNotationPlayer.tsx, src/components/PsalmNotationPlayerClient.tsx, src/components/AbcPlayerSection.tsx</files>
  <read_first>
    - Search for ALL references to each file before deletion: `grep -rn "PsalmNotationPlayer\|AbcPlayerSection" src/ scripts/ --include="*.ts" --include="*.tsx"` — every hit must point to a file slated for deletion or the new NotationRendererClient. If any other consumer exists (e.g. tests, other pages), STOP and report it.
  </read_first>
  <behavior>
    - After deletion, `grep -rn "PsalmNotationPlayer" src/` returns zero hits.
    - After deletion, `grep -rn "AbcPlayerSection" src/` returns zero hits.
    - `npx tsc --noEmit` still exits 0 (no dangling imports).
    - `npm run build` still exits 0.
  </behavior>
  <action>
1. Run the reference search first:
   ```bash
   grep -rn "PsalmNotationPlayer\|AbcPlayerSection" src/ scripts/ --include="*.ts" --include="*.tsx" || true
   ```
   The ONLY expected hits are inside the three files being deleted. If anything else shows up, abort and revise Plan 04/05 to handle that consumer.

2. Delete the three files:
   ```bash
   rm src/components/PsalmNotationPlayer.tsx
   rm src/components/PsalmNotationPlayerClient.tsx
   rm src/components/AbcPlayerSection.tsx
   ```

3. Type-check: `npx tsc --noEmit`. Any failure indicates a missed consumer — fix the consumer, do NOT recreate the file.

4. Run `npm run build` again to ensure the static generation still works after deletion.
  </action>
  <verify>
    <automated>! test -f src/components/PsalmNotationPlayer.tsx && ! test -f src/components/PsalmNotationPlayerClient.tsx && ! test -f src/components/AbcPlayerSection.tsx && echo "deleted-OK"; grep -rn "PsalmNotationPlayer\|AbcPlayerSection" src/ --include="*.ts" --include="*.tsx" | grep -v "^#" | wc -l; npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Verify command prints "deleted-OK".
    - The grep for old component names returns 0 lines.
    - `npx tsc --noEmit` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Three legacy files deleted; no dangling references; production build still succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Playwright UAT script covering NOTATION-01..06</name>
  <files>scripts/uat/04.9.2-notation.js</files>
  <read_first>
    - /data/home/psalter/CLAUDE.md (Playwright setup: `/usr/lib/node_modules/playwright`, chromium at `/tmp/pw-browsers/chromium-1217/chrome-linux/chrome`, run with `NODE_PATH=/usr/lib/node_modules node`)
    - .planning/phases/04.9.2-dynamic-abc-polish/04.9.2-RESEARCH.md lines 732-744 (Phase Requirements -> Test Map)
    - .planning/phases/04.9.2-dynamic-abc-polish/04.9.2-VALIDATION.md (Wave 0 Requirements + Manual-Only Verifications)
    - Any existing UAT scripts under scripts/uat/ for shape/style reference (if none, model after CLAUDE.md inline example)
  </read_first>
  <behavior>
    Script entry point: `scripts/uat/04.9.2-notation.js`. Run command:
    `NODE_PATH=/usr/lib/node_modules node scripts/uat/04.9.2-notation.js`

    Targets: `http://localhost:3000` (or the dev server URL provided via PSALTER_BASE_URL env var). Default to localhost.

    Test cases (each writes a one-line PASS/FAIL to stdout; final summary line):
    1. **N01-syllable** (NOTATION-01): Nav to /psalms/23 at 768px. Wait for the abcjs SVG to mount (`page.locator('svg').first()`). Evaluate that the rendered DOM contains at least one element with class containing `abcjs-l` (lyric line) — i.e. w: line rendered.
    2. **N02-solfege** (NOTATION-02): Same page. Click the "Solfège" toggle button. Expect an `<img>` whose src matches `/solfege/` (or `solfegeJpgUrl`). Toggle back to Staff; expect SVG re-appears.
    3. **N03-responsive** (NOTATION-03): Reload at viewport 375x800. Measure the rendered SVG's `getBoundingClientRect().width`. Assert width <= 375 AND > 200 (responsive resize working).
    4. **N04-asize** (NOTATION-04): At 768px viewport on /psalms/23, read computed CSS variable `--staff-base-size` on the NotationRenderer root (use document.querySelector with a stable selector like `[data-notation-renderer]` — Plan 04 task SHOULD add `data-notation-renderer` attribute to the root div; if it didn't, this UAT will note that and the executor adds it). Click A+ once; assert variable went from "14px" to "16px". Reload the page; assert it's still "16px" (localStorage persistence).
    5. **N04-persistent-modes**: Switch to "Lyrics Only" view; assert A+/A- buttons are still visible AND clickable (NOT hidden as in the old PsalmNotationPlayer).
    6. **N05-fullscreen** (NOTATION-05): Click the Expand button. Assert document body's `overflow` computed style is `hidden`. Assert the renderer root element has `position: fixed` (querySelector for `.fixed.inset-0.z-50`). Press Escape; assert the fixed overlay is gone and body overflow restored.
    7. **N06-control-groups** (NOTATION-06): Count vertical dividers in the control bar — assert `>= 2` (separates the 3 logical groups: size / playback / view).
    8. **Tune page parity** (D-18): Nav to /tunes/1 (or first tune with abcNotation). Assert NotationRenderer mounted (same selector as on psalm page). Confirms tune pages use the same component.

    On the first failure, print the diagnostic and continue running remaining tests. Final summary: `PASS: X / 8, FAIL: Y / 8`. Exit code 0 if all pass, 1 otherwise.

    Test browser configuration:
    ```js
    const { chromium } = require('/usr/lib/node_modules/playwright');
    const browser = await chromium.launch({
      executablePath: '/tmp/pw-browsers/chromium-1217/chrome-linux/chrome',
      args: ['--no-sandbox']
    });
    ```

    NOTE on N04-asize: If NotationRenderer doesn't already have `data-notation-renderer` on its root div, this UAT will fail. The executor should add the attribute as part of this task (a one-line tweak to NotationRenderer.tsx) and document it.
  </behavior>
  <action>
1. Create `scripts/uat/04.9.2-notation.js`. Use the Playwright shape from CLAUDE.md.

2. Structure: an `async function runTest(name, fn)` helper that catches errors, logs `PASS/FAIL <name>: <details>`, and pushes results into a shared array. Use this for all 8 cases.

3. Each test acquires a fresh `page` from the same browser context, navigates, performs its assertions, and reports.

4. If `data-notation-renderer` is missing from NotationRenderer's root div, edit `src/components/notation/NotationRenderer.tsx` to add `data-notation-renderer=""` on the outermost div (both non-fullscreen and fullscreen render paths). Document the addition in this plan's SUMMARY.

5. Start a dev server (`npm run dev`) in the background BEFORE running the UAT. Poll the server with curl until it's ready, then run the UAT. Stop the dev server after.

   Alternative: run against the production deployment at `https://psalter.gsdlabs.dev` by setting `PSALTER_BASE_URL=https://psalter.gsdlabs.dev` and running the script. This is the FINAL gate in Task 4 (human checkpoint).

6. Run command:
   ```bash
   PSALTER_BASE_URL=http://localhost:3000 NODE_PATH=/usr/lib/node_modules node scripts/uat/04.9.2-notation.js
   ```
   Expected output ends with `PASS: 8 / 8`.
  </action>
  <verify>
    <automated>test -f scripts/uat/04.9.2-notation.js && echo "exists"; grep -c "chromium.launch" scripts/uat/04.9.2-notation.js; grep -c "psalms/23\|psalms/1\|psalms/" scripts/uat/04.9.2-notation.js; grep -c "tunes/" scripts/uat/04.9.2-notation.js; grep -c "Escape\|staff-base-size\|fixed inset-0" scripts/uat/04.9.2-notation.js</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/uat/04.9.2-notation.js` exists.
    - `grep -c "chromium.launch" scripts/uat/04.9.2-notation.js` == 1.
    - `grep -cE "psalms/" scripts/uat/04.9.2-notation.js` >= 1.
    - `grep -cE "tunes/" scripts/uat/04.9.2-notation.js` >= 1.
    - `grep -c "staff-base-size" scripts/uat/04.9.2-notation.js` >= 1.
    - `grep -c "Escape" scripts/uat/04.9.2-notation.js` >= 1.
    - When run against a live dev server, prints `PASS: 8 / 8` (all 8 tests succeed). If any FAIL, the executor fixes the underlying issue (this is a NEW component; small UI bugs are expected and must be ironed out here before checkpoint).
  </acceptance_criteria>
  <done>UAT script exists; runs against dev server; reports 8/8 PASS; data-notation-renderer attribute confirmed on NotationRenderer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human visual verification at 375/768/1024 + manual-only checks</name>
  <what-built>Phase 4.9.2 is complete: phrase-aware ABC notation with syllable-aligned lyrics under the staff, A+/A- sizing (12-18px, persisted), Staff/Solfège/Lyrics-only view modes, phrase navigation for double-meter tunes, CSS pseudo-fullscreen with sticky bottom bar, control bar grouped into size/playback/view sections with dividers. Wired into BOTH /psalms/[id] and /tunes/[id]; legacy PsalmNotationPlayer and AbcPlayerSection deleted; Playwright UAT 8/8 PASS.</what-built>
  <how-to-verify>
    Run against the LIVE deployment (https://psalter.gsdlabs.dev) — local dev server is acceptable if the user has not yet deployed.

    Automated re-run (already green from Task 3, but confirms against live):
    ```bash
    PSALTER_BASE_URL=https://psalter.gsdlabs.dev NODE_PATH=/usr/lib/node_modules node scripts/uat/04.9.2-notation.js
    ```
    Expect: `PASS: 8 / 8`.

    Manual checks (per VALIDATION.md Manual-Only Verifications):

    1. **Syllable alignment quality** — Open `/psalms/23` at viewport 375px, 768px, and 1024px (resize browser). At each width:
       - Each syllable sits directly under its note (no horizontal drift).
       - Hyphens render between split syllables (e.g. "Shep-herd").
       - No syllable overlaps the next.
       - Multiple stanzas stack vertically under the staff (verse rows).

    2. **A+/A- behaviour across modes** — On /psalms/23:
       - In Staff mode: click A+ four times (12 -> 14 -> 16 -> 18); confirm A+ becomes disabled at 18px and the staff visibly enlarges each step.
       - Click A- four times back to 12px; A- disables at 12px.
       - Switch to Lyrics Only; A+/A- still visible and adjust verse-text size.
       - Switch to Solfège; A+/A- still visible (their effect may be minimal here since the image scales by CSS, but the buttons themselves must be reachable per NOTATION-04).

    3. **Fullscreen on a double-meter tune** — Find a tune with meter "DCM" or similar (e.g. an 8.6.8.6.8.6.8.6 tune). Click Expand. Confirm:
       - Body scroll is locked (try scrolling the page behind — should not move).
       - Sticky bottom bar shows [Prev | Exit | Next].
       - Prev/Next pages through phrases (>=2 per page per D-16); both buttons disable at start/end.
       - Escape key exits and restores body scroll.
       - The renderer reverts to its non-fullscreen position with state preserved (phrase index, view mode).

    4. **Tune page parity (D-18)** — Open `/tunes/[some-tune-id-with-abc]`. Confirm the same NotationRenderer is in use: A+/A- buttons present, phrase navigation works (if applicable), fullscreen works.

    5. **iOS Safari fullscreen smoke** (if a physical iPhone is available) — VALIDATION.md flags this as manual-only. Open `/psalms/23` on iPhone Safari, tap Expand, rotate to landscape, page phrases, exit. Confirm no scroll-lock leaks.

    Reply with: "approved" + any spot-check notes, OR list specific defects observed.
  </how-to-verify>
  <resume-signal>Type "approved" if 8/8 automated PASS and all 5 manual checks pass. Otherwise describe each defect; the executor will create a Plan 06 (gap-closure) to address them.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exits 0 (static export of /psalms/[id] and /tunes/[id] preserved).
- `npx tsc --noEmit` exits 0.
- `npx vitest run` exits 0 (all unit tests from Plans 01/02 still green).
- `grep -rn "PsalmNotationPlayer\|AbcPlayerSection" src/` returns 0 lines.
- `scripts/uat/04.9.2-notation.js` exits 0 against the chosen base URL.
</verification>

<success_criteria>
1. Both pages render NotationRendererClient — confirmed by source inspection AND by Playwright (test 8 hits /tunes/, tests 1-7 hit /psalms/).
2. Three legacy component files deleted; no dangling references.
3. Production build succeeds with static rendering of both routes.
4. 8/8 automated UAT pass.
5. Human approval recorded in Task 4 reply.
</success_criteria>

<output>
After completion, create `.planning/phases/04.9.2-dynamic-abc-polish/04.9.2-05-SUMMARY.md` documenting:
- The final prop wiring on each page (lyrics source for /tunes/[id])
- Any UI tweaks made during UAT debugging
- The final 8/8 UAT output paste
- Human approval signal
- Confirmation that PsalmNotationPlayer.tsx, PsalmNotationPlayerClient.tsx, AbcPlayerSection.tsx are deleted

Also update `.planning/STATE.md`:
- Add accumulated-context entries for Plans 01-05 (key decisions per plan)
- Mark Phase 4.9.2 row in the progress table as "Complete"
- Update Recent Trend
- Move v1 Requirements NOTATION-01..06 to "Complete" in REQUIREMENTS.md traceability table
</output>
