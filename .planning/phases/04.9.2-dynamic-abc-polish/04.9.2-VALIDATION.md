---
phase: 4.9.2
slug: dynamic-abc-polish
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-14
---

# Phase 4.9.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Detailed dimensions and per-task verification rows are populated by the planner in PLAN.md files and reconciled into the table below after Wave 0 lands.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest (unit, lib/*) + Playwright/Chromium (UAT, browser) |
| **Config file** | `vitest.config.ts` (or to be added in Wave 0 if missing) + `playwright.config.ts` |
| **Quick run command** | `npx vitest run src/lib` (~5–15 s) |
| **Full suite command** | `npx vitest run && NODE_PATH=/usr/lib/node_modules node scripts/uat/04.9.2-notation.js` |
| **Estimated runtime** | Vitest ~30 s, Playwright UAT ~60–90 s |

> Vitest already used elsewhere in the repo; confirm in Wave 0 task and install if absent. Playwright is installed globally at `/usr/lib/node_modules/playwright`, browser at `/tmp/pw-browsers/chromium-1217/`.

---

## Sampling Rate

- **After every task commit:** Run quick command for the touched file area (e.g. `npx vitest run src/lib/lyrics.test.ts`).
- **After every plan wave:** Full unit suite + targeted Playwright UAT for the wave's user-visible surface.
- **Before `/gsd-verify-work`:** Full suite green; Playwright golden-path scripts pass on Chromium at 375 / 768 / 1024 viewports.
- **Max feedback latency:** 30 s for unit, 120 s for UAT.

---

## Per-Task Verification Map

> Populated by the planner. Each plan's tasks declare `<automated>` commands; this table is the cross-plan reconciliation produced post-Wave 0.

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| _to be filled by planner_ | | | | | | | |

---

## Wave 0 Requirements

- [ ] `src/lib/abc-phrases.test.ts` — unit stubs for phrase splitting (NOTATION-01, NOTATION-03)
- [ ] `src/lib/abc-phrase-meter-map.test.ts` — meter → phrase-count lookup
- [ ] `src/lib/lyrics.test.ts` — extend with `buildAbcWithSyllables` + `splitStanzaIntoPhrasePortions` tests (NOTATION-01, D-16)
- [ ] `src/lib/stanza-cycles.test.ts` — unit stubs for `groupStanzasIntoCycles` + `mapCycleToPhraseSyllableLines` (D-19/D-20: universal phrase-row + paired-stanza cycles + reverse-mismatch console.warn fallback)
- [ ] `scripts/annotate-phrase-breaks.test.ts` — idempotent annotation script for existing `abc_notation` rows
- [ ] `scripts/uat/04.9.2-notation.js` — Playwright scaffold (golden path: load /psalms/[id], change A+/A−, page stanzas, toggle fullscreen, switch view modes)
- [ ] Confirm `vitest` install; add to `package.json` and `vitest.config.ts` if absent

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Visual correctness of syllable alignment under staff | NOTATION-01 | abcjs SVG layout cannot be asserted pixel-perfectly across viewport sizes; visual review at three viewports required after Wave 2. | Open `/psalms/23` at 375/768/1024 — verify each syllable sits under its note; no overlap between syllables; hyphens render between split syllables. |
| iOS Safari fullscreen overlay behavior on real device | NOTATION-05 | CSS `position: fixed` overlay (per D-15) cannot be Playwright-verified on iOS Safari; emulation diverges from real device behavior. | Manual smoke on iPhone Safari: enter fullscreen, rotate to landscape, page phrases, exit fullscreen. Verify no scroll lock leak. |
| Solfège JPG quality at A+/A− extremes | NOTATION-02 | JPG scaling is a visual fidelity concern (font/text legibility), not a functional one. | View Solfège mode at 12px and 18px — verify legible at min size, no excessive whitespace at max. |
| Tune-half pagination on real mobile landscape (D-21 axis B) | NOTATION-03/05 | matchMedia + landscape rotation behaviour is device-specific; Playwright headless emulation does not faithfully reproduce iOS Safari orientation events. | On a real iPhone in Safari, open a DCM psalm/tune, rotate to landscape. Confirm tune splits into two halves; Prev/Next cycles half-A → half-B → next stanza-group's half-A (D-21 cycle order). |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags (`vitest run`, not `vitest`)
- [ ] Feedback latency < 30 s (unit) / 120 s (UAT)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
