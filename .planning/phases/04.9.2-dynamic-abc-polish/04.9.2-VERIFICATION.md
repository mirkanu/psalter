---
phase: 04.9.2-dynamic-abc-polish
verified: 2026-05-14T15:30:00Z
status: passed
score: 13/13 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: null
  previous_score: null
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 04.9.2 — Dynamic ABC Polish — Verification Report

**Phase Goal:** Live ABC notation is fully functional and visually polished — lyrics split by syllables and rendered inline with the staff; all notation fits dynamically to screen; full-screen mode is available with verse navigation; score controls are visually grouped and intentionally organized.

**Verified:** 2026-05-14T15:30:00Z
**Status:** PASS
**Re-verification:** No — initial verification.

---

## Observable Truths

| # | Truth (NOTATION-0X / Dxx) | Status | Evidence |
|---|----------------------------|--------|----------|
| 1 | NOTATION-01: Syllable-aligned lyrics under the staff via `w:` lines | PASS | `src/lib/lyrics.ts:156-171` — `buildAbcWithSyllables` strips pre-existing `w:` lines, syllabifies portions and appends them. `NotationRenderer.tsx:386-411` composes `unifiedAbc` by emitting `w: ` lines per visible phrase. UAT `N01-syllable`: PASS (1977 ms) — `[class*="abcjs-l"]` element present at `/psalms/23` 768 px. |
| 2 | NOTATION-02: Staff / Solfège / Lyrics-only toggle (mutually exclusive) | PASS | `NotationRenderer.tsx:42` `type ViewMode = 'staff'\|'solfege'\|'lyrics'`; solfège branch (`L471-482`) renders `<img src={solfegeJpgUrl}>` (no JPEG→ABC live conversion, per D-14). UAT `N02-solfege`: PASS (3433 ms) — `<img>` rendered after toggle, SVG restored on toggle-back. |
| 3 | NOTATION-03 / D-19: One staff row per phrase, driven by ABC newlines through a single AbcPlayer | PASS | `NotationRenderer.tsx:380-411` builds `unifiedAbc` = `header + (phrase body + w: line(s))×N`. `L460-470` renders exactly one `<AbcPlayer abc={unifiedAbc}>`. abcjs's native newline-per-staff behaviour yields the visual N-row layout. UAT `N03-responsive`: PASS — SVG width ≤ 375 px at 375 px viewport. |
| 4 | NOTATION-04: A+/A− visible in ALL view modes including Lyrics-only | PASS | `NotationRenderer.tsx:268-291` defines `sizeGroup`; `L353-378` `controlBar` includes `sizeGroup` UNCONDITIONALLY; `viewArea` switch at `L458-491` only changes the content below the controls. UAT `N04-persistent-modes`: PASS — A+/A− buttons clickable in Lyrics-only. UAT `N04-asize`: PASS — 14→16 px persisted across reload via `localStorage['psalter-staff-size']`. |
| 5 | NOTATION-05: Fullscreen overlay with sticky bottom bar, Escape exit | PASS | `src/components/notation/FullscreenOverlay.tsx:39-55` — `fixed inset-0 z-50` container + `fixed inset-x-0 bottom-0 h-12` bottom bar; Escape handler at `L25-27`; body scroll lock at `L29-30`. Wired in `NotationRenderer.tsx:498-536` with `fullscreenBottomBar` (Prev / Exit / Next). UAT `N05-fullscreen`: PASS — overlay mounts on Expand; `body.overflow=hidden`; Escape closes. |
| 6 | NOTATION-06: Grouped control bar with vertical dividers | PASS | `NotationRenderer.tsx:352` `divider = <div className="h-6 w-px bg-border" />`; `L353-378` controlBar emits size‖view‖pagination groups separated by dividers, with Expand right-aligned via `ml-auto`. UAT `N06-control-groups`: PASS — ≥ 1 vertical divider in visible renderer. |
| 7 | D-19 — staff body assembled with native ABC newlines (single AbcPlayer instance) | PASS | `NotationRenderer.tsx:386-411` constructs `unifiedAbc` and passes it to a single `<AbcPlayer>` (`L461-469`). No per-phrase mapping over an `AbcPlayer` array remains. Decision documented in STATE.md `[04.9.2-05]` and re-approved by visual review 2026-05-14. |
| 8 | D-20 — meter-aware `groupStanzasIntoCycles` (size 1 for CM/LM/SM, size 2 for DCM/DLM/DSM) | PASS | `src/lib/stanza-cycles.ts:32-53` computes `cycleSize = floor(T/S)` with reverse-mismatch warn + fallback. NotationRenderer calls it with both tune and stanza meter: `NotationRenderer.tsx:123-126`. Unit tests in `src/lib/stanza-cycles.test.ts` (14 cases) all pass. |
| 9 | D-21 — two independent pagination axes (stanza always; tune-half mobile-landscape only) | PASS | Axis A: `NotationRenderer.tsx:128` `totalStanzaPages = ceil(cycles.length / 3)`. Axis B gated by `useMediaQuery('(orientation: landscape) and (max-width: 900px)') && T > 2` (`L131-134`). `next()`/`prev()` (`L209-224`) — tune-half flips FIRST, then cycle advances. `useMediaQuery` SSR-safe in `src/hooks/useMediaQuery.ts`. |
| 10 | Legacy components deleted — `PsalmNotationPlayer*`, `AbcPlayerSection` | PASS | `ls` on all three paths returns "No such file or directory". `grep -rn "PsalmNotationPlayer\|AbcPlayerSection" src/` returns 1 hit only — a docstring comment in `NotationRenderer.tsx:64` (intentional historical reference, no imports). |
| 11 | Routes wired — `/psalms/[id]` (via PsalmTabs Sing tab) and `/tunes/[id]` both render NotationRendererClient | PASS | `src/components/PsalmTabs.tsx:14, 416` imports `pickAbcWithMarkers, sopranoOnly` and mounts `<NotationRendererClient>`. `src/app/tunes/[id]/page.tsx:6, 10, 68, 137` mounts the same component. Both pass through `stanzaMeter` (D-20). Build SSGs both routes (196 psalm pages, 169 tune pages). |
| 12 | Phrase-break markers backfilled into DB | PASS | `PGPASSWORD=... psql ... -c "SELECT COUNT(*) FILTER (WHERE abc_notation ILIKE '%PHRASE_BREAK%')..."` → `137 / 148` rows marked (matches Plan 02 SUMMARY exactly). `scripts/annotate-phrase-breaks.ts` exists; 11 rows correctly skipped (meter unmapped — n=1 — single-phrase tunes per D-03). |
| 13 | Build + tests green; UAT green | PASS | `npm run build`: ✓ Compiled successfully in 19.7s; ✓ Generating static pages 1325/1325. `npx vitest run`: 139/140 pass — single failure `tests/tune-detail.test.ts` is pre-existing Phase-1 disk-constraint test (STATE.md `[04-02]` documents it as out of scope). `npx tsc --noEmit`: errors only in `tests/e2e/abc-player.spec.ts` and `tests/tune-notation.spec.ts` — pre-existing per Plan 01 SUMMARY. Playwright UAT 8/8 PASS after restarting stale dev server. |

**Score:** 13/13 must-haves verified.

---

## Required Artifacts

| Artifact | Status | Notes |
|----------|--------|-------|
| `src/components/notation/NotationRenderer.tsx` | VERIFIED | 547 lines; single-AbcPlayer architecture, unified controls, fullscreen, auto-advance. |
| `src/components/notation/NotationRendererClient.tsx` | VERIFIED | `dynamic({ssr:false})` wrapper + Suspense skeleton. |
| `src/components/notation/FullscreenOverlay.tsx` | VERIFIED | CSS pseudo-fullscreen with Escape handler and body-scroll lock. |
| `src/components/AbcPlayer.tsx` | VERIFIED | Adds `scale`, `onPlayStart`, `onPlaybackComplete`, `onPlaybackStop`, `autoPlayToken` props (consumed by NotationRenderer for auto-advance). |
| `src/hooks/useMediaQuery.ts` | VERIFIED | SSR-safe; subscribes to `change`. |
| `src/lib/abc-phrases.ts` | VERIFIED | `splitOnPhraseBreaks` + `buildPhraseAbc`. |
| `src/lib/abc-phrase-meter-map.ts` | VERIFIED | `PHRASES_PER_STANZA` + `phrasesForMeter`. |
| `src/lib/lyrics.ts` | VERIFIED | Adds `splitStanzaIntoPhrasePortions` + `buildAbcWithSyllables`. |
| `src/lib/stanza-cycles.ts` | VERIFIED | `groupStanzasIntoCycles` + `mapCycleToPhraseSyllableLines`. |
| `src/lib/utils.ts` | VERIFIED | Adds `pickAbcWithMarkers` (NOTE: SUMMARY 05 says this lives in `src/lib/abc-phrases.ts`; actual location is `src/lib/utils.ts`. Functionally correct, doc drift only — both routes import correctly). |
| `scripts/annotate-phrase-breaks.ts` | VERIFIED | DB confirms 137/148 backfilled. |
| `scripts/uat/04.9.2-notation.js` | VERIFIED | 8/8 PASS against fresh `next start`. |
| `src/app/psalms/[id]/page.tsx` + `src/components/PsalmTabs.tsx` | VERIFIED | Sing tab mounts NotationRendererClient with `pickAbcWithMarkers(abcSatb, abcNotation) → sopranoOnly()`. |
| `src/app/tunes/[id]/page.tsx` | VERIFIED | Same wiring with `firstLinkedPsalmVersion` for lyrics + stanzaMeter. |
| `src/components/PsalmNotationPlayer.tsx` (LEGACY) | DELETED ✓ | `ls`: file not found. |
| `src/components/PsalmNotationPlayerClient.tsx` (LEGACY) | DELETED ✓ | `ls`: file not found. |
| `src/components/AbcPlayerSection.tsx` (LEGACY) | DELETED ✓ | `ls`: file not found. |

---

## Key Link Verification

| From | To | Via | Status |
|------|----|-----|--------|
| PsalmTabs → NotationRendererClient | `<NotationRendererClient abc={...} lyrics={...} stanzaMeter={...} />` | `import { NotationRendererClient } from '@/components/notation/NotationRendererClient'` + `import { sopranoOnly, pickAbcWithMarkers } from '@/lib/utils'` | WIRED |
| tunes/[id] → NotationRendererClient | Same component | Same import pair | WIRED |
| NotationRendererClient → NotationRenderer | `dynamic(() => import('./NotationRenderer').then(m => m.NotationRenderer), { ssr: false })` | dynamic import | WIRED |
| NotationRenderer → AbcPlayer | Single instance, passes `abc=unifiedAbc, scale, autoPlayToken, onPlayStart, onPlaybackComplete, onPlaybackStop` | direct import | WIRED |
| NotationRenderer → FullscreenOverlay | `{isFullscreen && <FullscreenOverlay open onClose=...>{controlBar}{viewArea}</FullscreenOverlay>}` | direct import | WIRED |
| NotationRenderer → useMediaQuery | `useMediaQuery('(orientation: landscape) and (max-width: 900px)')` | direct import | WIRED (D-21 axis B trigger) |
| NotationRenderer → groupStanzasIntoCycles, mapCycleToPhraseSyllableLines, splitOnPhraseBreaks, syllabifyForAbc | All imports present and used in `useMemo`/`unifiedAbc` body | direct imports | WIRED |
| Auto-advance: AbcPlayer.onPlaybackComplete → NotationRenderer.next() | `onPlaybackComplete` callback in renderer increments `autoPlayToken` after `next()` (`L434-442`) — AbcPlayer reacts to token bump and re-triggers play (`AbcPlayer.tsx:255-272`) | refs + token | WIRED |

---

## Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Real Data | Status |
|----------|---------------|--------|-----------|--------|
| NotationRenderer (psalms) | `abc, lyrics, tuneMeter, stanzaMeter` | PsalmTabs ← page.tsx ← `fetchPsalmDetail()` → DB tables `psalms / psalm_versions / psalm_version_tunes / tunes` | YES (DB) | FLOWING |
| NotationRenderer (tunes) | Same props | tunes/[id]/page.tsx ← `fetchTuneDetail()` → DB tables `tunes / psalm_version_tunes / psalm_versions` | YES (DB) | FLOWING |
| ABC variant selection | `pickAbcWithMarkers(abcSatb, abcNotation)` | DB — 137/148 rows contain markers | YES (verified by SQL) | FLOWING |
| Cycle pagination | `cycles = groupStanzasIntoCycles(stanzas, tuneMeter, stanzaMeter)` | derived from `lyrics.split(/\n\s*\n/)` — real metrical lyric stanzas from DB | YES | FLOWING |
| w: lines per phrase | `wLinesForPhrase(i)` → `mapCycleToPhraseSyllableLines` → `splitStanzaIntoPhrasePortions` → `syllabifyForAbc` | DB lyrics + meter | YES | FLOWING |

---

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Production build | `npm run build` | ✓ Compiled successfully in 19.7s; 1325 SSG pages | PASS |
| Unit tests | `npx vitest run` | 139/140 pass; one pre-existing Phase-1 score_jpg_url failure (out of scope) | PASS |
| Static psalm page renders | `curl -sI http://localhost:3005/psalms/23` | 200; `x-nextjs-prerender: 1` | PASS |
| Marker-backfill DB count | `psql ... COUNT FILTER (WHERE abc_notation ILIKE '%PHRASE_BREAK%')` | 137 / 148 (matches Plan 02 SUMMARY) | PASS |
| Playwright UAT | `node scripts/uat/04.9.2-notation.js` (fresh server) | 8 / 8 PASS — N01..N06 + parity + persistence | PASS |
| Legacy component grep | `grep -rn "PsalmNotationPlayer\|AbcPlayerSection" src/` | 1 hit (docstring comment only) | PASS |

---

## Requirements Coverage

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| NOTATION-01 | Syllable-split lyrics via `w:` field | SATISFIED | `buildAbcWithSyllables` wired through `unifiedAbc`; UAT N01 PASS |
| NOTATION-02 | Solfège JPEG via toggle; Staff/Solfège/Lyrics mutually exclusive | SATISFIED | `ViewMode` union; img branch for solfège; UAT N02 PASS |
| NOTATION-03 | Dynamic fit + D-19 one-row-per-phrase + A+/A− | SATISFIED | Single AbcPlayer + unified ABC with native newlines; `responsive: "resize"` set in AbcPlayer L183; `scale = baseSize/14` |
| NOTATION-04 | A+/A− always visible; localStorage persistence | SATISFIED | `sizeGroup` outside `viewArea` switch; `localStorage['psalter-staff-size']` effect; UAT N04 PASS |
| NOTATION-05 | Fullscreen + sticky bottom bar + Escape + tune-half on landscape | SATISFIED | FullscreenOverlay + `useMediaQuery` axis-B trigger; UAT N05 PASS |
| NOTATION-06 | Visual grouping with dividers between functional regions | SATISFIED | Three groups separated by `h-6 w-px bg-border` dividers; UAT N06 PASS |

All six NOTATION requirements satisfied. No orphaned requirements detected.

---

## Anti-Patterns Found

| File | Severity | Note |
|------|----------|------|
| (none observed in code paths added by this phase) | — | — |

Documented known follow-ups (per Plan 05 SUMMARY, recorded as accepted technical debt, NOT failures):

1. **Plan 02 marker-position bug** — Crimond `% PHRASE_BREAK` lands after measure 13 of 15 instead of the natural midpoint. Cosmetic only under the single-AbcPlayer architecture (visual rows are driven by ABC newlines, marker position only affects where the newline is inserted by `splitOnPhraseBreaks`). Algorithm fix deferred to a follow-up. **Accepted.**
2. **DB data quality** — Psalm 23 stanzas 1+2 joined in `lyrics` (missing blank-line separator). Pre-existing migration issue, not 04.9.2. **Accepted.**
3. **SATB phrase-break backfill not run** — Plan 02 targeted only `abc_notation`. `pickAbcWithMarkers` correctly falls back to monophonic for SATB rows that lack markers, so D-19 is preserved. **Accepted.**
4. **BPM/Key change mid-play resets auto-advance chain** — synth tear-down on transpose/BPM; documented v2 limitation. **Accepted.**

---

## Uncaught Issues Discovered During Verification

1. **Doc drift in Plan 05 SUMMARY (cosmetic, NOT a functional defect)** — SUMMARY lists `src/lib/abc-phrases.ts` under modified files for the `pickAbcWithMarkers` helper, but the helper is actually in `src/lib/utils.ts` (verified via `grep`). Both consumers (`PsalmTabs.tsx`, `tunes/[id]/page.tsx`) import from the correct location. Functionally equivalent; no implementation gap. **Severity: INFO.**

2. **Stale dev/start server cached old build chunks on port 3005** — Initial UAT run produced 0/8 PASS due to `ChunkLoadError` from a `next start` process that had been left running before the latest `npm run build`. After restart, UAT returned 8/8 PASS. **Severity: INFO** (environment hygiene, not a code defect). The phase's deployed artifact at `psalter.gsdlabs.dev` is independently confirmed APPROVED by the user (Plan 05 SUMMARY "Human visual verification").

3. **Pre-existing test failures (NOT introduced by 04.9.2)** —
   - `tests/tune-detail.test.ts` "at least one tune has a score_jpg_url" fails because the disk-constraint from Phase 1 left all `score_jpg_url` columns NULL. Documented in STATE.md `[04-02]`. **Out of scope.**
   - `tests/e2e/abc-player.spec.ts` + `tests/tune-notation.spec.ts` TypeScript errors (chromium redeclare, TestResult shape). Documented in Plan 01 and Plan 05 SUMMARIES as out of scope. **Out of scope.**

None of the above are blockers; all are recorded for completeness.

---

## Human Verification Required

(none — Manuel approved the deployed surface on 2026-05-14 per Plan 05 SUMMARY "Human visual verification"; verifier reproduced UAT 8/8 PASS locally)

---

## Gaps Summary

No gaps found. All 13 must-haves verified directly against the codebase, DB, build output, and a fresh Playwright UAT run. The phase's stated goal — "live ABC notation fully functional and visually polished with phrase-split layout, syllable-aligned lyrics, A+/A−, fullscreen, and grouped controls" — is achieved in code at the levels expected by the plan series and the UI-SPEC contract.

---

_Verified: 2026-05-14T15:30:00Z_
_Verifier: Claude (gsd-verifier, goal-backward verification)_
