---
phase: 04.7-psalm-listing-overhaul
plan: 01
subsystem: testing, ui
tags: [shadcn, checkbox, playwright, tailwind, globals.css]

requires:
  - phase: 04.6-psalm-detail-ui-polish
    provides: existing globals.css with [data-snippet] b rule at font-weight 600

provides:
  - shadcn Checkbox component at src/components/ui/checkbox.tsx
  - globals.css [data-snippet] b font-weight corrected to 700
  - Playwright test stubs for PLR-01, PLR-02, PLR-03 at /tmp/

affects:
  - 04.7-02 (Wave 1 components depend on checkbox.tsx; Playwright stubs are the verification gates)
  - 04.7-03 (Wave 2 UAT uses all three /tmp/ test scripts)

tech-stack:
  added:
    - shadcn Checkbox (base-nova style, Tailwind CSS 4)
  patterns:
    - Wave 0 test-first: Playwright stubs written before any component implementation

key-files:
  created:
    - src/components/ui/checkbox.tsx
    - /tmp/test-plr-01.js
    - /tmp/test-plr-02.js
    - /tmp/test-plr-03.js
  modified:
    - src/app/globals.css

key-decisions:
  - "Playwright stubs live in /tmp/ (ephemeral) — they are not tracked by git; they exist to provide runnable gates from Wave 1 onwards"
  - "font-weight 700 (not 600) for [data-snippet] b — bolder match text benefits both /psalms and /search pages"

patterns-established:
  - "Wave 0 test stubs: write failing tests before any component work; gate each subsequent wave on the same scripts"

requirements-completed:
  - PLR-01
  - PLR-02
  - PLR-03

duration: 8min
completed: 2026-05-09
---

# Phase 4.7 Plan 01: Psalm Listing Overhaul — Wave 0 Infrastructure Summary

**shadcn Checkbox installed, globals.css bold weight corrected to 700, and three Playwright test stubs written for PLR-01/02/03 before any component work begins**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-05-09T15:05:00Z
- **Completed:** 2026-05-09T15:13:00Z
- **Tasks:** 2
- **Files modified:** 2 (+ 3 ephemeral /tmp/ scripts)

## Accomplishments

- shadcn Checkbox component created at `src/components/ui/checkbox.tsx` via shadcn CLI (base-nova style)
- `[data-snippet] b` font-weight corrected from 600 to 700 in `src/app/globals.css`
- Three syntactically-valid Playwright test stubs written to /tmp/ — they will FAIL until Wave 1 implements the PsalmListingGrid; that is intentional

## Task Commits

1. **Task 1: Install shadcn Checkbox + fix globals.css bold weight** - `e0cb674` (chore)
2. **Task 2: Write Playwright test stubs** - (no git commit — /tmp/ artifacts are ephemeral by plan spec)

## Files Created/Modified

- `src/components/ui/checkbox.tsx` - shadcn Checkbox component (used by PsalmListingGrid Advanced Filters panel in Wave 1)
- `src/app/globals.css` - `[data-snippet] b` font-weight 600 → 700
- `/tmp/test-plr-01.js` - PLR-01 smoke test: 150 psalm boxes visible on /psalms via `[data-psalm-box]`
- `/tmp/test-plr-02.js` - PLR-02 smoke test: number sort, bold snippet via `<strong>`, Enter→/psalms/N navigation
- `/tmp/test-plr-03.js` - PLR-03 smoke test: Advanced Filters toggle, #show-first-line, #show-meter, meter select

## Decisions Made

- Playwright stubs written to /tmp/ (ephemeral, not git-tracked) — plan spec lists them as /tmp/ artifacts intentionally; they serve as runnable gates for subsequent waves
- font-weight 700 is intentional global change: both /psalms search snippets and Phase 3 /search page benefit from bolder highlighted matches

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Known Stubs

None — this plan delivers infrastructure only (no UI rendering logic).

## Self-Check: PASSED

- `src/components/ui/checkbox.tsx` exists: FOUND
- `src/app/globals.css` contains `font-weight: 700` for `[data-snippet] b`: FOUND
- `/tmp/test-plr-01.js` exists and passes `node --check`: FOUND
- `/tmp/test-plr-02.js` exists and passes `node --check`: FOUND
- `/tmp/test-plr-03.js` exists and passes `node --check`: FOUND
- Commit `e0cb674` exists: FOUND

## Next Phase Readiness

- Wave 1 (04.7-02) can proceed: `checkbox.tsx` is available for import, test stubs exist as automated gates
- `/tmp/test-plr-01.js` will be the first green gate once PsalmListingGrid renders 150 `[data-psalm-box]` elements

---
*Phase: 04.7-psalm-listing-overhaul*
*Completed: 2026-05-09*
