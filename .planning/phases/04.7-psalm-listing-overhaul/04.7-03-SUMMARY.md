---
phase: 04.7-psalm-listing-overhaul
plan: 03
subsystem: ui, loading
tags: [psalm-listing, loading-skeleton, cleanup, playwright]

requires:
  - phase: 04.7-02
    provides: PsalmListingGrid and PsalmNumberBox replace PsalmGrid and PsalmCard; all three PLR Playwright tests pass

provides:
  - Dense 6/8/10-col loading skeleton for /psalms matching live grid shape
  - Deletion of dead PsalmGrid.tsx and PsalmCard.tsx components

affects:
  - /psalms page perceived performance (loading state matches live layout — no layout shift)

tech-stack:
  added: []
  patterns:
    - Array.from({ length: 72 }) for skeleton box generation
    - grid-cols-6 sm:grid-cols-8 md:grid-cols-10 skeleton matching live PsalmListingGrid columns

key-files:
  modified:
    - src/app/psalms/loading.tsx — rewritten: max-w-7xl, dense 10-col skeleton, 72 boxes, search bar skeleton
  deleted:
    - src/components/PsalmGrid.tsx — superseded by PsalmListingGrid (Wave 1)
    - src/components/PsalmCard.tsx — superseded by PsalmNumberBox (Wave 1)

key-decisions:
  - "72 skeleton boxes chosen to fill ~7-8 rows at 10-col desktop — covers typical viewport without excessive DOM"
  - "No Advanced panel skeleton (panel is collapsed by default — zero height)"
  - "Playwright stubs corrected from /tmp/pw-browsers path to /home/claude/.cache/ms-playwright path before running"

duration: 15min
completed: 2026-05-09
---

# Phase 4.7 Plan 03: Psalm Listing Overhaul — Wave 2 Cleanup and Verification Summary

**Dense loading skeleton matches live grid; PsalmGrid and PsalmCard deleted; all three PLR Playwright smoke tests pass green**

## Performance

- **Duration:** ~15 min
- **Completed:** 2026-05-09
- **Tasks:** 1 auto + 1 checkpoint (human-verify)
- **Files modified:** 1 (loading.tsx)
- **Files deleted:** 2 (PsalmGrid.tsx, PsalmCard.tsx)

## Accomplishments

- `src/app/psalms/loading.tsx` rewritten: `max-w-7xl` wrapper, heading skeleton (h-9 + h-5), search bar `h-10` skeleton, dense `grid-cols-6 sm:grid-cols-8 md:grid-cols-10` grid with 72 `h-12` skeleton boxes — matches live PsalmListingGrid column layout, eliminates layout shift on page load
- `src/components/PsalmGrid.tsx` deleted — no remaining consumers after Wave 1 replaced it with PsalmListingGrid
- `src/components/PsalmCard.tsx` deleted — no remaining consumers after Wave 1 replaced it with PsalmNumberBox
- `npx tsc --noEmit` exits clean (0 errors)
- All three Playwright smoke tests pass green against deployed production site

## Task Commits

1. **Task 1: loading.tsx rewrite + dead component deletion** — `0ee5c6e` (feat)

## Playwright Results

| Test | Result | Details |
|------|--------|---------|
| PLR-01 | PASS | 150 psalm boxes rendered |
| PLR-02a | PASS | Number sort: "23" → Psalm 23 first |
| PLR-02b | PASS | Bold snippet: 112 `<strong>` matches for "Lord" |
| PLR-02c | PASS | Enter navigation → /psalms/7 |
| PLR-03a | PASS | Advanced panel collapsed by default |
| PLR-03b | PASS | Panel opens on click |
| PLR-03c | PASS | #show-first-line and #show-meter checkboxes present |
| PLR-03d | PASS | Meter filter select present |
| PLR-03e | PASS | Panel collapses on second click |

## Files Modified

- `src/app/psalms/loading.tsx` — rewritten from 14 lines (max-w-4xl, 24 sparse boxes) to 20 lines (max-w-7xl, 72 dense boxes, search skeleton)

## Files Deleted (Intentional)

- `src/components/PsalmGrid.tsx` — dead code; replaced by PsalmListingGrid in Plan 02
- `src/components/PsalmCard.tsx` — dead code; replaced by PsalmNumberBox in Plan 02

## Decisions Made

- 72 skeleton boxes chosen (72 = 8 cols × 9 rows) — fills typical 1280px viewport without excessive DOM overhead
- No Advanced Filters skeleton — the panel is collapsed by default, contributing zero height to initial layout
- Playwright stubs had wrong Chromium path (`/tmp/pw-browsers/...` instead of `/home/claude/.cache/ms-playwright/...`) — corrected in place before running (ephemeral fix, stubs are /tmp artifacts)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocker] Playwright stub Chromium path was incorrect**
- **Found during:** Task 1 verification (pre-checkpoint Playwright run)
- **Issue:** Test stubs written in Wave 0 referenced `/tmp/pw-browsers/chromium-1217/chrome-linux/chrome` — path does not exist on this system
- **Fix:** `sed -i` substitution to correct path `/home/claude/.cache/ms-playwright/chromium-1217/chrome-linux/chrome` in all three /tmp stubs before running
- **Files modified:** /tmp/test-plr-01.js, /tmp/test-plr-02.js, /tmp/test-plr-03.js (ephemeral /tmp — not committed)
- **Commit:** N/A (ephemeral fix)

## Known Stubs

None — all data is live from DB; loading.tsx is a pure skeleton with no data dependencies.

## Threat Surface Scan

No new network endpoints or auth paths introduced. loading.tsx is a static RSC with no user input, no data fetching.

## Self-Check: PASSED

- `src/app/psalms/loading.tsx` exists and contains `grid-cols-6`: VERIFIED
- `src/components/PsalmGrid.tsx` deleted: VERIFIED (`test ! -f` passes)
- `src/components/PsalmCard.tsx` deleted: VERIFIED (`test ! -f` passes)
- No remaining imports of PsalmGrid or PsalmCard in src/: VERIFIED (grep returns empty)
- `npx tsc --noEmit` clean: VERIFIED (0 error TS lines)
- PLR-01 PASS: VERIFIED (150 boxes)
- PLR-02 PASS: VERIFIED (number sort, 112 bold matches, Enter nav)
- PLR-03 PASS: VERIFIED (panel toggle, checkboxes, meter select)
- Commit `0ee5c6e` exists: VERIFIED

## Checkpoint Status

Task 1 (auto) complete. Awaiting checkpoint:human-verify — human visual check at https://psalter.gsdlabs.dev/psalms.
