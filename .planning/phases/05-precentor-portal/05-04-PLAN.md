---
phase: 05-precentor-portal
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - src/app/api/precent/[id]/reorder/route.ts
  - src/app/precent/[id]/page.tsx
  - src/app/precent/[id]/loading.tsx
  - src/components/precent/SetDetail.tsx
  - src/components/precent/SetItemsSortableList.tsx
  - src/components/precent/SetItemRow.tsx
autonomous: true
requirements: [PREC-03, PREC-04]
user_setup: []

must_haves:
  truths:
    - "A precentor can open /precent/[id] and see the set header plus an ordered psalm table (Psalm, Verses, Meter, Tune, actions)"
    - "A precentor can drag-reorder rows; the new order persists and reverts with a toast on API error"
    - "Rows where the tune's meter differs from the psalm's meter are amber-highlighted with a 'Meter mismatch' indicator"
    - "Each row has a play icon (start precenting here) and a delete icon (with confirmation)"
    - "The 'Add Psalm' and 'Start Precenting' (amber) buttons are present"
  artifacts:
    - path: "src/app/api/precent/[id]/reorder/route.ts"
      provides: "POST batch position update in a transaction"
      exports: ["POST"]
    - path: "src/app/precent/[id]/page.tsx"
      provides: "RSC set detail page joining items + psalm + tune + meters"
    - path: "src/components/precent/SetItemsSortableList.tsx"
      provides: "dnd-kit sortable table with optimistic reorder"
    - path: "src/components/precent/SetItemRow.tsx"
      provides: "single draggable row: handle, psalm, verses, meter, tune cell, mismatch, play, delete"
    - path: "src/components/precent/SetDetail.tsx"
      provides: "set detail shell: header + action bar + table + modals"
  key_links:
    - from: "src/components/precent/SetItemsSortableList.tsx"
      to: "/api/precent/[id]/reorder"
      via: "fetch POST on dragEnd"
      pattern: "reorder"
    - from: "src/components/precent/SetDetail.tsx"
      to: "PsalmPickerModal + TunePickerModal"
      via: "modal triggers"
      pattern: "PsalmPickerModal"
    - from: "src/app/precent/[id]/page.tsx"
      to: "precentingSets + setItems"
      via: "db.query findFirst with relations"
      pattern: "setItems"
---

<objective>
Build the set detail experience (PREC-03 reorder + PREC-04 overview): the batch reorder API, the `/precent/[id]` RSC page (joins items with psalm/tune/meter data), the `SetDetail` shell (header, Add Psalm + Start Precenting buttons, modals), and the dnd-kit sortable table (`SetItemsSortableList` + `SetItemRow`) with optimistic reorder, meter-mismatch highlight, play, and delete-with-confirmation. Includes `loading.tsx`.

Purpose: PREC-03 (edit/reorder slots) + PREC-04 (view set overview).
Output: Fully interactive /precent/[id] page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-precentor-portal/05-CONTEXT.md
@.planning/phases/05-precentor-portal/05-RESEARCH.md
@.planning/phases/05-precentor-portal/05-PATTERNS.md
@.planning/phases/05-precentor-portal/05-UI-SPEC.md

<interfaces>
<!-- dnd-kit v6 classic API (installed plan 01). Pattern from RESEARCH.md Pattern 1.
  {...listeners} ONLY on GripVertical handle, NOT on <tr>.
  <tr> gets ref={setNodeRef} + style={{ transform: CSS.Transform.toString(transform), transition }}.
  opacity-50 on dragging row. -->

Item API (plan 03):
  POST   /api/precent/[id]/items          → { psalmId, tuneId?, verseRange? } → { ok, item }
  PATCH  /api/precent/[id]/items/[itemId] → { tuneId?, verseRange? }
  DELETE /api/precent/[id]/items/[itemId]
Reorder (this plan): POST /api/precent/[id]/reorder → { ids: number[] }

Picker modals (plan 03): PsalmPickerModal { open, onClose, psalms, onAdd }, TunePickerModal { open, onClose, tunes, psalmMeter, onSelect }.

Meter mismatch (RESEARCH Open Question 2 / Assumption A2): set_items.psalm_id → psalms; meter lives on psalm_versions. Use the psalm's PRIMARY-version meter. fetchPsalmListRows() returns PsalmRow[] with a `meter` field per row — use that as the psalm meter source (id → meter map).

PsalmRow.meter and TuneRow.meter are normalised abbreviations (CM, LM, SM...). Mismatch = both non-null AND not equal (after trim/uppercase).

SingingView amber color tokens + UI-SPEC §Color: mismatch row `bg-amber-50 dark:bg-amber-900/20` + `border-l-2 border-amber-400`; warning badge `bg-amber-100 text-amber-800 text-xs rounded px-1.5 py-0.5`.
"Start Precenting" button: `bg-amber-100 text-amber-900 hover:bg-amber-200 border border-amber-300`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Batch reorder API route</name>
  <files>src/app/api/precent/[id]/reorder/route.ts</files>
  <read_first>
    - src/app/api/dev/melisma-save/route.ts (transaction + await params + validation)
    - src/app/api/precent/[id]/reorder/route.test.ts (RED stub from plan 01 — make it pass)
    - src/db/schema.ts (setItems)
  </read_first>
  <behavior>
    - POST with ids not an array → 400
    - POST with ids containing a non-number → 400
    - POST valid ids → updates position = index for each id, scoped to setId, returns { ok: true }
  </behavior>
  <action>
Create `src/app/api/precent/[id]/reorder/route.ts` with `export const dynamic = 'force-dynamic'`:

```typescript
export async function POST(req: Request, { params }: { params: Promise<{ id: string }> }) {
  const { id } = await params
  const setId = parseInt(id)
  if (isNaN(setId)) return NextResponse.json({ error: 'invalid set id' }, { status: 400 })

  let body: { ids?: unknown }
  try { body = await req.json() } catch { return NextResponse.json({ error: 'invalid JSON body' }, { status: 400 }) }

  const ids = body.ids
  if (!Array.isArray(ids) || !ids.every((n) => typeof n === 'number')) {
    return NextResponse.json({ error: 'ids must be number[]' }, { status: 400 })
  }

  await db.transaction(async (tx) => {
    for (let i = 0; i < ids.length; i++) {
      await tx.update(setItems).set({ position: i })
        .where(and(eq(setItems.id, ids[i] as number), eq(setItems.setId, setId)))   // T-05-03: set-scoped
    }
  })
  return NextResponse.json({ ok: true })
}
```

Imports: `NextResponse` from 'next/server'; `db` from '@/db'; `setItems` from '@/db/schema'; `eq, and` from 'drizzle-orm'. The `setId` scope on each update (T-05-03) prevents reordering items from a different set via crafted ids.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/precent/[id]/reorder/route.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/precent/[id]/reorder/route.ts exports POST
    - contains "Array.isArray(ids)" and "typeof n === 'number'" and "status: 400"
    - contains "db.transaction" and "set({ position: i })"
    - contains "and(eq(setItems.id" and "eq(setItems.setId" (set-scoped)
    - `npx vitest run src/app/api/precent/[id]/reorder/route.test.ts` exits 0
  </acceptance_criteria>
  <done>reorder/route.test.ts GREEN; batch position update is transactional and set-scoped.</done>
</task>

<task type="auto">
  <name>Task 2: Set detail RSC page + loading + SetDetail shell</name>
  <files>src/app/precent/[id]/page.tsx, src/app/precent/[id]/loading.tsx, src/components/precent/SetDetail.tsx</files>
  <read_first>
    - src/app/explore/topics/[slug]/page.tsx (dynamic RSC + notFound + data-to-client)
    - src/app/psalms/loading.tsx (skeleton)
    - src/components/precent/PsalmPickerModal.tsx + TunePickerModal.tsx (plan 03 — props)
    - src/db/queries/psalms.ts (fetchPsalmListRows) + src/db/queries/tunes.ts (fetchAllTunes)
    - .planning/phases/05-precentor-portal/05-UI-SPEC.md (/precent/[id] Layout Contract + Color + Copywriting)
  </read_first>
  <action>
**`src/app/precent/[id]/page.tsx`** (RSC, `export const dynamic = 'force-dynamic'`):
- `const { id } = await params; const setId = parseInt(id); if (isNaN(setId)) notFound()`.
- Fetch the set with items ordered by position:
  ```typescript
  const set = await db.query.precentingSets.findFirst({
    where: eq(precentingSets.id, setId),
    with: { setItems: { orderBy: [asc(setItems.position)],
      with: { psalm: { columns: { id: true, bibleTitle: true } },
              tune: { columns: { id: true, name: true, meter: true } } } } },
  })
  if (!set) notFound()
  ```
- `const psalmListRows = await fetchPsalmListRows()` (provides psalm meter per row) and `const allTunes = await fetchAllTunes()` (for the tune picker). Build a `psalmMeterById: Record<number, string | null>` from psalmListRows for mismatch detection and pass it down.
- Render `<SetDetail set={serializableSet} psalmListRows={psalmListRows} allTunes={allTunes} psalmMeterById={psalmMeterById} />`. Map dates/timestamps to strings for client serialization.

**`src/app/precent/[id]/loading.tsx`** — container `max-w-4xl mx-auto px-4 py-6`. Set-header skeleton (two lines: `h-6 w-64` + `h-4 w-40`), `<Separator />`, then 4 row skeletons each `h-14 w-full` (matches real `h-14` rows), `space-y-2`. `Skeleton` from '@/components/ui/skeleton'.

**`src/components/precent/SetDetail.tsx`** (`'use client'`):
- Props include the serialized `set` (id, date, type, note, precentorName, setItems[]), `psalmListRows: PsalmRow[]`, `allTunes: TuneRow[]`, `psalmMeterById: Record<number, string|null>`.
- Header card: formatted date, type, note (read-only) with an Edit pencil affordance (a button that toggles inline edit fields → PATCH /api/precent/[id]; minimal inline edit is acceptable). `<Separator className="mt-6 mb-4" />`.
- Action bar (`flex justify-between`): left "Add Psalm" button (opens PsalmPickerModal), right "Start Precenting" button styled amber `bg-amber-100 text-amber-900 hover:bg-amber-200 border border-amber-300 active:scale-[0.97] transition-transform duration-75` → navigates to `/precent/[id]/sing/1` (disabled when there are zero items).
- Render `<SetItemsSortableList setId={set.id} items={set.setItems} allTunes={allTunes} psalmMeterById={psalmMeterById} />`.
- Manage modal open state. `PsalmPickerModal onAdd` → `POST /api/precent/${set.id}/items` then `router.refresh()`. `TunePickerModal onSelect` (opened per-row via a callback from the row) → `PATCH /api/precent/${set.id}/items/${itemId}` with `{ tuneId }` then `router.refresh()`. Empty-state when no items: heading "No psalms added" + body "Press 'Add Psalm' to build your service set."
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "precent/\[id\]/(page|loading)|SetDetail" || echo "no type errors in set detail page/shell"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/precent/[id]/page.tsx contains "export const dynamic = 'force-dynamic'", "await params", "notFound()", "asc(setItems.position)"
    - src/app/precent/[id]/loading.tsx contains "Skeleton" and "h-14"
    - src/components/precent/SetDetail.tsx contains "'use client'", "Add Psalm", "Start Precenting", "bg-amber-100", "PsalmPickerModal", "TunePickerModal", "No psalms added"
    - src/components/precent/SetDetail.tsx contains a fetch to "/api/precent/" + items (add) and router.refresh()
    - `npx tsc --noEmit` no NEW errors in these files
  </acceptance_criteria>
  <done>/precent/[id] server-fetches set + items + meters; SetDetail shell renders header, action bar, table, and wires both modals.</done>
</task>

<task type="auto">
  <name>Task 3: dnd-kit sortable table (SetItemsSortableList + SetItemRow) with mismatch, play, delete</name>
  <files>src/components/precent/SetItemsSortableList.tsx, src/components/precent/SetItemRow.tsx</files>
  <read_first>
    - .planning/phases/05-precentor-portal/05-RESEARCH.md (Pattern 1 dnd-kit v6 — full example)
    - .planning/phases/05-precentor-portal/05-PATTERNS.md (SetItemsSortableList/SetItemRow section + Optimistic update pattern)
    - .planning/phases/05-precentor-portal/05-UI-SPEC.md (Set detail table layout, Drag-and-drop, Delete confirmation, Meter mismatch, Click feedback)
    - src/components/ui/tooltip.tsx + src/components/ui/dialog.tsx (for tooltips + delete confirm)
  </read_first>
  <action>
**`src/components/precent/SetItemsSortableList.tsx`** (`'use client'`) — props `{ setId: number; items: SetItemView[]; allTunes: TuneRow[]; psalmMeterById: Record<number, string|null> }`. Define a local `SetItemView` type (id, psalmId, psalm {id,bibleTitle}, tuneId, tune {id,name,meter}|null, verseRange, position).
- `const [optimisticItems, setOptimisticItems] = useState(items)`. `const sensors = useSensors(useSensor(PointerSensor))`. `const [, startTransition] = useTransition()`.
- `<DndContext sensors={sensors} collisionDetection={closestCenter} onDragEnd={handleDragEnd}>` wrapping `<SortableContext items={optimisticItems.map(i => i.id)} strategy={verticalListSortingStrategy}>`. Render a `<table>` (or div grid) with column headers `# | Psalm | Verses | Meter | Tune | Actions` and one `<SetItemRow>` per item.
- `handleDragEnd(event)`: if no `over` or same id, return. Compute oldIndex/newIndex, `const reordered = arrayMove(optimisticItems, oldIndex, newIndex)`, `setOptimisticItems(reordered)`. `startTransition(async () => { const res = await fetch(`/api/precent/${setId}/reorder`, { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ ids: reordered.map(i => i.id) }) }); if (!res.ok) { setOptimisticItems(items); toast.error("Reorder didn't save. The list has been restored.") } })`.
- Delete handler: optimistically remove from `optimisticItems`, `DELETE /api/precent/${setId}/items/${itemId}`; on error restore + `toast.error("Couldn't remove the psalm. Try again.")`.

**`src/components/precent/SetItemRow.tsx`** (`'use client'`) — props `{ item: SetItemView; index: number; psalmMeter: string | null; onDelete: (itemId: number) => void; onTuneClick: (item: SetItemView) => void }`.
- `const { attributes, listeners, setNodeRef, transform, transition, isDragging } = useSortable({ id: item.id })`. `style = { transform: CSS.Transform.toString(transform), transition, opacity: isDragging ? 0.5 : 1 }`.
- `<tr ref={setNodeRef} style={style} className="h-14 ...mismatch?...">`. Mismatch = `psalmMeter && item.tune?.meter && psalmMeter.trim().toUpperCase() !== item.tune.meter.trim().toUpperCase()`. When mismatch, add row classes `bg-amber-50 dark:bg-amber-900/20 border-l-2 border-amber-400`.
- Cells:
  - Handle cell: a `<button {...listeners} {...attributes} className="p-2 touch-none cursor-grab active:cursor-grabbing" aria-label="Drag to reorder"><GripVertical className="h-4 w-4 text-muted-foreground" /></button>`. `{...listeners}` ONLY here, never on `<tr>`.
  - `#`: `text-xs text-muted-foreground tabular-nums` showing `index + 1`.
  - Psalm: link to `/psalms/${item.psalmId}` `target="_blank"` showing psalm number + `item.psalm.bibleTitle`.
  - Verses: `item.verseRange ?? 'All'`.
  - Meter: `<Badge variant="secondary">{psalmMeter ?? '—'}</Badge>`.
  - Tune: a button `onClick={() => onTuneClick(item)}` showing `item.tune?.name ?? '—'`; when mismatch, append a warning badge `<span className="bg-amber-100 text-amber-800 text-xs rounded px-1.5 py-0.5 ml-1">Meter mismatch</span>` wrapped in a shadcn `Tooltip` with content "Psalm is {psalmMeter}, tune is {item.tune?.meter}. Choose a matching tune or proceed as arranged."
  - Actions: Play icon (`PlayCircle`, `Link` to `/precent/${setId}/sing/${index + 1}`, aria-label "Start precenting from this psalm", min 44px touch target via padding, Tooltip "Start precenting from here") + Delete icon (`Trash2`, opens a confirm `Dialog` titled "Remove psalm from set?" body "This will remove [Psalm N] from the set. This cannot be undone." actions Cancel + "Remove" `variant="destructive"`; on confirm call `onDelete(item.id)`, aria-label "Remove psalm from set", Tooltip "Remove from set").

Imports for dnd-kit exactly as RESEARCH Pattern 1. Use the project's sonner `toast` (check existing import path; e.g. `import { toast } from 'sonner'`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "SetItemsSortableList|SetItemRow" || echo "no type errors in sortable list/row"; npx vitest run --reporter=dot 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/components/precent/SetItemsSortableList.tsx contains "DndContext", "SortableContext", "arrayMove", "PointerSensor", and a fetch to "/reorder"
    - src/components/precent/SetItemsSortableList.tsx contains "setOptimisticItems(items)" (revert) and "Reorder didn't save"
    - src/components/precent/SetItemRow.tsx contains "useSortable", "CSS.Transform.toString", "GripVertical" with "{...listeners}", "touch-none"
    - src/components/precent/SetItemRow.tsx contains "Meter mismatch", "bg-amber-50", "PlayCircle", "Trash2", "Remove psalm from set?"
    - SetItemRow does NOT spread "{...listeners}" on the `<tr>` element (only on the handle button)
    - `npx tsc --noEmit` no NEW errors in these files
  </acceptance_criteria>
  <done>Sortable table with handle-only drag, optimistic+revert reorder, meter-mismatch amber highlight, play link, and delete-with-confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/precent/[id]/reorder | Untrusted ids array + path setId cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | POST reorder | mitigate | Validate `ids` is `number[]`; update each position scoped to BOTH item id AND setId so foreign-set ids are no-ops; transactional |
| T-05-09 | Tampering | reorder with foreign ids | mitigate | `and(eq(setItems.id, ids[i]), eq(setItems.setId, setId))` — ids not belonging to this set update zero rows |
| T-05-10 | Injection | reorder route | mitigate | Drizzle parameterised queries; positions are loop indices (server-owned), never client-supplied position values |

V4 Access Control: unprotected by design in Phase 5 (D-10); Phase 05.1 adds protection.
</threat_model>

<verification>
- `npx vitest run src/app/api/precent/[id]/reorder/route.test.ts` exits 0
- `npx vitest run` full suite green (plans 01-04 routes all GREEN)
- /precent/[id] renders header + sortable psalm table with mismatch/play/delete
</verification>

<success_criteria>
PREC-03 + PREC-04 satisfied: set detail shows the ordered psalm table; drag-reorder persists (optimistic + revert); meter mismatches highlighted; rows support play and delete.
</success_criteria>

<output>
After completion, create `.planning/phases/05-precentor-portal/05-04-SUMMARY.md`
</output>
