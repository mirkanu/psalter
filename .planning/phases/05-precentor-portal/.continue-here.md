---
context: phase
phase: 05-precentor-portal
task: post-execution-uat-polish
status: in_progress
last_updated: "2026-06-15T13:15:17.602Z"
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| TuneTable row navigation conflict | TuneTable rows contain `<Link href="/tunes/[id]">` for the tune name — clicking fires BOTH the `onSelectTune` tr onClick AND the Link navigation, opening /tunes/[id] instead of just selecting | advisory | When onSelectTune is provided, make the tune name a `<button>` or `<span>` instead of a `<Link>`, and stopPropagation on any inner links |

<current_state>
Phase 05 (Precentor Portal) execution is complete. All 5 plans shipped and verified (9/9 must-haves, PREC-01–06 satisfied). Two rounds of UAT gap fixes (batches 1 and 2) have also been committed. The user performed manual UAT and raised a third round of polish items that have NOT yet been implemented.

The app is deployed and running at psalter.gsdlabs.dev (PM2 process "psalter", port 3005).
</current_state>

<completed_work>

**Phase 05 core execution (all shipped):**
- 05-01: Schema (precenting_sets + set_items), dnd-kit, Precent nav, RED test stubs
- 05-02: /api/precent CRUD, /precent list page, CreateSetForm
- 05-03: Item API routes, PsalmPickerModal, TunePickerModal (now uses TuneTable)
- 05-04: Set detail page, dnd-kit sortable reorder, meter-mismatch highlights
- 05-05: PrecentingBar + /precent/[id]/sing/[pos] page + Playwright specs GREEN

**UAT Batch 1 (shipped):**
- Titles: "My Psalm Sets for Precenting" and "Psalm Set for Precenting"
- Psalm numbers column in set list
- Psalm column: number only + click opens picker to change psalm
- Tune column: shows recommended tune by default (muted)
- Reactivity: router.refresh() after every mutation

**UAT Batch 2 (shipped):**
- Edit dialog for Date & Type + Delete with confirmation
- Created/Modified metadata display
- TunePickerModal now wraps TuneTable (same as /tunes page)
- Desktop modal width fixed (max-w-5xl)
- Precenting bar redesigned: thin amber label "Precenting Mode — N/total"
- PsalmTopBar arrows amber + navigate between precenting items
</completed_work>

<remaining_work>

**UAT Round 3 — NOT YET IMPLEMENTED — all items from user's last message:**

### 1. "My Precenting Sets" list page (PrecentingSetList.tsx + page.tsx)
(a) Smart date formatting: "today", "tomorrow", "in X days" for future; "10Aug2026" short format for past. Type shortened: "AM" / "PM" / "Other". Precentor column moved to last.
(b) New action columns: pencil icon (opens /precent/[id]) and amber "play" button (opens /precent/[id]/sing/1). These should NOT trigger the row click navigation — stopPropagation needed.

### 2. "Psalm Set for Precenting" detail page (SetDetail.tsx + SetItemRow.tsx + page.tsx)
(a) Remove # (position number) column
(b) Change meter-mismatch error highlight from yellow/amber to RED
(c) Replace "Psalm Set for Precenting" h1 with a breadcrumb "Set List > Psalm Set" (similar to /explore breadcrumbs) and make the formatted date the page heading again.

### 3. Tune selector modal (TunePickerModal.tsx + TuneTable.tsx)
(a) Hide "Listen to all the tune recordings..." SoundCloud link text (line ~186 in TuneTable.tsx — hide when hideExport or in picker mode)
(b) Pre-select psalm meter in the meter filter when modal opens (add `initialMeter` prop to TuneTable, set from psalmMeter in TunePickerModal)
(c) When a non-recommended tune is currently selected and modal re-opens: show "Recommended tune: [name] [Select button]" at top above search bar. TunePickerModal needs to receive `recommendedTuneName` and `currentTuneId` props; SetDetail must pass them.
(d) Mobile: do NOT auto-focus search input on modal open (iOS keyboard), do NOT open filters collapsible on mobile. Add `noAutoFocus?: boolean` prop to TuneTable; default filters collapsed on mobile.
(e) BUG: selecting a tune fires onSelectTune BUT ALSO navigates to /tunes/[id] because the tune name is a `<Link>`. Fix: when `onSelectTune` is provided, render tune name as `<button>` or `<span>` (not Link), or call e.preventDefault()/stopPropagation() on the Link click.

### 4. Precenting sing view (PrecentingBar.tsx)
Three-column layout for the amber bar:
- Left: "← Back to List" link → /precent/[setId]
- Center: "Precenting Mode"
- Right: "2/3" counter

PrecentingBar needs `setId` prop re-added (removed in batch 2) and the three-column flex layout. Also update sing/[pos]/page.tsx to pass setId back.
</remaining_work>

<decisions_made>

- TunePickerModal now wraps TuneTable (not TuneGrid) — decided in UAT batch 2. Do not revert.
- PrecentingBar `setId` prop was REMOVED in batch 2 (arrows moved to PsalmTopBar). Round 3 adds it BACK for the "Back to List" link only.
- Meter-mismatch highlight: currently amber. User wants RED — change `bg-amber-*` classes on mismatch rows/badges in SetItemRow.tsx to `bg-red-*`/`text-red-*`.
- PsalmTopBar arrow overrides (`precentingPrevHref`, `precentingNextHref`) are already wired and working — do not change that logic.
- Auth is intentionally deferred to Phase 05.1 (separate phase directory exists: .planning/phases/05.1-auth-gate/). Do not add auth in Round 3 fixes.
</decisions_made>

<blockers>
- None blocking deployment. All UAT Round 3 items are UI polish only.
</blockers>

## Required Reading (in order)
1. `src/components/TuneTable.tsx` — understand the onSelectTune, hideExport props added in batch 2; the SoundCloud link at ~line 186; the meter filter state; the tune name Link at ~line 467
2. `src/components/precent/TunePickerModal.tsx` — current interface (tunes, onClose, psalmMeter, onSelect); needs recommendedTuneName + currentTuneId props added
3. `src/components/precent/SetDetail.tsx` — how items are rendered; where tunePickerOpen/tunePickerItemId state is; what gets passed to TunePickerModal
4. `src/components/precent/SetItemRow.tsx` — meter mismatch styling (look for amber classes); # column location
5. `src/components/precent/PrecentingSetList.tsx` — current column layout for items 1a/1b
6. `src/components/precent/PrecentingBar.tsx` — current thin label implementation (no setId); needs 3-column layout + setId back
7. `src/app/precent/[id]/sing/[pos]/page.tsx` — currently passes no setId to PrecentingBar; needs setId added back

## Infrastructure State
- App: psalter.gsdlabs.dev — PM2 process "psalter" (id 3), online
- DB: psalter-db Docker container, PostgreSQL 16
- Build: clean (all TypeScript errors fixed in UAT batch 2)
- Git: on master branch, pushed to origin

<context>
Paused mid-UAT-polish cycle. Phase 05 core is done and verified. We are in a third round of user-reported UAT fixes. The user is actively testing at psalter.gsdlabs.dev and providing precise feedback. All Round 3 items are well-specified — no ambiguity. The tune modal fix (3e) is the most important: selecting a tune currently navigates to /tunes/[id] which is a regression introduced by the TuneTable reuse in batch 2.
</context>

<next_action>
Start with: Read the 7 files listed in Required Reading, then implement UAT Round 3 items in this order:
1. Fix tune selection navigation bug (3e) first — it's a regression that blocks basic usage
2. Then TuneTable modal improvements (3a, 3b, 3c, 3d)
3. Then set list changes (1a, 1b)
4. Then set detail changes (2a, 2b, 2c)
5. Then precenting bar 3-column layout (4)
Deploy with `npm run build && pm2 restart psalter` after each batch. Push with `git push origin master`.
</next_action>
