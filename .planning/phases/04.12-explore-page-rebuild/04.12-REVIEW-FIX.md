---
phase: 04.12-explore-page-rebuild
fixed_at: 2026-06-14T00:00:00Z
review_path: .planning/phases/04.12-explore-page-rebuild/04.12-REVIEW.md
iteration: 1
findings_in_scope: 9
fixed: 8
skipped: 1
status: partial
---

# Phase 04.12: Code Review Fix Report

**Fixed at:** 2026-06-14
**Source review:** `.planning/phases/04.12-explore-page-rebuild/04.12-REVIEW.md`
**Iteration:** 1

**Summary:**
- Findings in scope: 9 (CR-01 through CR-04, WR-01 through WR-05)
- Fixed: 8
- Skipped: 1 (WR-04 — already resolved by current test state)

---

## Fixed Issues

### CR-01: Multi-topic Nave's entries silently discarded

**Files modified:** `src/db/schema.ts`, `scripts/migrate-explore-04-12.ts`
**Commit:** `b8274d2`
**Applied fix:** Added `unique` import to schema. Changed `navesTopicEntries` from a single `.unique()` on `airtableId` to a compound `unique('uq_entry_topic').on(airtableId, navesTopicId)` table constraint (dropping the column-level unique). Updated migration `onConflictDoUpdate` target from the single `airtableId` column to the array `[airtableId, navesTopicId]`. A DB migration is required to drop the old constraint and add the new one before re-running the migration script.

---

### CR-02: HeidelbergCatechism crashes if `url` is NULL

**Files modified:** `src/components/HeidelbergCatechism.tsx`, `src/db/queries/explore.ts`
**Commit:** `14e5b2e` (combined with WR-02)
**Applied fix:** `CatechismRow.url` typed `string | null`. The `href` attribute on line 54 now guards with a ternary: `row.url ? (row.url.startsWith('http') ? row.url : 'https://'+row.url) : '#'`. The query return type updated to `url: string | null` too.

---

### CR-03: MessianicByTopic produces unambiguous slugs that may 404

**Files modified:** `src/components/MessianicByTopic.tsx`, `src/app/explore/page.tsx`
**Commit:** `1fa7c60`
**Applied fix:** `MessianicTopic` interface now requires a `slug: string` field. The local `slugify` function is removed from the component. `explore/page.tsx` maps `messianicByTopic` through `navesSlugMap` (the already-built disambiguated map) before passing to `<MessianicByTopic>`, so every link uses the same route that `buildNavesSlugMap` would resolve.

---

### CR-04: fetchNavesTopicsWithCounts queried old junction table

**Files modified:** `src/db/queries/explore.ts`
**Commit:** `adc6b9e`
**Applied fix:** `fetchNavesTopicsWithCounts` now joins through `naves_topic_entries` → `verse_naves_topic_entries` → `verses`, matching exactly what `fetchNavesSubTopics` uses on the detail page. Badge counts on the Explore grid and psalm list on the detail page now draw from the same data source.

---

### WR-01: AuthorsTable sort direction contradicts "earliest date" label

**Files modified:** `src/components/AuthorsTable.tsx`
**Commit:** `b57b27b`
**Applied fix:** Comparator changed from `a.dateBC - b.dateBC` to `b.dateBC - a.dateBC`. Larger BC number = earlier in history. Moses (~1406 BC) now sorts to the top as the label promises.

---

### WR-02: fetchHeidelbergCatechism groups by (question_number, url)

**Files modified:** `src/db/queries/explore.ts`
**Commit:** `14e5b2e` (combined with CR-02)
**Applied fix:** SQL now groups only by `cr.question_number` and selects `MIN(cr.url) AS url`. Eliminates duplicate rows and duplicate React `key={row.question_number}` warnings if the same question has two URL variants in the DB.

---

### WR-03: Migration silently drops multi-verse creedal references

**Files modified:** `scripts/migrate-explore-04-12.ts`
**Commit:** `66c68c7`
**Applied fix:** Added `console.warn` before taking `linkedVerses[0]` when `linkedVerses.length > 1`. The schema limitation (single `verse_id`) is unchanged; the warn makes the data loss explicit on each migration run.

---

### WR-05 + IN-01: slugify / buildNavesSlugMap duplicated across two files

**Files modified:** `src/lib/naves-slugs.ts` (new), `src/app/explore/page.tsx`, `src/app/explore/naves/[slug]/page.tsx`
**Commit:** `fed1c23`
**Applied fix:** Created `src/lib/naves-slugs.ts` exporting `slugify` and `buildNavesSlugMap`. Both page files now import from this shared module. Local duplicate function definitions removed from both files. `explore/page.tsx` uses `buildNavesSlugMap` via an alias `buildDisambiguatedSlugMap` for the non-Nave's topic slug maps (same algorithm, different name for clarity at the call sites).

---

## Skipped Issues

### WR-04: E2E test mobile column-hiding assertion is a false positive

**File:** `tests/e2e/explore-rebuild.spec.ts:222-234`
**Reason:** The current test (as it exists in the repo after the Phase 04.12 UAT fix in commit `d516e3f`) already asserts that the "Date B.C." column is *visible* at 375px with the comment "They use hidden sm:table-cell — All columns remain visible at 375px — table scrolls horizontally (overflow-x-auto)". The test checks `isVisible()` and passes when the element is visible in the DOM. `AuthorsTable.tsx` has no `hidden sm:table-cell` classes (all columns always render), so the table scrolls horizontally at 375px as intended. The false-positive path described in the review (where the test fell through to an else-branch treating missing DOM element as a pass) is no longer present in the current test code. No change required.

---

_Fixed: 2026-06-14_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
