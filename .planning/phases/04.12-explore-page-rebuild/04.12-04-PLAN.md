---
phase: 04.12-explore-page-rebuild
plan: 04
type: execute
wave: 3
depends_on: ["04.12-01", "04.12-02"]
files_modified:
  - src/components/AuthorsTable.tsx
  - src/app/explore/naves/[slug]/page.tsx
autonomous: true
requirements: [EXPLORE-05, EXPLORE-06]
must_haves:
  truths:
    - "AuthorsTable shows all 150 psalms in a sortable table, filterable by 8 author pills"
    - "AuthorsTable sorts by date_bc ascending with nulls last; rows are clickable to /psalms/[id]"
    - "On mobile the Date B.C. and Occasion columns are hidden"
    - "/explore/naves/[slug] shows sub-topic groups with quotation text instead of a flat psalm list"
  artifacts:
    - path: "src/components/AuthorsTable.tsx"
      provides: "Section 5 filterable authors table"
      contains: "useState"
    - path: "src/app/explore/naves/[slug]/page.tsx"
      provides: "Rebuilt Nave's topic detail page with sub-topic drill-down"
      contains: "fetchNavesSubTopics"
  key_links:
    - from: "src/app/explore/naves/[slug]/page.tsx"
      to: "fetchNavesSubTopics"
      via: "import from @/db/queries/explore"
      pattern: "fetchNavesSubTopics"
    - from: "src/components/AuthorsTable.tsx"
      to: "/psalms/[id]"
      via: "router.push on row click"
      pattern: "router.push"
---

<objective>
Build Section 5 (the filterable Authors table) and rebuild the `/explore/naves/[slug]` detail route to show sub-topic groups (drill-down) instead of a flat psalm list. These two artifacts touch independent files from Plan 03, so the two UI plans run in parallel.

Purpose: AuthorsTable is the most complex new client component (filter + sort state). The naves detail rebuild is what makes Section 4 ("Other Topics") drill-down work — clicking a Nave's topic now lands on a sub-topic-grouped page powered by the new naves_topic_entries data.
Output: 1 new client component + 1 rebuilt route page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.12-explore-page-rebuild/04.12-UI-SPEC.md
@.planning/phases/04.12-explore-page-rebuild/04.12-PATTERNS.md

<interfaces>
<!-- Prop contract + query signatures (from Plan 02). -->

AuthorsTable props (from fetchPsalmsWithAuthorData):
  interface PsalmWithAuthorData { id: number; author: string | null; dateBC: number | null; occasion: string | null }
  export function AuthorsTable({ psalms }: { psalms: PsalmWithAuthorData[] })

fetchNavesSubTopics(topicId: number) returns:
  Array<{ id: number; sub_topic: string | null; quotation: string | null;
    verses: Array<{ psalmId: number; verseNumber: number | null }> }>

AUTHORS list (8): Asaph, David, Ethan the Ezrahite, Heman the Ezrahite, Moses, Solomon, Sons of Korah, Unknown
AUTHOR_COLORS map (PATTERNS.md lines 478–487): David bg-blue-100 text-blue-800; Asaph bg-purple-100 text-purple-800;
  Sons of Korah bg-green-100 text-green-800; Moses bg-amber-100 text-amber-800; Solomon bg-yellow-100 text-yellow-800;
  Ethan the Ezrahite bg-rose-100 text-rose-800; Heman the Ezrahite bg-orange-100 text-orange-800;
  Unknown bg-muted text-muted-foreground

Existing /explore/naves/[slug]/page.tsx already has: export const dynamic = 'force-dynamic', buildNavesSlugMap,
  generateStaticParams, generateMetadata, breadcrumb. It currently imports fetchPsalmDetailsByNavesTopic — REPLACE that
  consumption with fetchNavesSubTopics. Keep slug resolution and breadcrumb unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuthorsTable client component</name>
  <read_first>
    - src/components/NavesExpand.tsx (analog — 'use client', useState filter, pill toggle button styling)
    - .planning/phases/04.12-explore-page-rebuild/04.12-PATTERNS.md (section "src/components/AuthorsTable.tsx" — full table JSX, AUTHOR_COLORS, filter button pattern, nulls-last sort)
    - .planning/phases/04.12-explore-page-rebuild/04.12-UI-SPEC.md (Section 5 — columns, widths, responsive rules, attribution copy)
  </read_first>
  <action>
Create `src/components/AuthorsTable.tsx` — `'use client'`. Imports: `useState` (react), `useRouter` (next/navigation).

- Define `const AUTHORS = ['Asaph','David','Ethan the Ezrahite','Heman the Ezrahite','Moses','Solomon','Sons of Korah','Unknown']` and the `AUTHOR_COLORS` map exactly as listed in the interfaces block.
- State: `const [selectedAuthor, setSelectedAuthor] = useState<string | null>(null)`; `const router = useRouter()`.
- `const filtered = selectedAuthor ? psalms.filter(p => (p.author ?? 'Unknown') === selectedAuthor) : psalms`
- Sort (nulls last): `const sorted = [...filtered].sort((a,b) => { if (a.dateBC === null) return 1; if (b.dateBC === null) return -1; return a.dateBC - b.dateBC })`
- Attribution line ABOVE the filter row: `<p className="text-sm text-muted-foreground mb-4">Sorted from earliest date; some data is conjecture. Source: blueletterbible.org</p>`
- Filter pills: `<div className="flex flex-wrap gap-2 mb-4">` mapping AUTHORS to `<button onClick={() => setSelectedAuthor(selectedAuthor === author ? null : author)} className={selectedAuthor === author ? "bg-primary text-primary-foreground rounded-full px-3 py-1 text-sm" : "border border-border rounded-full px-3 py-1 text-sm bg-background hover:bg-muted"}>{author}</button>`
- Table (use the exact wrapper from PATTERNS.md table block): `<div className="overflow-x-auto rounded-md border border-border"><table className="w-full text-sm">` with thead columns: Psalm (w-20 text-right), Author (w-40), Date B.C. (`hidden sm:table-cell`), Occasion (`hidden sm:table-cell`).
- Each row: `<tr key={p.id} onClick={() => router.push(`/psalms/${p.id}`)} className="cursor-pointer hover:bg-muted transition-colors border-b border-border last:border-0">` with cells:
  - Psalm #: `font-mono text-sm` right-aligned `{p.id}`
  - Author pill: `<span className={`px-2 py-0.5 rounded text-xs ${AUTHOR_COLORS[p.author ?? 'Unknown'] ?? 'bg-muted text-muted-foreground'}`}>{p.author ?? 'Unknown'}</span>`
  - Date B.C. (`hidden sm:table-cell text-muted-foreground`): `{p.dateBC != null ? `~${p.dateBC} B.C.` : 'Unknown'}`
  - Occasion (`hidden sm:table-cell text-muted-foreground truncate max-w-[300px]` with `title={p.occasion ?? ''}`): `{p.occasion ? p.occasion.slice(0,60) + (p.occasion.length > 60 ? '…' : '') : ''}`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "AuthorsTable" || echo "AUTHORSTABLE TYPECHECK CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'use client'" src/components/AuthorsTable.tsx` returns 1
    - `grep -c "useState<string | null>(null)" src/components/AuthorsTable.tsx` returns 1
    - `grep -c "router.push" src/components/AuthorsTable.tsx` is >= 1
    - `grep -c "hidden sm:table-cell" src/components/AuthorsTable.tsx` is >= 2 (Date B.C. + Occasion hidden on mobile)
    - `grep -c "blueletterbible.org" src/components/AuthorsTable.tsx` returns 1
    - all 8 authors present: `grep -oE "Asaph|Ethan the Ezrahite|Heman the Ezrahite|Sons of Korah" src/components/AuthorsTable.tsx | sort -u | wc -l` returns 4
    - `grep -c "if (a.dateBC === null) return 1" src/components/AuthorsTable.tsx` returns 1 (nulls-last sort)
    - `npx tsc --noEmit` reports no errors in AuthorsTable.tsx
  </acceptance_criteria>
  <done>AuthorsTable compiles, filters by 8 author pills, sorts date_bc asc nulls-last, hides 2 columns on mobile, rows navigate to /psalms/[id].</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild /explore/naves/[slug] with sub-topic drill-down</name>
  <read_first>
    - src/app/explore/naves/[slug]/page.tsx (read FULLY — keep export const dynamic, buildNavesSlugMap, generateStaticParams, generateMetadata, breadcrumb; replace the body that renders the flat psalm list)
    - src/db/queries/explore.ts (confirm fetchNavesSubTopics signature added in Plan 02)
    - .planning/phases/04.12-explore-page-rebuild/04.12-PATTERNS.md (section "src/app/explore/naves/[slug]/page.tsx" — sub-topic group render + h1 font-semibold fix)
    - .planning/phases/04.12-explore-page-rebuild/04.12-UI-SPEC.md (Section 4 detail-page contract)
  </read_first>
  <action>
Rebuild `src/app/explore/naves/[slug]/page.tsx`:

1. Change the import: replace `fetchPsalmDetailsByNavesTopic` (or whichever flat-list query it uses) with `fetchNavesSubTopics` from `@/db/queries/explore`. Keep `fetchNavesTopicsWithCounts` if it is used for slug resolution.
2. Resolve the topic id from the slug exactly as the existing file does (buildNavesSlugMap + lookup); call `const entries = await fetchNavesSubTopics(topicId)`.
3. Fix the h1: it currently uses `font-bold`; change to `font-semibold` (UI-SPEC typography rule — h1 MUST be font-semibold). Keep `text-3xl md:text-4xl`.
4. Replace the flat psalm list render with sub-topic groups:
   ```tsx
   {entries.map((entry) => (
     <div key={entry.id} className="mb-4">
       <h2 className="text-xl font-semibold mb-2">{entry.sub_topic ?? 'General'}</h2>
       {entry.verses.map((v) => (
         <div key={`${v.psalmId}-${v.verseNumber}`} className="mb-1">
           <Link href={`/psalms/${v.psalmId}`} className="text-sm font-semibold hover:text-primary">
             Psalm {v.psalmId}{v.verseNumber != null ? `:${v.verseNumber}` : ''}
           </Link>
           {entry.quotation && (
             <p className="text-sm text-muted-foreground italic ml-4">{entry.quotation}</p>
           )}
         </div>
       ))}
     </div>
   ))}
   ```
5. Keep the breadcrumb block (Explore › Nave's Topics) unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "naves/\[slug\]" || echo "NAVES DETAIL TYPECHECK CLEAN"; npx next build 2>&1 | grep -iE "explore/naves" | head -3 || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fetchNavesSubTopics" "src/app/explore/naves/[slug]/page.tsx"` is >= 1
    - `grep -c "fetchPsalmDetailsByNavesTopic" "src/app/explore/naves/[slug]/page.tsx"` returns 0 (old flat query removed)
    - `grep -c "font-bold" "src/app/explore/naves/[slug]/page.tsx"` returns 0 (h1 fixed to semibold)
    - `grep -c "entry.sub_topic ?? 'General'" "src/app/explore/naves/[slug]/page.tsx"` returns 1
    - `grep -c "export const dynamic = 'force-dynamic'" "src/app/explore/naves/[slug]/page.tsx"` returns 1 (kept)
    - `npx tsc --noEmit` reports no errors in the file
  </acceptance_criteria>
  <done>The naves detail route renders sub-topic groups with quotation text, uses font-semibold h1, keeps slug resolution + breadcrumb + force-dynamic, and no longer references the flat-list query.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| [slug] route param → query | URL slug resolved to a topic id before querying |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.12-08 | Injection | naves/[slug] slug param | mitigate | Slug resolved via buildNavesSlugMap to a known integer id before fetchNavesSubTopics; unknown slug → notFound(); no raw slug in SQL |
| T-04.12-09 | Tampering | AuthorsTable router.push target | accept | psalmId is a typed number from server data; client navigation only |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for both files
- naves detail route uses sub-topic drill-down + font-semibold h1
</verification>

<success_criteria>
AuthorsTable and the rebuilt naves detail route exist and compile; Plan 05 imports AuthorsTable into page.tsx; the detail route is independently navigable.
</success_criteria>

<output>
After completion, create `.planning/phases/04.12-explore-page-rebuild/04.12-04-SUMMARY.md`
</output>
