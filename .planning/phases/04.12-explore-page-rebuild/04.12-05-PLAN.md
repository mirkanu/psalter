---
phase: 04.12-explore-page-rebuild
plan: 05
type: execute
wave: 4
depends_on: ["04.12-02", "04.12-03", "04.12-04"]
files_modified:
  - src/app/explore/page.tsx
  - src/app/explore/loading.tsx
  - tests/e2e/explore-rebuild.spec.ts
autonomous: false
requirements: [EXPLORE-02, EXPLORE-03, EXPLORE-04, EXPLORE-05, EXPLORE-06]
must_haves:
  truths:
    - "/explore renders all 6 sections in order with anchor IDs and separators"
    - "Section 2 By Theme is split into Main Topic / Mood / Song Type sub-groups"
    - "Section 3 shows Quoted-in-NT collapsibles and Messianic-by-Topic grid"
    - "Section 5 shows the filterable AuthorsTable; Section 6 shows the catechism collapsibles"
    - "ExploreAnchorNav is sticky and tracks the active section"
    - "loading.tsx shows a 6-section skeleton instantly"
  artifacts:
    - path: "src/app/explore/page.tsx"
      provides: "Composed 6-section explore page"
      contains: "ExploreAnchorNav"
    - path: "src/app/explore/loading.tsx"
      provides: "6-section skeleton"
    - path: "tests/e2e/explore-rebuild.spec.ts"
      provides: "Playwright UAT for all 6 sections"
  key_links:
    - from: "src/app/explore/page.tsx"
      to: "fetchQuotedInNT / fetchMessianicByTopic / fetchPsalmsWithAuthorData / fetchHeidelbergCatechism / fetchTopicsByType"
      via: "Promise.all data fetch"
      pattern: "Promise.all"
    - from: "src/app/explore/page.tsx"
      to: "AuthorsTable / QuotedInNT / MessianicByTopic / HeidelbergCatechism / ExploreAnchorNav"
      via: "component import + render"
      pattern: "ExploreAnchorNav"
---

<objective>
Compose the rebuilt `/explore` page: fetch all section data in parallel, render the 6 sections with anchor IDs and separators, wire in the four Plan-03 components and the Plan-04 AuthorsTable, update the loading skeleton to a 6-section shape, and verify the live deployment with a Playwright UAT plus a human visual sign-off.

Purpose: This is the integration plan — it is the only plan that owns `page.tsx`, so it runs last after all leaf components and queries exist. It proves the phase goal end to end.
Output: Rebuilt page.tsx, updated loading.tsx, a Playwright spec, and a human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.12-explore-page-rebuild/04.12-UI-SPEC.md
@.planning/phases/04.12-explore-page-rebuild/04.12-PATTERNS.md

<interfaces>
<!-- Query + component contracts from Plans 02/03/04. -->

From @/db/queries/explore (Plan 02):
  fetchWhenYouTopics() -> existing
  fetchTopicsByType('Main Topic'|'Mood'|'Song Type') -> {id,name,count}[]
  fetchQuotedInNT() -> NTEntry[]
  fetchMessianicByTopic() -> {id,name,messianic}[]
  fetchNavesTopicsWithCounts() -> existing (Section 4 NavesExpand data)
  fetchPsalmsWithAuthorData() -> {id,author,dateBC,occasion}[]
  fetchHeidelbergCatechism() -> {question_number,url,verses[]}[]

Components:
  <ExploreAnchorNav /> (client, no props)  — src/components/ExploreAnchorNav.tsx
  <QuotedInNT entries={...} /> (client)     — src/components/QuotedInNT.tsx
  <MessianicByTopic topics={...} /> (server)— src/components/MessianicByTopic.tsx
  <AuthorsTable psalms={...} /> (client)    — src/components/AuthorsTable.tsx
  <HeidelbergCatechism rows={...} /> (client)— src/components/HeidelbergCatechism.tsx
  <NavesExpand ... /> (existing, Section 4) — unchanged

Existing page.tsx (read fully) already has: export const dynamic = 'force-dynamic', slugify, buildDisambiguatedSlugMap,
  the page wrapper (max-w-7xl ...), h1 (font-semibold), topic pill grid, Separator usage. Reuse all of these.

Section order + anchor ids (UI-SPEC): when-you, by-theme, in-the-nt, other-topics, authors, catechism.
Copy strings: see UI-SPEC "Copywriting Contract" (headings, attribution, "97 references across 35 questions", etc.).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild explore/page.tsx with all 6 sections + update loading.tsx</name>
  <read_first>
    - src/app/explore/page.tsx (read FULLY — reuse dynamic export, slugify, buildDisambiguatedSlugMap, page wrapper, h1, topic pill grid, Separator)
    - src/app/explore/loading.tsx (read FULLY — extend the existing bg-muted animate-pulse skeleton to 6 sections)
    - .planning/phases/04.12-explore-page-rebuild/04.12-PATTERNS.md (section "src/app/explore/page.tsx" — Promise.all fetch, section+anchor pattern, pill grid; section "src/app/explore/loading.tsx")
    - .planning/phases/04.12-explore-page-rebuild/04.12-UI-SPEC.md (Section-by-Section contracts + Page-Level Layout + Copywriting + Loading/Skeleton)
  </read_first>
  <action>
Rebuild `src/app/explore/page.tsx` (keep `export const dynamic = 'force-dynamic'` — do NOT add generateStaticParams, RESEARCH pitfall 5):

1. Import the 6 query functions and the 4 new components (+ existing NavesExpand and Separator).
2. Fetch all section data in parallel at the top:
   ```tsx
   const [whenYouTopics, mainTopics, moodTopics, songTopics, quotedInNT,
          messianicTopics, navesTopics, psalmsWithAuthors, catechism] = await Promise.all([
     fetchWhenYouTopics(),
     fetchTopicsByType('Main Topic'),
     fetchTopicsByType('Mood'),
     fetchTopicsByType('Song Type'),
     fetchQuotedInNT(),
     fetchMessianicByTopic(),
     fetchNavesTopicsWithCounts(),
     fetchPsalmsWithAuthorData(),
     fetchHeidelbergCatechism(),
   ])
   ```
3. Render `<ExploreAnchorNav />` directly below the h1 block (it is sticky, so it sits in the flow).
4. Render 6 `<section id="...">` blocks separated by `<Separator className="my-8" />`, in this order with these headings (h2 = `text-xl font-semibold`):
   - `id="when-you"` — "When you're feeling..." — existing pill grid over whenYouTopics (reuse current render + slug map).
   - `id="by-theme"` — "By Theme" — three sub-groups, each an h3 `text-sm font-semibold uppercase tracking-wide mb-2 mt-4` ("Main Topic", "Mood", "Song Type") followed by the pill grid over mainTopics/moodTopics/songTopics. Link target `/explore/topics/[slug]`.
   - `id="in-the-nt"` — "In the New Testament" — h3 "Quoted in the New Testament" then `<QuotedInNT entries={quotedInNT} />`; a `<Separator className="my-4" />`; h3 "Messianic by Topic" then `<MessianicByTopic topics={messianicTopics} />`; plus the retained full-width "All Messianic Psalms" link card to `/explore/messianic` (PATTERNS.md lines 664–673).
   - `id="other-topics"` — "Other Topics" — existing `<NavesExpand>` over navesTopics (unchanged).
   - `id="authors"` — "Authors" — `<AuthorsTable psalms={psalmsWithAuthors} />`.
   - `id="catechism"` — "Heidelberg Catechism" — subtitle `<p className="text-sm text-muted-foreground mb-4">97 references across 35 questions</p>` then `<HeidelbergCatechism rows={catechism} />`.

5. Update `src/app/explore/loading.tsx`: extend the existing `bg-muted animate-pulse` skeleton to 6 section blocks. For When-you/By-theme use the 4-col grid of 8 `h-11` pill skeletons; for In-the-NT and Catechism use 4–5 `h-11` row skeletons; for Authors add 8 pill skeletons + 10 table-row `h-8` skeletons. Page shell (h1 width block) renders first.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -iE "explore/page|explore/loading" || echo "PAGE+LOADING TYPECHECK CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - all 6 anchor ids present: `grep -oE 'id="(when-you|by-theme|in-the-nt|other-topics|authors|catechism)"' src/app/explore/page.tsx | sort -u | wc -l` returns 6
    - `grep -c "Promise.all" src/app/explore/page.tsx` returns 1
    - `grep -c "ExploreAnchorNav" src/app/explore/page.tsx` is >= 1
    - `grep -c "<AuthorsTable" src/app/explore/page.tsx` is >= 1
    - `grep -c "<QuotedInNT" src/app/explore/page.tsx` is >= 1
    - `grep -c "<HeidelbergCatechism" src/app/explore/page.tsx` is >= 1
    - `grep -c "fetchTopicsByType('Mood')" src/app/explore/page.tsx` returns 1
    - `grep -c "97 references across 35 questions" src/app/explore/page.tsx` returns 1
    - `grep -c "generateStaticParams" src/app/explore/page.tsx` returns 0
    - `grep -c "font-bold" src/app/explore/page.tsx` returns 0 (h1/h2 use font-semibold)
    - `grep -c "animate-pulse" src/app/explore/loading.tsx` is >= 1
    - `npx tsc --noEmit` reports no errors in either file
  </acceptance_criteria>
  <done>page.tsx renders all 6 sections in order with anchor ids, parallel fetch, all 4 new components + NavesExpand wired; loading.tsx is a 6-section skeleton; build types clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build, deploy, and Playwright UAT against live deployment</name>
  <read_first>
    - tests/e2e/ (read an existing *.spec.ts to match the Playwright harness conventions used in this project)
    - /data/home/psalter/CLAUDE.md (Playwright section — executablePath, --no-sandbox, NODE_PATH; daemon at localhost:3099 per global CLAUDE.md is preferred)
    - .planning/phases/04.12-explore-page-rebuild/04.12-UI-SPEC.md (section contracts to assert)
  </read_first>
  <action>
1. Build and restart the deployment: `npm run build` then `pm2 restart psalter` (port 3005, served at psalter.gsdlabs.dev). Confirm build succeeds with no type errors.

2. Create `tests/e2e/explore-rebuild.spec.ts` (or a Node Playwright script following the project's CLAUDE.md harness — use the shared playwright-daemon at http://localhost:3099 if available, else the local executablePath pattern). Navigate to `https://psalter.gsdlabs.dev/explore` and assert:
   - All 6 `section[id=...]` are present (when-you, by-theme, in-the-nt, other-topics, authors, catechism).
   - Section "by-theme" contains three h3 sub-group headings: "Main Topic", "Mood", "Song Type".
   - Section "in-the-nt" contains the text "Messianic by Topic" and at least one collapsible row that expands on click to reveal a `a[href^="/psalms/"]` link.
   - Section "authors" contains a `<table>` with at least 150 data rows when no filter is applied, and clicking the "David" filter pill reduces the row count.
   - Section "catechism" contains at least one collapsible row; expanding it reveals an external link whose href starts with `https://relight.app` (assert the https:// prefix — RESEARCH pitfall 4).
   - The sticky `nav` anchor bar is present with 6 links.
   - Navigate to a `/explore/naves/[slug]` page (pick any topic link) and assert sub-topic `h2` groups render with `a[href^="/psalms/"]` links.

3. Run the spec to GREEN. Report the actual row counts observed.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npx playwright test tests/e2e/explore-rebuild.spec.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (no type errors)
    - `tests/e2e/explore-rebuild.spec.ts` exists and runs to GREEN
    - The spec asserts all 6 section ids, the 3 by-theme sub-groups, the authors table row count, the David-filter reduction, and the catechism https:// external link
    - Playwright run output shows 0 failed assertions
  </acceptance_criteria>
  <done>Live /explore at psalter.gsdlabs.dev renders all 6 sections; Playwright UAT passes all assertions including the authors filter and the catechism https:// link.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The fully rebuilt /explore page with all 6 sections (When you're feeling, By Theme split, In the NT with collapsibles + messianic grid, Other Topics drill-down, filterable Authors table, Heidelberg Catechism), a sticky anchor nav, and a 6-section loading skeleton. Deployed live and Playwright-verified.</what-built>
  <how-to-verify>
1. Visit https://psalter.gsdlabs.dev/explore on desktop.
2. Confirm the sticky anchor nav appears and highlights the section you scroll to.
3. Section "By Theme": confirm three labelled sub-groups (Main Topic / Mood / Song Type).
4. Section "In the NT": expand a "Quoted in the New Testament" row — confirm psalm links + italic quotation text appear; confirm the "Messianic by Topic" pill grid is present.
5. Section "Authors": click "David" — confirm the table filters; click a row — confirm it navigates to that psalm; verify the "~N B.C." dates and occasion text look right.
6. Section "Heidelberg Catechism": expand a question — confirm the external link opens relight.app in a new tab (NOT psalter.gsdlabs.dev/relight.app...).
7. Click a Nave's topic under "Other Topics" — confirm the detail page shows sub-topic groups with quotation text.
8. Resize to 375px (mobile): confirm the Authors table hides the Date B.C. + Occasion columns and the anchor nav scrolls horizontally.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/behaviour issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /explore page → client components | Server data passed as props to client islands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.12-10 | Info Disclosure | force-dynamic /explore | accept | Public read-only page; only published catechism/topic data rendered |
| T-04.12-11 | Tampering | outbound catechism links | mitigate | Verified in UAT that external href starts with https://relight.app (no same-origin redirect); rel=noopener set in Plan 03 |
</threat_model>

<verification>
- `npm run build` exits 0
- Playwright UAT GREEN with all section + filter + external-link assertions
- Human visual sign-off obtained
</verification>

<success_criteria>
The rebuilt /explore page matches the UI-SPEC across all 6 sections, is live at psalter.gsdlabs.dev, passes the Playwright UAT, and is approved by the user.
</success_criteria>

<output>
After completion, create `.planning/phases/04.12-explore-page-rebuild/04.12-05-SUMMARY.md`
</output>
