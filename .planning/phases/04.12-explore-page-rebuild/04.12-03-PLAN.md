---
phase: 04.12-explore-page-rebuild
plan: 03
type: execute
wave: 3
depends_on: ["04.12-01", "04.12-02"]
files_modified:
  - src/components/QuotedInNT.tsx
  - src/components/MessianicByTopic.tsx
  - src/components/HeidelbergCatechism.tsx
  - src/components/ExploreAnchorNav.tsx
autonomous: true
requirements: [EXPLORE-04, EXPLORE-06]
must_haves:
  truths:
    - "QuotedInNT renders collapsible sub-topic rows with verse links and quotation text"
    - "MessianicByTopic renders a pill grid of messianic Nave's topics"
    - "HeidelbergCatechism renders collapsible question rows with external links carrying https:// prefix"
    - "ExploreAnchorNav highlights the active section via IntersectionObserver"
  artifacts:
    - path: "src/components/QuotedInNT.tsx"
      provides: "Section 3a collapsible NT-quotation list"
      contains: "Collapsible"
    - path: "src/components/MessianicByTopic.tsx"
      provides: "Section 3b messianic topic grid"
    - path: "src/components/HeidelbergCatechism.tsx"
      provides: "Section 6 catechism collapsible list"
      contains: "https://"
    - path: "src/components/ExploreAnchorNav.tsx"
      provides: "Sticky section anchor nav"
      contains: "IntersectionObserver"
  key_links:
    - from: "src/components/HeidelbergCatechism.tsx"
      to: "relight.app external URL"
      via: "https-prefixed anchor href"
      pattern: "startsWith\\('http'\\)"
---

<objective>
Build the four leaf components for the explore rebuild that are NOT the authors table or the detail route — the three data-display components for Sections 3a, 3b, 6, plus the sticky anchor nav. All receive their data as props (fetched in page.tsx in Plan 05), so they have zero DB coupling and can be built in parallel with Plan 04.

Purpose: These are the reusable pieces Plan 05 composes into the rebuilt page. Building them as standalone prop-driven components keeps page.tsx mostly static (per RESEARCH anti-pattern: do not make the whole page 'use client').
Output: 4 new components under src/components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.12-explore-page-rebuild/04.12-UI-SPEC.md
@.planning/phases/04.12-explore-page-rebuild/04.12-PATTERNS.md

<interfaces>
<!-- Prop contracts each component must accept (returned by Plan 02 query functions). -->

QuotedInNT props (from fetchQuotedInNT):
  interface NTEntry { id: number; sub_topic: string | null; quotation: string | null;
    verses: Array<{ verseId: number; psalmId: number; verseNumber: number | null }> }
  export function QuotedInNT({ entries }: { entries: NTEntry[] })

MessianicByTopic props (from fetchMessianicByTopic):
  interface MessianicTopic { id: number; name: string; messianic: string | null }
  export function MessianicByTopic({ topics }: { topics: MessianicTopic[] })

HeidelbergCatechism props (from fetchHeidelbergCatechism):
  interface CatechismRow { question_number: number; url: string;
    verses: Array<{ psalmId: number; verseNumber: number | null }> }
  export function HeidelbergCatechism({ rows }: { rows: CatechismRow[] })

ExploreAnchorNav: no props. SECTIONS ids: when-you, by-theme, in-the-nt, other-topics, authors, catechism.

shadcn imports available after Plan 01:
  import { Collapsible, CollapsibleContent, CollapsibleTrigger } from "@/components/ui/collapsible"
  import { Badge } from "@/components/ui/badge"
  lucide-react: ChevronDown, ChevronUp, ExternalLink
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: QuotedInNT + HeidelbergCatechism (collapsible client components)</name>
  <read_first>
    - src/components/NavesExpand.tsx (analog client component — useState pattern, pill/link styling)
    - src/components/ui/collapsible.tsx (the primitive installed in Plan 01)
    - .planning/phases/04.12-explore-page-rebuild/04.12-PATTERNS.md (sections "src/components/QuotedInNT.tsx" and "src/components/HeidelbergCatechism.tsx" — exact JSX)
    - .planning/phases/04.12-explore-page-rebuild/04.12-UI-SPEC.md (Section 3a and Section 6 contracts)
  </read_first>
  <action>
Create `src/components/QuotedInNT.tsx` — `'use client'` (shadcn Collapsible needs React state per RESEARCH pitfall 2). Use the NTEntry interface above.

- Wrap the list in a div. For each entry render a `CollapsibleRow` (a local helper component using `useState(false)` for open):
  - Trigger: `<CollapsibleTrigger className="flex w-full items-center justify-between px-3 py-2 min-h-[44px] hover:bg-muted rounded-md">` showing `entry.sub_topic ?? 'General'` (left) and a right group of `<Badge variant="secondary">{entry.verses.length} verses</Badge>` + `{open ? <ChevronUp className="h-4 w-4"/> : <ChevronDown className="h-4 w-4"/>}`.
  - Content: `<CollapsibleContent className="px-3 py-2 text-sm text-muted-foreground">` listing each verse as `<Link href={`/psalms/${v.psalmId}`} className="text-sm font-semibold hover:text-primary">Psalm {v.psalmId}{v.verseNumber != null ? `:${v.verseNumber}` : ''}</Link>`, and the quotation as `<p className="text-sm text-muted-foreground italic ml-4">{entry.quotation}</p>` when present.

Create `src/components/HeidelbergCatechism.tsx` — `'use client'`. Use the CatechismRow interface above. Same CollapsibleRow structure, but:
  - Trigger label: `Question {row.question_number}`, badge `{row.verses.length} verses`.
  - Content: for each verse render an external anchor:
    ```tsx
    <a
      href={row.url.startsWith('http') ? row.url : `https://${row.url}`}
      target="_blank" rel="noopener noreferrer"
      className="text-sm hover:text-primary block"
    >
      Psalm {v.psalmId}{v.verseNumber != null ? `:${v.verseNumber}` : ''} — Question {row.question_number}
      <ExternalLink className="inline ml-1 h-3 w-3" />
    </a>
    ```
    The `startsWith('http') ? row.url : \`https://${row.url}\`` guard is MANDATORY (RESEARCH pitfall 4 — Airtable stores "relight.app/resource/Heidelberg.26" with no protocol; a bare href navigates to psalter.gsdlabs.dev/relight.app/...).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -iE "QuotedInNT|HeidelbergCatechism" || echo "COLLAPSIBLE COMPONENTS TYPECHECK CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'use client'" src/components/QuotedInNT.tsx` returns 1
    - `grep -c "CollapsibleTrigger" src/components/QuotedInNT.tsx` is >= 1
    - `grep -c "italic" src/components/QuotedInNT.tsx` is >= 1 (quotation styling present)
    - `grep -c "'use client'" src/components/HeidelbergCatechism.tsx` returns 1
    - `grep -c "startsWith('http')" src/components/HeidelbergCatechism.tsx` returns 1
    - `grep -c "target=\"_blank\"" src/components/HeidelbergCatechism.tsx` is >= 1
    - `npx tsc --noEmit` reports no errors in either file
  </acceptance_criteria>
  <done>Both collapsible components compile, are client components, render verse links + quotations, and the catechism component guarantees an https:// prefix on external links.</done>
</task>

<task type="auto">
  <name>Task 2: MessianicByTopic (server) + ExploreAnchorNav (client IntersectionObserver)</name>
  <read_first>
    - src/app/explore/messianic/page.tsx (analog — grouped display, slugify usage, pill grid)
    - src/components/NavesExpand.tsx (analog client component for ExploreAnchorNav useState/useEffect)
    - .planning/phases/04.12-explore-page-rebuild/04.12-PATTERNS.md (sections "src/components/MessianicByTopic.tsx" and "src/components/ExploreAnchorNav.tsx" — exact JSX, SECTIONS array, IntersectionObserver rootMargin)
    - .planning/phases/04.12-explore-page-rebuild/04.12-UI-SPEC.md (Section 3b + Sticky Anchor Navigation contracts)
  </read_first>
  <action>
Create `src/components/MessianicByTopic.tsx` — a SERVER component (no `'use client'`). Use the MessianicTopic interface above. Include a local `slugify(name)` copy (same impl as explore/page.tsx: lowercase, strip apostrophes, spaces→`-`, strip non-alphanumerics). Render:
- `<p className="text-xs uppercase tracking-wide text-muted-foreground mb-1">Messianic</p>` (all 12 rows are "Messianic" per RESEARCH — a single group, no enum branching).
- A pill grid `grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-2`; each topic is `<Link href={`/explore/naves/${slugify(t.name)}`} className="inline-flex items-center px-3 py-2 rounded-md border border-border text-sm hover:bg-muted hover:border-primary/30 transition-colors min-h-[44px]">{t.name}</Link>`.

Create `src/components/ExploreAnchorNav.tsx` — `'use client'`. Implement the IntersectionObserver pattern from PATTERNS.md lines 506–557:
- `const SECTIONS = [{id:'when-you',label:"When you're feeling"},{id:'by-theme',label:"By Theme"},{id:'in-the-nt',label:"In the NT"},{id:'other-topics',label:"Other Topics"},{id:'authors',label:"Authors"},{id:'catechism',label:"Catechism"}]`
- `useState` for activeId; `useEffect` creating `new IntersectionObserver((entries)=>{ for (const e of entries) if (e.isIntersecting) setActiveId(e.target.id) }, { rootMargin: '-10% 0px -85% 0px' })` (RESEARCH pitfall 5 — single root margin band to avoid flicker), observing `document.getElementById(id)` for each section, with `observer.disconnect()` cleanup.
- Nav wrapper: `<nav className="sticky top-[64px] z-10 bg-background/95 backdrop-blur-sm border-b border-border h-10 overflow-x-auto flex whitespace-nowrap">` with each item an `<a href={`#${id}`}>` styled active vs inactive exactly per PATTERNS.md (active: `text-foreground font-semibold border-b-2 border-primary -mb-px`; inactive: `text-muted-foreground hover:text-foreground`), base classes `text-sm px-3 py-2 transition-colors`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -iE "MessianicByTopic|ExploreAnchorNav" || echo "MESSIANIC+ANCHORNAV TYPECHECK CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'use client'" src/components/MessianicByTopic.tsx` returns 0 (it is a server component)
    - `grep -c "grid-cols-2 sm:grid-cols-3 lg:grid-cols-4" src/components/MessianicByTopic.tsx` is >= 1
    - `grep -c "'use client'" src/components/ExploreAnchorNav.tsx` returns 1
    - `grep -c "IntersectionObserver" src/components/ExploreAnchorNav.tsx` returns 1
    - `grep -c "rootMargin: '-10% 0px -85% 0px'" src/components/ExploreAnchorNav.tsx` returns 1
    - `grep -c "sticky top-\[64px\]" src/components/ExploreAnchorNav.tsx` returns 1
    - all 6 section ids present: `grep -oE "when-you|by-theme|in-the-nt|other-topics|authors|catechism" src/components/ExploreAnchorNav.tsx | sort -u | wc -l` returns 6
    - `npx tsc --noEmit` reports no errors in either file
  </acceptance_criteria>
  <done>MessianicByTopic is a server pill-grid component; ExploreAnchorNav is a sticky client nav driven by IntersectionObserver with the anti-flicker root margin and all 6 section ids.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component → external relight.app link | User clicks an outbound link |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.12-06 | Tampering | HeidelbergCatechism external href | mitigate | `target="_blank" rel="noopener noreferrer"` on all outbound links; https:// forced prefix prevents same-origin redirect hijack |
| T-04.12-07 | Spoofing | ExploreAnchorNav | accept | Pure client navigation to in-page anchors; no trust decision |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for all 4 components
- Collapsible components are client; MessianicByTopic is server
- Catechism links carry https:// prefix
</verification>

<success_criteria>
Four prop-driven components exist and compile; ready for Plan 05 to import and feed with query data.
</success_criteria>

<output>
After completion, create `.planning/phases/04.12-explore-page-rebuild/04.12-03-SUMMARY.md`
</output>
