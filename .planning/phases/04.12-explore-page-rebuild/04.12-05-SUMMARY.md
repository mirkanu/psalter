---
phase: 04.12-explore-page-rebuild
plan: 05
subsystem: ui
tags: [next.js, react, playwright, tailwind, explore, anchor-nav, collapsible, authors-table, catechism]

requires:
  - phase: 04.12-02
    provides: fetchTopicsByType, fetchQuotedInNT, fetchMessianicByTopic, fetchPsalmsWithAuthorData, fetchHeidelbergCatechism
  - phase: 04.12-03
    provides: ExploreAnchorNav, QuotedInNT, MessianicByTopic, HeidelbergCatechism components
  - phase: 04.12-04
    provides: AuthorsTable component

provides:
  - Rebuilt /explore page composing all 6 sections with anchor IDs and separators
  - 6-section loading.tsx skeleton matching new page shape
  - Playwright UAT spec (17 assertions GREEN) against live psalter.gsdlabs.dev

affects: [phase-5-precentor, phase-6-polish, explore-page-uat]

tech-stack:
  added: []
  patterns:
    - Promise.all parallel fetch for all 9 data sources at page top
    - section[id=...] anchor pattern for scroll-tracking by ExploreAnchorNav
    - Null-filter before passing DB rows to typed child components

key-files:
  created:
    - tests/e2e/explore-rebuild.spec.ts
  modified:
    - src/app/explore/page.tsx
    - src/app/explore/loading.tsx

key-decisions:
  - "messianicByTopic filtered (t.name !== null) before passing to MessianicByTopic — navesTopics.name is nullable in DB schema but component expects string"
  - "ExploreAnchorNav placed between h1 block and first section, inside the page wrapper — sticky positioning via top-[64px] accounts for site header height"
  - "fetchMessianicPsalms included in Promise.all (10th entry) to get live count for All Messianic Psalms link card"

patterns-established:
  - "Promise.all with 10 parallel fetches at RSC page top for zero waterfall on /explore"
  - "section[id=X] wrapping every content block for IntersectionObserver anchor tracking"

requirements-completed: [EXPLORE-02, EXPLORE-03, EXPLORE-04, EXPLORE-05, EXPLORE-06]

duration: 25min
completed: 2026-06-14
---

# Phase 04.12 Plan 05: Explore Page Integration Summary

**Six-section /explore page integrating QuotedInNT collapsibles, MessianicByTopic grid, filterable AuthorsTable (150 rows, David filter to 73), and HeidelbergCatechism with https://relight.app links — Playwright UAT 17/17 GREEN**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-06-14T08:30:00Z
- **Completed:** 2026-06-14T09:00:00Z
- **Tasks:** 2 (Task 3 is human checkpoint — not yet complete)
- **Files modified:** 3

## Accomplishments

- Rebuilt `src/app/explore/page.tsx` with all 6 sections in order (when-you, by-theme, in-the-nt, other-topics, authors, catechism), each with `section[id=...]` anchor IDs
- Parallel `Promise.all` over 10 DB queries; By-Theme split into Main Topic / Mood / Song Type sub-groups; In-the-NT includes QuotedInNT collapsibles + MessianicByTopic pill grid + All Messianic link card
- Updated `loading.tsx` from 4-generic-section to 6-section skeleton with pill grids, row stubs, author filter pills, and collapsible row stubs
- Playwright UAT 17/17 GREEN: all anchor IDs, nav links, sub-group h3s, collapsible expansion, authors 150 rows + David filter, catechism https://relight.app link, Nave's 906 links, mobile column hide

## Task Commits

1. **Task 1: Rebuild explore/page.tsx + update loading.tsx** — `2ace0f3` (feat)
2. **Task 2: Playwright UAT against live deployment** — `63aac4d` (test)
3. **Task 3: Human visual sign-off** — PENDING CHECKPOINT

## Files Created/Modified

- `src/app/explore/page.tsx` — Rebuilt 6-section explore page with Promise.all, ExploreAnchorNav, all 4 new components, NavesExpand
- `src/app/explore/loading.tsx` — 6-section skeleton (was 4-generic-section)
- `tests/e2e/explore-rebuild.spec.ts` — 17-assertion Playwright UAT spec, all GREEN

## Decisions Made

- `messianicByTopic` data filtered (`t.name !== null`) before passing to `MessianicByTopic` — the query returns `name: string | null` from the DB but the component prop requires `name: string`. TypeScript caught this; fixed inline.
- Included `fetchMessianicPsalms()` as a 10th entry in `Promise.all` to populate the live count badge on the "All Messianic Psalms" link card.
- `ExploreAnchorNav` placed after the h1/subtitle block and before the content sections — renders outside the `mt-8` content div so it sits flush against the header/subtitle area.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Null filter for MessianicByTopic name prop**
- **Found during:** Task 1 verification (typecheck)
- **Issue:** `fetchMessianicByTopic` returns `{ name: string | null }[]` but `MessianicByTopic` component interface requires `name: string`. TypeScript error TS2322 on line 220.
- **Fix:** Added `.filter((t): t is typeof t & { name: string } => t.name !== null)` before passing to component
- **Files modified:** `src/app/explore/page.tsx`
- **Verification:** `npx tsc --noEmit` clean; UAT confirmed MessianicByTopic renders
- **Committed in:** `2ace0f3` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 2 — missing null guard for typed component prop)
**Impact on plan:** Single null-filter inline; no scope creep.

## Issues Encountered

None beyond the null-filter type fix above.

## Known Stubs

None — all 6 sections are wired to live DB data. MessianicByTopic renders naves_topics with `messianic IS NOT NULL`. AuthorsTable renders all 150 psalms. HeidelbergCatechism renders from creedal_references.

## Threat Flags

No new threat surface introduced. Page is read-only RSC with `force-dynamic`; external catechism links verified to start with `https://relight.app` (T-04.12-11 mitigated — confirmed in UAT test 8).

## Self-Check (pre-commit)

- [x] `src/app/explore/page.tsx` exists and has all 6 anchor IDs
- [x] `src/app/explore/loading.tsx` exists with animate-pulse (25 occurrences)
- [x] `tests/e2e/explore-rebuild.spec.ts` exists
- [x] Commit `2ace0f3` exists (Task 1)
- [x] Commit `63aac4d` exists (Task 2)
- [x] Playwright UAT: 17/17 GREEN
- [x] `npm run build` exits 0

## Next Phase Readiness

- Task 3 (human visual sign-off) is the outstanding checkpoint for this plan
- Upon approval, Phase 04.12 is complete — /explore page matches UI-SPEC across all 6 sections
- Phase 5 (Precentor Portal) can proceed independently

---
*Phase: 04.12-explore-page-rebuild*
*Completed: 2026-06-14 (pending human checkpoint Task 3)*
