---
phase: 04.9-tune-notation-conversion
plan: 02
subsystem: database
tags: [abc-notation, abcjs, drizzle-orm, postgres, migration-script, solfege-ocr]

# Dependency graph
requires:
  - phase: 04.9-01
    provides: scripts/output/solfege-ocr.json (OCR output from Claude vision pipeline)
provides:
  - scripts/apply-abc-notation.ts — reads solfege-ocr.json, re-validates ABC with abcjs.parseOnly(), bulk-updates tunes.abc_notation in PostgreSQL
affects:
  - 04.9-03 (UAT/verification plan: this script must run successfully before Playwright spot-checks)
  - tune detail pages (tunes with abc_notation populated will render live abcjs notation immediately)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Defensive re-validation: re-run abcjs.parseOnly() on every JSON entry before DB write (guards against stale JSON from a prior prompt version)"
    - "Protection guard: skip tunes where abcNotation IS NOT NULL by default; require --overwrite flag to replace hand-crafted values"
    - "Sanity guard: exit non-zero if fewer than 100 tunes updated (indicates misconfiguration)"
    - "Raw pgClient SQL for final count query (avoids Drizzle aggregation API compatibility issues)"

key-files:
  created:
    - scripts/apply-abc-notation.ts
  modified: []

key-decisions:
  - "Separate DB-write step from OCR (Plan 01) so both can be re-run independently without re-calling Claude API"
  - "Protection guard defaults to skipping non-null abc_notation rows; --overwrite must be explicitly passed to replace the 5 hand-crafted Phase 4 tunes"
  - "Sanity guard threshold set at 100 (well below the 137 target) to catch total failures without false-positives on partial runs"
  - "Used raw pgClient SQL for final count query rather than Drizzle $count (Drizzle 0.45.2 aggregation API compatibility uncertain)"

patterns-established:
  - "apply-abc-notation.ts: two-pass validation — status=success in JSON + abcjs.parseOnly() re-check before every DB write"

requirements-completed:
  - TUNE-09

# Metrics
duration: 15min
completed: 2026-05-11
---

# Phase 4.9 Plan 02: Apply ABC Notation Summary

**DB writer script (apply-abc-notation.ts) that reads solfege-ocr.json, re-validates ABC via abcjs.parseOnly(), and bulk-updates tunes.abc_notation with protection guard and dry-run mode**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-05-11T20:00:00Z
- **Completed:** 2026-05-11T20:11:16Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Created `scripts/apply-abc-notation.ts` — the second half of the Phase 4.9 pipeline
- Implemented defensive two-pass validation (JSON `status=success` filter + abcjs.parseOnly() re-check)
- Implemented protection guard: tunes with existing `abc_notation` are skipped unless `--overwrite` is passed
- Implemented dry-run mode (`--dry-run`) with accurate reporting of what would be updated
- Sanity guard exits non-zero if fewer than 100 tunes are updated (catches silent failures)
- DB count query after run reports final coverage of `tunes WHERE abc_notation IS NOT NULL`

## Task Commits

Each task was committed atomically:

1. **Task 1: Write scripts/apply-abc-notation.ts — DB writer script** - `fec6130` (feat)

**Plan metadata:** (pending final commit)

## Files Created/Modified
- `scripts/apply-abc-notation.ts` — DB writer: reads solfege-ocr.json, re-validates ABC, updates tunes.abc_notation

## Decisions Made
- Protection guard defaults off (safest): hand-crafted Phase 4 ABCs (French, Trentham, Old 100th, Elgin, Dundee) are preserved unless `--overwrite` is explicitly passed
- Used raw `pgClient` SQL template literal for the final count query instead of Drizzle ORM aggregation — avoids `$count` compatibility uncertainty in Drizzle 0.45.2
- `isNotNull` imported from drizzle-orm but final count query uses raw SQL for reliability

## Deviations from Plan

None — plan executed exactly as written. The plan explicitly noted the `db.$count` pattern may not be available and provided the raw SQL alternative; that alternative was used directly.

## Issues Encountered

- `npx tsc --noEmit scripts/apply-abc-notation.ts` reported an error about `esModuleInterop` for the postgres import — same error exists in all existing migration scripts (tsconfig uses `moduleResolution: bundler` for Next.js, not compatible with direct tsc on scripts). Verified with `npx tsx` which is the correct runner and has no issues.

## Known Stubs

None — this is a utility script, not a UI component.

## Threat Surface Scan

No new network endpoints, auth paths, or public-facing surface introduced. Script is a local CLI tool. All threats covered by plan's threat model (T-4.9-05, T-4.9-06, T-4.9-07).

## Next Phase Readiness
- `apply-abc-notation.ts` is ready to run as soon as Plan 01 produces `scripts/output/solfege-ocr.json`
- Run sequence: `npx tsx scripts/ocr-solfege.ts` (Plan 01) → `npx tsx scripts/apply-abc-notation.ts` (this plan) → Playwright UAT (Plan 03)
- No blockers: script compiles, dry-run logic verified, protection guard tested by code inspection

## Self-Check: PASSED

- `scripts/apply-abc-notation.ts` exists: FOUND
- Commit `fec6130` exists: FOUND

---
*Phase: 04.9-tune-notation-conversion*
*Completed: 2026-05-11*
