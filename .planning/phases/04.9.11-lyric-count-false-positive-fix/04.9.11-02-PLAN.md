---
phase: 04.9.11
plan: 02
type: execute
wave: 2
depends_on: ["04.9.11-01"]
files_modified:
  - scripts/annotate-phrase-breaks.ts
  - src/lib/abc-melisma.ts
  - src/lib/lyrics.ts
  - src/components/notation/NotationRenderer.tsx
  - scripts/output/staff-alignment-report.json
  - .planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-VERIFICATION.md
autonomous: false
requirements: []
tags:
  - alignment
  - notation
  - abcjs
  - uat
must_haves:
  truths:
    - "The chosen fix path from Plan 01 Task 4 checkpoint is implemented (Path A, B, C, or D)"
    - "After the fix, the full 150-psalm sweep using the fixed verify-staff-alignment.js checker shows passed ≥ (active_tune_total - documented_skips)"
    - "Pass target is 135+/139 active tunes (excluding ~11 documented SKIPs per RESEARCH.md), as required by D-08"
    - "No previously-passing tune regresses to failing — delta vs Plan 01 baseline is monotone improvement on the failing set"
    - "VERIFICATION.md enumerates: total checked, passed, skipped (with reason: meter-n1 or data-gap), failed (with per-tune root-cause label), and confirms or amends the 11-tune exception list"
  artifacts:
    - path: ".planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-VERIFICATION.md"
      provides: "Post-fix authoritative pass/fail enumeration"
      contains: "passed"
    - path: "scripts/output/staff-alignment-report.json"
      provides: "Final post-fix sweep report (overwrites Plan 01 baseline)"
  key_links:
    - from: "fix file (chosen by checkpoint)"
      to: "verify-staff-alignment.js sweep"
      via: "production build + pm2 restart (if app-tier fix) OR DB --overwrite (if annotation fix)"
      pattern: "pm2 restart psalter|annotate-phrase-breaks.*--overwrite"
---

<objective>
Apply the fix path selected at the Plan 01 Task 4 decision checkpoint, then re-run the full Playwright sweep to confirm 135+/139 active tunes pass the comprehensive alignment check (D-08).

Purpose: Plan 01 produced a true baseline and identified the root cause. Plan 02 closes RENDER-08 properly (Phase 4.9.10 closure was based on a false positive). Without this plan the alignment data remains broken on production.

Output:
- Code change on one of: `scripts/annotate-phrase-breaks.ts` (Path A), `src/lib/abc-melisma.ts` or `src/lib/lyrics.ts` (Path B), `src/components/notation/NotationRenderer.tsx` (Path C), or a mixed set (Path D)
- Re-annotation (if Path A) or production build + pm2 restart (if Path B/C)
- Updated `scripts/output/staff-alignment-report.json` (true post-fix result)
- `.planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-VERIFICATION.md` enumerating final pass/fail/skip with per-tune root-cause labels
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-CONTEXT.md
@.planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-RESEARCH.md
@.planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-PATTERNS.md
@.planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-INVESTIGATION.md
@.planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-01-SUMMARY.md

@scripts/uat/verify-staff-alignment.js
@scripts/annotate-phrase-breaks.ts
@src/lib/abc-melisma.ts
@src/lib/lyrics.ts
@src/components/notation/NotationRenderer.tsx

<interfaces>
<!-- The fix touches one or more of these surfaces depending on the chosen path -->

From scripts/annotate-phrase-breaks.ts:
- `getSplitPointsForMeter(meter: string, n: number): number[] | undefined` — Path A targets this
- `insertPhraseBreaks(abc: string, splitPoints: number[]): string` — orchestrator
- CLI accepts `--dry-run`, `--overwrite`, `--tunes <name1,name2>`

From src/lib/abc-melisma.ts:
- `buildWLineFromSolfa(...)` — Path B target; produces w: tokens from solfège OCR

From src/lib/lyrics.ts:
- `syllabifyForAbc(...)` — Path B target; fallback w: builder when no solfège OCR
- `PSALM_SYLLABLE_OVERRIDES` — Path B may extend

From src/components/notation/NotationRenderer.tsx (line refs from CONTEXT.md):
- `padWLineToNoteCount()` at line 763 — Path C target
- `buildWLineFromSolfa` usage at line 747
- `splitMusicIntoSubLines` at line 696 — Path C may target
- This is a 'use client' component; changes require `npm run build` + `pm2 restart psalter` to take effect on localhost:3005

Build/restart contract (verified Phase 4.9.10):
- `npm run build` from /data/home/psalter
- `pm2 restart psalter` (process is `psalter`, port 3005)
- Verify via `curl -s -o /dev/null -w "%{http_code}" http://localhost:3005/psalms/23?view=staff` returns 200
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement chosen fix path (Path A / B / C / D)</name>
  <files>scripts/annotate-phrase-breaks.ts OR src/lib/abc-melisma.ts OR src/lib/lyrics.ts OR src/components/notation/NotationRenderer.tsx (depends on Plan 01 Task 4 decision)</files>
  <read_first>
    - .planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-INVESTIGATION.md (selected fix path + per-tune evidence)
    - .planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-01-SUMMARY.md (confirmed path from human checkpoint)
    - scripts/output/phrase4-investigation.json (raw classifications)
    - The specific file targeted by the chosen path (full read):
      - Path A: scripts/annotate-phrase-breaks.ts
      - Path B: src/lib/abc-melisma.ts AND src/lib/lyrics.ts (whichever INVESTIGATION.md names)
      - Path C: src/components/notation/NotationRenderer.tsx (focus lines 690-857 per RESEARCH.md)
      - Path D: all files named in the per-tune assignment table
  </read_first>
  <action>
    Read `04.9.11-INVESTIGATION.md` and `04.9.11-01-SUMMARY.md` to confirm the selected path. Implement EXACTLY the path listed:

    **If Path A (re-annotate ABCs — split-mismatch dominant):**
    1. Edit `getSplitPointsForMeter` in `scripts/annotate-phrase-breaks.ts` to fix the offending split-point array. The specific fix is dictated by INVESTIGATION.md per-tune evidence: typically a CM split-points adjustment from `[8, 14, 22]` to a corrected sequence, or a new meter case added. INVESTIGATION.md must specify the new split-point values; if it does not, halt and ask.
    2. Run dry-run: `npx tsx scripts/annotate-phrase-breaks.ts --dry-run` and inspect diff for the failing tunes named in INVESTIGATION.md.
    3. Run live: `npx tsx scripts/annotate-phrase-breaks.ts --overwrite` — this updates `tunes.abc_notation` in DB for all affected tunes.
    4. Spot-check via SQL: `psql $DATABASE_URL -c "SELECT name, char_length(abc_notation), (SELECT COUNT(*) FROM regexp_matches(abc_notation, 'PHRASE_BREAK', 'g')) AS markers FROM tunes WHERE name IN ('<failing-tune-1>', '<failing-tune-2>') ;"` — confirm marker count matches `phrasesForMeter - 1`.

    **If Path B (fix w: token production):**
    1. Edit `buildWLineFromSolfa` in `src/lib/abc-melisma.ts` (if solfège-OCR path is undercounting) or `syllabifyForAbc` in `src/lib/lyrics.ts` (if fallback path is undercounting). The specific defect is named in INVESTIGATION.md.
    2. Add or update unit tests in the matching `.test.ts` (if file exists) covering the failing case from INVESTIGATION.md.
    3. Run unit tests: `npm test -- --run`.
    4. Build: `npm run build` (from `/data/home/psalter`).
    5. Restart: `pm2 restart psalter`.
    6. Confirm server: `curl -s -o /dev/null -w "%{http_code}" http://localhost:3005/psalms/23?view=staff` returns 200.

    **If Path C (fix NotationRenderer counting):**
    1. Edit `padWLineToNoteCount` (line ~763) or `splitMusicIntoSubLines` (line ~696) in `src/components/notation/NotationRenderer.tsx`. INVESTIGATION.md must name which function and the specific defect.
    2. Build: `npm run build`.
    3. Restart: `pm2 restart psalter`.
    4. Confirm server: 200 OK on `/psalms/23?view=staff`.

    **If Path D (mixed):**
    Apply each per-tune assignment from INVESTIGATION.md's table. Group by file to minimize edits. After ALL edits, run `npm run build` + `pm2 restart psalter` once. If Path A is part of the mix, also run `annotate-phrase-breaks.ts --overwrite` with the `--tunes <comma-list>` flag restricted to the tunes assigned to Path A.

    Whichever path is chosen, do NOT modify `verify-staff-alignment.js` (already fixed in Plan 01) and do NOT modify the 11 documented SKIP tunes' data.
  </action>
  <verify>
    <automated>cd /data/home/psalter &amp;&amp; npm run typecheck 2&gt;&amp;1 | tail -20 &amp;&amp; npm test -- --run 2&gt;&amp;1 | tail -10 &amp;&amp; curl -s -o /dev/null -w "psalm23=%{http_code}\n" http://localhost:3005/psalms/23?view=staff</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 (or matches current baseline if pre-existing failures)
    - `npm test -- --run` exits 0 (all unit tests pass — no regression in 39 existing annotate-phrase-breaks.test.ts tests)
    - `curl http://localhost:3005/psalms/23?view=staff` returns HTTP 200
    - If Path A: `psql $DATABASE_URL -tc "SELECT COUNT(*) FROM tunes WHERE abc_notation IS NOT NULL AND meter = 'CM'"` matches pre-fix count (no tune lost ABC) AND `psql -tc "SELECT COUNT(*) FROM tunes WHERE abc_notation LIKE '%PHRASE_BREAK%'"` is within the same ballpark as before (no tune lost its markers)
    - If Path B or C: `grep -c "build" .next/BUILD_ID 2>/dev/null || ls -la .next/BUILD_ID` shows fresh build artifact, AND `pm2 list | grep psalter | grep -E "online|fork"` confirms process running
    - The chosen file actually changed: `git diff --name-only | grep -E "annotate-phrase-breaks|abc-melisma|lyrics\.ts|NotationRenderer"` returns at least one match
  </acceptance_criteria>
  <done>
    Chosen fix path implemented; type-check + unit tests green; dev server returns 200 on psalm 23 staff view; ready for sweep verification.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full 150-psalm sweep + author VERIFICATION.md</name>
  <files>scripts/output/staff-alignment-report.json, .planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-INVESTIGATION.md (Plan 01 baseline + recommended fix path)
    - .planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-RESEARCH.md (Phase Scope: Known Exceptions — the 11 documented SKIP tunes)
    - scripts/output/staff-alignment-report.json (current state — will be overwritten by this task's sweep)
    - .planning/phases/04.9.10-phrase-break-re-annotation/04.9.10-VERIFICATION.md (prior false-positive verification — for comparison/contrast)
  </read_first>
  <action>
    Step A — Pre-flight: confirm dev server is up after Task 1's restart:
    ```bash
    curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3005/psalms/23?view=staff
    ```
    Must be 200. If not, run `pm2 restart psalter && sleep 5` and re-curl.

    Step B — Quick spot check on the previously-failing tunes named in INVESTIGATION.md's top-10 table. Pick the psalm numbers that use those tunes (cross-reference via the DB if needed):
    ```bash
    cd /data/home/psalter
    PSALM_LIST=23,46,100,119,<add up to 5 more from INVESTIGATION.md top-10> NODE_PATH=/usr/lib/node_modules node scripts/uat/verify-staff-alignment.js
    ```
    Confirm `scripts/output/staff-alignment-report.json` shows these psalms now PASS (or document any that don't).

    Step C — Full sweep:
    ```bash
    cd /data/home/psalter
    NODE_PATH=/usr/lib/node_modules node scripts/uat/verify-staff-alignment.js
    ```
    ~8 minutes per VALIDATION.md. Writes `scripts/output/staff-alignment-report.json` (overwrites Plan 01 baseline — that's intentional; the new file IS the post-fix authoritative result).

    Step D — Author `.planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-VERIFICATION.md` with:
    1. **Sweep result** — totals: passed, skipped, failed. Cite the JSON report by path.
    2. **Comparison to Plan 01 baseline** — before-fix counts (from Plan 01 SUMMARY) vs after-fix counts; delta on failed set.
    3. **Pass target check** — `passed >= 135` against active = 144 - skipped target; PASS or FAIL the D-08 bar (135+/139 active tunes).
    4. **Skipped enumeration** — list each SKIPped psalm + reason from RESEARCH.md exception list:
       - meter-n1: Stuttgart, I Need Thee, Webb, Lennox, Aurelia, Old 124th, Darwall, St. John, Shepherd, Clarkeville
       - data-gap: Bays of Harris, Effingham, Farrant, Kingsfold, Wallace
       Confirm every actual SKIP maps to one of these or surface a new "unexpected SKIP" category.
    5. **Residual failures** — per-tune table: psalm number, tune name, sub-stave with trailingEmpty, expected vs actual notes (from the report's `failureDetails` array). Each entry labeled `data-gap | meter-edge | regression | new-failure`.
    6. **RENDER-08 closure statement** — explicit assertion: "RENDER-08 is CLOSED" if D-08 met; else "RENDER-08 partial — N residual failures listed above; recommend follow-up phase or accept as documented exceptions".
    7. **No-regression check** — list any tune that previously passed (per `.planning/phases/04.9.10-phrase-break-re-annotation/04.9.10-VERIFICATION.md` — note: that was a false positive but it lists which psalms were "checked"; cross-reference any psalm that previously had `trailingEmpty=0` in the unfixed checker and now FAILS the fixed checker). Flag these as regressions.

    Step E — If D-08 NOT met (passed < active_total - SKIPs), do NOT loop back automatically. Stop and surface to Task 3 checkpoint with the residual failure table. The user decides whether to add a follow-up plan or accept residuals.
  </action>
  <verify>
    <automated>cd /data/home/psalter &amp;&amp; test -f .planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-VERIFICATION.md &amp;&amp; node -e "const r = JSON.parse(require('fs').readFileSync('scripts/output/staff-alignment-report.json','utf8')); if (r.psalmsChecked !== 150) { console.error('not full sweep:', r.psalmsChecked); process.exit(1) } const active = r.psalmsChecked - r.skipped; console.log('passed=' + r.passed + ' active=' + active + ' failed=' + r.failed + ' skipped=' + r.skipped)" &amp;&amp; grep -E "Sweep result|Pass target check|Residual failures|RENDER-08" .planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-VERIFICATION.md | grep -v '^#' | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/output/staff-alignment-report.json` exists, `psalmsChecked === 150`
    - `.planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-VERIFICATION.md` exists
    - VERIFICATION.md contains literal headings: `grep -v '^#' .planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-VERIFICATION.md | grep -c "Sweep result"` >=1, same for "Pass target check", "Residual failures", "RENDER-08"
    - VERIFICATION.md lists at least 10 of the 15 known SKIP tune names from RESEARCH.md: `grep -c -F -e "Stuttgart" -e "Webb" -e "Aurelia" -e "Old 124th" -e "Bays of Harris" -e "Effingham" -e "Farrant" -e "Kingsfold" -e "Wallace" -e "Darwall" .planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-VERIFICATION.md` returns >= 8
    - VERIFICATION.md contains a `passed: NN` or `passed=NN` token that matches the report JSON: `node -e "const r=JSON.parse(require('fs').readFileSync('scripts/output/staff-alignment-report.json','utf8')); const v=require('fs').readFileSync('.planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-VERIFICATION.md','utf8'); if(!v.includes(String(r.passed))){console.error('passed mismatch report=' + r.passed); process.exit(1)} console.log('ok ' + r.passed)"`
    - Either "RENDER-08 is CLOSED" or "RENDER-08 partial" appears literally in VERIFICATION.md (case-sensitive): `grep -c -E "RENDER-08 is CLOSED|RENDER-08 partial" .planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-VERIFICATION.md` >= 1
  </acceptance_criteria>
  <done>
    Full sweep run with fixed checker + applied fix; VERIFICATION.md publishes authoritative pass/skip/fail with per-tune labels; RENDER-08 closure declared explicitly (closed or partial).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify the fix on representative psalms + sign off RENDER-08</name>
  <what-built>
    Plan 01 fixed the checker; Plan 02 Task 1 applied the chosen fix; Plan 02 Task 2 ran the full sweep and wrote VERIFICATION.md. The pass count should now reflect real alignment, not a false positive. Three representative psalms span CM/LM/SM and include the prior-failing cases from RESEARCH.md.
  </what-built>
  <how-to-verify>
    1. Visit http://localhost:3005/psalms/23?view=staff (CM + Crimond — canonical case)
       - Confirm all 4 rows of stanza 1 show full word coverage under every note
       - Confirm "The Lord's my shepherd..." through "...the quiet waters by." appears
       - Confirm no note has a blank space underneath where a syllable should be
    2. Visit http://localhost:3005/psalms/100?view=staff (LM + Old 100th)
       - Confirm all 4 rows show clean alignment
       - "All people that on earth do dwell" through "...his courts with praise approach with mirth"
    3. Visit http://localhost:3005/psalms/119?view=staff (was the severe z2 bug case)
       - Confirm last row has 6 words (per Phase 4.9.8 success criteria), not 3 words + blank
    4. Cross-check VERIFICATION.md: open `.planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-VERIFICATION.md`
       - Sweep result count matches what you see (passed N / failed M)
       - Residual failures list is acceptable (data-gap and meter-edge cases are pre-approved per RESEARCH.md)
       - RENDER-08 closure statement matches your judgment (closed or partial)
    5. If failures look real (regressions you did not expect), reject and provide the psalm numbers and what you saw vs expected.
  </how-to-verify>
  <resume-signal>Type "approved" to close RENDER-08, or describe issues / which psalms regressed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Path A: annotate-phrase-breaks.ts → psalter-db | Writes to `tunes.abc_notation`; reuses established `--overwrite` flow vetted in Phase 4.9.10 |
| Path B/C: app code → React render in client | Pure client-side render of ABC strings into SVG; no untrusted input added |
| Playwright sweep → localhost:3005 | Same as Plan 01 — read-only DOM inspection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.9.11-05 | T (Tampering) | DB writes via annotate-phrase-breaks.ts --overwrite (Path A only) | mitigate | Dry-run first (`--dry-run`), then `--overwrite`; commit DB state immediately so it is recoverable; established Phase 4.9.10 pattern |
| T-04.9.11-06 | I (Information disclosure) | NotationRenderer changes (Path B/C) | accept | Renders only `tunes.abc_notation` / `solfege_ocr_text` — both already public; no new data surfaced |
| T-04.9.11-07 | D (Denial of service) | Build + pm2 restart during live UAT (Path B/C/D) | mitigate | Restart sequence: build first (atomic .next/BUILD_ID swap), then `pm2 restart psalter`; psalter.gsdlabs.dev brief 502 acceptable; pre-flight curl confirms 200 before sweep starts |
| T-04.9.11-08 | E (Elevation of privilege) | None | N/A | No auth surface in this phase |

No new attack surface. All changes either operate on already-public tune metadata or on local-only Playwright/investigation tooling.
</threat_model>

<verification>
- Plan 02 Task 1: code changed on selected path; typecheck + unit tests pass; dev server returns 200
- Plan 02 Task 2: full sweep run; VERIFICATION.md published with pass/skip/fail enumeration + RENDER-08 closure statement
- Plan 02 Task 3: human visual verify on psalms 23, 100, 119; user signs off "approved" or names regressions
- `scripts/output/staff-alignment-report.json` shows `passed >= 135` against `active = psalmsChecked - skipped` baseline (D-08)
</verification>

<success_criteria>
1. Chosen fix path from Plan 01 Task 4 checkpoint implemented exactly (no scope drift)
2. `npm run typecheck` + `npm test -- --run` green; no regression in 39 existing tests
3. Full Playwright sweep produces passed ≥ 135 against active tune set (D-08 satisfied) OR VERIFICATION.md explicitly documents "RENDER-08 partial" with named residuals
4. VERIFICATION.md enumerates every SKIP with reason matching RESEARCH.md exception list
5. Human signs off via Task 3 checkpoint — RENDER-08 closed or partial-closed with explicit residual list
</success_criteria>

<output>
After completion, create `.planning/phases/04.9.11-lyric-count-false-positive-fix/04.9.11-02-SUMMARY.md` capturing:
- Selected fix path + files changed
- Before vs after sweep counts (passed/skipped/failed)
- RENDER-08 closure status
- Pointer to VERIFICATION.md for the authoritative residual list
- Human sign-off outcome
</output>
