---
phase: 04.6-psalm-detail-ui-polish
plan: "03"
type: execute
wave: 2
depends_on: ["04.6-01"]
files_modified:
  - src/components/SiteHeader.tsx
  - src/components/PsalmTabs.tsx
autonomous: false
requirements:
  - UI-03

user_setup: []

must_haves:
  truths:
    - "At mobile viewports (<768px) the header shows only the logo and a hamburger button — no overflow to two lines"
    - "Tapping the hamburger button opens a Sheet slide-in panel with all nav links"
    - "At desktop viewports (768px+) the original horizontal nav links are visible and the hamburger is hidden"
    - "At mobile viewports the tab strip is a sticky bottom bar (fixed bottom-0) that scrolls horizontally"
    - "Tab content has bottom padding on mobile so the last line is not obscured by the sticky tab bar"
    - "External links in the Study tab are text-blue-600 dark:text-blue-400 (not text-primary)"
    - "Playwright UAT passes at 375px, 768px, and 1280px"
  artifacts:
    - path: "src/components/SiteHeader.tsx"
      provides: "Hamburger + Sheet for mobile; hidden md:flex desktop nav"
      contains: "md:hidden"
    - path: "src/components/ui/sheet.tsx"
      provides: "shadcn Sheet component"
      contains: "SheetContent"
  key_links:
    - from: "src/components/SiteHeader.tsx"
      to: "src/components/ui/sheet.tsx"
      via: "import Sheet, SheetTrigger, SheetContent"
      pattern: "from '@/components/ui/sheet'"
    - from: "src/components/PsalmTabs.tsx"
      to: "viewport bottom"
      via: "fixed bottom-0 left-0 right-0 z-40 on mobile TabsList wrapper"
      pattern: "fixed bottom-0"
---

<objective>
Install shadcn Sheet, add a hamburger mobile menu to SiteHeader, convert the tab strip to a
sticky bottom bar on mobile, fix external link colors in the Study tab, and run Playwright UAT
at all three breakpoints.

Purpose: Mobile usability — the header no longer overflows to two lines; the tab strip no longer
wraps and bleeds into content; links are visually distinguishable on light theme.

Output: Updated SiteHeader with Sheet hamburger, updated PsalmTabs with sticky mobile tab bar
and blue links, Playwright UAT passing at 375/768/1280px.
</objective>

<execution_context>
@/data/home/psalter/.claude/get-shit-done/workflows/execute-plan.md
@/data/home/psalter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/data/home/psalter/.planning/PROJECT.md
@/data/home/psalter/.planning/ROADMAP.md
@/data/home/psalter/.planning/phases/04.6-psalm-detail-ui-polish/04.6-CONTEXT.md
@/data/home/psalter/.planning/phases/04.6-psalm-detail-ui-polish/04.6-RESEARCH.md
@/data/home/psalter/.planning/phases/04.6-psalm-detail-ui-polish/04.6-PATTERNS.md

<interfaces>
<!-- SiteHeader.tsx — current 46-line file -->
'use client'
import Link from "next/link"
import { usePathname } from "next/navigation"

const navLinks = [
  { href: "/psalms", label: "Psalms" },
  { href: "/tunes", label: "Tunes" },
  { href: "/search", label: "Search" },
  { href: "/explore", label: "Explore" },
  { href: "/daily", label: "Daily Plan" },
]

export function SiteHeader() {
  const pathname = usePathname()
  const isActive = (href: string) =>
    href === '/' ? pathname === '/' : pathname.startsWith(href)

  return (
    <header className="sticky top-0 z-50 bg-background/95 backdrop-blur border-b border-border">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex items-center justify-between h-14">
          <Link href="/" ...>CPRC Psalter</Link>
          {/* REPLACE this single <nav> with desktop nav + Sheet hamburger */}
          <nav className="flex items-center gap-1" aria-label="Primary">
            {navLinks.map(...)}
          </nav>
        </div>
      </div>
    </header>
  )
}

<!-- shadcn Sheet API (verified from Context7) -->
import { Sheet, SheetTrigger, SheetContent } from '@/components/ui/sheet'
// SheetContent side="right" — slides in from right
// SheetTrigger asChild — renders children as the trigger button
// Sheet manages open/close state internally — no useState needed

<!-- PsalmTabs.tsx — current TabsList at line 43 -->
<TabsList className="flex flex-wrap h-auto gap-1 mb-6 bg-transparent p-0">
// 8 TabsTrigger children — Tune (md:hidden), Overview, 365 Days, Study,
//   Messianic, Parallel, Backup Tunes, Historical Tunes

<!-- z-index ladder (from RESEARCH.md Pitfall 3) -->
// SiteHeader: z-50
// shadcn Sheet portal: renders above z-50 (Radix/base-ui portal)
// Sticky tab bar: z-40  ← must be BELOW SiteHeader and Sheet
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Sheet component and add hamburger to SiteHeader</name>
  <files>src/components/SiteHeader.tsx, src/components/ui/sheet.tsx</files>
  <read_first>
    - src/components/SiteHeader.tsx (full 46-line file — understand current nav structure)
    - .planning/phases/04.6-psalm-detail-ui-polish/04.6-PATTERNS.md (Pattern section for SiteHeader.tsx — exact target nav JSX)
    - .planning/phases/04.6-psalm-detail-ui-polish/04.6-RESEARCH.md (Pattern 2: shadcn Sheet for Hamburger Menu; Pitfall 1: Sheet Not Installed)
    - src/components/ui/ directory listing (confirm sheet.tsx is absent before running install)
  </read_first>
  <action>
    **Step 1 — Install shadcn Sheet:**
    Run from `/data/home/psalter`:
    ```bash
    npx shadcn@latest add sheet
    ```
    This creates `src/components/ui/sheet.tsx`. The project uses `style: "base-nova"` in
    `components.json` — no `--style` flag needed. Accept any prompts with yes.

    Verify install: `ls src/components/ui/sheet.tsx` — must exist.

    **Step 2 — Rewrite SiteHeader.tsx:**
    Replace the file content with:

    ```tsx
    'use client'
    import Link from "next/link"
    import { usePathname } from "next/navigation"
    import { Menu } from 'lucide-react'
    import { Sheet, SheetTrigger, SheetContent } from '@/components/ui/sheet'

    const navLinks = [
      { href: "/psalms", label: "Psalms" },
      { href: "/tunes", label: "Tunes" },
      { href: "/search", label: "Search" },
      { href: "/explore", label: "Explore" },
      { href: "/daily", label: "Daily Plan" },
    ]

    export function SiteHeader() {
      const pathname = usePathname()
      const isActive = (href: string) =>
        href === '/' ? pathname === '/' : pathname.startsWith(href)

      const linkClass = (href: string) =>
        isActive(href)
          ? 'text-foreground bg-muted text-sm font-medium px-3 py-2 rounded-md transition-colors'
          : 'text-muted-foreground hover:text-foreground hover:bg-muted text-sm font-medium px-3 py-2 rounded-md transition-colors'

      return (
        <header className="sticky top-0 z-50 bg-background/95 backdrop-blur border-b border-border">
          <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div className="flex items-center justify-between h-14">
              <Link
                href="/"
                className="font-semibold text-foreground text-base hover:text-primary transition-colors"
              >
                CPRC Psalter
              </Link>

              {/* Desktop nav — hidden on mobile */}
              <nav className="hidden md:flex items-center gap-1" aria-label="Primary">
                {navLinks.map((link) => (
                  <Link key={link.href} href={link.href} className={linkClass(link.href)}>
                    {link.label}
                  </Link>
                ))}
              </nav>

              {/* Mobile hamburger — hidden on desktop */}
              <Sheet>
                <SheetTrigger asChild>
                  <button
                    aria-label="Open navigation menu"
                    className="md:hidden p-2 rounded-md hover:bg-muted transition-colors"
                  >
                    <Menu className="size-5" />
                  </button>
                </SheetTrigger>
                <SheetContent side="right" className="w-64">
                  <nav className="flex flex-col gap-1 pt-6" aria-label="Mobile primary">
                    {navLinks.map((link) => (
                      <Link key={link.href} href={link.href} className={linkClass(link.href)}>
                        {link.label}
                      </Link>
                    ))}
                  </nav>
                </SheetContent>
              </Sheet>
            </div>
          </div>
        </header>
      )
    }
    ```

    Note: `SheetTrigger asChild` renders its child button as the trigger; the `className="md:hidden"`
    is on the inner `<button>` not on `SheetTrigger` itself. This avoids the `asChild` className
    forwarding issue.
  </action>
  <verify>
    <automated>cd /data/home/psalter && test -f src/components/ui/sheet.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - test -f src/components/ui/sheet.tsx exits 0 (Sheet installed)
    - grep -c "md:hidden" src/components/SiteHeader.tsx returns at least 2 (hamburger button + one other md:hidden element, or at minimum 1 for the hamburger button)
    - grep -c "hidden md:flex" src/components/SiteHeader.tsx returns 1 (desktop nav hidden on mobile)
    - grep -c "SheetContent" src/components/SiteHeader.tsx returns 1 (Sheet wired)
    - grep -c "Menu" src/components/SiteHeader.tsx returns 2 (import + JSX usage)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    sheet.tsx installed in src/components/ui/. SiteHeader shows hamburger on mobile, horizontal
    nav on desktop (md+). TypeScript clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sticky bottom tab bar on mobile + fix Study tab link colors</name>
  <files>src/components/PsalmTabs.tsx</files>
  <read_first>
    - src/components/PsalmTabs.tsx (full 347-line file — focus on: TabsList at line 43, TabsContent wrappers for all 8 tabs, external link classNames at lines 208, 218, 228)
    - .planning/phases/04.6-psalm-detail-ui-polish/04.6-PATTERNS.md (Pattern section for PsalmTabs.tsx — mobile sticky bar JSX and link color fix)
    - .planning/phases/04.6-psalm-detail-ui-polish/04.6-RESEARCH.md (Pattern 3: Sticky Bottom Tab Bar; Pitfall 2: TabsList h-8; Pitfall 5: content padding; z-index section)
  </read_first>
  <action>
    **Part A — Mobile sticky bottom tab bar (D-03):**

    Replace the single `<TabsList>` element (currently line 43 with `className="flex flex-wrap h-auto gap-1 mb-6 bg-transparent p-0"`) with TWO sibling elements: a desktop TabsList and a mobile sticky wrapper.

    The new structure (replace line 43 through the closing `</TabsList>` at line 57):

    ```tsx
    {/* Desktop tab list — hidden on mobile, normal flow */}
    <TabsList className="hidden md:flex flex-wrap h-auto gap-1 mb-6 bg-transparent p-0">
      {/* Mobile "Tune" tab trigger hidden on desktop */}
      <TabsTrigger value="tune" className="md:hidden">
        Tune
      </TabsTrigger>
      <TabsTrigger value="overview">Overview</TabsTrigger>
      <TabsTrigger value="365days">365 Days</TabsTrigger>
      <TabsTrigger value="study">Study</TabsTrigger>
      <TabsTrigger value="messianic">Messianic</TabsTrigger>
      <TabsTrigger value="parallel">Parallel</TabsTrigger>
      <TabsTrigger value="backup-tunes">Backup Tunes</TabsTrigger>
      <TabsTrigger value="historical-tunes">Historical Tunes</TabsTrigger>
    </TabsList>

    {/* Mobile sticky bottom tab bar — hidden on desktop */}
    <div className="md:hidden fixed bottom-0 left-0 right-0 z-40 bg-background/95 backdrop-blur border-t border-border">
      <TabsList
        className="flex h-auto gap-0 bg-transparent p-0 w-full overflow-x-auto [&::-webkit-scrollbar]:hidden"
        style={{ maskImage: 'linear-gradient(to right, black calc(100% - 3rem), transparent)' }}
      >
        <TabsTrigger value="tune" className="flex-shrink-0 px-4 py-3 text-xs">
          Tune
        </TabsTrigger>
        <TabsTrigger value="overview" className="flex-shrink-0 px-4 py-3 text-xs">
          Overview
        </TabsTrigger>
        <TabsTrigger value="365days" className="flex-shrink-0 px-4 py-3 text-xs">
          365 Days
        </TabsTrigger>
        <TabsTrigger value="study" className="flex-shrink-0 px-4 py-3 text-xs">
          Study
        </TabsTrigger>
        <TabsTrigger value="messianic" className="flex-shrink-0 px-4 py-3 text-xs">
          Messianic
        </TabsTrigger>
        <TabsTrigger value="parallel" className="flex-shrink-0 px-4 py-3 text-xs">
          Parallel
        </TabsTrigger>
        <TabsTrigger value="backup-tunes" className="flex-shrink-0 px-4 py-3 text-xs">
          Backup Tunes
        </TabsTrigger>
        <TabsTrigger value="historical-tunes" className="flex-shrink-0 px-4 py-3 text-xs">
          Historical Tunes
        </TabsTrigger>
      </TabsList>
    </div>
    ```

    NOTE: Both TabsList components render triggers for the same Tabs context — this is valid
    because base-ui Tabs uses value matching, not DOM position, to activate content.

    **Part B — Add bottom padding to all TabsContent** (Pitfall 5 — content hidden behind bar):

    For EVERY `<TabsContent>` element in the file, add `pb-16 md:pb-0` to prevent content
    being obscured by the 56px (h-14 ≈ py-3 top+bottom) sticky bar on mobile.

    The TabsContent className pattern:
    - If it has an existing className string: append `pb-16 md:pb-0`
    - If it has no className: add `className="pb-16 md:pb-0"`

    Affected elements (by value):
    - value="tune" — add pb-16 md:pb-0
    - value="overview" — add pb-16 md:pb-0
    - value="365days" — add pb-16 md:pb-0
    - value="study" — add pb-16 md:pb-0
    - value="messianic" — add pb-16 md:pb-0
    - value="parallel" — add pb-16 md:pb-0
    - value="backup-tunes" — add pb-16 md:pb-0
    - value="historical-tunes" — add pb-16 md:pb-0

    **Part C — Fix external link colors (D-13):**

    In the Study tab External Resources section, find the three `<a>` tags with
    `className="text-primary underline underline-offset-2"` (lines ~208, 218, 228).
    Change ALL THREE to:
    ```tsx
    className="text-blue-600 dark:text-blue-400 underline underline-offset-2"
    ```
  </action>
  <verify>
    <automated>cd /data/home/psalter && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "fixed bottom-0" src/components/PsalmTabs.tsx returns 1 (sticky bar wrapper)
    - grep -c "z-40" src/components/PsalmTabs.tsx returns 1 (correct z-index below SiteHeader z-50)
    - grep -c "overflow-x-auto" src/components/PsalmTabs.tsx returns 1 (horizontal scroll on mobile bar)
    - grep -c "pb-16 md:pb-0" src/components/PsalmTabs.tsx returns 8 (all 8 TabsContent have padding)
    - grep -c "text-primary underline" src/components/PsalmTabs.tsx returns 0 (no more near-black links)
    - grep -c "text-blue-600" src/components/PsalmTabs.tsx returns 3 (all 3 external links fixed)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    Desktop TabsList unchanged (hidden on mobile). Mobile sticky bottom bar fixed at bottom-0 z-40
    with horizontal scroll and right-edge fade. All TabsContent have pb-16 md:pb-0. External links
    are text-blue-600 dark:text-blue-400. TypeScript clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Playwright UAT + human verify at 375px/768px/1280px</name>
  <what-built>
    Plan 03 automation:
    - shadcn Sheet installed; SiteHeader has hamburger (mobile) + horizontal nav (desktop)
    - PsalmTabs has sticky bottom tab bar on mobile (fixed bottom-0 z-40, overflow-x-auto)
    - All TabsContent have pb-16 md:pb-0 to avoid content being obscured by sticky bar
    - External links in Study tab are text-blue-600 dark:text-blue-400
    - Plans 01+02 also complete: notation-first layout, no duplicate stanzas, A±, error boundaries
  </what-built>
  <how-to-verify>
    Run the Playwright UAT script inline. The script must be run by the executor (not the user).

    Create and run `/tmp/psalter-uat-4.6.js`:

    ```js
    const { chromium } = require('/usr/lib/node_modules/playwright')
    const BASE = 'http://localhost:3005'

    async function run() {
      const browser = await chromium.launch({
        executablePath: '/tmp/pw-browsers/chromium-1217/chrome-linux/chrome',
        args: ['--no-sandbox'],
      })
      const results = []

      // Helper
      async function check(page, label, fn) {
        try {
          await fn(page)
          console.log('[PASS]', label)
          results.push({ label, pass: true })
        } catch (e) {
          console.log('[FAIL]', label, e.message)
          results.push({ label, pass: false, err: e.message })
        }
      }

      // --- 375px tests ---
      const m = await browser.newPage()
      await m.setViewportSize({ width: 375, height: 812 })
      await m.goto(`${BASE}/psalms/100`, { waitUntil: 'networkidle' })

      await check(m, '375px: hamburger button visible (no nav overflow)', async (p) => {
        const hamburger = await p.locator('button[aria-label="Open navigation menu"]')
        await hamburger.waitFor({ state: 'visible', timeout: 5000 })
      })
      await check(m, '375px: desktop nav hidden on mobile', async (p) => {
        const nav = p.locator('nav.hidden.md\\:flex')
        // Element exists in DOM but should not be visible
        const box = await nav.boundingBox()
        if (box !== null) throw new Error(`Desktop nav has bounding box at 375px: ${JSON.stringify(box)}`)
      })
      await check(m, '375px: hamburger opens Sheet', async (p) => {
        await p.click('button[aria-label="Open navigation menu"]')
        await p.waitForSelector('[data-slot="sheet-content"]', { timeout: 3000 })
      })
      await check(m, '375px: sticky bottom tab bar present', async (p) => {
        const bar = p.locator('.fixed.bottom-0')
        await bar.waitFor({ state: 'visible', timeout: 3000 })
      })
      await check(m, '375px: no duplicate Metrical Lyrics heading', async (p) => {
        const count = await p.locator('text=Metrical Lyrics').count()
        if (count > 1) throw new Error(`Found ${count} "Metrical Lyrics" headings (expected 0 or 1)`)
      })
      await check(m, '375px: notation player renders above tab strip', async (p) => {
        const notation = p.locator('[data-testid="notation-player"], .PsalmNotationPlayer, [class*="space-y-3"]').first()
        const tabs = p.locator('[role="tablist"]').first()
        const nBox = await notation.boundingBox()
        const tBox = await tabs.boundingBox()
        if (nBox && tBox && nBox.y >= tBox.y) throw new Error('Notation player is not above tab strip')
      })

      await m.close()

      // --- 768px tests ---
      const t = await browser.newPage()
      await t.setViewportSize({ width: 768, height: 1024 })
      await t.goto(`${BASE}/psalms/100`, { waitUntil: 'networkidle' })

      await check(t, '768px: desktop nav visible', async (p) => {
        const nav = p.locator('nav.hidden.md\\:flex')
        const box = await nav.boundingBox()
        if (!box) throw new Error('Desktop nav not visible at 768px')
      })
      await check(t, '768px: no sticky bottom bar', async (p) => {
        const bar = p.locator('.fixed.bottom-0.md\\:hidden')
        const count = await bar.count()
        // bar is in DOM but should be hidden (md:hidden means display:none on md+)
        // Check via CSS
        if (count > 0) {
          const display = await bar.evaluate((el) => window.getComputedStyle(el).display)
          if (display !== 'none') throw new Error(`Sticky bar is visible at 768px (display: ${display})`)
        }
      })

      await t.close()

      // --- 1280px tests ---
      const d = await browser.newPage()
      await d.setViewportSize({ width: 1280, height: 900 })
      await d.goto(`${BASE}/psalms/100`, { waitUntil: 'networkidle' })

      await check(d, '1280px: desktop nav visible with all links', async (p) => {
        await p.waitForSelector('nav.hidden.md\\:flex', { timeout: 3000 })
        const links = await p.locator('nav.hidden.md\\:flex a').count()
        if (links < 5) throw new Error(`Only ${links} nav links visible (expected 5)`)
      })
      await check(d, '1280px: A+ button present in notation player', async (p) => {
        const btn = p.locator('button[aria-label="Increase lyrics font size"]')
        await btn.waitFor({ state: 'visible', timeout: 3000 })
      })
      await check(d, '1280px: Study tab link is blue not near-black', async (p) => {
        await p.click('[role="tab"][data-value="study"], button:has-text("Study")').catch(() =>
          p.click('text=Study')
        )
        await p.waitForTimeout(300)
        const link = p.locator('a:has-text("SermonAudio")').first()
        const color = await link.evaluate((el) =>
          window.getComputedStyle(el).color
        )
        // blue-600 = rgb(37, 99, 235) approximately; just verify it's not near-black
        if (color === 'rgb(0, 0, 0)' || color === 'rgb(2, 8, 23)' || color === 'rgba(2, 8, 23, 1)') {
          throw new Error(`Study link color is near-black: ${color}`)
        }
      })

      await d.close()
      await browser.close()

      const passed = results.filter((r) => r.pass).length
      const failed = results.filter((r) => !r.pass)
      console.log(`\nResults: ${passed}/${results.length} passed`)
      if (failed.length > 0) {
        console.log('FAILURES:')
        failed.forEach((f) => console.log(`  - ${f.label}: ${f.err}`))
        process.exit(1)
      }
    }

    run().catch((e) => { console.error(e); process.exit(1) })
    ```

    Run with:
    ```bash
    cd /data/home/psalter && npm run build && docker restart psalter
    # Wait ~5s for container restart, then:
    NODE_PATH=/usr/lib/node_modules node /tmp/psalter-uat-4.6.js
    ```

    Expected: all checks PASS. If any fail, fix before marking complete.

    Additionally, do a quick visual spot-check:
    - Load http://localhost:3005/psalms/100 at 375px — confirm no header overflow, sticky bar
      visible at bottom, right-edge fade on tab bar, notation above tabs
    - Load at 1280px — confirm two-column layout (notation left, SoundCloud placeholder right),
      all 5 nav links visible, A±  buttons in controls row
  </how-to-verify>
  <resume-signal>Type "approved" if all Playwright checks pass and visual spot-check confirms correct layout. Describe any failures to fix first.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User interaction → Sheet | Hamburger triggers Sheet open; Sheet is a Radix/base-ui portal — focus trap and escape key handled by the library |
| Sticky tab bar z-index | Tab bar at z-40; SiteHeader at z-50; Sheet portal above both — correct layering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.6-06 | Elevation of Privilege | SiteHeader Sheet | accept | Sheet contains only internal nav links — no user-controlled content; XSS not possible |
| T-4.6-07 | Tampering | External links Study tab | mitigate | Links changed to text-blue-600 for visual clarity; href values remain hardcoded static strings in PsalmTabs.tsx — no DB-driven href, no injection surface |
| T-4.6-08 | Denial of Service | Sticky tab bar z-index | accept | z-40 bar is below Sheet portal — Sheet correctly covers bar when open; no UI lockout possible |
</threat_model>

<verification>
After all tasks complete:

1. Build + restart: `cd /data/home/psalter && npm run build && docker restart psalter`
2. TypeScript: `npx tsc --noEmit` — exits 0
3. Unit tests: `npm test -- --run` — all green
4. Playwright UAT: `NODE_PATH=/usr/lib/node_modules node /tmp/psalter-uat-4.6.js` — all checks pass
5. Manual: sheet.tsx exists in src/components/ui/
6. Link colors: `grep -c "text-blue-600" src/components/PsalmTabs.tsx` — returns 3
</verification>

<success_criteria>
- SiteHeader: hamburger + Sheet on mobile; horizontal nav on desktop; no layout overflow at 375px
- PsalmTabs: sticky bottom bar on mobile (z-40, overflow-x-auto, mask-image right-fade); desktop tab list unchanged
- All TabsContent have pb-16 md:pb-0 so last content line is not obscured
- External links in Study tab are text-blue-600 dark:text-blue-400
- Playwright UAT: all checks pass at 375/768/1280px
- TypeScript clean, build clean, all unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/04.6-psalm-detail-ui-polish/04.6-03-SUMMARY.md`
following the template at `.claude/get-shit-done/templates/summary.md`.
</output>
