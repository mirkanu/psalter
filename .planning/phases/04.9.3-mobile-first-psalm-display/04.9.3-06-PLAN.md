---
phase: 04.9.3
plan: 06
type: execute
wave: 5
depends_on: ["04.9.3-05"]
files_modified:
  - scripts/uat/mobile-psalm-display-01.js
  - scripts/uat/mobile-psalm-display-02.js
  - scripts/uat/mobile-psalm-display-03.js
  - scripts/uat/mobile-psalm-display-tune-switch.js
autonomous: false
requirements: [MOBILE-01, MOBILE-02, MOBILE-03]
user_setup: []
must_haves:
  truths:
    - "All four Playwright UAT scripts created in Plan 01 now PASS against the dev server running the wired SingingView from Plan 05"
    - "Screenshots captured at 375px, 768px, and 1024px confirm the singing-view layout matches UI-SPEC"
    - "Legacy /psalms/23/study still renders the 7-tab page (regression check)"
    - "User has visually verified the singing view on a real mobile-sized window and approved"
  artifacts:
    - path: "scripts/uat/screenshots/mobile-singing-default-375.png"
      provides: "Captured default singing view at 375px"
    - path: "scripts/uat/screenshots/mobile-fab-sheet-open.png"
      provides: "Captured FAB sheet open state at 375px"
    - path: "scripts/uat/screenshots/mobile-overflow-375.png"
      provides: "Captured no-overflow proof at 375px"
    - path: "scripts/uat/screenshots/mobile-overflow-768.png"
      provides: "Captured no-overflow proof at 768px (after resize)"
    - path: "scripts/uat/screenshots/mobile-tune-switched.png"
      provides: "Captured tune-switch result"
  key_links:
    - from: "Plan 01 RED tests"
      to: "GREEN after Plans 02–05 wired the selectors"
      via: "running scripts against localhost:3005"
      pattern: "PASS:"
---

<objective>
Run the four Playwright UAT scripts created in Plan 01 against the now-wired
SingingView from Plan 05. Each script asserts a specific MOBILE-* requirement.
Fix any selectors that don't match (the scripts were written to anticipate the
markers Plans 02–05 emitted, but mismatches happen). Capture screenshots.
Pause for human visual verification before closing the phase.

Output: All four UAT scripts pass; five screenshots committed; human approval recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.9.3-mobile-first-psalm-display/04.9.3-RESEARCH.md (§Validation Architecture)
@.planning/phases/04.9.3-mobile-first-psalm-display/04.9.3-UI-SPEC.md
@scripts/uat/mobile-psalm-display-01.js
@scripts/uat/mobile-psalm-display-02.js
@scripts/uat/mobile-psalm-display-03.js
@scripts/uat/mobile-psalm-display-tune-switch.js
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Boot dev server (shared setup for Tasks 1b–1e)</name>
  <files></files>
  <read_first>
    - .planning/phases/04.9.3-mobile-first-psalm-display/04.9.3-05-PLAN.md (verify Plan 05 markers landed)
  </read_first>
  <action>
    Boot the dev server in the background and wait for `/psalms/23` to return 200. Persist the PID for cleanup at the end of the plan.

    ```
    cd /data/home/psalter
    npm run dev > /tmp/psalter-dev.log 2>&1 &
    echo $! > /tmp/psalter-dev.pid
    for i in $(seq 1 45); do
      curl -sf http://localhost:3005/psalms/23 -o /dev/null && break
      sleep 1
    done
    curl -sf http://localhost:3005/psalms/23 -o /dev/null || { echo "FAIL: dev server did not come up"; cat /tmp/psalter-dev.log | tail -40; exit 1; }
    echo "PASS: dev server up at http://localhost:3005"
    ```

    Leave the server running. Tasks 1b–1e run scripts against it. Task 1f kills it.
  </action>
  <verify>
    <automated>test -s /tmp/psalter-dev.pid &amp;&amp; curl -sf http://localhost:3005/psalms/23 -o /dev/null &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/psalter-dev.pid` exists and contains a valid PID
    - `curl http://localhost:3005/psalms/23` returns 200
  </acceptance_criteria>
  <done>Dev server is up and reachable on port 3005.</done>
</task>

<task type="auto">
  <name>Task 1b: Run mobile-psalm-display-01.js (default layout @ 375px); iterate on selector mismatches</name>
  <files>scripts/uat/mobile-psalm-display-01.js</files>
  <read_first>
    - scripts/uat/mobile-psalm-display-01.js (current contents)
    - src/components/singing/SingingView.tsx (data-singing-view marker)
    - src/components/singing/PsalmTopBar.tsx (data-singing-topbar)
    - src/components/singing/TuneSubBar.tsx (data-singing-subbar)
    - src/components/singing/PsalmActionsFAB.tsx (data-singing-fab)
  </read_first>
  <action>
    Run the script and iterate until it PASSes against the live dev server:
    ```
    NODE_PATH=/usr/lib/node_modules node scripts/uat/mobile-psalm-display-01.js
    ```

    If FAIL: read the exact error line, then EDIT THE SCRIPT (not production code) to match the actual marker the component emits. Common fixes:
    - selector typo (`[data-singing-fab]` vs `[data-singing-FAB]`)
    - add `await page.waitForSelector(SEL, { timeout: 5000 })` before count assertions for content that mounts post-hydration
    - shadcn Sheet may put `role="dialog"` on an outer wrapper; adjust selector accordingly
    - If `[data-legacy-psalm-chrome]` marker is absent in production, fall back to asserting `[role="tablist"]` count === 0 (no tabs visible at /psalms/23)

    DO NOT touch any file under `src/**`. If you find a production code BUG (marker missing entirely), STOP and add a `## PRODUCTION BUG FOUND` note to the SUMMARY plus exit 2; do not patch production silently in a UAT plan.

    Re-run until exit 0. Capture screenshot `scripts/uat/screenshots/mobile-singing-default-375.png` (the script does this on PASS).
  </action>
  <verify>
    <automated>cd /data/home/psalter &amp;&amp; NODE_PATH=/usr/lib/node_modules node scripts/uat/mobile-psalm-display-01.js &amp;&amp; test -s scripts/uat/screenshots/mobile-singing-default-375.png</automated>
  </verify>
  <acceptance_criteria>
    - Script exits 0
    - `scripts/uat/screenshots/mobile-singing-default-375.png` exists with non-zero size
    - No file under `src/**` was modified
  </acceptance_criteria>
  <done>Script 01 (default layout) passes; screenshot captured.</done>
</task>

<task type="auto">
  <name>Task 1c: Run mobile-psalm-display-02.js (FAB sheet + view-mode switching)</name>
  <files>scripts/uat/mobile-psalm-display-02.js</files>
  <read_first>
    - scripts/uat/mobile-psalm-display-02.js
    - src/components/singing/PsalmActionsFAB.tsx (sheet markup, view-mode buttons)
    - src/components/notation/NotationRenderer.tsx (data-view-mode attribute from Plan 02)
  </read_first>
  <action>
    Run:
    ```
    NODE_PATH=/usr/lib/node_modules node scripts/uat/mobile-psalm-display-02.js
    ```
    Same iteration discipline as Task 1b — edit the SCRIPT, not production code. Sheet animation timing is the most common gotcha here; prefer `waitForSelector` over fixed `setTimeout`.
  </action>
  <verify>
    <automated>cd /data/home/psalter &amp;&amp; NODE_PATH=/usr/lib/node_modules node scripts/uat/mobile-psalm-display-02.js &amp;&amp; test -s scripts/uat/screenshots/mobile-fab-sheet-open.png</automated>
  </verify>
  <acceptance_criteria>
    - Script exits 0
    - `scripts/uat/screenshots/mobile-fab-sheet-open.png` exists
    - No production code modified
  </acceptance_criteria>
  <done>Script 02 (FAB sheet) passes; screenshot captured.</done>
</task>

<task type="auto">
  <name>Task 1d: Run mobile-psalm-display-03.js (overflow + resize + orientationchange — MOBILE-03 gate)</name>
  <files>scripts/uat/mobile-psalm-display-03.js</files>
  <read_first>
    - scripts/uat/mobile-psalm-display-03.js
    - src/components/AbcPlayer.tsx (Plan 01 hardened: outer wrapper, staffwidth-16, orientation listener)
    - src/components/notation/NotationRenderer.tsx (data-notation-body marker)
    - src/components/singing/SingingView.tsx (`<main data-notation-region overflow-x-hidden>`)
  </read_first>
  <action>
    Run:
    ```
    NODE_PATH=/usr/lib/node_modules node scripts/uat/mobile-psalm-display-03.js
    ```

    This is the MOBILE-03 gate — the most important script in the plan. Iterate the script ONLY for selector/timing fixes. If `document.documentElement.scrollWidth > window.innerWidth` is asserted and FAILS, this is a PRODUCTION BUG (Plan 01 AbcPlayer hardening did not actually clamp the SVG). STOP and report — do not work around it in the script.

    Special timing notes:
    - After `page.setViewportSize(...)`, wait at least 350ms for ResizeObserver (150ms debounce) + repaint
    - After dispatching `orientationchange`, wait at least 350ms (the new listener Plan 01 added uses a 150ms setTimeout debounce)
    - For SVG bbox checks, use `await page.locator('[data-notation-body] svg').first().boundingBox()` and compare to viewport width
  </action>
  <verify>
    <automated>cd /data/home/psalter &amp;&amp; NODE_PATH=/usr/lib/node_modules node scripts/uat/mobile-psalm-display-03.js &amp;&amp; test -s scripts/uat/screenshots/mobile-overflow-375.png &amp;&amp; test -s scripts/uat/screenshots/mobile-overflow-768.png</automated>
  </verify>
  <acceptance_criteria>
    - Script exits 0
    - Both `mobile-overflow-375.png` and `mobile-overflow-768.png` exist
    - No production code modified
    - If MOBILE-03 assertion fails after reasonable script tuning, the failure is reported as a production bug and Plan 01 is reopened (not silently worked around)
  </acceptance_criteria>
  <done>MOBILE-03 verified end-to-end at 375px, 768px, and after orientationchange.</done>
</task>

<task type="auto">
  <name>Task 1e: Run mobile-psalm-display-tune-switch.js (tune switch via sub-bar sheet)</name>
  <files>scripts/uat/mobile-psalm-display-tune-switch.js</files>
  <read_first>
    - scripts/uat/mobile-psalm-display-tune-switch.js
    - src/components/singing/TuneSubBar.tsx, TuneSwitcherSheet.tsx (markers + click targets)
    - src/components/singing/SingingView.tsx (useSearchParams flow — confirms client-side tune selection)
  </read_first>
  <action>
    Run:
    ```
    NODE_PATH=/usr/lib/node_modules node scripts/uat/mobile-psalm-display-tune-switch.js
    ```
    Iterate as in Task 1b. Specific gotchas: `router.replace` updates `window.location.search` only after Next.js's transition completes — prefer `await page.waitForURL(/\?tune=/, { timeout: 5000 })` over polling `page.url()`.
  </action>
  <verify>
    <automated>cd /data/home/psalter &amp;&amp; NODE_PATH=/usr/lib/node_modules node scripts/uat/mobile-psalm-display-tune-switch.js &amp;&amp; test -s scripts/uat/screenshots/mobile-tune-switched.png</automated>
  </verify>
  <acceptance_criteria>
    - Script exits 0
    - `mobile-tune-switched.png` exists
    - No production code modified
  </acceptance_criteria>
  <done>Tune-switch via sub-bar verified; URL `?tune=` round-trip works without remount.</done>
</task>

<task type="auto">
  <name>Task 1f: Kill dev server (cleanup)</name>
  <files></files>
  <read_first></read_first>
  <action>
    Kill the dev server started in Task 1a:
    ```
    if [ -s /tmp/psalter-dev.pid ]; then
      kill "$(cat /tmp/psalter-dev.pid)" 2>/dev/null || true
      rm -f /tmp/psalter-dev.pid
    fi
    pkill -f 'next dev' 2>/dev/null || true
    ```
  </action>
  <verify>
    <automated>! curl -sf http://localhost:3005/psalms/23 -o /dev/null --max-time 2 2>/dev/null; echo "server down (exit $?)"</automated>
  </verify>
  <acceptance_criteria>
    - Port 3005 no longer answers (curl fails)
    - `/tmp/psalter-dev.pid` removed
  </acceptance_criteria>
  <done>Dev server cleanly shut down.</done>
</task>

<task type="auto">
  <name>Task 2: Regression check — verify /psalms/[id]/study still renders the legacy 7-tab surface</name>
  <files></files>
  <read_first>
    - src/app/psalms/[id]/study/page.tsx (created in Plan 01 — should be unchanged)
  </read_first>
  <action>
    Boot dev server. Run a minimal Playwright assertion inline (or as an ad-hoc script in `/tmp`) confirming that `/psalms/23/study` returns 200 AND contains a `[role="tablist"]` (the PsalmTabs tabs). Example one-liner:

    ```
    NODE_PATH=/usr/lib/node_modules node -e "
    const { chromium } = require('/usr/lib/node_modules/playwright');
    (async () => {
      const browser = await chromium.launch({
        executablePath: '/tmp/pw-browsers/chromium-1217/chrome-linux/chrome',
        args: ['--no-sandbox']
      });
      const page = await browser.newPage();
      const resp = await page.goto('http://localhost:3005/psalms/23/study', { waitUntil: 'networkidle' });
      if (!resp || !resp.ok()) { console.error('FAIL: study route status', resp?.status()); process.exit(1); }
      const tabs = await page.locator('[role=\"tablist\"]').count();
      if (tabs < 1) { console.error('FAIL: no tablist on /study'); process.exit(1); }
      await page.screenshot({ path: 'scripts/uat/screenshots/study-route-regression.png' });
      console.log('PASS: /psalms/23/study renders tabs');
      await browser.close();
      process.exit(0);
    })();
    "
    ```

    Capture the result. Save the screenshot to `scripts/uat/screenshots/study-route-regression.png`.
  </action>
  <verify>
    <automated>cd /data/home/psalter &amp;&amp; npm run dev > /tmp/psalter-dev.log 2>&amp;1 &amp; DEV=$!; for i in $(seq 1 30); do curl -sf http://localhost:3005/psalms/23/study -o /dev/null &amp;&amp; break; sleep 1; done &amp;&amp; NODE_PATH=/usr/lib/node_modules node -e "const{chromium}=require('/usr/lib/node_modules/playwright');(async()=>{const b=await chromium.launch({executablePath:'/tmp/pw-browsers/chromium-1217/chrome-linux/chrome',args:['--no-sandbox']});const p=await b.newPage();const r=await p.goto('http://localhost:3005/psalms/23/study',{waitUntil:'networkidle'});if(!r||!r.ok()){console.error('FAIL',r?.status());process.exit(1)};const t=await p.locator('[role=\"tablist\"]').count();if(t<1){console.error('FAIL no tablist');process.exit(1)};await p.screenshot({path:'scripts/uat/screenshots/study-route-regression.png'});console.log('PASS');await b.close()})()"; RC=$?; kill $DEV 2>/dev/null; exit $RC</automated>
  </verify>
  <acceptance_criteria>
    - `/psalms/23/study` returns HTTP 200
    - Page contains `[role="tablist"]` (PsalmTabs is mounted)
    - `scripts/uat/screenshots/study-route-regression.png` exists with non-zero size
    - Exit code 0
  </acceptance_criteria>
  <done>
    Legacy 7-tab study surface is intact and reachable; no regression from the
    phase-04.9.3 rewrite.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual verify the singing view</name>
  <what-built>
    The single psalm page has been rethought as a singing instrument:
    - 375px viewport: slim top bar `← Psalm N →`, sub-bar `Tune (M): TuneName ✎`, full-viewport notation + integrated lyrics, FAB bottom-right
    - FAB opens a bottom sheet with View / Audio / About sections
    - Tapping the tune sub-bar opens a tune-switcher sheet (Current / Recommended / Other)
    - Tapping the psalm label opens a psalm selector sheet
    - abc notation never overflows the viewport at any zoom
    - Legacy 7-tab psalm surface preserved at /psalms/[id]/study
  </what-built>
  <how-to-verify>
    1. Open a Chromium-based browser, set window width to 375px (DevTools → device emulation → iPhone 12 or custom 375×812).
    2. Visit https://psalter.gsdlabs.dev/psalms/23 (or http://localhost:3005/psalms/23 if checking pre-deploy).
    3. Confirm visually:
       a. Top bar shows `← Psalm 23 →` with prev/next chevrons; sub-bar below shows `Tune (C.M.): Crimond ✎`.
       b. Below sub-bar: notation fills the viewport with lyrics aligned to notes; no tab strip; no metadata blocks.
       c. Bottom-right: circular FAB.
       d. Tap FAB → sheet slides up from bottom showing three sections: View (Staff/Lyrics only/Solfège), Audio (Play/Key/BPM), About this psalm (with "Open full study view →" link).
       e. Tap "Lyrics only" → sheet closes ~120ms after tap; body switches to lyrics-only.
       f. Tap the tune sub-bar → tune-switcher sheet opens with Current/Recommended/Other sections.
       g. Tap an alt tune → URL gets `?tune=NN`, sheet closes, notation re-renders for the new tune.
       h. Tap the `Psalm 23` label → psalm selector sheet opens (mini /psalms grid).
       i. Resize the window to 768px → notation re-fits without horizontal scroll. Try orientation change (DevTools device frame rotate).
       j. Visit /psalms/23/study → see the legacy 7-tab page intact.

    4. Confirm: no horizontal scroll EVER appears (at 375px or 768px or after zoom).
    5. Confirm: prev/next arrows are disabled at Psalm 1 / Psalm 150.
    6. Confirm: keyboard left/right arrows navigate between psalms.

    If everything looks correct, type `approved` below to close the checkpoint.
    If there are issues, describe them concretely and I'll route them into a Wave 6 fix plan.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Tasks 1b/1c/1d/1e each exit 0 (one UAT script per task; mismatches debuggable in isolation per Warning 6)
- Task 1f cleanly shuts down the dev server
- Study-route regression check (Task 2) exits 0
- Six screenshots in `scripts/uat/screenshots/` (five from main UATs + one regression)
- Human has typed `approved` at Task 3
</verification>

<success_criteria>
- All three phase requirements (MOBILE-01 / MOBILE-02 / MOBILE-03) have been demonstrated against a running build
- Visual approval recorded
- Phase 04.9.3 is shippable
</success_criteria>

<output>
After completion, create `.planning/phases/04.9.3-mobile-first-psalm-display/04.9.3-06-SUMMARY.md`
and update `.planning/ROADMAP.md` to mark Phase 4.9.3 as Complete.
</output>
