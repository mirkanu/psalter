---
phase: 04.9.15-mobile-inline-staff-optimization
plan: 03
subsystem: notation
tags: [abcjs, w-line, lyrics, vitest, tdd]

# Dependency graph
requires:
  - phase: 04.9.15
    provides: "Phase 04.9.15 plan 01 groundwork for mobile inline-staff optimization"
provides:
  - "mapCycleToPhraseSyllableLines glues Line.bibleVerseRef onto the first syllable of verse-opening lines in inline-Staff w: text"
affects: [notation-renderer, lyric-alignment]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Digit-glue precedent from serialiseLyrics reused for w: line generation — plain string concatenation, no markup, preserves token count"

key-files:
  created: []
  modified:
    - src/lib/stanza-cycles.ts
    - src/lib/stanza-cycles.test.ts

key-decisions:
  - "Glue the plain Bible-verse digit directly to the front of the first syllable token (no separator, no <sup>) since abcjs w: lines are plain SVG text with no JSX/markup injection point"

patterns-established:
  - "w: line construction: any per-line metadata (like bibleVerseRef) that must appear in output text is glued onto the existing first token rather than inserted as a new token, preserving the 1-token-per-note alignment invariant"

requirements-completed: [MOBILE-06]

# Metrics
duration: 10min
completed: 2026-07-16
---

# Phase 04.9.15 Plan 03: Inline-Staff verse-number digit-glue Summary

**mapCycleToPhraseSyllableLines now glues Line.bibleVerseRef as a plain digit prefix onto the first syllable of verse-opening lines, matching the serialiseLyrics convention, with zero change to w: line token count.**

## Performance

- **Duration:** 10 min
- **Started:** 2026-07-16T18:41:00Z
- **Completed:** 2026-07-16T18:44:36Z
- **Tasks:** 2 completed
- **Files modified:** 2

## Accomplishments
- Added 5 new unit test cases in `stanza-cycles.test.ts` (RED) covering: digit-glue on the syllabified path, no-ref passthrough, the `Line.syllables` hand-override path, token-count preservation, and the `bibleVerseRef === 0` edge case (`!== undefined` check, not truthiness).
- Implemented the digit-glue in `mapCycleToPhraseSyllableLines` (GREEN) — a one-line change inside the existing slice loop that leaves grouping, under-fill, and phrasesPerCycle contracts untouched.
- All 27 tests in `stanza-cycles.test.ts` pass, including the pre-existing B3 static-guard test (no CMD/DCM/DLM/DSM literals introduced).

## Task Commits

Each task was committed atomically:

1. **Task 1: Add failing tests for bibleVerseRef digit-glue in mapCycleToPhraseSyllableLines** - `0ed47b8` (test)
2. **Task 2: Glue bibleVerseRef onto the first syllable in mapCycleToPhraseSyllableLines** - `96d3226` (feat)

_Note: TDD task pair — RED then GREEN, no refactor commit needed (change was already minimal)._

## Files Created/Modified
- `src/lib/stanza-cycles.ts` - `mapCycleToPhraseSyllableLines` now prefixes `${l.bibleVerseRef}` (when defined) onto the syllabified/hand-overridden text for each line before pushing into the phrase-slot array.
- `src/lib/stanza-cycles.test.ts` - New `describe('mapCycleToPhraseSyllableLines — MOBILE-06 verse-number digit-glue', ...)` block with 5 cases; added `syllabifyForAbc` import needed by the new assertions.

## Decisions Made
- Followed the plan's locked decision 3 exactly: plain digit-glue, no `<sup>`/markup, since abcjs renders `w:` lines into standalone SVG `<text>` nodes with no injection point for styled markup.
- Adjusted an in-code comment (originally quoting the plan's example verbatim, which said "no `<sup>`, ... superscript") because the acceptance-criteria grep (`grep -Fq "sup"` must return nothing) would otherwise fail on the comment text itself, not on any actual superscript implementation. Reworded to "no markup" / "raised/small styled digits" to preserve the same meaning without the literal substring `sup`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Reworded explanatory comment to avoid tripping the "no sup" acceptance-criteria grep**
- **Found during:** Task 2 verification
- **Issue:** The plan's suggested code comment used the words "no `<sup>`" and "superscript", both containing the substring `sup`, which the plan's own acceptance criterion (`grep -Fq "sup" src/lib/stanza-cycles.ts` returns nothing) flags as a failure — a self-contradiction in the plan text (comment vs. verification command), not an actual superscript implementation.
- **Fix:** Reworded the comment to convey the identical meaning ("no markup, no separator"; "raised/small styled digits are not achievable here") without using the substring `sup`.
- **Files modified:** `src/lib/stanza-cycles.ts`
- **Verification:** `grep -Fq "sup" src/lib/stanza-cycles.ts` now returns nothing; all 27 tests still pass.
- **Committed in:** `96d3226` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug — comment wording only, no logic change)
**Impact on plan:** Cosmetic only; the actual glue logic matches the plan's specification exactly. No scope creep.

## Issues Encountered
None beyond the comment-wording deviation above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `mapCycleToPhraseSyllableLines` now carries verse numbers through to inline-Staff `w:` lines; `NotationRenderer.tsx` consumes this output unchanged (no caller-side changes needed since the function signature/shape is identical — only string content changed).
- Manual/Playwright verification (rendering a psalm with a mid-stanza `bibleVerseRef` in inline Staff view and confirming the glued digit appears) is called out in the plan's `<verification>` section but was not run in this autonomous execution — no live dev server was started as part of this plan's tasks. Recommend a quick Playwright spot-check before closing out Phase 04.9.15 if not already covered by another plan in this phase.

---
*Phase: 04.9.15-mobile-inline-staff-optimization*
*Completed: 2026-07-16*
