---
phase: 04.9.15-mobile-inline-staff-optimization
plan: 05
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - src/app/globals.css
  - src/components/AbcPlayer.tsx
autonomous: false
requirements: [MOBILE-04, MOBILE-07]
must_haves:
  truths:
    - "In mobile inline Staff, the repeated clef + key-signature glyph group renders at 60% on every wrapped row (row 2+); row 1 stays full size"
    - "The 5-line staff itself is not vertically misaligned by the clef shrink — only the leading clef/key glyphs shrink within their allotted space"
    - "Non-last staff rows fill the available notation width — no wasted trailing whitespace before the wrap (rows 1 and 2 included, matching the last row's fill)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Scoped clef/key 60% transform on wrapped inline-Staff rows"
      contains: "abcjs-clef"
    - path: "src/components/AbcPlayer.tsx"
      provides: "Corrected staffwidth/stretch input so non-last rows justify to full width"
      contains: "staffwidth"
  key_links:
    - from: "src/app/globals.css"
      to: "[data-notation-body][data-view-mode=\"staff\"] .abcjs-staff-wrapper:not(:first-of-type)"
      via: "transform: scale(0.6) on the confirmed clef/key selector"
      pattern: "abcjs-clef"
    - from: "src/components/AbcPlayer.tsx"
      to: "abcjs.renderAbc staffwidth"
      via: "effectiveStaffWidth / staffWidthFactor input feeding calcHorizontalSpacing"
      pattern: "effectiveStaffWidth"
---

<objective>
Items (a) + (d), both notation-geometry fixes for mobile inline Staff, verified via the Wave 0
diagnostic (Plan 01) and re-run after the fix:
  (a) MOBILE-04 — shrink (not hide) the repeated clef + key-signature glyph group to 60% on
      wrapped continuation rows (row 2+); row 1 (reader's orientation point) stays full size.
      Use the CONFIRMED selector from the Plan 01 diagnostic — NOT the UI-SPEC's wrong
      `.abcjs-top-line` anchor (that is a staff ruling line, RESEARCH Pitfall 1).
  (d) MOBILE-07 — fix wasted whitespace on non-last rows. abcjs already auto-stretches non-last
      rows by default (RESEARCH §3 / Pitfall 2), so the bug is almost certainly this codebase's
      own `effectiveStaffWidth` / `staffWidthFactor` input producing a target width already
      within ~2px of the content width, short-circuiting the stretch. Apply the fix the Plan 01
      Part B VERDICT points to.

This plan is autonomous:false — it ends with a human visual verification checkpoint because both
items are CSS/SVG-geometry judgments that unit tests cannot fully confirm.

Purpose: The mobile inline staff looks like a clean hymnal system — compact leading clef on
continuation rows, and every row filling the width.
Output: A scoped globals.css rule + a targeted AbcPlayer staffwidth fix, both diagnostic-verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.9.15-mobile-inline-staff-optimization/04.9.15-RESEARCH.md
@.planning/phases/04.9.15-mobile-inline-staff-optimization/04.9.15-PATTERNS.md
@.planning/phases/04.9.15-mobile-inline-staff-optimization/04.9.15-UI-SPEC.md
@.planning/phases/04.9.15-mobile-inline-staff-optimization/04.9.15-01-SUMMARY.md

<interfaces>
<!-- Root chromeless element (NotationRenderer.tsx:1412): the ONLY element carrying BOTH
     data-notation-body AND data-view-mode — scope item (a) here so desktop non-chromeless
     staff (line 1394, no data-notation-body) is untouched. -->
<div data-notation-renderer data-notation-body data-view-mode={viewMode} ...>

<!-- Existing verse/clef CSS block lives in src/app/globals.css around lines 148-171. -->

<!-- AbcPlayer staffwidth computation (AbcPlayer.tsx:~406-444, from RESEARCH/PATTERNS):
     const containerWidth = Math.max(0, (staffWidth || 600) - 16)
     const effectiveScale = scale ?? 1
     const targetStaffwidth = (containerWidth * staffWidthFactor) / effectiveScale
     const effectiveStaffWidth = Math.max(120, Math.floor(targetStaffwidth))
     abcjs.renderAbc(el, abcForRender, { scale:1, staffwidth: effectiveStaffWidth,
       responsive:'resize', format:{ stretchlast:1 } })
     staffWidthFactor for chromeless inline staff (NotationRenderer.tsx:532-538) is 0.55 at <768px. -->

<!-- abcjs default (RESEARCH §3): non-last lines auto-stretch to targetWidth UNLESS
     Math.abs(targetWidth - lineWidth) < 2. There is NO "stretch all rows" opt-in flag. -->
</interfaces>

<pitfall>
Pitfall 3 interaction: Plan 02 changed inline-Staff `scale` to a fixed
`INLINE_STAFF_NOTATION_SCALE` (13/14). Since AbcPlayer folds `scale` into
`effectiveStaffWidth = containerWidth*staffWidthFactor/effectiveScale`, the row-stretch fix must
be measured AGAINST the post-Plan-02 scale (that is why this plan depends_on 02). Re-run the
Plan 01 diagnostic on the current build before AND after the AbcPlayer edit.
</pitfall>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Item (a) — 60% clef/key shrink on wrapped inline-Staff rows (CSS)</name>
  <read_first>
    - .planning/phases/04.9.15-mobile-inline-staff-optimization/04.9.15-01-SUMMARY.md (the CONFIRMED clef/key selector + exact class nesting from the Plan 01 Part A diagnostic)
    - src/app/globals.css lines 148-171 (existing abcjs + verse-number rules, scoping style)
    - src/components/notation/NotationRenderer.tsx lines 1410-1414 (data-notation-body + data-view-mode root)
  </read_first>
  <action>
Using the selector CONFIRMED by the Plan 01 diagnostic (Part A / 04.9.15-01-SUMMARY.md), add a
rule to `src/app/globals.css` (near the existing abcjs block, ~line 171). The expected selector,
per RESEARCH Code Examples §1, is `.abcjs-staff-extra.abcjs-clef` / `.abcjs-staff-extra.abcjs-key-signature`
(NOT `.abcjs-top-line`). Scope to the chromeless inline-Staff root and to wrapped rows only:
```css
/* MOBILE-04: shrink (not hide) the repeated clef + key-signature glyph group on
   wrapped continuation rows of the mobile inline Staff view. Row 1 (first system)
   stays full size — it is the reader's orientation point. Scoped to
   [data-view-mode="staff"] so split-leaf / solfège / desktop non-chromeless staff
   are untouched. Selector confirmed live via tests/diagnostics/inline-staff-clef-stretch-diff.mjs. */
[data-notation-body][data-view-mode="staff"] .abcjs-staff-wrapper:not(:first-of-type) .abcjs-clef,
[data-notation-body][data-view-mode="staff"] .abcjs-staff-wrapper:not(:first-of-type) .abcjs-key-signature {
  transform: scale(0.6);
  transform-origin: left center;
}
```
If the Plan 01 SUMMARY shows a different actual nesting (Assumption A1), use that exact selector
instead — the invariant is: target the clef + key-signature glyph group inside every
`.abcjs-staff-wrapper` EXCEPT the first, scoped to `[data-view-mode="staff"]`, at 60% with
`transform-origin: left center`. Do NOT target `.abcjs-top-line`. If `transform: scale()` visibly
misaligns the 5 staff ruling lines (UI-SPEC §a vertical-alignment caveat), keep the staff lines
full size by scoping the transform strictly to the clef/key `<g>` (not the wrapper) — the
diagnostic screenshots confirm which.
  </action>
  <verify>
    <automated>node tests/diagnostics/inline-staff-clef-stretch-diff.mjs; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "data-view-mode=\"staff\"] .abcjs-staff-wrapper:not(:first-of-type)" src/app/globals.css`
    - `grep -q "transform: scale(0.6)" src/app/globals.css` AND `grep -q "transform-origin: left center" src/app/globals.css`
    - The new rule references `.abcjs-clef` / `.abcjs-key-signature` (or the exact confirmed selector), NOT `.abcjs-top-line` (`grep -q "abcjs-top-line" src/app/globals.css` must return nothing new)
    - Post-rebuild, the Plan 01 diagnostic reports row-0 clef at full size and rows 1+ clef/key visibly smaller (its CLEF-ROW output / screenshots confirm the shrink)
  </acceptance_criteria>
  <done>Wrapped-row clef/key blocks render at 60%; row 1 unchanged; split-leaf/solfège/desktop staff unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Item (d) — non-last rows fill available width (staffwidth/stretch fix)</name>
  <read_first>
    - .planning/phases/04.9.15-mobile-inline-staff-optimization/04.9.15-01-SUMMARY.md (Part B VERDICT: under-fill YES/NO + worst fill ratio + per-row widths)
    - src/components/AbcPlayer.tsx lines 395-455 (containerWidth, effectiveScale, targetStaffwidth, effectiveStaffWidth, renderAbc options)
    - src/components/notation/NotationRenderer.tsx lines 511-538 (staffWidthFactor derivation for chromeless inline staff)
    - .planning/phases/04.9.15-mobile-inline-staff-optimization/04.9.15-RESEARCH.md (Pitfall 2, Code Examples §3)
  </read_first>
  <action>
Apply the fix the Plan 01 Part B VERDICT points to. Do NOT add a nonexistent abcjs "stretch all
rows" flag — non-last rows already auto-stretch UNLESS `abs(targetWidth - lineWidth) < 2`
(RESEARCH §3). The most likely root cause (RESEARCH Assumption A2): the chromeless inline-Staff
`staffWidthFactor` (0.55 at <768px) narrows `effectiveStaffWidth` so far that per-phrase content
already meets it, short-circuiting the stretch and leaving trailing whitespace.

In `src/components/AbcPlayer.tsx` (the `effectiveStaffWidth`/`targetStaffwidth` computation),
apply the minimal numeric/formula correction indicated by the diagnostic so that, for inline
Staff (non-split), each non-last row's rendered width comes within 2px of the notation container
width. Concretely, the expected correction is to ensure the `staffwidth` passed to abcjs for
inline Staff is the FULL notation container width (so abcjs justifies each row to it), rather than
a sub-1-narrowed value — mirroring how the 260712-kov fix already set split-leaf's
`staffWidthFactor` to 1 to restore geometry parity. If the VERDICT instead isolates a different
input (e.g. the `-16` padding subtraction or the `Math.floor` flooring), correct that specific
input. Keep the change scoped so split-leaf Staff, Solfège, and desktop non-chromeless paths are
byte-identical (gate on the same `chromeless` + inline-`staff` condition already used for
`compactSplitMobile`). Add a comment citing MOBILE-07 and the diagnostic VERDICT.

Re-run the Plan 01 diagnostic after the change and confirm `underFilled=false` for all non-last
rows (fill ratio ≥ 0.97).
  </action>
  <verify>
    <automated>node tests/diagnostics/inline-staff-clef-stretch-diff.mjs; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "MOBILE-07" src/components/AbcPlayer.tsx` (fix is documented with the requirement + diagnostic reference)
    - No new abcjs format key invented — `grep -Eq "format:\s*{[^}]*stretch[^l]" src/components/AbcPlayer.tsx` returns nothing (only the pre-existing `stretchlast` remains)
    - Post-rebuild, the Plan 01 diagnostic Part B VERDICT reports `non-last rows under-fill = NO` (worst fill ratio ≥ 0.97) on both sampled tunes
    - The change is gated to chromeless inline Staff — split-leaf/desktop staffwidth math is unchanged (`git diff` shows the edit inside the inline-staff branch only)
  </acceptance_criteria>
  <done>Non-last inline-Staff rows justify to the container width; no wasted whitespace; other view modes unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual verification of clef shrink + row fill on a real device</name>
  <what-built>
    Item (a): 60% clef/key shrink on wrapped rows of mobile inline Staff (row 1 full size).
    Item (d): non-last rows now fill the available width (no trailing whitespace).
    Automated Playwright diagnostic (Plan 01, re-run) already confirmed the DOM/geometry numbers;
    this checkpoint is the final human eyeball on real rendering.
  </what-built>
  <how-to-verify>
    1. Ensure the site is rebuilt + running (the executor rebuilds psalter and pm2-restarts, then
       reports the URL, e.g. https://psalter.gsdlabs.dev).
    2. On a phone (or 375px viewport), open a multi-row CM psalm in inline Staff, e.g.
       https://psalter.gsdlabs.dev/psalms/23 (Crimond) and https://psalter.gsdlabs.dev/psalms/78.
       (Use an APPROVED tune — Plan 04 gates inline Staff; the executor lists an approved tune in
       04.9.15-04-SUMMARY.md.)
    3. Confirm: row 1's clef + key signature are full size; every wrapped row below shows a
       noticeably smaller (≈60%) clef/key block, and the 5 staff lines are NOT vertically skewed.
    4. Confirm: every row except the last fills the full width — no big empty gap before the row
       wraps. The last row may be shorter (expected).
    5. Confirm A+/A− still only resizes lyric text (regression check for Plan 02).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks wrong (row, viewport, tune).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS / abcjs render options → SVG output | Pure presentation; no data or auth boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.9.15-09 | Tampering | globals.css scoped rule | accept | CSS transform scoped to `[data-view-mode="staff"]` wrapped rows only; cannot affect other views or data. No user input. |
| T-04.9.15-10 | Denial of Service | AbcPlayer staffwidth change | accept | Change only adjusts a numeric render input on an already-running client render path; no new loops or network calls. Verified via diagnostic that render still completes. |
</threat_model>

<verification>
- `node tests/diagnostics/inline-staff-clef-stretch-diff.mjs` exits 0 and, post-rebuild, reports
  wrapped-row clef shrink (Part A) and `under-fill = NO` (Part B).
- Human checkpoint (Task 3) approves the visual result on a real device.
</verification>

<success_criteria>
- MOBILE-04 + MOBILE-07 satisfied: 60% clef/key on wrapped rows (row 1 full), non-last rows fill
  width; no regression to split-leaf/solfège/desktop staff; human sign-off recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/04.9.15-mobile-inline-staff-optimization/04.9.15-05-SUMMARY.md`.
Record the final clef selector used and the AbcPlayer input that was corrected, plus the human
verdict.
</output>
