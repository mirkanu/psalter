---
phase: 04.9.15-mobile-inline-staff-optimization
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/diagnostics/inline-staff-clef-stretch-diff.mjs
autonomous: true
requirements: [MOBILE-04, MOBILE-07]
must_haves:
  truths:
    - "A committed Playwright diagnostic renders a multi-row inline Staff tune and reports the exact abcjs SVG class(es) wrapping the clef + key-signature glyph group on wrapped rows"
    - "The same diagnostic reports, per staff row, the rendered row width vs the notation container width, revealing whether non-last rows fail to fill available width"
    - "The diagnostic never throws and always exits 0 (prints DIAGNOSTIC-UNAVAILABLE on failure), matching the repo's diagnostic convention"
  artifacts:
    - path: "tests/diagnostics/inline-staff-clef-stretch-diff.mjs"
      provides: "Clef/key selector confirmation (item a) + per-row stretch measurement (item d) before the fixes"
      contains: "runPlaywright"
      min_lines: 120
  key_links:
    - from: "tests/diagnostics/inline-staff-clef-stretch-diff.mjs"
      to: "http://localhost:3099 (playwright-daemon)"
      via: "require('/home/services/playwright-daemon/client.js')"
      pattern: "playwright-daemon/client"
---

<objective>
Produce the Wave 0 diagnostic that MUST run before the item (a) clef/key-signature
shrink and item (d) row-stretch fixes (locked decision 4). This confirms two facts
that static source reading could not:
  1. (item a) The exact abcjs SVG class(es) that wrap the repeated clef + key-signature
     glyph group on wrapped continuation rows — RESEARCH Pitfall 1 proved the UI-SPEC's
     `.abcjs-top-line` anchor is WRONG (it is a staff ruling line). The real candidates are
     `.abcjs-staff-extra.abcjs-clef` / `.abcjs-staff-extra.abcjs-key-signature`, but the
     precise DOM nesting relative to `.abcjs-staff-wrapper` is unconfirmed (Assumption A1).
  2. (item d) Whether non-last staff rows under-fill the container width, and the numeric
     gap between each row's rendered width and the notation container width, so the fix
     (Plan 05) targets the real root cause (RESEARCH Pitfall 2 / Assumption A2: likely the
     codebase's own `effectiveStaffWidth` input, NOT a missing abcjs format option).

Purpose: De-risk two visual-geometry fixes whose root cause cannot be read from source.
Output: One committed diagnostic script mirroring `tests/diagnostics/split-leaf-staff-diff.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.9.15-mobile-inline-staff-optimization/04.9.15-RESEARCH.md
@.planning/phases/04.9.15-mobile-inline-staff-optimization/04.9.15-PATTERNS.md

<interfaces>
<!-- Canonical diagnostic shape to mirror (full file read required). Reuse verbatim:
     - const { getStatus, runPlaywright } = require('/home/services/playwright-daemon/client.js')
     - waitForBrowserReady() 10x/3s poll helper
     - page.addInitScript(() => localStorage.setItem('psalter_tour_v2','done')) to suppress the tour
     - navigate → [data-singing-gear] click → getByRole('radio',{name:'Staff'}).click()
       → getByRole('radio',{name:'Inline'}).click() → Escape
     - always exit 0; print 'DIAGNOSTIC-UNAVAILABLE: <reason>' on any failure -->
From tests/diagnostics/split-leaf-staff-diff.mjs (reference implementation).

Root element the SingingView renders (chromeless): src/components/notation/NotationRenderer.tsx:1412
  <div data-notation-renderer data-notation-body data-view-mode={viewMode} ...>
The abcjs SVG + `.abcjs-staff-wrapper` rows live INSIDE this element.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit inline-staff clef/key + row-stretch diagnostic (items a + d root cause)</name>
  <read_first>
    - tests/diagnostics/split-leaf-staff-diff.mjs (copy its structure verbatim: daemon client require, waitForBrowserReady, tour-suppression init script, Gear-popover navigation, never-throws/exit-0 contract, OUT_DIR screenshots)
    - src/components/notation/NotationRenderer.tsx lines 1390-1420 (confirm data-notation-renderer / data-notation-body / data-view-mode attributes on the chromeless root)
    - .planning/phases/04.9.15-mobile-inline-staff-optimization/04.9.15-RESEARCH.md (Pitfall 1, Pitfall 2, Assumption A1, Assumption A2, Code Examples §1 and §3)
  </read_first>
  <action>
Create `tests/diagnostics/inline-staff-clef-stretch-diff.mjs`, structurally mirroring
`tests/diagnostics/split-leaf-staff-diff.mjs`. Use the shared Playwright daemon
(`require('/home/services/playwright-daemon/client.js')`) — NEVER spawn raw Chromium.
Poll `waitForBrowserReady()` (10 tries, 3s apart); if never ready, print
`DIAGNOSTIC-UNAVAILABLE: ...` and `process.exit(0)`. Wrap `main()` in a `.catch` that also
prints `DIAGNOSTIC-UNAVAILABLE:` and exits 0.

BASE = `process.env.TEST_BASE_URL ?? 'http://localhost:3005'`. Target a multi-row CM tune
that wraps to 4+ rows on a 375px viewport — use `${BASE}/psalms/23` (Crimond, canonical) and
also `${BASE}/psalms/78` (Azmon/Denfield, CM) as a second sample. Viewport: 375x667 (mobile).

For each URL, in one daemon job:
  1. `page.addInitScript(() => { try { window.localStorage.setItem('psalter_tour_v2','done') } catch {} })`
  2. `page.setViewportSize({ width: 375, height: 667 })`, `page.goto(url, { waitUntil:'load', timeout:15000 })`
  3. Wait for `[data-notation-renderer] svg` visible (12000ms).
  4. Ensure inline Staff is active: click `[data-singing-gear]`, wait 200ms,
     `getByRole('radio',{name:'Staff'}).click()`, `getByRole('radio',{name:'Inline'}).click()`,
     press Escape, wait 600ms.

PART A — clef/key selector confirmation (item a). Inside `page.evaluate`, scope to the
chromeless root `document.querySelector('[data-notation-body][data-view-mode="staff"]')`.
Collect `.abcjs-staff-wrapper` rows (NodeList → array). For EACH row report:
  - the full `class` attribute of every element matching `.abcjs-clef`, `.abcjs-key-signature`,
    `.abcjs-staff-extra`, and the topmost `<g>`/`<text>` before the first note — so the executor
    of Plan 05 sees the ACTUAL nesting.
  - a boolean `hasClefClass` / `hasKeySigClass` per row.
Return this per-row class map for row 0 (first system) and rows 1..N (wrapped continuation rows).
Print each row's classes with a `CLEF-ROW <i>:` prefix. This resolves Assumption A1: it tells
Plan 05 exactly which selector (`.abcjs-staff-extra.abcjs-clef` etc.) actually exists and how it
nests under `.abcjs-staff-wrapper`.

PART B — per-row stretch measurement (item d). Inside `page.evaluate`, measure:
  - `container = document.querySelector('[data-notation-viewarea]') || [data-notation-renderer]`;
    record `containerWidth = container.getBoundingClientRect().width`.
  - for EACH `.abcjs-staff-wrapper` row: `rowWidth = row.getBoundingClientRect().width` and
    `fillRatio = rowWidth / containerWidth`.
  - flag `underFilled = fillRatio < 0.97` for any NON-LAST row (rows 0..N-2).
Print `STRETCH-ROW <i>: rowWidth=<px> container=<px> fill=<ratio> underFilled=<bool>`.
Print a `VERDICT` line: `non-last rows under-fill = YES/NO` and the worst-case fill ratio,
so Plan 05 knows whether the bug reproduces and by how much.

Capture a full-page screenshot per URL into `OUT_DIR = join(__dirname,'out')` (mkdir recursive).
Add a run header comment block documenting the run command:
`node tests/diagnostics/inline-staff-clef-stretch-diff.mjs` and the
`TEST_BASE_URL=https://psalter.gsdlabs.dev` variant.
  </action>
  <verify>
    <automated>node tests/diagnostics/inline-staff-clef-stretch-diff.mjs; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/diagnostics/inline-staff-clef-stretch-diff.mjs` succeeds
    - `grep -q "playwright-daemon/client" tests/diagnostics/inline-staff-clef-stretch-diff.mjs`
    - `grep -q "psalter_tour_v2" tests/diagnostics/inline-staff-clef-stretch-diff.mjs` (tour suppressed)
    - `grep -q "abcjs-clef" tests/diagnostics/inline-staff-clef-stretch-diff.mjs` AND `grep -q "abcjs-key-signature" tests/diagnostics/inline-staff-clef-stretch-diff.mjs` (Part A inspects the corrected selectors, NOT `.abcjs-top-line`)
    - `grep -qv "abcjs-top-line" ...` note: script MUST NOT scale/target `.abcjs-top-line` (it may mention it only in a comment). Confirm no `transform` mutation of `.abcjs-top-line`.
    - `grep -q "CLEF-ROW" tests/diagnostics/inline-staff-clef-stretch-diff.mjs` and `grep -q "STRETCH-ROW" ...` and `grep -q "VERDICT" ...`
    - `grep -q "DIAGNOSTIC-UNAVAILABLE" tests/diagnostics/inline-staff-clef-stretch-diff.mjs` (never-throws contract)
    - Running the script exits 0 (even if the daemon/dev server is unavailable it must print DIAGNOSTIC-UNAVAILABLE and exit 0)
  </acceptance_criteria>
  <done>
    Diagnostic committed; when the dev server + daemon are up it prints per-row clef/key class
    maps (Part A) and per-row width/fill ratios with a VERDICT (Part B), giving Plan 05 the
    confirmed selector and the reproduced-or-not stretch bug before any fix code is written.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| diagnostic → local dev server / daemon | Read-only browser automation against localhost; no untrusted external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.9.15-01 | Information Disclosure | diagnostic script | accept | Local-only measurement script; renders public psalm pages; no secrets, no PII, no network egress beyond the pinned abcjs CDN already used by the sibling diagnostic. |
| T-04.9.15-02 | Denial of Service | playwright-daemon | mitigate | Reuse the shared single-Chromium daemon (per CLAUDE.md); never spawn raw Chromium; poll browserReady before submitting jobs. |
</threat_model>

<verification>
- `node tests/diagnostics/inline-staff-clef-stretch-diff.mjs` exits 0.
- Output contains CLEF-ROW, STRETCH-ROW, and VERDICT lines when the dev server is running.
</verification>

<success_criteria>
- Committed diagnostic confirms the real clef/key selector (item a) and reproduces/quantifies
  the non-last-row under-fill (item d) BEFORE Plan 05 writes any fix.
</success_criteria>

<output>
After completion, create `.planning/phases/04.9.15-mobile-inline-staff-optimization/04.9.15-01-SUMMARY.md`.
Record the CONFIRMED clef/key selector (exact class nesting from Part A) and the Part B VERDICT
(under-fill YES/NO + worst fill ratio) — Plan 05 depends on both.
</output>
