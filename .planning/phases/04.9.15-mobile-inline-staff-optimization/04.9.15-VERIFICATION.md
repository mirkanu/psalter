---
phase: 04.9.15-mobile-inline-staff-optimization
verified: 2026-07-19T14:14:42Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 1
overrides:
  - must_have: "In mobile inline Staff, the repeated clef + key-signature glyph group renders at 60% on every wrapped row (row 2+); row 1 stays full size"
    reason: "Human checkpoint (04.9.15-05, Task 3, round 1 feedback) explicitly overrode the plan's 'row 1 stays full size' text — the human decided consistency across all rows (including row 1) was preferred over the reader's-orientation-point rationale. Final implementation shrinks the clef/key/time-signature group on EVERY row, confirmed live and approved by the human across 4 checkpoint rounds ('Approved — perfect!')."
    accepted_by: "user (04.9.15-05-PLAN.md Task 3 checkpoint, rounds 1-4)"
    accepted_at: "2026-07-17T00:00:00Z"
---

# Phase 04.9.15: Mobile Inline Staff Optimization — Verification Report

**Phase Goal:** Optimise the mobile inline staff view surfaced from exploration of screenshots:
(a) Shrink (not hide) the repeated clef/key-signature block at the start of every wrapped row
(b) A+/A- buttons control lyric font size only (not staff notation size)
(c) Add verse numbers to inline staff view, matching how they display in lyrics-only and split-leaf views; also fix verse-initial letters not being capitalised
(d) Fix row-stretch/justification bug causing wasted whitespace on non-last rows
(e) Gate staff-inline abc view behind explicit 'approved' status in tuneMelismaDecisions

**Verified:** 2026-07-19T14:14:42Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1a | Repeated clef+key-signature glyph group is shrunk (not hidden) on wrapped rows; row 1 stays full size (ORIGINAL plan text) | PASSED (override) | Human checkpoint explicitly overrode this to "ALL rows including row 1 shrink" — see override entry above. |
| 1a-revised | Clef+key-signature (+time-signature, extended by user follow-up) glyph GROUP shrinks to 60% as one tightly-packed unit on EVERY row (approved behavior) | ✓ VERIFIED | `applyLeadingGlyphShrink` in `src/components/AbcPlayer.tsx:65-202`, gated to chromeless inline (non-split) Staff via `staffWidthFactor < 1` (`AbcPlayer.tsx:620-622`). Live diagnostic + screenshot (`tests/diagnostics/out/psalm-23-inline-staff.png`) confirms `.abcjs-leading-glyph-shrink-group` present on row 0 AND rows 1-3; visually every row shows an equally small clef/key glyph. |
| 1b | 5-line staff is not vertically misaligned by the clef shrink | ✓ VERIFIED | `applyLeadingGlyphShrink` computes `staffCenterY` from the actual `.abcjs-staff` bbox per row and sets `transform-origin` to land exactly on it (`transform-box: fill-box`), documented and measured in 04.9.15-05-SUMMARY.md (time-signature center 131.43 vs staff center 131.44). Visually confirmed in screenshot — no skew. |
| 2 | Pressing A+/A− in inline Staff changes lyric text size only — abcjs staff notation does NOT grow/shrink | ✓ VERIFIED | Live production test (psalm 23, 375px viewport): 5× A+ clicks changed `--staff-base-size` from 23px→33px while `[data-notation-renderer] svg` bounding box stayed byte-identical (326.355×241 before and after). Code: `computeNotationScale` (`src/lib/notation-scale.ts`) branches inline-Staff scale on `notationBaseSize` (viewport-resize-driven), never on `baseSize` (A+/A−-driven) — proven by 9/9 passing unit tests. |
| 3 | A+/A− step 2px, floor 8px, ceiling 120px (soft-40 non-clamping) | ✓ VERIFIED | `GlassBottomBar.tsx:71-81`: `Math.max(8, baseSize - SIZE_STEP)` / `Math.min(MAX_SIZE, baseSize + SIZE_STEP)`, `MAX_SIZE=120`, `SIZE_STEP=2` exported from `NotationRenderer.tsx:153-155`. Old 40-clamp removed (`grep -Fq "40, baseSize"` returns nothing). |
| 4 | Inline-Staff notation scale keeps rescaling on viewport resize/orientation change, decoupled from A+/A− only | ✓ VERIFIED | `SingingView.tsx`: dual-state pattern — `notationBaseSize` (state) driven exclusively by the repurposed 04.9.4-03 resize heuristic (`referenceWidthRef`, `clamp(x,8,40)`, 150ms debounce), `baseSize` driven exclusively by `handleBaseSizeChange` (A+/A−), which no longer resets `referenceWidthRef`. Both states are provably independent per `notation-scale.test.ts`. |
| 5 | Verse numbers appear in inline-Staff w: lines as a plain digit glued to the first syllable | ✓ VERIFIED | `src/lib/stanza-cycles.ts:117`: `l.bibleVerseRef !== undefined ? `${l.bibleVerseRef}${syllabified}` : syllabified`. 27/27 `stanza-cycles.test.ts` tests pass including 5 new MOBILE-06 cases (digit-glue, no-ref passthrough, syllables-override path, token-count preservation, `bibleVerseRef===0` edge case). Live screenshot of psalm 23 shows "1The Lord's", "2He", "3My soul", "4Yea,though" glued exactly as specified. |
| 6 | Verse-initial letters are capitalised (not lowercased) in the w: lines | ✓ VERIFIED | Confirmed pre-existing fix from quick task 260716-dtm (commit `148d44e`, dated 2026-07-16T10:05 — before this phase's plans started at 14:23 the same day), in `src/lib/lyrics.ts:155-189` (`syllabifyForAbc` restores original casing letter-by-letter). Explicitly documented as out-of-scope/already-fixed in `04.9.15-RESEARCH.md` and `04.9.15-UI-SPEC.md`. Live screenshot confirms capitalized verse-initial words ("The", "My", "Yea", "He", "In", "For"). |
| 7 | Non-last staff rows fill the available notation width — no wasted whitespace on rows 1/2 vs the last row | ✓ VERIFIED | Live production diagnostic re-run (psalm-23, psalm-78): rows now converge to within 1-3px of each other and match the last row's fill ratio (psalm-23: 0.909/0.908/0.909/0.908 for rows 0-3; psalm-78: 0.898/0.902/0.902/0.902). Compare to the Plan 01 pre-fix baseline (psalm-78: 0.807/0.807/0.897/0.897, 33px spread). Root cause (abcjs `expandToWidest` render option) documented and code-verified in `AbcPlayer.tsx:590,611`. Absolute fill ratio (~0.90-0.91) does not reach the plan task's own internal note of "≥0.97" but the must-have truth ("matching the last row's fill") IS met — rows are now uniform, and the remaining ~9-10% gap was traced to intrinsic `px-2` container padding + abcjs SVG margins present equally on every row (documented in 04.9.15-05-SUMMARY.md "Issues Encountered"), not a recurrence of the reported bug. Human checkpoint approved this final state ("perfect!"). |
| 8 | Inline Staff is available ONLY when the active tune's latest `tuneMelismaDecisions.status` is exactly `'approved'`; null and `'not_approved'` both disable it | ✓ VERIFIED | `src/lib/inline-staff-gating.ts`: `resolveStaffInlineApproved(status) { return status === 'approved' }` — positive allow-list, 13/13 unit tests pass. Live production test on psalm 4 (tune 71 "Wallace", `not_approved`): Gear→Layout→Inline button `aria-disabled="true"` with tooltip exactly `"Inline Staff notation isn't approved for this tune yet"`. |
| 9 | Navigation into inline Staff on a non-approved tune falls back to Split-Leaf + locked toast | ✓ VERIFIED | Live production test: forcing `localStorage['psalter-score-mode']='staff'` then reloading psalm 4 resulted in `data-view-mode="staff-split"` and the toast text `"Inline Staff not available for this tune — showing Split-Leaf. Pick a different view in Settings."` present in the DOM. Code: `SingingView.tsx:232-243`, `shouldFallbackToSplit` (13/13 unit tests). |

**Score:** 9/9 sub-truths verified (5/5 phase-goal items a-e), 1 via documented override (row-1 clause of item a, superseded by human-directed revision)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `tests/diagnostics/inline-staff-clef-stretch-diff.mjs` | Wave-0 diagnostic confirming clef/key selector + row-stretch measurement | ✓ VERIFIED | Exists, runs against live production, exits 0, produces `CLEF-ROW`/`STRETCH-ROW`/`VERDICT` output as designed. Re-run live during this verification. |
| `src/lib/notation-scale.ts` | Pure `computeNotationScale()` decoupling inline-Staff scale from A+/A− | ✓ VERIFIED | Exists, exports `computeNotationScale`, `INLINE_STAFF_NOTATION_SCALE`, `SPLIT_LEAF_NOTATION_SCALE`. Zero React/DOM/abcjs imports (pure). 9/9 tests pass. |
| `src/lib/notation-scale.test.ts` | Unit coverage for 3-way scale branch | ✓ VERIFIED | 9 cases, all passing, covering split/inline/desktop branches. |
| `src/lib/stanza-cycles.ts` | `mapCycleToPhraseSyllableLines` glues `bibleVerseRef` onto first syllable | ✓ VERIFIED | Line 117 confirmed; 27/27 tests pass. |
| `src/lib/stanza-cycles.test.ts` | Digit-glue + no-ref passthrough cases | ✓ VERIFIED | 5 new MOBILE-06 cases present and passing. |
| `src/lib/inline-staff-gating.ts` | Pure gating helpers + `MelismaStatus` type | ✓ VERIFIED | `resolveStaffInlineApproved`, `computeInlineLayoutDisabled`, `shouldFallbackToSplit` all present, pure (no React/DB imports), 13/13 tests pass. |
| `src/db/queries/tunes.ts` | `melismaStatus` surfaced on `AlternateTune` | ✓ VERIFIED | `MelismaStatus` type, `fetchTuneMelismaStatus`, batch status attach in `fetchTunesByMeter` all present. |
| `src/app/globals.css` | Scoped clef/key 60% transform (per original plan) | ⚠️ SUPERSEDED (documented) | Static CSS rule was removed entirely per 04.9.15-05-SUMMARY.md; replaced by the more capable JS pass (`applyLeadingGlyphShrink` in `AbcPlayer.tsx`) after CSS proved incapable of correct vertical alignment/space-reclaim. This supersession is explicitly documented and human-approved — not a gap. |
| `src/components/AbcPlayer.tsx` | Corrected staffwidth/stretch input (item d) + clef shrink (item a) | ✓ VERIFIED | `applyLeadingGlyphShrink`, `expandToWidest`, `CLEF_KEY_SHRINK_SCALE = 0.6` all present and gated correctly to chromeless inline (non-split) Staff only. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `NotationRenderer.tsx` | `notation-scale.ts` | `computeNotationScale({ viewMode, chromeless, baseSize, notationBaseSize })` | ✓ WIRED | Confirmed at `NotationRenderer.tsx:511`. |
| `SingingView.tsx` | `NotationRenderer.tsx` | `notationBaseSize` prop, independent resize-driven state | ✓ WIRED | Confirmed at `SingingView.tsx:833` (`notationBaseSize={notationBaseSize}`) and dual-state derivation lines 144-163. |
| `GlassBottomBar.tsx` | `NotationRenderer.tsx` | `import { MAX_SIZE, SIZE_STEP }` | ✓ WIRED | Confirmed at `GlassBottomBar.tsx:5`. |
| `stanza-cycles.ts` | `NotationRenderer.tsx` | `mapCycleToPhraseSyllableLines` output feeds `w:` lines | ✓ WIRED | Confirmed via live screenshot showing glued verse digits rendered in the actual SVG w: text. |
| `db/queries/tunes.ts` | `tuneMelismaDecisions` | latest non-null status per tune | ✓ WIRED | Confirmed via live gating test — non-approved tune (71/Wallace) correctly resolved to disabled state. |
| `GearPopover.tsx` | `inline-staff-gating.ts` | `computeInlineLayoutDisabled({ isStaff, staffInlineApproved, solfegeInlineAvailable })` | ✓ WIRED | Confirmed at `GearPopover.tsx:70`, and live-tested (`aria-disabled="true"` + correct tooltip on non-approved tune). |
| `SingingView.tsx` | `inline-staff-gating.ts` | `shouldFallbackToSplit` — navigation fallback + toast | ✓ WIRED | Confirmed at `SingingView.tsx:238`, live-tested (forced-navigation fallback to `staff-split` + toast fired). |
| `globals.css`/`AbcPlayer.tsx` | `[data-notation-body][data-view-mode="staff"] .abcjs-staff-wrapper` | scoped transform on confirmed clef/key selector | ✓ WIRED (via JS, not CSS) | `applyLeadingGlyphShrink` operates on `.abcjs-staff-wrapper` rows within the chromeless-staff-scoped render path; confirmed live via diagnostic + screenshot. |
| `AbcPlayer.tsx` | `abcjs.renderAbc` | `effectiveStaffWidth`/`expandToWidest` feeding row-stretch fix | ✓ WIRED | Confirmed at `AbcPlayer.tsx:598-612`, gated to `staffWidthFactor < 1` (chromeless inline non-split only — split-leaf/desktop unaffected, confirmed via `NotationRenderer.tsx:547-553`). |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `GearPopover` Inline button disabled state | `staffInlineApproved` | `resolveStaffInlineApproved(activeTune?.melismaStatus)` ← `fetchTunesByMeter`/`fetchTuneMelismaStatus` DB query on `tuneMelismaDecisions` | ✓ FLOWING | Live-tested: psalm 4 (tune 71, real DB row with `status='not_approved'`) correctly produced a disabled button; psalm 23 (tune with `status='approved'`) correctly rendered live inline-Staff SVG. Not a static/hardcoded gate. |
| Inline-Staff `w:` lines | `bibleVerseRef` glue | `Line.bibleVerseRef` from parsed `lyricsStructured` DB data | ✓ FLOWING | Live screenshot of psalm 23 shows real per-verse digits (1/2/3/4) glued onto real syllabified text, not placeholder/static text. |
| `--staff-base-size` CSS var | `baseSize` | React state, driven by `handleBaseSizeChange` (button clicks) | ✓ FLOWING | Live-tested: 5 A+ clicks moved the value from 23px to 33px in the live DOM. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Clef/key/time-sig shrink renders on all rows, live production | `TEST_BASE_URL=https://psalter.gsdlabs.dev node tests/diagnostics/inline-staff-clef-stretch-diff.mjs` | `.abcjs-leading-glyph-shrink-group` present on every row; screenshot confirms visual shrink | ✓ PASS |
| Non-last rows fill width, live production | same diagnostic, Part B | psalm-23: 0.909/0.908/0.909/0.908 (rows 0-3); psalm-78: 0.898/0.902/0.902/0.902 — rows converge, matching last row | ✓ PASS |
| A+/A− decoupled from staff notation, live production | Custom Playwright script (5× click `[aria-label="Increase size"]`, compare SVG bbox before/after) | `--staff-base-size` 23px→33px; SVG bbox unchanged (326.355×241 both times) | ✓ PASS |
| Non-approved tune gates Inline + shows tooltip, live production | Custom Playwright script (psalm 4 / tune 71 "Wallace", `status='not_approved'`) | `aria-disabled="true"`, tooltip exactly `"Inline Staff notation isn't approved for this tune yet"` | ✓ PASS |
| Forced navigation into non-approved inline Staff falls back + toasts, live production | Same script, forced `localStorage` + reload | `data-view-mode="staff-split"`, toast text present in DOM | ✓ PASS |
| Unit test suites for all 3 pure libs | `npx vitest run src/lib/notation-scale.test.ts src/lib/stanza-cycles.test.ts src/lib/inline-staff-gating.test.ts` | 49/49 tests passed | ✓ PASS |
| TypeScript compiles clean in all touched files | `npx tsc --noEmit -p tsconfig.json` | Zero errors in any of the 11 files this phase touched; remaining errors confined to pre-existing, unrelated `tests/e2e/*.spec.ts` files (documented in Plan 02/04 summaries) | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| MOBILE-04 | Plan 01, Plan 05 | Shrink repeated clef/key-sig block on wrapped rows | ✓ SATISFIED | Revised via human override to all rows; live-verified. REQUIREMENTS.md checkbox already ticked `[x]`, wording matches revised scope. |
| MOBILE-05 | Plan 02 | A+/A- lyric-only, correct range | ✓ SATISFIED | Live-verified (SVG unchanged across A+ presses). REQUIREMENTS.md checkbox still `[ ]` — administrative bookkeeping gap only, not a functional gap (code + tests + live behavior all confirm satisfaction). |
| MOBILE-06 | Plan 03 | Verse numbers in inline-Staff w: lines | ✓ SATISFIED | Live-verified via screenshot + unit tests. REQUIREMENTS.md checkbox still `[ ]` — same administrative note as above. |
| MOBILE-07 | Plan 01, Plan 05 | Fix row-stretch/justification bug | ✓ SATISFIED | Live-verified; rows converge and match last row's fill. REQUIREMENTS.md checkbox already ticked `[x]`. |
| MOBILE-08 | Plan 04 | Explicit-approval gate on inline Staff | ✓ SATISFIED | Live-verified (disabled state + fallback + toast on a real non-approved tune). REQUIREMENTS.md checkbox still `[ ]` — same administrative note as above. |

No orphaned requirements: all 5 IDs (MOBILE-04 through MOBILE-08) declared across the phase's plans are accounted for in REQUIREMENTS.md, and all map to satisfied truths.

**Note:** REQUIREMENTS.md shows MOBILE-05/06/08 as unchecked (`[ ]`) despite all evidence confirming they are functionally satisfied. This appears to be a bookkeeping gap (checkboxes not updated post-completion) rather than an implementation gap — recommend ticking these three during phase-completion bookkeeping.

### Anti-Patterns Found

Advisory only — a full code review already ran (`04.9.15-REVIEW.md`, status: `issues_found`, 2 critical + 9 warnings) and is not duplicated here. Neither of the 2 critical findings undermines any of the phase's 5 goal items (a-e):

- **CR-01** (precenting sing page silently substitutes wrong tune when precentor's assigned tune is outside the psalm's default meter) — touches `src/app/precent/[id]/sing/[pos]/page.tsx`, a file this phase's Plan 04 modified, but the defect is in pre-existing tune-assignment fallback logic unrelated to inline-Staff mobile optimization (items a-e). Tracked in the review, not a phase-goal blocker.
- **CR-02** (toggling "Show original" mid-playback leaves audio uncontrollably playing) — touches `AbcPlayer.tsx`, modified by Plan 05, but the defect is in the pre-existing `showOriginal` JPG-toggle render path, unrelated to the clef-shrink/row-stretch work this phase added. Tracked in the review, not a phase-goal blocker.

Both are legitimate code-quality issues worth fixing in a follow-up but do not affect achievement of this phase's stated goal.

### Human Verification Required

None. The one item requiring human visual judgment (items a+d, geometry/CSS) was already run as a blocking checkpoint within Plan 05 itself (4 rounds, final verdict "Approved — perfect!", documented in `04.9.15-05-SUMMARY.md`) and is corroborated by this verification's independent live re-run of the diagnostic against the current production deployment. Items b, c, e were independently confirmed via live behavioral testing during this verification pass (not just static code/grep evidence).

### Gaps Summary

No gaps. All 5 phase-goal items (a-e) are implemented, wired, and confirmed working on the live production deployment (`https://psalter.gsdlabs.dev`) via a combination of passing unit tests (49/49), clean TypeScript compilation in every touched file, and fresh live Playwright verification performed during this verification pass (not merely trusting SUMMARY.md claims).

One documented override applies: item (a)'s literal "row 1 stays full size" plan text was superseded by an explicit, human-approved in-flight revision to shrink all rows uniformly — this is the correct, approved behavior, not a deviation requiring further action.

Two advisory code-review findings (CR-01, CR-02) exist in files this phase touched but concern functionality outside the phase's 5 stated goal items; they do not block this phase's completion and are already tracked in `04.9.15-REVIEW.md`.

One minor administrative note: REQUIREMENTS.md checkboxes for MOBILE-05/06/08 are not yet ticked despite functional completion — recommend updating during phase-completion bookkeeping.

---

_Verified: 2026-07-19T14:14:42Z_
_Verifier: Claude (gsd-verifier)_
