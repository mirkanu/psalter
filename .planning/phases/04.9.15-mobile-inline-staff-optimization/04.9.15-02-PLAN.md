---
phase: 04.9.15-mobile-inline-staff-optimization
plan: 02
type: execute
wave: 1
depends_on: ["01"]
files_modified:
  - src/lib/notation-scale.ts
  - src/lib/notation-scale.test.ts
  - src/components/notation/NotationRenderer.tsx
  - src/components/singing/GlassBottomBar.tsx
  - src/components/singing/SingingView.tsx
autonomous: true
requirements: [MOBILE-05]
must_haves:
  truths:
    - "In inline Staff view (chromeless, viewMode==='staff'), pressing A+/A− changes lyric text size only — the abcjs staff notation does NOT grow or shrink"
    - "A+/A− step by 2px, floor at 8px, hard ceiling at 120px; 40px is a soft threshold that A+ passes without clamping"
    - "Inline-Staff notation scale keeps rescaling on viewport resize/orientation change via the existing 04.9.4-03 proportional-zoom heuristic (clamp(x × newWidth/refWidth, 8, 40)) — it is decoupled from the A+/A− lyric control ONLY, never hardcoded to a single fixed number"
    - "Split-leaf notation scale (=1) and non-chromeless desktop staff scale (baseSize/14) are unchanged"
  artifacts:
    - path: "src/lib/notation-scale.ts"
      provides: "Pure computeNotationScale() with SPLIT_LEAF_NOTATION_SCALE constant and a viewport-derived notationBaseSize input for inline Staff"
      exports: ["computeNotationScale", "INLINE_STAFF_NOTATION_SCALE", "SPLIT_LEAF_NOTATION_SCALE"]
    - path: "src/lib/notation-scale.test.ts"
      provides: "Unit coverage for the 3-way scale branch, including that inline-Staff scale tracks notationBaseSize (not baseSize)"
      contains: "computeNotationScale"
  key_links:
    - from: "src/components/notation/NotationRenderer.tsx"
      to: "src/lib/notation-scale.ts"
      via: "computeNotationScale({ viewMode, chromeless, baseSize, notationBaseSize })"
      pattern: "computeNotationScale"
    - from: "src/components/singing/SingingView.tsx"
      to: "src/components/notation/NotationRenderer.tsx"
      via: "notationBaseSize prop — driven by the existing referenceWidthRef/resize heuristic, independent of the lyric-only baseSize"
      pattern: "notationBaseSize"
    - from: "src/components/singing/GlassBottomBar.tsx"
      to: "src/components/notation/NotationRenderer.tsx"
      via: "import { MAX_SIZE } for the A+ ceiling"
      pattern: "MAX_SIZE"
---

<objective>
Item (b): decouple the inline-Staff A+/A− control so it sizes lyric text ONLY, not the
abcjs staff notation — extending the `SPLIT_LEAF_NOTATION_SCALE` decoupling pattern shipped
in 04.9.14-01 to inline Staff. Correct the A+/A− range to step 2px / floor 8 / soft-40 / hard-120.

Per UI-SPEC §(b), inline-Staff notation scale must stay governed by the EXISTING 04.9.4-03
proportional-zoom heuristic (`referenceWidthRef` / viewport-resize-driven
`clamp(x × newWidth/refWidth, 8, 40)` in `SingingView.tsx`) — just decoupled from the new
lyric-only A+/A− control, NOT decoupled from viewport responsiveness. Concretely: `SingingView`
gains a SECOND piece of size state, `notationBaseSize`, seeded at the current mobile default
(13) and driven ONLY by the existing resize/orientation-change heuristic (never by A+/A−
button presses). `baseSize` (lyric-only from this plan on) keeps being driven ONLY by A+/A−
and is no longer touched by the resize heuristic. `computeNotationScale` reads
`notationBaseSize` (falling back to the `INLINE_STAFF_NOTATION_SCALE` constant, 13/14, only
when no `notationBaseSize` is supplied) for the chromeless-staff branch — so the inline-Staff
scale keeps rescaling on resize/orientation change exactly as it does today, it just no longer
moves when the user presses A+/A−.

Purpose: On mobile, precentors must be able to enlarge lyric text for readability without
the staff ballooning off-screen, while the staff itself keeps fitting the viewport on
resize/rotate exactly as it does today.
Output: A pure scale helper + wired NotationRenderer/GlassBottomBar/SingingView changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.9.15-mobile-inline-staff-optimization/04.9.15-RESEARCH.md
@.planning/phases/04.9.15-mobile-inline-staff-optimization/04.9.15-PATTERNS.md
@.planning/phases/04.9.15-mobile-inline-staff-optimization/04.9.15-UI-SPEC.md

<interfaces>
<!-- Existing coupled scale (NotationRenderer.tsx lines 494-496) — REPLACE: -->
const isSplitForScale = isSplitMode(viewMode)
const SPLIT_LEAF_NOTATION_SCALE = 1
const scale = isSplitForScale ? SPLIT_LEAF_NOTATION_SCALE : baseSize / 14

<!-- Existing size constants (NotationRenderer.tsx lines 138-140), currently NOT exported: -->
const MIN_SIZE = 4
const MAX_SIZE = 120
const SIZE_STEP = 2

<!-- Existing GlassBottomBar A+/A− (lines 70,78) — WRONG range/step per UI-SPEC: -->
onClick={() => onBaseSizeChange(Math.max(8, baseSize - 1))}   // A−
onClick={() => onBaseSizeChange(Math.min(40, baseSize + 1))}  // A+

<!-- Existing SingingView resize heuristic (lines 178-217) — currently recomputes `baseSize`
     itself on viewport resize/orientation change. This plan REPURPOSES this exact block to
     drive a NEW `notationBaseSize` state instead, leaving lyric `baseSize` untouched by resize: -->
const referenceWidthRef = useRef<number>(...)   // becomes the notation-only reference width
const baseSizeRef = useRef<number>(baseSize)     // stays as-is (lyric baseSize tracking)
// resize handler body — change target from `baseSize` to `notationBaseSize`:
const current = notationBaseSizeRef.current       // was: baseSizeRef.current
const computed = current * (newWidth / refWidth)
const clamped = Math.max(8, Math.min(40, computed))  // unchanged clamp bounds — notation-only
...
if (rounded !== current) setNotationBaseSize(rounded)  // was: setBaseSize(rounded)

<!-- handleBaseSizeChange (line 221) — currently resets referenceWidthRef on every manual
     A+/A− press. Since referenceWidthRef now belongs to the NOTATION heuristic (not lyric
     baseSize), remove that reset — a lyric-only A+/A− press must NOT perturb the notation
     scale's resize reference: -->
const handleBaseSizeChange = useCallback((newSize: number) => {
  setBaseSize(newSize)   // no more referenceWidthRef.current = window.innerWidth here
}, [])

<!-- ViewMode union (NotationRenderer.tsx:135):
     'staff' | 'solfege' | 'staff-split' | 'solfege-split' | 'lyrics' -->
</interfaces>

<pitfall>
RESEARCH Pitfall 3: AbcPlayer keeps abcjs's own `scale:1` fixed and folds the `scale` PROP
into `targetStaffwidth = (containerWidth * staffWidthFactor) / effectiveScale`. So the
inline-Staff scale value must MATCH the current default inline-Staff density (mobile
chromeless default baseSize = 13 → 13/14 ≈ 0.929) at the initial viewport width to avoid a
visible size jump on first deploy. Seed `notationBaseSize` at `13` (matching
`MOBILE_DEFAULT_SIZE_CHROMELESS`) and seed its reference width at the initial
`window.innerWidth`, exactly mirroring how the pre-existing heuristic seeded `baseSize`
before this plan. abcjs `responsive:'resize'` scales the SVG viewBox to fill the container
width regardless, so the notation still fills width on all viewports.
</pitfall>

<pitfall>
RESEARCH Assumption A3 / Pitfall 3 (revision note): a BARE fixed `INLINE_STAFF_NOTATION_SCALE`
constant (inert to any input) would stop inline Staff from rescaling on viewport
resize/orientation change — a real regression UI-SPEC §(b) explicitly forbids ("continues to
be governed exclusively by the existing 04.9.4-03 proportional zoom heuristic"). Do NOT ship
`computeNotationScale` such that `{ viewMode:'staff', chromeless:true, baseSize:8 }` and
`{ ..., baseSize:40 }` are its ONLY possible outputs for a given call — the function MUST
accept a `notationBaseSize` input that SingingView drives from `window.innerWidth`, and the
returned scale must vary when `notationBaseSize` varies, independent of `baseSize`.
</pitfall>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pure computeNotationScale() helper + tests</name>
  <read_first>
    - src/components/notation/NotationRenderer.tsx lines 130-160 (ViewMode union, MIN/MAX/STEP), lines 488-538 (isSplitForScale, staffWidthFactor, chromeless)
    - .planning/phases/04.9.15-mobile-inline-staff-optimization/04.9.15-RESEARCH.md (Pattern 1, Pitfall 3)
    - src/lib/stanza-cycles.test.ts (vitest describe/it convention — pure-lib test style used in this codebase)
  </read_first>
  <behavior>
    - computeNotationScale({ viewMode:'staff-split', chromeless:true, baseSize:40 }) === 1 (SPLIT_LEAF_NOTATION_SCALE)
    - computeNotationScale({ viewMode:'solfege-split', chromeless:true, baseSize:40 }) === 1
    - computeNotationScale({ viewMode:'staff', chromeless:true, baseSize:40, notationBaseSize:20 }) === 20/14 — tracks notationBaseSize, NOT baseSize
    - computeNotationScale({ viewMode:'staff', chromeless:true, baseSize:8, notationBaseSize:20 }) === 20/14 (same notationBaseSize, different baseSize ⇒ same scale — proves decoupling from the lyric control)
    - computeNotationScale({ viewMode:'staff', chromeless:true, baseSize:40, notationBaseSize:12 }) !== computeNotationScale({ viewMode:'staff', chromeless:true, baseSize:40, notationBaseSize:24 }) (scale VARIES when notationBaseSize varies — proves it is NOT an inert fixed constant)
    - computeNotationScale({ viewMode:'staff', chromeless:true, baseSize:40 }) === INLINE_STAFF_NOTATION_SCALE (13/14) — fallback ONLY when notationBaseSize is omitted (uncontrolled caller safety net)
    - computeNotationScale({ viewMode:'staff', chromeless:false, baseSize:28 }) === 28/14 (desktop /tunes staff still couples — out of scope for decoupling)
    - computeNotationScale({ viewMode:'lyrics', chromeless:true, baseSize:20 }) === 20/14
    - INLINE_STAFF_NOTATION_SCALE === 13/14 and SPLIT_LEAF_NOTATION_SCALE === 1
  </behavior>
  <action>
Create `src/lib/notation-scale.ts` (pure — no React/DOM/abcjs imports):
```ts
export const SPLIT_LEAF_NOTATION_SCALE = 1
// Fallback ONLY — used when the caller doesn't supply notationBaseSize (defensive default
// for an uncontrolled chromeless-staff caller). Matches the CURRENT inline-Staff density at
// the pre-A+/A- mobile default (mobile chromeless default baseSize was 13, scaled as
// baseSize/14). The REAL, normal-path value for inline Staff is `notationBaseSize / 14`,
// where `notationBaseSize` is driven by SingingView's existing viewport-resize proportional-
// zoom heuristic (04.9.4-03) — decoupled from the lyric-only A+/A− control, but NOT decoupled
// from viewport responsiveness (UI-SPEC §b). Do not treat this constant as the scale itself.
export const INLINE_STAFF_NOTATION_SCALE = 13 / 14

export function computeNotationScale(opts: {
  viewMode: string
  chromeless: boolean
  baseSize: number
  /** Viewport-resize-driven notation size for chromeless inline Staff (SingingView state,
   *  independent of the lyric-only `baseSize`). Ignored for all other branches. */
  notationBaseSize?: number
}): number {
  const isSplit = opts.viewMode === 'staff-split' || opts.viewMode === 'solfege-split'
  if (isSplit) return SPLIT_LEAF_NOTATION_SCALE
  // Decouple ONLY the chromeless (mobile singing) inline Staff view from the lyric-only
  // baseSize. Scale still varies with notationBaseSize (viewport-width-derived) so the
  // staff keeps rescaling on resize/orientation change — it is NOT a bare fixed constant.
  // Non-chromeless desktop /tunes/[id] staff keeps the coupled baseSize/14 behaviour (out of scope).
  if (opts.chromeless && opts.viewMode === 'staff') {
    return (opts.notationBaseSize ?? 13) / 14
  }
  return opts.baseSize / 14
}
```
Create `src/lib/notation-scale.test.ts` with the cases listed in <behavior> (vitest
describe/it, matching src/lib/stanza-cycles.test.ts style).
  </action>
  <verify>
    <automated>npx vitest run src/lib/notation-scale.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/notation-scale.test.ts` exits 0 with all cases passing
    - `grep -q "INLINE_STAFF_NOTATION_SCALE = 13 / 14" src/lib/notation-scale.ts`
    - `grep -q "notationBaseSize" src/lib/notation-scale.ts` (the chromeless-staff branch reads a viewport-derived input, not a bare constant)
    - `grep -q "chromeless && opts.viewMode === 'staff'" src/lib/notation-scale.ts`
    - notation-scale.ts contains no `import` of React, abcjs, or NotationRenderer (pure module)
  </acceptance_criteria>
  <done>Pure scale helper exists and is green; the 3-way branch (split / inline-staff-tracks-notationBaseSize / other) is proven by unit test, including that inline-Staff scale varies with notationBaseSize independent of baseSize.</done>
</task>

<task type="auto">
  <name>Task 2: Wire scale decoupling + correct A+/A− range across NotationRenderer, GlassBottomBar, SingingView</name>
  <read_first>
    - src/components/notation/NotationRenderer.tsx lines 138-140, 488-496 (constants + scale)
    - src/components/singing/GlassBottomBar.tsx lines 64-83 (A+/A− buttons)
    - src/components/singing/SingingView.tsx lines 178-226 (proportional-zoom resize heuristic + handleBaseSizeChange)
    - src/lib/notation-scale.ts (Task 1 output)
  </read_first>
  <action>
1. In `src/components/singing/SingingView.tsx` (do this FIRST — Task's other steps depend on
   the new `notationBaseSize` prop existing):
   - Add a new state `const [notationBaseSize, setNotationBaseSize] = useState<number>(13)`
     (13 matches `NotationRenderer.tsx`'s `MOBILE_DEFAULT_SIZE_CHROMELESS`, the pre-existing
     default inline-Staff density) and a ref `notationBaseSizeRef` kept in sync via a
     `useEffect` (mirror the existing `baseSizeRef` sync pattern at lines 161-163).
   - REPURPOSE the existing proportional-zoom resize effect (lines 182-217) to drive
     `notationBaseSize` instead of `baseSize`: inside the debounced `handler`, change
     `const current = baseSizeRef.current` to `const current = notationBaseSizeRef.current`,
     and `setBaseSize(rounded)` to `setNotationBaseSize(rounded)`. Leave `referenceWidthRef`,
     the `clamp(..., 8, 40)` math, the 150ms debounce, and the `< 8px` delta suppression
     UNCHANGED — only the state variable being read/written changes. (`referenceWidthRef` now
     conceptually belongs to the notation heuristic, not the lyric `baseSize`.)
   - In `handleBaseSizeChange` (lines 221-226, fired by A+/A− button presses), REMOVE the
     `referenceWidthRef.current = window.innerWidth` reset — a lyric-only A+/A− press must no
     longer perturb the notation-scale resize reference (they are now fully independent state).
     `handleBaseSizeChange` becomes just `setBaseSize(newSize)`.
   - Pass `notationBaseSize={notationBaseSize}` to the `<NotationRendererClient>` render call
     (alongside the existing `baseSize={baseSize}` at line ~497).

2. In `src/components/notation/NotationRenderer.tsx`:
   - Export the size constants so GlassBottomBar can reuse the ceiling: change lines 138-140 to
     `export const MIN_SIZE = 4`, `export const MAX_SIZE = 120`, `export const SIZE_STEP = 2`.
   - Add `import { computeNotationScale } from '@/lib/notation-scale'`.
   - Add `notationBaseSize?: number` to `NotationRendererProps` (documented: viewport-resize-
     driven notation size for chromeless inline Staff, independent of the lyric-only
     `baseSize`; ignored for all other view modes) and destructure it in the component
     signature.
   - Replace lines 494-496 (the `isSplitForScale` / local `SPLIT_LEAF_NOTATION_SCALE` / `scale`
     ternary) with:
     `const scale = computeNotationScale({ viewMode, chromeless, baseSize, notationBaseSize })`.
     (Delete the now-unused local `isSplitForScale` and local `SPLIT_LEAF_NOTATION_SCALE` at
     494-495 — verify neither identifier is referenced elsewhere before deleting; `isSplitMode`
     is still used for `isSplitForWidth` at line 531 and stays.)

3. In `src/components/singing/GlassBottomBar.tsx`:
   - Add `import { MAX_SIZE, SIZE_STEP } from '@/components/notation/NotationRenderer'`.
   - Change the A− button (currently `onBaseSizeChange(Math.max(8, baseSize - 1))`) to
     `onClick={() => onBaseSizeChange(Math.max(8, baseSize - SIZE_STEP))}` and add
     `disabled={baseSize <= 8}` plus the disabled styling `disabled:opacity-40 disabled:pointer-events-none`.
   - Change the A+ button (currently `onBaseSizeChange(Math.min(40, baseSize + 1))`) to
     `onClick={() => onBaseSizeChange(Math.min(MAX_SIZE, baseSize + SIZE_STEP))}` and add
     `disabled={baseSize >= MAX_SIZE}` plus the same disabled styling.
     40 is NOT a clamp — A+ steps by 2 all the way to 120 (soft-40, hard-120 per UI-SPEC §b).
     This button drives lyric `baseSize` ONLY — it has no effect on `notationBaseSize`.
   - Do NOT change the `aria-label`s (`Decrease size` / `Increase size`) — UI-SPEC copy locked.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npx vitest run src/lib/notation-scale.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "computeNotationScale({ viewMode, chromeless, baseSize, notationBaseSize })" src/components/notation/NotationRenderer.tsx`
    - `grep -Eq "export const MAX_SIZE = 120" src/components/notation/NotationRenderer.tsx`
    - `grep -q "notationBaseSize" src/components/notation/NotationRenderer.tsx` (prop threaded through, not just baseSize)
    - `grep -q "setNotationBaseSize" src/components/singing/SingingView.tsx`
    - `grep -q "notationBaseSize={notationBaseSize}" src/components/singing/SingingView.tsx`
    - `grep -q "Math.min(MAX_SIZE, baseSize + SIZE_STEP)" src/components/singing/GlassBottomBar.tsx`
    - `grep -q "Math.max(8, baseSize - SIZE_STEP)" src/components/singing/GlassBottomBar.tsx`
    - `grep -q "disabled={baseSize >= MAX_SIZE}" src/components/singing/GlassBottomBar.tsx`
    - GlassBottomBar A+/A− `aria-label`s still read exactly `Decrease size` and `Increase size` (`grep -q 'aria-label="Decrease size"'` and `'aria-label="Increase size"'`)
    - `grep -Fq "40, baseSize" src/components/singing/GlassBottomBar.tsx` returns NOTHING (old 40-clamp removed)
    - `npx tsc --noEmit` reports no new errors in the four touched files
  </acceptance_criteria>
  <done>
    Inline-Staff notation scale tracks the viewport-resize-driven `notationBaseSize` (rescales
    on resize/orientation change exactly as before this phase); A+/A− drives only
    `--staff-base-size` (lyrics/verse text) with step 2, floor 8, soft-40, hard-120, and no
    longer touches notation scale OR the resize reference width; split-leaf and desktop staff
    scaling unchanged; TypeScript compiles.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → client React state | A+/A− button presses mutate local component state only; no server or storage-format change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.9.15-03 | Tampering | localStorage `psalter-staff-size` | accept | baseSize is read back through the existing `isBaseSize`/`readStoredBaseSize` clamp (4..120); an out-of-range stored value is ignored. No new trust surface. |
| T-04.9.15-04 | Denial of Service | notation re-render on A+/A− | accept | Fixed inline-Staff scale means A+/A− no longer triggers an abcjs re-render of the staff (only CSS var change), REDUCING render churn vs today. |
</threat_model>

<verification>
- `npx vitest run src/lib/notation-scale.test.ts` green.
- Manual/Playwright (Plan 05 diagnostic re-run): at 375px, pressing A+/A− changes lyric text
  size while the staff SVG dimensions stay constant.
- Manual/Playwright: resizing the viewport (or rotating orientation) while lyric baseSize is
  untouched still rescales the inline-Staff notation, proving `notationBaseSize` — not a bare
  constant — drives inline-Staff scale.
</verification>

<success_criteria>
- MOBILE-05 satisfied: inline-Staff A+/A− is lyric-only, range 8–120 step 2 soft-40; notation
  scale tracks viewport width via `notationBaseSize` (the existing 04.9.4-03 proportional-zoom
  heuristic, decoupled only from A+/A−, never from viewport resize); no regression to split-leaf
  or desktop staff.
</success_criteria>

<output>
After completion, create `.planning/phases/04.9.15-mobile-inline-staff-optimization/04.9.15-02-SUMMARY.md`.
Note the seeded `notationBaseSize` initial value (13) and confirm the resize heuristic was
repurposed (not duplicated) from the pre-existing `baseSize` proportional-zoom effect.
</output>
