---
phase: 4.9.15
slug: mobile-inline-staff-optimization
status: final
nyquist_compliant: true
wave_0_complete: false
created: 2026-07-16
---

# Phase 4.9.15 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest (`vitest.config.mts`) |
| **Config file** | `/home/services/psalter/vitest.config.mts` |
| **Quick run command** | `npx vitest run <specific-file>` |
| **Full suite command** | `npx vitest run` |
| **Estimated runtime** | ~30 seconds (existing suite size) |

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run <touched-file>.test.ts`
- **After every plan wave:** Run `npx vitest run` (full suite)
- **Before `/gsd-verify-work`:** Full suite must be green; a Playwright live-render pass is also required (non-negotiable gate) for the CSS/visual-geometry items (a) and (d), per this codebase's established pattern for prior abcjs sizing bugs (quick tasks 260712-kov, 260712-szw, 260715-s7b, 260716-dtm)
- **Max feedback latency:** ~30 seconds (unit); Playwright diagnostics run manually per-task for items (a)/(d)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01-T1 | 01 | 0 | MOBILE-04, MOBILE-07 | — | N/A | diagnostic | `node tests/diagnostics/inline-staff-clef-stretch-diff.mjs; test $? -eq 0` | ✅ created by this task | ⬜ pending |
| 02-T1 | 02 | 1 | MOBILE-05 | — | N/A | unit | `npx vitest run src/lib/notation-scale.test.ts` | ✅ created by this task | ⬜ pending |
| 02-T2 | 02 | 1 | MOBILE-05 | T-04.9.15-04 | Fixed-scale re-render reduction (accept) | unit + typecheck | `npx tsc --noEmit -p tsconfig.json 2>&1 \| grep -E "NotationRenderer\|GlassBottomBar\|SingingView\|notation-scale" \| head; npx vitest run src/lib/notation-scale.test.ts` | ✅ existing files, wired this task | ⬜ pending |
| 03-T1 | 03 | 1 | MOBILE-06 | — | N/A | unit | Extend `npx vitest run src/lib/stanza-cycles.test.ts` | ✅ existing, needs new cases (this task) | ⬜ pending |
| 04-T1 | 04 | 2 | MOBILE-08 | T-04.9.15-06 | Explicit allow-list gate (mitigate) | unit | `npx vitest run src/lib/inline-staff-gating.test.ts` | ✅ created by this task | ⬜ pending |
| 04-T2 | 04 | 2 | MOBILE-08 | T-04.9.15-08 | Non-sensitive editorial read (accept) | typecheck | `npx tsc --noEmit -p tsconfig.json; test $? -eq 0` | ✅ existing file, extended this task | ⬜ pending |
| 04-T3 | 04 | 2 | MOBILE-08 | T-04.9.15-06, T-04.9.15-07 | Gate wiring + navigation fallback (mitigate/accept) | typecheck + unit | `npx tsc --noEmit -p tsconfig.json 2>&1 \| grep -E "GearPopover\|SingingView\|psalms/\[id\]\|precent" \| head; npx vitest run src/lib/inline-staff-gating.test.ts` | ✅ existing files, wired this task | ⬜ pending |
| 05-T1 | 05 | 2 | MOBILE-04 | — | N/A | diagnostic (fix re-run) | `node tests/diagnostics/inline-staff-clef-stretch-diff.mjs; test $? -eq 0` | ✅ diagnostic exists (Plan 01) | ⬜ pending |
| 05-T2 | 05 | 2 | MOBILE-07 | — | N/A | diagnostic (fix re-run) | `node tests/diagnostics/inline-staff-clef-stretch-diff.mjs; test $? -eq 0` | ✅ diagnostic exists (Plan 01) | ⬜ pending |
| 05-T3 | 05 | 2 | MOBILE-04, MOBILE-07 | — | N/A | manual (human visual, `autonomous: false`) | Human visual check on a real device per Plan 05 Task 3 instructions | n/a — manual gate | ⬜ pending |

Task IDs follow `<plan>-T<task>` (e.g. `02-T1` = Plan 02, Task 1), matching the finalized 5-plan
structure (Plan 01 Wave 0 diagnostic; Plans 02/03 Wave 1; Plans 04/05 Wave 2).

---

## Wave 0 Requirements

- [x] `tests/diagnostics/inline-staff-clef-stretch-diff.mjs` — committed Playwright diagnostic (Plan 01, Wave 0) confirming the real clef/key-signature selector (item a) and the per-row stretch root cause (item d) before Plan 05's fix
- [ ] `src/lib/notation-scale.ts` + `src/lib/notation-scale.test.ts` (Plan 02) — pure scale-decoupling logic, unit-tested; no component test file needed since the branching logic was extracted to a pure lib rather than tested inside `NotationRenderer.tsx` directly
- [ ] `src/lib/inline-staff-gating.ts` + `src/lib/inline-staff-gating.test.ts` (Plan 04) — pure gating logic, unit-tested; same pure-extraction pattern, no `GearPopover.test.tsx` needed
- [ ] Extend `src/lib/stanza-cycles.test.ts` (Plan 03) — existing file, needs new assertions for `bibleVerseRef` handling per the plain-digit-glue decision (item c)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Clef/key-signature visually shrinks to 60% on wrapped rows without becoming illegible | MOBILE-04 | CSS/SVG geometry — visual judgment call, not a pure-function assertion | Render a multi-row psalm in inline Staff on a 375px viewport via Playwright/manual screenshot; confirm row 1 clef/key is full size and row 2+ is visibly smaller but still readable |
| Row-stretch fix eliminates visible whitespace gaps on non-last rows | MOBILE-07 | Geometry-dependent, requires live abcjs render — not testable as a pure function | Render a known-affected tune (per Wave 0 diagnostic) in inline Staff; visually confirm rows 1 and 2 fill the available width, matching the last row's fill behavior |
| Non-approved tune fallback toast displays correct copy and lands on Split-Leaf when reached via prev/next navigation | MOBILE-08 | Requires interactive navigation flow, not a pure unit assertion | Navigate to a non-approved tune while in Staff-inline mode via next/prev; confirm JPEG split-leaf renders and the toast reads "Inline Staff not available for this tune — showing Split-Leaf. Pick a different view in Settings." |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (05-T3 is the sole intentional manual-only gate, per `autonomous: false` on Plan 05)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 (Plan 01) covers both MISSING diagnostic references (items a, d) needed before Plan 05
- [x] No watch-mode flags
- [x] Feedback latency < 30s (unit); Playwright diagnostics manual per-task
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** final — table reflects the 5-plan structure (01 Wave 0, 02/03 Wave 1, 04/05 Wave 2)
