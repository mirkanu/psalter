---
phase: 4.9.15
slug: mobile-inline-staff-optimization
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-16
---

# Phase 4.9.15 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest (`vitest.config.mts`) |
| **Config file** | `/home/services/psalter/vitest.config.mts` |
| **Quick run command** | `npx vitest run <specific-file>` |
| **Full suite command** | `npx vitest run` |
| **Estimated runtime** | ~30 seconds (existing suite size) |

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run <touched-file>.test.ts`
- **After every plan wave:** Run `npx vitest run` (full suite)
- **Before `/gsd-verify-work`:** Full suite must be green; a Playwright live-render pass is also required (non-negotiable gate) for the CSS/visual-geometry items (a) and (d), per this codebase's established pattern for prior abcjs sizing bugs (quick tasks 260712-kov, 260712-szw, 260715-s7b, 260716-dtm)
- **Max feedback latency:** ~30 seconds (unit); Playwright diagnostics run manually per-task for items (a)/(d)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD-01 | TBD | 0 | MOBILE-07 | — | N/A | diagnostic | Playwright script mirroring `tests/diagnostics/split-leaf-staff-diff.mjs` | ❌ W0 | ⬜ pending |
| TBD-02 | TBD | 1 | MOBILE-04 | — | N/A | manual/Playwright visual | Playwright DOM inspection of rendered SVG transform values on `.abcjs-staff-extra.abcjs-clef` / `.abcjs-staff-extra.abcjs-key-signature` | ❌ W0 | ⬜ pending |
| TBD-03 | TBD | 1 | MOBILE-05 | — | N/A | unit | `npx vitest run src/components/notation/NotationRenderer.test.tsx` (new file) | ❌ W0 | ⬜ pending |
| TBD-04 | TBD | 1 | MOBILE-06 | — | N/A | unit | Extend `npx vitest run src/lib/stanza-cycles.test.ts` | ✅ existing, needs new cases | ⬜ pending |
| TBD-05 | TBD | 1 | MOBILE-07 | — | N/A | manual/Playwright visual | Playwright diagnostic (fix verification, same script as Wave 0) | ❌ W0 | ⬜ pending |
| TBD-06 | TBD | 1 | MOBILE-08 | — | N/A | unit + integration | `npx vitest run src/components/singing/GearPopover.test.tsx` (new) + Playwright for fallback-toast UX | ❌ W0 | ⬜ pending |

*Task IDs and wave assignments are placeholders — the planner assigns final IDs.*

---

## Wave 0 Requirements

- [ ] `src/components/notation/NotationRenderer.test.tsx` — no test file exists today for this 1467-line component; at minimum needs unit coverage for the new `INLINE_STAFF_NOTATION_SCALE` branch and the verse-number `w:` line change
- [ ] `src/components/singing/GearPopover.test.tsx` — no test file exists; needs coverage for the new `staffInlineApproved` gating branch
- [ ] A committed or throwaway Playwright diagnostic script for clef/key-sig scale verification (item a) and row-stretch root-cause verification (item d) — this codebase's precedent is one-off `.mjs` scripts under `tests/diagnostics/`, not a persisted regression suite; planner decides whether to commit these as permanent regression checks
- [ ] Extend `src/lib/stanza-cycles.test.ts` — existing file, needs new assertions for `bibleVerseRef` handling per the plain-digit-glue decision (item c)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Clef/key-signature visually shrinks to 60% on wrapped rows without becoming illegible | MOBILE-04 | CSS/SVG geometry — visual judgment call, not a pure-function assertion | Render a multi-row psalm in inline Staff on a 375px viewport via Playwright/manual screenshot; confirm row 1 clef/key is full size and row 2+ is visibly smaller but still readable |
| Row-stretch fix eliminates visible whitespace gaps on non-last rows | MOBILE-07 | Geometry-dependent, requires live abcjs render — not testable as a pure function | Render a known-affected tune (per Wave 0 diagnostic) in inline Staff; visually confirm rows 1 and 2 fill the available width, matching the last row's fill behavior |
| Non-approved tune fallback toast displays correct copy and lands on Split-Leaf when reached via prev/next navigation | MOBILE-08 | Requires interactive navigation flow, not a pure unit assertion | Navigate to a non-approved tune while in Staff-inline mode via next/prev; confirm JPEG split-leaf renders and the toast reads "Inline Staff not available for this tune — showing Split-Leaf. Pick a different view in Settings." |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s (unit); Playwright diagnostics manual per-task
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
