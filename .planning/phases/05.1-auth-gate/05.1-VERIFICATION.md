---
phase: 05.1-auth-gate
verified: 2026-06-19T00:00:00Z
status: human_needed
score: 3/4 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Confirm the logged-in user's name appears on the set detail page"
    expected: "Opening a precenting set at /precent/[id] shows the precentor's name (e.g. 'Admin' or the account's display name) visibly on the page — not just stored in the DB"
    why_human: "SetDetail.tsx line 232 has the precentor name display commented out ({/* precentorName removed (D-18) */}). The data wiring is correct (userId FK → user.name via JOIN) and the value flows through the serialized prop, but the JSX render was removed. The UAT passed SC3 vacuously (no sets existed at UAT time, so the check returned pass=true with an informational note). Cannot verify user-visible display without a real browser session with an existing set."
---

# Phase 05.1: Auth Gate Verification Report

**Phase Goal:** Better Auth login, /precent/* route protection, admin-only account management, ownership-scoped precentor data
**Verified:** 2026-06-19
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Roadmap Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | A precentor can log in with email and password via Better Auth; session persists across page reloads | VERIFIED | `src/lib/auth.ts` — `betterAuth()` with `emailAndPassword.enabled: true`, 30-day session; `LoginForm.tsx` calls `authClient.signIn.email`; UAT SC1 PASS confirmed login + reload persistence |
| 2 | All /precent/* routes redirect unauthenticated users to the login page; no precentor data accessible without a valid session | VERIFIED | `middleware.ts` — `matcher: ['/dev/:path*', '/precent/:path*']` with `getSessionCookie` fast check; UAT SC2 PASS confirmed redirect to `/login?callbackUrl=`; server components also call `auth.api.getSession` and redirect as defense-in-depth |
| 3 | The Precentor field on Precenting Sets is automatically populated from the logged-in user's display name (no manual entry) | UNCERTAIN | Data wiring is correct: `precentingSets.userId` FK → `users.name` via Drizzle relation; pages pass `precentorName: s.user?.name ?? ''` to components. However, `SetDetail.tsx` line 232 has the rendering commented out — the `SerializedSet.precentorName` field exists in the interface but is never rendered to the user in JSX. UAT SC3 passed vacuously (no sets existed at check time; code returned `pass: true` with note "No sets exist yet"). Needs human browser verification with an actual set |
| 4 | An admin can create precentor accounts via a CLI script or minimal admin UI; no self-registration route exists | VERIFIED | `scripts/seed-admin.ts` — idempotent admin seed script confirmed working; `/dev/accounts` page with `AccountsClient` calls `authClient.admin.createUser`; `disableSignUp: true` in `src/lib/auth.ts`; UAT SC4 PASS: no signup text on /login, POST `/api/auth/sign-up/email` returned 400 |

**Score:** 3/4 truths verified (1 uncertain — human needed)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/auth.ts` | betterAuth() server config with admin plugin + disableSignUp | VERIFIED | Contains `disableSignUp: true`, `plugins: [admin()]`, 30-day session, drizzleAdapter with explicit schema mapping |
| `src/lib/auth-client.ts` | createAuthClient with adminClient plugin | VERIFIED | Contains `adminClient()`, exports `signIn`, `signOut`, `useSession` |
| `src/app/api/auth/[...all]/route.ts` | Better Auth catch-all HTTP handler | VERIFIED | Contains `toNextJsHandler(auth)` |
| `scripts/seed-admin.ts` | Idempotent admin seed script | VERIFIED | Contains `createUser`, idempotency via raw Drizzle `findFirst`; admin row confirmed in DB |
| `middleware.ts` | Route protection for /precent/* and /dev/* | VERIFIED | `runtime: 'nodejs'`, matcher `['/dev/:path*', '/precent/:path*']`, session cookie check for /precent, DB session + role check for /dev |
| `src/app/login/page.tsx` | Full-screen login gate page | VERIFIED | Centers `<LoginForm>` in Suspense; no site header rendered (SiteHeader returns null on /login) |
| `src/components/auth/LoginForm.tsx` | Client email+password form with callbackUrl redirect | VERIFIED | `authClient.signIn.email`, open-redirect guard (`raw.startsWith('/') && !raw.startsWith('//')`), no signup/register link |
| `src/lib/precent-auth.ts` | Shared ownership helper | VERIFIED | Exports `canAccessSet`, `getSessionOr401`, `loadSetWithAccess` |
| `src/app/api/precent/route.ts` | Session-attached set creation | VERIFIED | Calls `getSessionOr401`, attaches `userId` from session (admin viewing-as override supported) |
| `src/app/api/precent/[id]/route.ts` | Ownership-guarded set PATCH/DELETE | VERIFIED | Calls `getSessionOr401` + `loadSetWithAccess` on both PATCH and DELETE |
| `src/app/api/precent/[id]/items/route.ts` | Item add guarded by parent set | VERIFIED | Calls `getSessionOr401` + `loadSetWithAccess` |
| `src/app/api/precent/[id]/reorder/route.ts` | Reorder guarded by parent set | VERIFIED | Calls `getSessionOr401` + `loadSetWithAccess` |
| `src/app/api/precent/[id]/items/[itemId]/route.ts` | Item PATCH/DELETE guarded by parent set | VERIFIED | Both handlers call `getSessionOr401` + `loadSetWithAccess` |
| `src/app/precent/page.tsx` | Session-filtered list page | VERIFIED | Calls `auth.api.getSession`, redirects on no session, filters by `precentingSets.userId`, derives `precentorName` from `s.user?.name` |
| `src/app/precent/[id]/page.tsx` | Set detail with ownership check | VERIFIED | Calls `auth.api.getSession`, `notFound()` when caller is not owner and not admin, derives name from `set.user?.name` |
| `src/components/precent/ViewingAsBar.tsx` | Admin viewing-as dropdown | VERIFIED | `'use client'`, renders Select of users, navigates to `/precent?as=<id>` on change |
| `src/app/dev/accounts/page.tsx` | Admin-only account management page | VERIFIED | `force-dynamic`, redirects non-admins, fetches users via Drizzle (post-UAT fix: `auth.api.listUsers` replaced with direct `db.select`) |
| `src/app/dev/accounts/AccountsClient.tsx` | Create-account dialog | VERIFIED | Calls `authClient.admin.createUser`, `router.refresh()` on success |
| `scripts/uat/auth-gate-uat.js` | End-to-end Playwright UAT | VERIFIED | Implements SC1–SC4 checks using playwright-daemon; reads credentials from env; SC3 passes vacuously when no sets exist |
| `tests/auth/login.test.ts` | Wave 0 AUTH-01 test scaffold | VERIFIED | Exists, imports from `@/lib/auth`, active smoke test + todo stubs |
| `tests/auth/no-signup.test.ts` | Wave 0 AUTH-02 test scaffold | VERIFIED | Exists, asserts `auth.options.emailAndPassword?.disableSignUp` |
| `tests/auth/ownership.test.ts` | Wave 0 D-22 ownership predicate tests | VERIFIED | Imports `canAccessSet` from `@/lib/precent-auth`, 5 active assertions |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/lib/auth.ts` | `src/db` (db) + `src/db/schema` | `drizzleAdapter(db, { provider: 'pg', schema })` | WIRED | Explicit schema mapping: `{ user: schema.users, session: schema.sessions, account: schema.accounts, verification: schema.verifications }` |
| `middleware.ts` | `src/lib/auth (auth.api.getSession)` | Role check for /dev/* | WIRED | `auth.api.getSession({ headers: request.headers })` then `session.user.role !== 'admin'` |
| `LoginForm.tsx` | `/api/auth/sign-in/email` | `authClient.signIn.email` | WIRED | `authClient.signIn.email({ email, password })` called on form submit |
| `precenting_sets.user_id` | `user.id` | FK reference | WIRED | `references(() => users.id)` in schema; confirmed in DB: all rows have non-null `user_id`, `precentor_name` column gone |
| `src/app/api/precent/* routes` | `src/lib/auth` | `getSessionOr401` at handler entry | WIRED | All 5 route files import and call `getSessionOr401` at the top of each handler |
| `set ownership guard` | `precentingSets.userId` | `canAccessSet` predicate | WIRED | `set.userId === session.user.id || session.user.role === 'admin'` confirmed in precent-auth.ts |
| `src/app/precent/page.tsx` | `user table` | JOIN via `with: { user: { columns: { name: true } } }` | WIRED | `precentorName: s.user?.name ?? ''` confirmed in page.tsx |
| `AccountsClient.tsx` | `/api/auth/admin/create-user` | `authClient.admin.createUser` | WIRED | `authClient.admin.createUser({ body: { name, email, password, role } })` confirmed |

### Database Verification

| Check | Result |
|-------|--------|
| `user` table exists with admin row | CONFIRMED: `manuelkuhs@gmail.com / admin` |
| `session`, `account`, `verification` tables exist | CONFIRMED: all 4 auth tables in psalter-db |
| `precenting_sets.user_id` NOT NULL, all rows populated | CONFIRMED: `count(*) WHERE user_id IS NULL = 0` |
| `precenting_sets.precentor_name` removed | CONFIRMED: `count(*) FROM information_schema.columns WHERE column_name='precentor_name' = 0` |
| better-auth 1.6.19 installed | CONFIRMED: `node_modules/better-auth` version 1.6.19 |

### Requirements Coverage

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|---------|
| AUTH-01 | Precentor can log in with email and password | SATISFIED | `emailAndPassword.enabled: true`, LoginForm, UAT SC1 PASS |
| AUTH-02 | Precentor accounts are admin-created only (no public self-registration) | SATISFIED | `disableSignUp: true`, seed-admin.ts, /dev/accounts admin UI, UAT SC4 PASS (400 on sign-up POST) |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/components/precent/SetDetail.tsx` | 232 | `{/* precentorName removed (D-18) — name resolved via user JOIN */}` — rendered display commented out, field in interface but never shown in JSX | Warning | SC3 (ROADMAP success criterion 3) is not verifiable as a user-visible behavior; the data wiring is correct but the visible Precentor label on the set detail was removed |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| better-auth installed at correct version | `grep '"version"' node_modules/better-auth/package.json` | `"version": "1.6.19"` | PASS |
| `disableSignUp: true` in auth config | `grep disableSignUp src/lib/auth.ts` | Found on line 19 | PASS |
| Admin user in DB | `SELECT email, role FROM "user" WHERE role='admin'` | `manuelkuhs@gmail.com \| admin` | PASS |
| All routes guarded | `grep -n getSessionOr401 src/app/api/precent/**` | Found in all 5 route files | PASS |
| precentor_name column absent | `SELECT count(*) FROM information_schema.columns WHERE table_name='precenting_sets' AND column_name='precentor_name'` | 0 | PASS |
| user_id not null in precenting_sets | `SELECT count(*) FROM precenting_sets WHERE user_id IS NULL` | 0 | PASS |

### Human Verification Required

#### 1. SC3 Precentor Name Visible on Set Detail

**Test:** Log in as admin at https://psalter.gsdlabs.dev/precent. Create a new precenting set (or open an existing one). Navigate to the set detail page at /precent/[id].

**Expected:** The set detail page displays the logged-in user's display name (e.g. "Admin" or the account name) as the precentor — not blank, not "Manuel", not "null". The name should be visible to the user on the detail page.

**Why human:** `SetDetail.tsx` line 232 shows the precentor name display was commented out during Plan 04 implementation (the comment reads "precentorName removed (D-18) — name resolved via user JOIN in Plan 03"). The `precentorName` field exists in the `SerializedSet` interface and the pages pass it correctly from the user JOIN, but no JSX renders it. The UAT SC3 passed vacuously — when no sets existed, the UAT returned `pass: true` with the note "Feature is wired via Drizzle user JOIN. Create a set in /precent to fully verify." The human sign-off was given without this specific check being exercised. Cannot determine from code inspection alone whether the owner considers the visible display to be a requirement or whether the DB ownership record (which IS correctly set) is sufficient.

---

### Gaps Summary

No hard FAILED truths — all four success criteria have code implementations. However, SC3 (Precentor name auto-populated) is UNCERTAIN because the user-visible display was removed from `SetDetail.tsx` while the data infrastructure (userId FK, user JOIN, precentorName in serialized props) is wired correctly. The UAT's SC3 check did not exercise the rendering because no sets existed at UAT time.

The human verification step gates on whether the Precentor name is considered a required visible label on the set detail (per the ROADMAP wording "Precentor field on Precenting Sets is automatically populated from the logged-in user's display name") or whether DB-level ownership is sufficient for this phase.

---

_Verified: 2026-06-19_
_Verifier: Claude (gsd-verifier)_
