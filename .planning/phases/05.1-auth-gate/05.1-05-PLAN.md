---
phase: 05.1-auth-gate
plan: 05
type: execute
wave: 5
depends_on: [05.1-04]
files_modified:
  - scripts/uat/auth-gate-uat.js
autonomous: false
requirements: [AUTH-01, AUTH-02]

must_haves:
  truths:
    - "A precentor can log in and the session persists across reloads (AUTH-01)"
    - "Unauthenticated access to /precent and /dev redirects to /login (D-01/D-02)"
    - "The Precentor field on a set shows the logged-in user's name (success criterion 3)"
    - "No self-registration route exists (AUTH-02)"
  artifacts:
    - path: "scripts/uat/auth-gate-uat.js"
      provides: "End-to-end Playwright UAT covering all 4 phase success criteria"
      contains: "/login"
  key_links:
    - from: "scripts/uat/auth-gate-uat.js"
      to: "psalter.gsdlabs.dev (or localhost:3005)"
      via: "playwright-daemon runPlaywright"
      pattern: "runPlaywright"
---

<objective>
Build and run a Playwright UAT that verifies all four Phase 05.1 success criteria end-to-end against the running site, then pause for human sign-off. This is the phase gate.

Purpose: Prove the auth gate actually works in a browser — login, redirect-when-unauthenticated, session persistence, auto-populated precentor name, no self-registration — before closing the phase.
Output: scripts/uat/auth-gate-uat.js + a green UAT run + human verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-auth-gate/05.1-CONTEXT.md
@.planning/phases/05.1-auth-gate/05.1-04-SUMMARY.md

<interfaces>
Shared Playwright daemon (per global CLAUDE.md — DO NOT spawn raw Playwright):
```js
const { getStatus, runPlaywright } = require('/home/services/playwright-daemon/client.js')
const { browserReady } = await getStatus()
if (!browserReady) throw new Error('daemon not ready — retry in a few seconds')
const result = await runPlaywright(`
  await page.goto('https://psalter.gsdlabs.dev/precent', { waitUntil: 'networkidle' })
  return page.url()
`)
```
`runPlaywright(script)` evaluates the JS string with `page` and `browser` in scope and returns `{ result }` or `{ error }`.

Existing UAT analog: scripts/uat/wave-a-sample-uat.js (structure: build script string, call runPlaywright, assert on returned values).

Target site: deployed at https://psalter.gsdlabs.dev (port 3005 via Cloudflare Tunnel). For LOCAL verification run `npm run build && npm run start` (port 3005) and target http://localhost:3005. NOTE: /precent and /dev are now behind Cloudflare Access at the edge per project infra conventions — if CF Access blocks the daemon, run the UAT against localhost:3005 instead, where only the app-level Better Auth gate applies.

Test credentials: use PSALTER_ADMIN_EMAIL / PSALTER_ADMIN_PASSWORD (admin) and, if a precentor account was created in Plan 04, a precentor login. Read these from env — never hardcode in the committed UAT script (read via process.env at runtime).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write + run scripts/uat/auth-gate-uat.js covering all 4 success criteria</name>
  <files>scripts/uat/auth-gate-uat.js</files>
  <read_first>
    - scripts/uat/wave-a-sample-uat.js (daemon usage + assertion structure to mirror)
    - .planning/phases/05.1-auth-gate/05.1-CONTEXT.md (success criteria + D-01..D-22)
    - .planning/ROADMAP.md §"Phase 05.1" success criteria (the 4 truths to assert)
  </read_first>
  <action>
Create `scripts/uat/auth-gate-uat.js`. It uses the shared daemon (`require('/home/services/playwright-daemon/client.js')`) and reads credentials from `process.env` (PSALTER_ADMIN_EMAIL/PSALTER_ADMIN_PASSWORD; optional PSALTER_TEST_PRECENTOR_EMAIL/PASSWORD). Pick BASE_URL from `process.env.UAT_BASE_URL ?? 'http://localhost:3005'`.

Implement these checks, printing PASS/FAIL per check and exiting non-zero if any FAIL:

SC1 — Login works + session persists (AUTH-01):
  - goto `${BASE}/login`; fill email + password inputs; submit; wait for navigation.
  - assert landed on `/precent` (or the callbackUrl).
  - reload the page; assert still on /precent and NOT redirected to /login (session persists).

SC2 — Protected routes redirect when unauthenticated (D-01/D-02):
  - in a fresh context (clear cookies / new incognito-like page), goto `${BASE}/precent`; assert final URL contains `/login?callbackUrl=`.
  - goto `${BASE}/dev/accounts`; assert final URL contains `/login`.

SC3 — Precentor name auto-populated (success criterion 3):
  - logged in as admin, create a set via the UI (open CreateSetForm, pick a date + type, submit) OR navigate to an existing set; assert the rendered "Precentor:" label (SetDetail) shows the logged-in user's name (the admin's `name`, e.g. "Admin"), NOT "Manuel" and NOT empty.

SC4 — No self-registration (AUTH-02):
  - goto `${BASE}/login`; assert page has NO link/text matching /sign[- ]?up|register|create account/i.
  - POST `${BASE}/api/auth/sign-up/email` with a dummy body via `page.request.post(...)`; assert the response status is NOT 200 (sign-up disabled).

Structure each check as an async function returning {name, pass, detail}. Collect results, print a summary table, `process.exit(results.every(r=>r.pass) ? 0 : 1)`.

Before running, ensure the app is up: if targeting localhost, `npm run build` then start the server in the background on port 3005 (or confirm the pm2 `psalter` service is running for the deployed target). Check `getStatus().browserReady` before submitting jobs; if not ready, wait and retry.

Run it: `UAT_BASE_URL=http://localhost:3005 node scripts/uat/auth-gate-uat.js` (or against the deployed URL). Iterate until all four checks PASS. Capture the final output for the SUMMARY.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('scripts/uat/auth-gate-uat.js','utf8'); ['runPlaywright','/login','sign-up','/precent','/dev'].forEach(t=>{if(!s.includes(t)){console.error('MISSING',t);process.exit(1)}}); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/uat/auth-gate-uat.js exists, uses the shared daemon client, reads creds from process.env (no hardcoded secrets)
    - It implements all four checks (SC1 login+persist, SC2 redirect, SC3 precentor name, SC4 no-signup)
    - A run of the UAT prints all four checks as PASS and exits 0 (output captured in SUMMARY)
    - No secret values are committed in the script
  </acceptance_criteria>
  <done>UAT script exists and a run reports all four success criteria PASS against the running site.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of the auth gate (phase gate)</name>
  <action>Human-only verification step. Claude has already run the automated UAT in Task 1; this checkpoint asks the owner to confirm the auth gate behaves correctly in a real browser before the phase is closed. No code changes — pause and await the resume signal.</action>
  <what-built>Better Auth gate: /login page, middleware protecting /precent (any session) and /dev (admin only), per-user set ownership with auto-populated precentor name, admin viewing-as dropdown, and /dev/accounts account management. The Playwright UAT reports all four success criteria green.</what-built>
  <how-to-verify>
    1. Log out (or open a private window). Visit https://psalter.gsdlabs.dev/precent → you should be redirected to /login (no site header on that page).
    2. Log in with your admin email + password → you should land back on /precent showing only your sets. Reload → you stay logged in.
    3. Open a set → the "Precentor:" label shows your account name (not "Manuel").
    4. As admin, use the "Viewing as" dropdown to select another precentor (create one first at /dev/accounts if none exists) → the list switches to their sets; create a set while viewing-as → it belongs to that precentor. Navigate away and back → the dropdown resets to you.
    5. Visit /dev/accounts → you can see all accounts and create a new one. Log in as that new precentor in a private window → they see only their own sets and NO "Viewing as" dropdown.
    6. Confirm there is no "Sign up"/"Register" link anywhere on /login.
  </how-to-verify>
  <verify>Human confirms steps 1–6 pass in a real browser.</verify>
  <done>Owner types "approved" after verifying all six steps.</done>
  <resume-signal>Type "approved" or describe any issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT script → env secrets | Credentials must come from env, never committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-21 | Information disclosure | UAT credentials | mitigate | Script reads PSALTER_ADMIN_* from process.env; no secrets committed to the repo |
| T-05.1-22 | Spoofing | UAT false-green | mitigate | SC2 runs in a cookie-cleared context so a logged-in daemon session cannot mask the redirect check |
</threat_model>

<verification>
- All four UAT checks PASS (exit 0)
- Human checkpoint approved
- `npm run test` green; `npm run build` succeeds
</verification>

<success_criteria>
- AUTH-01: login works + session persists (UAT SC1)
- AUTH-02: no self-registration (UAT SC4)
- D-01/D-02: protected routes redirect (UAT SC2)
- Success criterion 3: precentor name auto-populated (UAT SC3)
- Human sign-off recorded
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-auth-gate/05.1-05-SUMMARY.md`
</output>
