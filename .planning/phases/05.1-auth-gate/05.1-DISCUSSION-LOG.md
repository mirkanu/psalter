# Phase 05.1: Auth Gate - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-18
**Phase:** 05.1-auth-gate
**Areas discussed:** Admin toggle on /precent, Account creation, Login page & session, Existing sets & precentor mapping

---

## Admin Toggle on /precent

| Option | Description | Selected |
|--------|-------------|----------|
| Dropdown: 'Viewing as [name]' | Select any precentor; sets filtered to that person | ✓ |
| See all sets, grouped by precentor | No toggle — admin always sees everyone's sets with a label | |
| Tabs: one tab per precentor | Each precentor gets a tab; own tab is default | |

**User's choice:** Dropdown "Viewing as [name]"
**Notes:** —

| Option | Description | Selected |
|--------|-------------|----------|
| Full control | Admin can create/edit/delete on behalf of any precentor | ✓ |
| Read-only | Admin can see but not change other precentors' sets | |
| You decide | Let Claude pick | |

**User's choice:** Full control — admin can create, edit, delete on behalf of any precentor
**Notes:** New sets created while viewing-as are owned by the selected precentor.

---

## Account Creation

| Option | Description | Selected |
|--------|-------------|----------|
| Admin page at /dev/accounts | Simple form under the already-admin-gated /dev/ area | ✓ |
| CLI script only | Node script run in terminal each time | |
| Both — CLI for seeding, /dev/accounts for ongoing | Hybrid approach | |

**User's choice:** Admin page at /dev/accounts

| Option | Description | Selected |
|--------|-------------|----------|
| Two roles: admin and precentor | Admin = /dev/* + /precent/*. Precentor = /precent/* only | ✓ |
| Three roles: admin, lead precentor, precentor | More granularity | |
| No roles — just an admin flag | All non-admin are precentors | |

**User's choice:** admin + precentor for now; user noted Visitor role will be needed soon (deferred)

| Option | Description | Selected |
|--------|-------------|----------|
| Seed script run once on first deploy | One-off script from env vars | ✓ |
| Create via /dev/accounts itself | Chicken-and-egg problem | |
| You decide | Let Claude choose safest approach | |

**User's choice:** Seed script from env vars (PSALTER_ADMIN_EMAIL + PSALTER_ADMIN_PASSWORD)

---

## Login Page & Session

| Option | Description | Selected |
|--------|-------------|----------|
| /login — simple full-page card | Email + password, no registration link | |
| /login — branded with psalter header/nav | Site chrome around the login form | |
| Modal overlay on top of the page | Login appears as dialog when protected route is hit | ✓ |

**User's choice:** Modal overlay

| Option | Description | Selected |
|--------|-------------|----------|
| Redirect to /login (dedicated page shows the modal) | Standard pattern; return URL preserved | ✓ |
| Stay on the protected route — blur/dim it behind modal | URL stays same; modal over content | |
| You decide | Let Claude pick the more reliable approach | |

**User's choice:** Redirect to /login (dedicated page), return to original URL after login

| Option | Description | Selected |
|--------|-------------|----------|
| 30 days | Log in once, stay for a month | ✓ |
| 7 days | More frequent logins | |
| Until explicit logout | Session never expires | |

**User's choice:** 30 days

---

## Existing Sets & Precentor Mapping

| Option | Description | Selected |
|--------|-------------|----------|
| Add userId FK, migrate existing rows to admin account | Cleanest long-term; sets owned by user record | ✓ |
| Keep precentorName as filter key | No schema change; text-based filter | |
| You decide | Let Claude pick robust approach | |

**User's choice:** Add userId FK to precenting_sets; migrate existing 'Manuel' rows to admin user ID

| Option | Description | Selected |
|--------|-------------|----------|
| Derive display name at query time via JOIN | Remove precentorName column; single source of truth | ✓ |
| Keep precentorName as denormalized copy | Easier queries; name changes require two updates | |

**User's choice:** Derive at query time via JOIN — remove precentorName column entirely

---

## Claude's Discretion

No areas fully deferred to Claude — user made explicit choices on all questions.

## Deferred Ideas

- **Visitor role** — User noted this is coming soon. Role system should be extensible (text enum, not boolean flags).
- **Password reset / forgot password** — Out of scope; precentors contact admin.
- **Email verification** — Not needed; admin creates accounts directly.
- **"Remember me" toggle** — Always 30 days; no per-login choice.
