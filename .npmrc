# Force pnpm to use flat (non-symlinked) node_modules so Vercel's Serverless Function
# packaging doesn't fail with "framework produced an invalid deployment package".
# See Plan 17-02 deviation log (auto-fix for Vercel + pnpm + Turbopack build error).
node-linker=hoisted