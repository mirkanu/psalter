# Continue Here — Psalter UI Fixes Batch 3
**Paused:** 2026-05-11  
**Resume with:** `/gsd-quick` (paste the task list below as the argument)

---

## What was in progress

A `/gsd-quick` was in flight with 6 tasks. The first executor was just spawned when the user interrupted (tool use rejected). **Nothing was committed yet for this batch.**

There are also **3 uncommitted modified files** from work that was partially done mid-session but never committed or pushed:
- `src/components/PsalmNotationPlayer.tsx`
- `src/components/PsalmNotationPlayerClient.tsx`
- `src/components/PsalmTabs.tsx`

**Action needed first:** Review those 3 files with `git diff` to see if the changes are good, then either commit or stash them before starting the new quick task.

---

## The full task list to resume (paste as /gsd-quick argument)

```
T2: this wasn't implemented? — in PsalmNotationPlayer.tsx ~line 186 inside scoreSection, still has old format "Tune: {name} (CM)" — change to "Tune (CM): {name}" (one occurrence was fixed at ~line 132 but not this one)

T4 mobile: change mobile behaviour - remove stickyScoreMode=true for mobile, instead pass mobileStickyScore=true so the score section gets `sticky top-[6.5rem] bg-background` and lyrics scroll via normal PAGE scroll (not internal overflow-y-auto). In PsalmTabs, mobile Sing tab should not use the height-constrained flex wrapper anymore.

T5: Meter tooltip doesn't work on mobile. Replace title= attribute with a tappable state-based tooltip in TuneTable.tsx: clicking the meter cell shows a popover with full meter value. Close on outside click.

T6: dark overlay still not visible below search bar — in GlobalSearch.tsx, change from `items-start pt-[15vh]` to `items-center` (center the modal vertically) so gray is visible both above and below.

T7: SelectPsalmDialog search should match against full lyrics text — add `lyrics: string | null` to fetchPsalmsByMeter in psalms.ts (add `lyrics: true` to psalmVersions columns), update PsalmOption interface in SelectPsalmDialog, add lyrics to filter, update PsalmsByTuneSection psalmsForMeter type.

T8: Multiple search bugs to fix:
  (a) /psalms grid shows strange "no results" behaviour — investigate
  (b) /psalms search keyword highlighting broken — "wrath" bolding random letters. Root cause likely in renderSnippet in PsalmNumberBox.tsx — the regex split with (gi) flag may be failing on certain characters. Fix the renderSnippet to be robust.
  (c) Global search doesn't highlight matched keywords — add highlight rendering in GlobalSearch.tsx results list
  (d) Global search returns only one version of multi-version psalms in text search — fix the API route.ts: in text search path, don't deduplicate by psalmId (or deduplicate correctly preserving a and b versions)
  (e) Consider extracting shared search utilities to src/lib/search-utils.ts: buildSnippet and renderSnippet used by both PsalmListingGrid, GlobalSearch, and API route
```

---

## Key file locations

| File | Purpose |
|------|---------|
| `src/components/PsalmNotationPlayer.tsx` | T2 fix + T4 mobileStickyScore prop |
| `src/components/PsalmNotationPlayerClient.tsx` | T4 prop pass-through |
| `src/components/PsalmTabs.tsx` | T4 mobile layout change |
| `src/components/TuneTable.tsx` | T5 tap tooltip |
| `src/components/GlobalSearch.tsx` | T6 backdrop, T8c highlight |
| `src/app/api/search/route.ts` | T8d a/b version fix |
| `src/components/PsalmNumberBox.tsx` | T8b renderSnippet fix |
| `src/components/PsalmListingGrid.tsx` | T8a no-results behaviour |
| `src/db/queries/psalms.ts` | T7 fetchPsalmsByMeter + lyrics |
| `src/components/SelectPsalmDialog.tsx` | T7 filter against lyrics |
| `src/components/PsalmsByTuneSection.tsx` | T7 type update |

---

## Context: what's already shipped (don't re-do)

All from the previous two `/gsd-quick` runs. Recent commits (already pushed):
- `65a558a` — search: results, backdrop, mobile keyboard
- `a8b5df2` — psalm selector shows first line
- `524ed5a` — mobile sticky score (partially working — needs T4 rework above)
- `515bcdb` — tune format "Tune (CM): Name" (one occurrence fixed, one missed)
- `fcca63e` — Enter key navigates in tune table + psalm selector
- `3f32bf9` — full column visibility in tunes table
- `b613dc8` — Sing This Tune section always shows

---

## Suggested executor split

**Executor A** (no file conflicts with B): T2 + T4 + T5 + T7  
**Executor B** (no file conflicts with A): T6 + T8 (a, b, c, d, e)
